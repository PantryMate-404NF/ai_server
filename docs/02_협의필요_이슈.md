# 협의 필요 이슈

> 작성자: 박재우 · 작성일: 2026-09-03

| 항목 | 내용 |
|---|---|
| 작성 | 2026-08-26 |
| 기준 문서 | [`01_추천시스템_설계.md`](01_추천시스템_설계.md) **v1.9** |
| 목적 | **혼자 결정할 수 없는 것**만 모음. 회의에 그대로 들고 갈 수 있는 형태 |

## 우선순위 기준

| 표시 | 의미 |
|---|---|
| 🔴 | **소급 불가.** 지금 안 정하면 나중에 되돌릴 방법이 없다 |
| 🟡 | 1주차 안에 결정. 늦으면 후속 작업이 막힌다 |
| ⚪ | 데이터·측정 결과를 보고 결정. 지금은 결정할 근거가 없다 |

---

## 요약 — 지금 당장 움직여야 하는 것 5개

| # | 대상 | 이슈 | 안 하면 |
|---|---|---|---|
| C-1 | 크롤러팀 | 재료 `group_name` 수집 | 품질 하락 · 나중에 받으려면 재크롤링 |
| C-2 | 크롤러팀 | **`raw_json` 에 원본 전체 보존** | 분류 4축 등 재파싱 불가 → 재크롤링 |
| B-1 | 백엔드팀 | `public` 스키마를 쓰지 않기로 합의 | 테이블명 충돌로 양쪽 다 깨짐 |
| A-1 | 전체 | 이벤트 로그 스키마 동결 | 랭킹 모델 학습 **영구 불가** |
| I-1 | AI 파트 | ~~Docker 설치 + `make bootstrap`~~ ✅ **완료** | — |
| **I-6** | AI 파트 | **실유저 100명 모집 설계** | **LightGBM v1 자체가 불가** |
| **I-8** | AI 파트 | ✅ **온보딩 설문 문항 확정 (09-01 · S0 ②)** — 5문항. 세부는 아래 '온보딩 최종 확정' 표가 정본이다| 모집 후 변경 시 데이터 반쯤 폐기 |
| **I-11** | 팀 | **`cuisine` 라벨 200건** (검수 스프린트에 2분) | kNN 분류 불가 |
| **I-17** | AI+인프라 | 🔴 **DB 분리 시 미해결 2건** *(v2.7)* — 인증·암호화 / DB 다운 폴백 | 원격 전환 전제 |
| **I-16** | AI 파트 | 🟡 **추천 2트랙 분리** *(v2.7)* — 트랙2 에 템플릿 사유를 남길지 등 5건 | 설명가능성·배치 cron |
| **I-15** | AI 파트 | ✅ **`flavor_vec` 산출 — 확정 (09-02)** — LLM 교차검증(MAE 0.044) · **시드 결함 2건 수정 완료** | `f_taste`(w=0.16) 정확도 |
| **C-5** | 크롤러 | **`reviews` 전량 여부 확인 1문장** *(v2.1)* — 요청 아님, 크롤링 영향 0 | 나중에 필요해지면 4.4만 건 재크롤 |
| **I-13** | 팀 | ✅ **쌍대비교 확정 (09-03)** — 20명 × 30쌍 = 600건. 검수와 같은 날 +5분. 쌍은 블록별 120쌍 층화 추출 (결정사항 D-18)| 가중치가 끝까지 추정치 |
| **I-14** | AI 파트 | **소비기한 데이터 출처** *(v1.9)* | 리포트에 "실측"이라 못 씀 |

---

# C. 크롤러팀과 협의

크롤링이 **진행 중**이다. 실제 출력 형태를 아직 보지 못했으므로,
**어느 형태로 오든 받아낼 수 있는 어댑터 층을 먼저 만들어 두었다.**

## ✅ 준비 완료 — 확인 절차만 남았다 *(2026-08-27)*

```bash
make probe SAMPLE=경로/샘플.json
```

[`app/services/ingest/`](../app/services/ingest/) 가 크롤러 출력을 우리 스키마로 매핑한다.
**실제 JSON 이 오면 `app/services/ingest/sources/mangae.yaml` 의 `paths` 만 고치면 되고**,
`adapter.py` · DB 스키마 · 정규화 파이프라인은 건드리지 않는다.

`probe` 가 리포트하는 것:

| | |
|---|---|
| 매핑 성공 | 어느 경로에서 잡혔는지 + 실제 값 |
| **재료 그룹명 보유율** | C-1 을 자동 판정 |
| 누락 · 폴백 | 🔴 치명 / 🟡 품질 하락 / ⚪ 무시 가능 + **없으면 무엇이 나빠지는지** |

합성 샘플 3종(그룹명 있음 · 없음 · 최소)으로 어댑터 자체는 검증했다 (`make probe-all`).

> **크롤링 담당자에게는 "이 필드 주세요"가 아니라 probe 출력을 그대로 보내면 된다.**
> "없으면 무엇이 나빠지는지"가 문장으로 적혀 있다.

---

### ✅ C-1. ~~재료 그룹명(`group_name`) 을 수집하고 있는가~~ — **해결. 요청 불필요**

> **실데이터로 확인됨** (`tests/fixtures/real/*.json` 3건).
> `ingredient_groups[].group_name` 은 **이미 수집되고 있다.** 다만 3/3 전부 `"기본재료"` 다.
>
> **답은 "쓸 수 없다"이지 "안 온다"가 아니다.** 크롤러에 요청할 것이 없고,
> `role` 판정은 어댑터 폴백(`is_staple`·`is_seasoning` + P4 규칙)으로 간다 (설계 4-5).
> 만개의레시피 자체가 그룹을 나누지 않는 레시피가 대부분인 것으로 보인다.

<details><summary>원래 이슈 (기록용)</summary>



**이슈**
만개의레시피는 재료가 `[재료] / [양념] / [고명]` 그룹으로 나뉘어 있다. 이 그룹명이
`role`(필수/양념/고명) 판별의 **1차 근거**다 (설계 4-5).

**없으면** *(2026-08-27 정정 — 어댑터의 폴백을 확인한 결과)*

`derive:group_from_flat` 폴백이 발동해 `is_staple`/`is_seasoning` 플래그와
수량 표현(`약간` `조금`)으로만 판별한다. 시드에 `is_seasoning=true` 가 110종 있으므로
양념은 상당 부분 잡히지만, **고명 판별이 어려워져 `essential` 과다 판정 →
추천 후보가 좁아지는 방향으로 실패**한다.

> **치명적이지는 않다.** 이전 판에 "4.4만 건 재크롤링"이라고 적었으나 과장이었다.
> 다만 **나중에 받으려면 재크롤링이므로 지금 받는 것이 훨씬 싸다.**

**확인 방법**
```bash
# 수집된 raw_json 샘플 하나를 열어 그룹 정보가 있는지 본다
python3 -c "import json;print(json.dumps(json.load(open('샘플.json')),ensure_ascii=False,indent=2))" | head -60
```

**제안** — `recipe_ingredient_raw.group_name` 에 원문 그대로 저장. 가공하지 않는다.

**기한** — 즉시. 크롤링이 더 진행될수록 재수집 비용이 커진다.

---

</details>

---

### ✅ C-2. ~~카테고리 4축 원문을 `raw_json` 에 담고 있는가~~ — **해결. 요청 불필요**

> **실데이터로 확인됨.** `categories` 는 **이미 수집되고 있다.** 다만 4축이 아니라
> **자유 태그**다 — `["칠리새우덮밥","새우덮밥","칠리새우","새우레시피","덮밥레시피","한그릇요리","집밥레시피"]`.
>
> **4축(`dish_type`·`situation`·`main_ing_cat`·`method`)은 원본에 존재하지 않는다.**
> 그래서 설계가 `cuisine` kNN 분류로 방향을 틀었다 (01 2-3-1 · 6-4-5-1).
> 크롤러에 요청할 것이 없다.

<details><summary>원래 이슈 (기록용)</summary>



**이슈**
만개의레시피는 **종류별 / 상황별 / 재료별 / 방법별** 4축으로 분류한다 (설계 2-3-1).
이 값들이 `raw_json` 에 있으면 파싱은 **언제든 나중에** 할 수 있다.

**없으면**
파싱은 미뤄도 되지만 **수집은 미룰 수 없다.** 안 긁으면 재크롤링이다.

**제안** — 페이지의 카테고리 영역만이 아니라 **크롤러가 받은 것을 통째로**
`raw_json` 에 넣는다. 구조화는 나중에 어댑터가 한다.

> **C-1 보다 C-2 가 더 중요하다.** `raw_json` 에 원본이 있으면 `group_name` 이든
> 분류 4축이든 **언제든 재파싱**할 수 있다. 원본이 없으면 어떤 필드든 재크롤링이다.

**기한** — 즉시. C-1 과 같은 작업에서 함께 처리 가능.

---

</details>

---

### 🟡 C-3. 원본 카테고리 체계가 실제로 어떤가

**이슈**
설계 문서 2-3-1 의 카테고리 값 목록은 **기억 기준 추정**이다. 실제와 다를 수 있다.

**확인 필요**
1. 4축이 다 있는가, 하나만 있는가
2. 실제 값이 문서와 같은가
3. **종류별 축에 `양식`·`퓨전` 이 실제로 섞여 있는가** ← `cuisine` 파생 설계의 전제

**결정 안 하면** — `cuisine` 분류 규칙 초안(`seeds/cuisine_taxonomy.yaml`)을 다시 짜야 한다.

**기한** — 1주차. 크롤링 샘플 100건이면 확인 가능.

---

### 🔴 C-5. `reviews` 를 원문 그대로 수집하고 있는가 *(v2.1 신규 — 최우선)*

## ✅ 해결 — 전량 적재 확정 *(2026-09-02)*

**크롤 데이터가 도착해 협의가 불필요해졌다.** "무엇을 받을지"가 아니라
"받은 것을 어떻게 적재할지"가 됐고, 그것은 우리 결정이다.

### 실측 (46,552건 전수)

| | |
|---|---|
| 후기 있는 레시피 | **46,353 / 46,552 (99.6%)** |
| 총 후기 | **627,610건** · 레시피당 평균 13.5 (중앙값 4 · 최대 1,079) |
| 고유 작성자 | **177,875명** |
| 2개 이상 쓴 사람 | 73,465명 (41.3%) · 5개 이상 23,801명 |
| (작성자, 레시피) 쌍 | **552,144** |
| **아이템당 상호작용** | **11.86건** |

> 🔴 fixture 3건으로 추정했던 2.0건의 **6.7배**다. 극소 표본 추정이 크게 빗나갔다.

### 🔴 딥러닝 판정이 흔들린다

`03` 이 *"모든 미채택 판정의 근거는 아이템당 상호작용 한 줄"* 이라고 못 박았는데
그 숫자가 바뀌었다. 우리 유저 로그 예상치(0.091)의 **130배**다.

```
K=8    필요 16건 →  11.9건 · 1.3배 부족
K=16   필요 32건 →  11.9건 · 2.7배 부족
```

**K=8 이 손에 닿는다.** 5개 이상 쓴 23,801명만으로도 `07` E-1 의
DeepFM(8,000명)·LightGCN(5,000명) 문턱을 넘는다.

> ⚠️ **다만 후기 작성자는 우리 유저가 아니다.** 냉장고도 온보딩도 없으므로
> `item-item CF` 와 `recipe×ingredient` 는 열려도, **우리 유저에게 개인화하는
> 모델은 여전히 안 열린다.** 이 구분을 리포트에서 흐리면 안 된다.

### 적재 방식

**전량(본문 포함) + 닉네임 해시.** `recipe_review` 신설 (약 200MB).
용량은 근거가 못 된다 — `raw_json` 이 이미 원본 219MB 를 통째로 담는다.

**닉네임은 원문을 저장하지 않는다.** 동의를 받을 수 없는 제3자 정보이기 때문이다.
`HMAC-SHA256(닉네임, salt)` 앞 16hex 를 쓴다 — 복원은 불가하지만 재계산은 되므로
삭제 요청자가 닉네임을 대면 그 행을 찾을 수 있다. salt 는 `.env`.

파싱은 두 형식이 섞여 있어 정규식을 넓혔다 (**99.14%** 성공).
닉네임이 없는 0.86% 는 `author_hash NULL` 로 둔다.

### 재크롤은 불가능하다

후기는 레시피 본문과 달리 **작성자·운영자가 지우면 사라지고**, 차단 위험이 있으며,
크롤링이 크리티컬 패스라 두 번째 자리가 없다. **지금 안 넣으면 영구 소실이다.**


**실측 fixture 3건 모두 `reviews` 배열에 작성자 닉네임 + 타임스탬프 + 텍스트가 들어 있다.**

```
리뷰어A  2026-08-23 06:58:11  (후기 본문 — 저장소에 원문을 싣지 않는다)
리뷰어B  2026-08-21 19:35:47  (후기 본문 — 저장소에 원문을 싣지 않는다)
```
> 🔴 실제 닉네임·후기 원문은 여기 적지 않는다. DB 에도 `author_hash`(HMAC)만 남긴다
> (`scripts/load_recipes.py:72`). 원문은 `raw_data/` 안에만 두고 저장소 밖으로 내보내지 않는다.

이것은 **진짜 `(user, recipe)` 상호작용**이다. 그런데 현재 어댑터는
`transform: count` 로 **개수만 세고 버린다** (`app/services/ingest/sources/mangae.yaml:103`).

#### 왜 최우선인가 — 크롤링 스펙 동결은 로그 스키마 동결과 같은 무게다

4.4만 건 크롤링에서 "reviews 는 무거우니 빼자"가 한 번 내려지면
재크롤 비용 + 차단 위험 + 삭제된 후기 소실로 **사실상 소급 불가**다.
그리고 head 레시피의 후기가 수백 건이면, `03` 이 유저 수 부족으로 기각한
**item-item CF·ALS 가 실유저 0명으로 열린다** (07 부록 Y C-1).

#### 🔑 요청이 아니라 **확인 한 문장**이다

`reviews` 도 **이미 수집되고 있다.** 크롤러의 진행 방식을 바꿀 것이 없다.
그리고 이것으로 열리는 item-item CF 는 **04 의 P0/P1/P2 어디에도 없는 범위 밖**이라
**8주 프로젝트에는 필수가 아니다.**

그럼에도 한 문장을 보내는 이유는 **비대칭** 때문이다.

| | |
|---|---|
| 지금 확인 | 메시지 한 줄 · 크롤링에 영향 0 |
| 나중에 필요해졌을 때 | 4.4만 건 재크롤 — 차단 위험 + 삭제된 후기 소실로 사실상 불가 |

**보낼 문장:**

> "샘플 3건 모두 `reviews` 가 2건씩 오는데 **전량인가요, 첫 페이지만인가요?**
> 그리고 **지금 주시는 필드 구성 그대로 4.4만 건 가시면 됩니다** — 바꿀 것 없습니다."

뒷문장이 실은 더 중요하다 — "후기는 무거우니 빼자"는 판단이 **나중에 조용히 내려지는 것**을 막는다.

- 닉네임은 개인정보다 — 적재 시 **가명화(해시) 규칙**을 A 절 동의 항목과 함께 정한다 (A-5)

---

### 🟡 C-4. 크롤링 완료 예상 시점

**이슈** — 전체 일정의 **크리티컬 패스**다 (설계 2-11 의 8번).

**결정 안 하면**
정규화 트랙(C 산출물), 커버리지 측정, `recipe_feature` 빌드, 모델 학습이 순차로 밀린다.
8주 중 어느 시점에 크롤링이 끝나느냐가 스트레치 목표(식단 플래닝) 착수 가능 여부를 가른다.

**필요한 답** — 예상 완료일 + 예상 레시피 수 + 중간 스냅샷 제공 가능 여부

**제안** — 전량 완료를 기다리지 말고 **1만 건 스냅샷을 먼저** 받는다. 정규화 파이프라인
개발과 커버리지 1차 측정에는 그 정도면 충분하다.

---

# B. 백엔드팀과 협의

### 🔴 B-1. `public` 스키마를 쓰지 않기로 합의

**이슈**
AI 파트 테이블은 `reco` 스키마에 있다. 그런데 테이블 이름이 이렇다.

```
recipe   app_user   event_log   ingredient
```

**극도로 일반적인 이름들**이라 백엔드팀이 `public` 에 `recipe` 를 만들면 충돌한다.

**결정 안 하면** — 양쪽 다 깨진다. 나중에 옮기면 테이블 + 모든 쿼리 + 함수 + 뷰를 전부 수정.

**제안**
```
recodb
├── reco     ← AI 파트 (우리가 생성. 완료)
├── app      ← 백엔드팀 (백엔드팀이 생성. 이름은 협의)
└── public   ← 확장만. 어느 팀도 테이블을 만들지 않는다
```

**주의** — `app` 스키마는 **우리가 만들지 않았다.** 백엔드팀이 직접 만들어야 한다.

---

### 🔴 B-2. 확장은 `public` 한 곳에만 설치

**이슈**
`vector` · `intarray` · `pg_trgm` · `ltree` 를 스키마마다 따로 설치하면
**타입이 스키마별로 달라져 조인·비교가 실패한다.**

**제안** — 확장 설치는 `public` 에만. `search_path` 에 `public` 을 항상 포함.
누가 설치하든 `CREATE EXTENSION ... SCHEMA public` 을 지킨다.

---

### 🟡 B-3. 역할 이름 규칙 — 스키마로 격리되지 않는다

**이슈**
역할(`ROLE`)은 **클러스터 전역**이다. 스키마 분리로 격리되지 않는다.

우리가 쓰는 이름: `reco_app` · `reco_batch` · `reco_ro`

**제안** — 파트 접두어를 강제한다 (`reco_*` / `app_*`). DB 관리자가 이름 대장을 관리.

---

### 🟡 B-4. 유저 계정을 어떻게 연결할 것인가

**이슈**
AI 파트에 `reco.app_user` 가 있고, 백엔드팀에도 유저 테이블이 생길 것이다.
같은 사람인데 두 곳에 있으면 어느 쪽이 SoT 인가?

**선택지**

| 안 | 내용 | 장단 |
|---|---|---|
| ① 완전 분리 | AI 파트는 자체 유저(팀원 + 시뮬)만. 연결 안 함 | 단순. 실서비스 연동 시 재작업 |
| ② FK 참조 | `reco.app_user.external_id` → 백엔드 유저 ID | 연동 쉬움. 백엔드 스키마에 의존 |
| ③ 백엔드 유저 직접 사용 | `reco.app_user` 폐기 | 시뮬 유저(`is_simulated`)를 둘 곳이 없어짐 |

**제안 — ①로 시작하고 `external_id` 컬럼만 미리 뚫어둔다 (②로 가는 문).**
현재 범위에 유저 앱이 없으므로(설계 0-2) 지금 연동할 이유가 없다. 다만 컬럼 하나
추가는 비용이 0이고, 나중에 필요해지면 소급이 어렵다.

**주의** — ③은 안 된다. `is_simulated` 로 시뮬 유저를 격리하는 구조가 무너진다 (설계 2-5).

---

### 🟡 B-5. DB 서버 정보

- 호스트 / 포트 / 접속 경로 (교내망 전용인가, VPN 필요한가)
- 사양 (CPU · RAM · 디스크) — `recipe_feature` 4.4만 행 + 인덱스로 **최소 5GB** 예상
- 백업 정책 — **크롤링 데이터는 재수집 비용이 매우 크다.** 최소 일 1회 덤프 필요
- 동시 접속 제한 — 20명 + 배치 + Grafana

---

# D. DB 관리자와 협의

### 🟡 D-1. 원격 역할·비밀번호 생성

**이슈**
`infra/init/05_roles.sql` 의 비밀번호는 **로컬 개발용**이다 (`reco_app_dev` 등).
이 파일을 원격에 그대로 적용하면 안 된다.

**필요한 것** — `reco_app` · `reco_batch` · `reco_ro` 3개를 다른 비밀번호로 생성.
권한 구성은 `05_roles.sql` 을 참고하되 비밀번호만 교체.

**핵심** — **`reco_app` 에 TRUNCATE 권한을 주지 않는다.** 정규화 재실행이 TRUNCATE
기반이라(설계 4-1), 앱 계정에 권한이 있으면 버그 하나로 크롤링 데이터가 날아간다.

---

### 🟡 D-2. `mlflowdb` 생성 권한

**이슈** — MLflow 백엔드용 별도 DB 가 필요하다. 공유 서버에서 `CREATE DATABASE` 권한이 없을 수 있다.

**대안** — 같은 DB 의 `mlflow` 스키마를 쓰도록 `BACKEND_URI` 조정.

---

### ⚪ D-3. 원격 전환 시점

**제안** — 크롤링 데이터가 나오기 전까지는 로컬로 충분하다.
**크롤링 완료 = 원격 전환 시점**으로 잡는다. 그전에 D-1·D-2 를 끝내둔다.

---

# A. 전체 팀 협의 (1주차 동결)

### 🔴 A-1. 이벤트 로그 스키마 동결

**이슈**
로그 스키마는 **소급이 불가능한 거의 유일한 영역**이다 (설계 결정 4).
3주 뒤에 컬럼이 필요하다는 걸 깨달아도 지난 3주치에는 채울 방법이 없다.

**동결 대상 10개** (설계 3-7 · v2.1 에서 2개 추가)

- [ ] `event_type` 8종의 이름과 의미 — 추가는 가능, **변경·삭제 불가**
- [ ] 학습 라벨 가중치 표 (설계 3-2) — 값은 조정 가능, **이벤트 종류는 고정**
- [ ] `stage_trace` v1 스키마
- [ ] **`event_log.position` 기록** — 없으면 position bias 보정 영구 불가
- [ ] **`recommendation_log.pantry_snapshot`** — 없으면 재현 영구 불가
- [ ] **`recommendation_log.mlflow_run_id`** — 없으면 온·오프라인 대조 불가
- [ ] `app_user.is_simulated` 필터를 모든 지표 쿼리의 기본값으로 강제
- [ ] **impression 을 서버측에서 자동 기록**
- [ ] **`event_log`·`recommendation_log` 의 `session_id` 컬럼** *(v2.1)* — 계약에는 있는데 **DDL 미착지**. 없으면 시퀀스 모델 영구 불가
- [ ] **`candidates[].propensity` 기록** *(v2.1)* — Thompson 슬롯은 **서빙 시점 MC 값이 유일본**(사후 재계산 불가). 없으면 off-policy 평가 영구 불가

> `impression` 이 없으면 랭킹 학습의 negative 샘플이 없어 **모델 학습 자체가 성립하지 않는다.**

**기한** — 1주차 종료 전.

---

### 🟡 A-2. API 계약 동결

**이슈** — 설계 문서 ⑦ 섹션이 아직 미작성이다.

**결정 안 하면** — 엔진 트랙과 대시보드 트랙이 서로를 기다린다.

**기한** — 산출물 D(프로젝트 스켈레톤) 착수 전.

---

# I. AI 파트 내부 결정

### ✅ I-1. Docker 설치 + `make bootstrap` 실행 — **완료 (2026-08-26)**

OrbStack 설치 후 실행 검증을 마쳤다. **SQL 오류 0건, 스모크 13건 전부 통과, p95 30.2ms.**
실행에서만 드러난 버그 3건(locale · ltree · 빈 배열)을 수정했다. 상세는 설계 문서 2-12.

<details><summary>원래 이슈 내용</summary>

### 🔴 I-1 (원문). Docker 설치 + `make bootstrap` 실행

**이슈**
현재 개발 머신에 Docker · PostgreSQL 이 없어 **SQL 을 한 번도 실행해보지 못했다.**

| 검증됨 | 미검증 |
|---|---|
| SQL **문법** (실제 PostgreSQL 파서 · 112 statements) | **실행** — 타입 오류 · 확장 동작 · FK 순서 |
| 함수 본문 4개 문법 | 함수 **결과가 맞는지** |
| 시드 정합성 · 참조 해석 | 실제 적재 |
| Python · YAML · shell 문법 | compose 기동 · Grafana · MLflow |
| — | `DO $$ ... $$` 블록 3개 (plpgsql 은 정적 검증 불가) |
| — | Retrieval 성능 (p95, GIN 인덱스 사용 여부) |

**결정 안 하면** — 미검증 코드가 계속 쌓인다. C·D 를 더 올리면 첫 실행 시 오류가
한꺼번에 터져 원인 분리가 어려워진다.

**기한** — 즉시. 아래 "Docker 타이밍" 절 참조.

</details>

---

### ⚪ I-2. `is_staple` ~~28종~~ **39종** 튜닝

**이슈** — staple 목록은 **튜닝 파라미터**다.

| 너무 많으면 | 너무 적으면 |
|---|---|
| 거의 모든 레시피가 "만들 수 있음"이 되어 Retrieval 이 무의미 | 한식 대부분이 "재료 부족"으로 걸러짐 |

**판단 지표** (설계 4-11)
- 평균 후보 수가 500을 크게 넘으면 → staple 을 줄인다
- 냉장고 8개 재료로 후보가 10개 미만이면 → staple 을 늘린다

**의도적 제외** — `달걀` `버터` `베이킹파우더` `베이킹소다`

**기한** — 크롤링 데이터로 1차 측정 후.

---

### ⚪ I-3. 저신뢰 alias 9개 판정

전부 **다의어**다. 문맥 없이 결정할 수 없다.

| alias | 후보 | 판정 근거 |
|---|---|---|
| `호박` | 애호박 / 단호박 / 늙은호박 | 레시피 문맥 |
| `다짐육` | 소고기 / 돼지고기 | 카테고리 축 |
| `치즈` | 슬라이스 / 모짜렐라 / 체다 | 요리 종류 |
| `오일` | 식용유 / 올리브유 | cuisine |
| `조미료` | 다시다 / 미원 | — |
| `시럽` | 물엿 / 올리고당 | — |
| `밀` | 밀가루 / 밀 (곡물) | — |
| `란` | 달걀 | 거의 확실 |
| `솔` | 부추 (전라 방언) | 다의어 위험 큼 |

**제안** — 크롤링 데이터에서 실제 등장 문맥을 본 뒤 판정. 애매하면
`normalization_queue` 로 내려 사람이 결정.

---

### ⚪ I-4. L4(임베딩 매칭) 유지 여부

**이슈** — 5% 커버리지를 얻는 대신 오탐 리스크를 안는 거래다 (설계 4-4).

**판단 기준** — 1주차 측정 후 **정밀도 0.8 미만이면 제거**한다.

---

### ⚪ I-5. `cuisine` 세분 축 유지 여부

**이슈** — 한국 사이트 크롤링이면 `korean` 이 70~80% 일 가능성이 높다 (설계 2-3-1).

**판단 기준** — 각 fine 값이 **200건 미만이면** `cuisine_taxonomy.active = false` 로 접는다.
`family` 축만으로 운영한다. 스키마 변경은 불필요하다.

---

### 🟡 I-6. 실유저 **100명** 모집 — 우선순위 상향 *(v1.0)*

**기술 스택이 여기서 결정된다.** 설계 6-6 로드맵 기준 50명과 100명 사이에 단절이 있다.

```
유저  50명 × 20요청 = LambdaRank 그룹 1,000  →  과적합 위험. 경계선
유저 100명 × 20요청 = LambdaRank 그룹 2,000  →  학습 가능
```

50명이면 **LightGBM v1 자체가 성립하지 않아** 선형 가중합(v0)으로 프로젝트가 끝난다.
100명을 넘기면 리포트에 "학습 기반 랭킹 모델"을 실을 수 있다.

**필요한 결정** — 모집 시점 / 안내 방법 / **1인당 최소 20회 사용을 어떻게 유도할지** /
개인정보 동의 절차

**제약** — 유저 앱이 범위 밖이므로 **대시보드 디버거가 유일한 수집 경로**다 (설계 0-5 판단 4).
100명이 디버거를 20회씩 쓰게 만드는 것은 UX 설계 문제이지 기술 문제가 아니다.

**기한** — 대시보드 디버거 완성 직후. 늦어질수록 로그 축적 기간이 줄어든다.

<details><summary>원래 항목</summary>

### ⚪ I-6 (원문). 실유저 50명 모집 시점·방법

**이슈** — 유저 앱이 범위 밖이므로 **대시보드의 추천 디버거**가 유일한 로그 유입구다 (설계 0-5 판단 4).

**필요한 결정** — 모집 시점 / 안내 방법 / 최소 참여 횟수 / 개인정보 동의 절차

**기한** — 대시보드 디버거 완성 시점에 맞춰.

</details>

---

### 🟡 I-8. 온보딩 설문 문항 확정 *(v1.1 신규)*

**이슈**
설계 6-3-4 의 **FM 모델이 선호도 설문에 직접 의존**한다. 유저 피처가 없으면
FM 은 아이템 피처끼리의 교차만 보게 되어 LightGBM 대비 이점이 사라진다.

**확정해야 할 것**

| 축 | 현재 스키마 | 결정 필요 |
|---|---|---|
| 맛 | `spicy_level` `sweet_level` `salty_level` (0~4) | 신맛·감칠맛·기름짐도 물을 것인가 (`flavor_vec` 은 6축) |
| 요리 계열 | `pref_cuisines[]` | 복수 선택인가 순위인가 |
| 요리 종류 | `pref_dish_types[]` | 문항 수 |
| 제약 | `max_cook_minutes` `skill_level` `diet_type` | |
| 재료 | `user_ingredient_pref(source='onboarding')` | **선호/기피 재료를 몇 개나 고르게 할 것인가** |

> ✅ **6축 유지로 확정 (2026-09-02).** 저장은 6축으로 한다 — 뒤 3축은 설문이 아니라
> **레시피 선택에서 파생**되므로 온보딩 시간이 늘지 않는다.
> **다만 실제 점수 계산에서 앞 3축만 쓸 수도 있다.** 어느 축을 쓸지는 W3 쌍대비교
> 학습에서 정한다. 이 결정은 소급 가능하다 — `flavor_vec` 은 시드에서 재생성되고
> 축을 빼는 것은 슬라이스만 바꾸면 된다.
> 반대로 3축으로 저장했다가 6축이 필요해지면 **유저를 다시 모아야 한다.**
>
> ⚠️ **`flavor_vec` 은 6축인데 설문은 3축만 묻는다.** 나머지 3축(신맛·감칠맛·기름짐)을
> 어떻게 채울지 정해야 `f_taste` 계산이 성립한다.

## ✅ 해결 방향 확정 — **대표 레시피 선택형** *(2026-08-27)*

문항을 늘리는 대신 **"이 중에 좋아하는 요리를 고르세요"** 방식을 쓴다.

```
레시피 전체 → 임베딩 클러스터링 K=20 → 각 클러스터 대표 1개
  ↓
설문에서 20개 제시 → 유저가 3~5개 선택
  ↓
선택한 레시피들의 flavor_vec 평균 = 유저 taste_vec (6축 전부)
```

| | 척도 문항 방식 | **대표 레시피 선택형** |
|---|---|---|
| 6축 커버 | ❌ 3축만 | ✅ **전부** |
| 문항 수 | 6개 이상 필요 | **1개** |
| 유저 부담 | "감칠맛 선호도는?" 답하기 어려움 | 사진 보고 고르기 |
| 전제 | 없음 | **레시피 임베딩** (6-4-3 에서 채택됨) |

**전제였던 임베딩이 이미 채택되어 추가 비용이 없다.**

### ✅ 남은 결정 — **측정으로 확정** *(2026-08-31 · scripts/bench/onboarding_size.py)*

`taste_vec = 고른 레시피들의 flavor_vec 평균` 이므로 **표본이 늘면 랜덤 오차가 1/√n 으로 준다.**
근거 없는 숫자를 하나도 만들지 않고 노이즈를 줄이는 유일한 방법이다.

| 선택 개수 | 랜덤오차 0.15 | 0.30 | 0.50 |
|---|---|---|---|
| 3 *(원안)* | 0.213 | 0.123 | 0.091 |
| **8** | **0.348** | **0.176** | 0.104 |
| 12 | 0.374 | 0.209 | 0.116 |
| 20 | 0.418 | 0.228 | 0.137 |

**8~12개가 무릎이다.** 20개까지 가도 오르지만 증가폭이 꺾이고 유저 부담이 는다.

#### 🔴 그런데 개수로 못 잡는 것이 있다 — 체계 편향

| 선택 개수 | 편향 0.0 | 0.2 | 0.4 |
|---|---|---|---|
| 3 | 0.132 | 0.124 | 0.132 |
| 8 | 0.175 | 0.179 | 0.184 |
| 20 | 0.238 | 0.242 | 0.234 |

**가로로 값이 변하지 않는다.** 랜덤 오차는 평균으로 줄지만 체계 편향은 안 준다 —
`ingredient_flavor.yaml` 을 **한 사람이 매겼으므로** 일관된 편향이 있을 가능성이 크다 (I-15).

#### 척도 문항이 그것을 깬다 (체계 편향 0.4 · 선택 8개)

| 척도 비중 | 3축만 척도 | 6축 전부 |
|---|---|---|
| 0.0 (선택만) | 0.173 | 0.174 |
| **0.5** | 0.218 | **0.269** |
| 1.0 (척도만) | 0.179 | 0.236 |

**절반쯤 섞을 때가 최고다** (0.173 → 0.218, +26%). 척도 문항은 `flavor_vec` 을 거치지 않는
**독립 관측**이라 그 편향이 묻지 않는다. 둘은 대체재가 아니라 **보완재**다.

> 6축 전부를 척도로 받는 것이 더 좋지만 **"감칠맛 선호도가 몇 점이세요?"는 답할 수 없다.**
> 3축(매움·단맛·짠맛)이 현실적 상한이다.

### ✅ 정규화 커버리지 실측 — 사전이 아니라 단위가 문제였다 *(2026-09-02)*

크롤 46,552건을 적재하고 전량 측정했다. **기존 79.4% 는 fixture 3건 기준이라 무의미했다.**

| 단계 | mention | 무엇을 했나 |
|---|---|---|
| 최초 실측 | **69.2%** | 크롤 전량 (452,374 언급) |
| 도구 필터 | 70.0% | `seeds/non_ingredient.yaml` 65종 제외 |
| **단위 보강** | **78.1%** | `measure_units.yaml` 에 실사용 단위 추가 |

#### 🔴 미매칭의 21.3% 는 사전 문제가 아니었다

미매칭 30,854종·127,132건을 성격별로 갈랐다.

```
수량이 안 떨어짐   6,299종 ·  27,071건 (21.3%)   ← P2 단위 시드 누락
도구 의심         1,377종 ·   6,321건 ( 5.0%)   ← 재료가 아님
재료 후보        23,178종 ·  93,740건 (73.7%)   ← 진짜 사전 보강 대상
```

`양파 1개` 는 되는데 **`소금 1꼬집` 이 통째로 미매칭**이었다.
단위 시드에 `꼬집`(2,810건)·`숟가락`(**15,775건**)·`줄`(1,391)·`봉지`(1,702)·
`cm`(972)·`Ts`(2,330) 가 없어서다. 사전을 늘릴 문제가 아니라 여기를 채울 문제였다.

> 🔴 **`COUNT_UNITS` 가 코드에 하드코딩돼 있던 것이 원인이다.**
> 시드에서 읽도록 고쳤다 — 단위가 늘 때 코드를 고치게 두면 안 된다.

#### 조리도구는 사전에 넣지 않고 걸러낸다

만개의레시피는 재료 목록에 도구를 섞는다 — `도마`×2,259 · `조리용나이프`×1,784 ·
`냄비`×1,124 · `요리스푼`×1,071.

**`ingredient` 에 등록하면 안 된다.** 그러면 레시피가 "도마를 재료로 쓴다"가 되어
`essential_ids` 에 들어가고 Retrieval 이 망가진다. 매칭 대상에서 빼는 것이 맞다.

`ParsedIngredient.is_non_ingredient` 로 **표시만 하고 지우지 않는다** —
`position` 이 어긋나면 안 되고, 무엇을 걸렀는지가 데이터 품질 지표다.

#### 남은 것 — 누적 빈도가 알려주는 것

```
상위    438종 → 미매칭의 50%
상위  2,506종 → 미매칭의 70%
상위 18,141종 → 미매칭의 90%
```

**30,854종을 다 볼 필요가 없다.** 상위 438종이면 절반이다.
`04` 의 검수 계획("20명 × 100건 = 2,000건")을 빈도순 절단 방식으로 다시 잡아야 한다.

목록은 `scripts/bench/out/unmatched.tsv` (재생성: `scripts/bench/unmatched_dump.py`).

### ✅ pantry 소진/폐기 1비트 — 확정 *(2026-09-02 · S0 ⑤)*

**"다 썼어요" 와 "상해서 버렸어요" 는 부호가 반대인 신호다.** 합치면 영원히 못 나눈다.
소비기한 낭비율 · `shelf_life_days` 추정 검증 · 재료 소진 시퀀스가 전부 여기 달려 있다.

#### UI — 행별 버튼 2개 + 축소 모달

삭제 버튼을 **"다 썼어요" / "버렸어요"** 둘로 나눈다. **추가 클릭이 0** 이다 —
어차피 눌러야 하는 버튼이 두 개인 것뿐이다. 라벨률 85% 추정.

전체 교체(`PUT`)로 조용히 사라진 건은 저장 후 **1회 모달**로 묻고,
건너뛰기 1클릭을 둔다. 병목은 구현이 아니라 마찰이다.

#### 🔴 사유를 3값으로 — 2값이면 NULL 이 두 가지를 뜻한다

```
NULL      물어보지 않았다 (버튼 없던 시절 · 조용히 사라진 건)
unknown   물었는데 유저가 건너뛰었다
consumed / discarded
```

스킵이 **폐기 쪽에 몰리면**(버린 걸 밝히기 싫어서) MNAR 이라,
이 둘을 못 가르면 **낭비율의 상한·하한조차 못 낸다.** 지금은 공짜, 나중은 소급 불가.

#### API — UI 방식을 서버가 알지 않는다

`PUT /v1/users/{id}/pantry` 바디에 선택 키 하나만 더한다.

```
removed: [{ingredient_id, reason}]
```

**버튼으로 받든 모달로 받든 클라이언트 사정이고 서버는 사유만 받는다.**
그래야 UI 를 바꿔도 API 가 안 바뀐다. 새 엔드포인트는 표면을 늘리고,
`POST /v1/events` 우회는 `event_log` 에 `ingredient_id` 가 없어 불가능하다.

#### 기대치는 낮게

유저 100명 × 8주면 **약 600건**, 폐기 20% 면 **120~140건**.
**피처가 아니라 검증 지표다** — 전체 낭비율 하나(±3%p)는 나오지만
재료 536종별 검증은 안 된다.

### ✅ 개인정보 동의 — **2단 동의(C안) 확정** *(2026-09-01 · S0 ③)*

> ⚠️ **법률 자문이 아니다.** 아래는 검토용 초안이고 **제출 전 사람이 확인한다.**
> `[파기일]`·`[연락처]` 는 팀이 채운다.

#### 문구 (초안)

```
「냉장고 레시피 추천 — 참여 안내」

부트캠프 최종 프로젝트입니다. 참여하시면 닉네임, 온보딩 답(맛 선호·고른 레시피·가구원 수),
알러지·기피 재료, 냉장고에 넣고 뺀 재료와 시점, 누른 기록(클릭·저장·조리·평가)이
저장됩니다.

알러지는 건강에 관한 정보이며 추천에서 그 재료를 빼는 데만 씁니다.

목적은 추천 개선과 프로젝트 리포트뿐이고 외부에 제공하지 않습니다.
리포트에는 개인이 특정되지 않는 집계값만 싣습니다.

닉네임은 [파기일] 에 지웁니다. 남는 기록은 닉네임 없이 보관하나,
참여자가 적어 완전한 익명은 아닙니다(가명처리).

삭제는 [연락처] 로 요청하실 수 있습니다.

☐ (선택) 종료 후에도 기록을 연구 목적으로 보관하는 데 동의합니다.
```

#### 왜 2단인가

**필수를 좁혀야 100명이 모인다.** 100명은 LightGBM 이 성립하는 하한선이고,
무기한 보관을 필수로 걸면 동의율이 떨어진다.
선택 체크 하나가 **8주 이후를 연다** — 없으면 파기일에 전부 지워야 한다.

#### 🔴 "익명" 이라고 쓰지 않는다

`07` 이 **"50~100명이면 재식별이 자유롭다"** 고 못 박았고,
`recommendation_log.pantry_detail` 과 `allergy_snapshot` 이 알러지·재료 값을
로그에 복제한다. 정확한 표현은 **가명처리**다.

#### 🔴 삭제 요청은 `DELETE` 가 아니라 가명화로 처리한다

`app_user` 행을 지우면 **CASCADE 가 로그 원장까지 지운다**
(`event_log`·`recommendation_log`·`pantry_item` 전부 `ON DELETE CASCADE`).
`username` 만 가명화해야 분석 데이터가 살아남는다.

#### 스키마

`app_user.consent_at` · `consent_version` · **`consent_research BOOLEAN`**(신규).
`consent_version` 은 날짜가 아니라 **`'v1-min'`**(필수만) / **`'v1-res'`**(연구 보관까지).
어느 문구에 동의했는지가 값만 보고 판별된다.

CHECK 두 개로 모순 상태를 막는다 — 동의 없이 버전만 있거나,
동의 없이 연구 보관에 체크된 상태는 들어가지 않는다.

### ✅✅ 온보딩 최종 확정 *(2026-09-01 · S0 ②)*

| 문항 | 형식 | 필수 | 소요 |
|---|---|---|---|
| **알러지** | 그룹 **10칩** + **재료명 직접 추가**(자동완성) | ✅ 필수 | ~20초 |
| **기피 재료** | **재료명 자동완성** 3개까지 | 스킵 가능 | ~20초 |
| **레시피 선택** | 제시 20개 중 **3개** | ✅ 필수 | ~27초 |
| **맛 척도** | **3축** (매움·짠맛·단맛) | ✅ 필수 | ~15초 |
| **가구원 수** | 숫자 1~5+ | 선택 | ~5초 |

**합계 약 1분 27초.** 3분이 100명 모집의 실질 상한이므로 여유가 있다.

#### ⏳ 파기일 시한 — 유저 모집 전까지

동의문의 `[파기일]`·`[연락처]` 는 **지금 비워 둬도 무방하다.** 다만 시한이 있다.

```
지금          빈칸 허용 — 타 파트와 협의 중
🔴 유저 모집 전   반드시 채워야 한다 (참여자가 읽고 동의하는 문서다)
모집 시작 후     ✗ 못 바꾼다 — 재동의 대상자를 다시 모을 수 없다
```

지금 5주차이고 모집이 W6 이므로 **약 1주 여유**가 있다.

#### 🔴 저장 시 `severity='allergy'` 를 반드시 명시한다

DB 기본값이 `'avoid'` 다. 생략하면 **그룹 확산 경로(③')가 조용히 꺼져**
본인이 적은 재료만 막히고 같은 알러젠 그룹은 통과한다.

```
severity 명시    아몬드 차단 · 호두 차단 · 잣 차단
severity 생략    아몬드 차단 · 호두 통과 · 잣 통과   ← 실측 (09-02)
```

에러가 나지 않아 더 위험하다 — 알러지가 등록돼 있고 본인이 적은 재료도
잘 걸러지므로 겉보기엔 정상이다. 백엔드 연동 시 반드시 전달할 것.

#### 알러지 — 그룹과 재료명을 **둘 다** 받는다

안전 관련이라 이중화한다 (2-2 원칙). 설계가 이미 4갈래 차단을 지원하므로
**스키마 변경이 없다** — 그룹 칩은 ltree 전개(②)와 `allergen_group`(③),
직접 추가한 재료명은 직접 지정(①)과 그룹 확산(④)을 탄다.

한쪽만 받으면 각각 구멍이 난다. **그룹만** 받으면 그룹이 안 붙은 339종을 놓치고
(실측: `allergen_group` 이 NULL 인 재료가 ~~525종 중 339종~~ **536종 중 350종**),
**재료명만** 받으면 견과류 알러지인 사람이 호두만 적고 아몬드를 빠뜨린다.

현재 그룹 분포 — `fish` 49 · `gluten` 32 · `soy` 29 · `dairy` 23 · `shellfish` 22 ·
`nut` 11 · `egg` 6 · `sesame` 6 · `buckwheat` 5 · `peach` 3.

#### 기피 재료 — 맛으로 대체되지 않는 유일한 축

**선호 재료는 뺐다.** 맛 척도가 상당 부분 대체한다.
**기피는 뺄 수 없다** — 대체 수단이 없기 때문이다.

> 고수를 못 먹는 사람에게 매운 고수 쌀국수를 추천하면 **맛은 맞는데 안 먹는다.**
> 오이·가지·고수·향신료는 알러지가 아니라 기피이고, 맛 6축으로는 표현되지 않는다.

재료명 자동완성으로 받는다. **표기 문제가 없다** — 시드 536종에서 고르게 하므로
"고수/코리앤더" 같은 이표기가 애초에 생기지 않는다.
흔한 기피 12종(고수·오이·가지·당근·피망·파프리카·셀러리·깻잎·미나리·브로콜리·
옥수수·완두콩)이 **전부 사전에 있음을 확인했다**.

`user_ingredient_pref` 에 `source='onboarding'` · `score=-0.8` 로 적재한다.
`f_ing_pref`(w=0.11) 의 입력이 된다.

#### ✅ 가구원 수 — 받는다 (선택 항목)

`user_preference.household_size` 와 `recipe.servings` 가 **양쪽 다 이미 스키마에 있고**,
크롤 원문에도 `serving` 이 실측된다 (fixture 3건: `'4인분'`·`'2인분'`·`'1인분'`).

> 🔴 **지금은 이 값을 쓰는 피처가 없다.** 17종 어디에도 가구원 수가 안 들어간다.
> 그래도 받는 이유는 **나중에 받을 수 없기 때문**이다 — 수료 후 재접촉이 불가능해
> 온보딩 항목은 사실상 전부 소급 불가다. 비용은 5초다.

쓸 자리는 보인다 — `servings` 파싱만 하면 `f_portion_fit` 을 붙일 수 있고,
`f_expiring` 도 정교해진다(1인 가구는 재료가 오래 남아 소비기한 압박이 다르다).

**선택 항목으로 둔다.** 1인 가구 여부는 밝히기 꺼릴 수 있고,
`household_size` 가 이미 nullable 이다.

#### ✅ 맛 척도 — **3축 확정** (MVP)

교차 측정 결과 (`scripts/bench/onboarding_grid.py` · 체계편향 0.4 · 랜덤오차 0.3):

```
레시피 3개 + 척도 3축   0.633   42초
레시피 8개 + 척도 3축   0.737   87초      ← 이전 확정안
레시피 3개 + 척도 6축   0.787   57초      ← 더 짧고 더 좋다
```

단가가 다르다 — **레시피 1개는 9초인데 척도 1축은 5초**다.
그리고 척도는 **체계 편향을 깨는 유일한 수단**이라, 레시피를 아무리 많이 골라도
"내가 된장 감칠맛을 일관되게 높게 본다" 같은 편향은 평균으로 줄지 않는다.

**결정: 3축(매움·짠맛·단맛).** MVP 단계에서 유저에게 묻는 문항을 늘리지 않는다.
6축이 측정상 낫다는 것을 알고 고른 것이므로, 리포트에 그렇게 쓴다 —
*"성능을 알고도 MVP 범위로 3축을 택했다"* 가 *"3축이 최선이라 믿었다"* 보다 정확하다.

##### 3축이면 나머지 3축은 어디서 오나

`taste_vec` 은 `flavor_vec` 과 축을 맞춰야 하므로 **6차원 그대로**다.
매움·짠맛·단맛은 **척도 답 + 레시피 평균**을 섞고,
신맛·감칠맛·기름짐은 **레시피 평균만** 쓴다.

즉 뒤 3축은 체계 편향이 그대로 남는다 — "내가 된장 감칠맛을 일관되게 높게 본다"가
평균으로 안 줄어든다. `f_taste` 품질이 축마다 다르다는 뜻이고,
나중에 축별 기여도를 볼 때 이 비대칭을 기억해야 한다.

##### 🔴 늘릴 거면 **유저 모집 전**에 늘려야 한다

3축 → 6축은 15초짜리 변경이지만, **모집 중에 바꾸면 두 집단이 생긴다** —
먼저 온 유저는 3축, 나중 유저는 6축이라 `taste_vec` 품질이 다르다.
그러면 학습에서 두 집단을 같이 쓸 수 없고, 나눠 쓰기엔 각각 표본이 부족하다.

**결정 창은 W6(유저 모집 시작) 까지**다. 그 전에 마음이 바뀌면 싸고, 그 후엔 비싸다.

### ~~✅ 확정된 설문~~ → **폐기 (2026-09-01)**

> 🔴 **아래 블록은 낡았다.** 위 「온보딩 최종 확정 (S0 ②)」이 최종이다.
> 선택 개수는 **8개가 아니라 3개**, 척도는 6축이 아니라 **3축**이다.
> 같은 문서에서 둘 다 ✅ 로 표시돼 있어 기능명세 대조에서 발견됐다 (09-02).
>
> **버린 것을 기록해 둔다** — 8개가 3개보다 `taste_vec` 랜덤오차를 63% 줄인다.
> 그럼에도 3개를 택한 이유는 온보딩 소요(~27초 vs ~72초)이고, 3분이
> 100명 모집의 실질 상한이기 때문이다. **정확도를 시간과 바꾼 것이다.**
>
> ⚠️ 되돌릴 창은 **W6(유저 모집 시작)** 까지다. 모집 중에 바꾸면 두 집단이 생겨
> 학습에 같이 못 쓴다.

```
1. "이 중 좋아하는 요리를 고르세요"   20개 제시 → 8개 선택    (6축 전부, 간접)
2. 매운맛 선호   ○○○○○  0~4                            (독립 관측)
3. 단맛 선호     ○○○○○
4. 짠맛 선호     ○○○○○
```

- [x] 제시 20개 · **선택 8개** *(3개 → 8개, 랜덤오차 63% 개선)*
- [x] **척도 문항 3축 병행** *(체계 편향을 깨는 유일한 수단)*
- [ ] 알러지 · 식이제약 · 조리시간 상한은 **별도 문항 필수** (선택형으로 못 받음)
- [ ] 선호/기피 **재료**를 몇 개나 고르게 할 것인가
- [x] **`pref_dish_types` 는 이미 있다** — 새로 받을 것이 아니라 `f_dish_type`(w=0.00) 을
      **켜는** 문제다. 쌍대비교 블록 학습에서 켤지 결정한다 (01 2-5-1)
- [ ] ~~식감(바삭·쫄깃) 축 추가~~ — 🔴 **보류.** 맛 6축과 독립적인 좋은 축이지만
      **레시피 쪽 짝이 없다.** 식감은 조리법이 결정하는데(감자: 삶으면 부드럽고 튀기면 바삭)
      재료 집계로는 원리적으로 못 잡는다. LLM 배치 라벨링이 열리면 재검토 (I-15)

**소급 비용** — `onboarding_version` 컬럼이 있어 버전 관리는 되지만,
**문항이 바뀌면 이전 유저 데이터와 축이 안 맞는다** (설계 2-5).
100명을 모은 뒤 문항을 바꾸면 그 데이터가 반쯤 버려진다.

**기한** — 유저 모집(I-6) 시작 **전**.

---

### 🟡 I-9. 식재료 API 확보 *(v1.1 신규)*

**이슈**
`ingredient.nutrition_100g` 와 `recipe_feature.nutrition` 이 **현재 비어 있다.**
채워지지 않으면 ④ MealPlan 의 kcal 제약이 동작하지 않는다 (설계 6-7).

**용도를 정확히** — 식재료 API 는 **재료 목록이 아니라 영양정보 소스**다.
식약처 DB 는 "식품"이지 "재료"가 아니라 `조리된 김치찌개` 같은 완제품이 섞여 있다
(`seeds/00_README.md` 경고). 그대로 넣으면 사전이 오염된다.

**확인 필요** — 어떤 API 인가 / 인증·쿼터 제한 / 재료명 매칭 가능 여부(우리 536종과) /
`kcal · carb · protein · fat · sodium` 제공 여부

**기한** — ④ MealPlan 착수 전. 스트레치이므로 여유 있음.

---

### 🟡 I-11. `cuisine` 라벨 200건 확보 *(2026-08-27 신규)*

**이슈**
크롤링 `categories` 가 4축이 아니라 자유 태그로 확인되어(설계 2-3-1), `cuisine` 을
원본에서 얻을 수 없다. 보완책으로 **임베딩 kNN 분류**를 채택했는데(6-4-5-1),
**라벨 200건이 전제**다.

**필요한 것** — 레시피 200건에 `cuisine` 부여
```
korean · chinese · korean_chinese · japanese · italian ·
american · mexican · thai · vietnamese · fusion · (모르겠음)
```

**비용** — 1건 5초 · 20명 × 10건 = **2분**. W4 검수 스프린트에 끼워넣는다.

**없으면** — kNN 이 동작하지 않고 `cuisine` 은 시그니처 재료 규칙으로만 채워진다
(커버리지 크게 하락).

---

### ⚪ I-12. 재료 대체 측정 결과 판단 *(2026-08-27 신규)*

**이슈**
재료 대체 자동 판정은 **구현 전에 측정**하기로 했다 (설계 6-4-6).
라벨은 확보했다 — [`seeds/substitutable_pairs.yaml`](../seeds/substitutable_pairs.yaml)
positive 69쌍 · negative 31쌍.

**판단 시점** — 임베딩 파이프라인 완성 후 (W5)

**판단 기준**
- 정탐 · 오탐 분포가 겹치면 → **자동 판정 불가.** 검수 큐 후보 제안으로만
- negative 가 "공기 멀다 + 의미 가깝다" 사분면에 몰리면 → 규칙으로 사용 가능

**결과가 나빠도 리포트 소재가 된다** — 4-4-1 과 같은 형태의 정직한 결론이다.

---

### ✅ I-13. 쌍대비교 라벨 600건 — **확정 (2026-09-03)**

> **① 20명 × 5분 요청 가능 확인.** 검수 스프린트(300종 75분)와 같은 날에 묶는다.
>
> **② 쌍 추출은 블록별 층화**로 확정했다 — 점수 차가 작은 쌍만 뽑으면
> 어떤 블록의 차분이 0 이 되어 **그 가중치가 식별되지 않는다.**
> 특히 `f_expiring`(0.15)은 피처가 하나뿐이라 대체할 것이 없다.
> 블록마다 120쌍씩 보장하고, 냉장고 시나리오에 임박 재료를 필수 포함한다.
> 상세는 `docs/draft/05_작업분담_결정사항.md` D-18.
>
> **③ 수집은 구글폼으로 한다** — C 트랙이 화면을 만들지 않는다(개발 0일).
> 쌍 추출 스크립트가 폼에 붙여넣을 TSV 를 바로 내보낸다. 문항에는 레시피 이름만
> 아니라 **재료·조리시간·부족 재료 수**를 같이 싣는다 — 이름만으로는 판단이 안 된다.
> 크롤에 이미지가 0건이라 텍스트로 낸다.
>
> ⏳ **실제 쌍은 `recipe_feature` 가 채워진 뒤(A-4 완료 후)에 뽑는다.**
> 지금은 0행이라 차분을 계산할 수 없다. 스크립트는 그전에 만들어 둘 수 있다.

### ~~🟡 I-13. 쌍대비교 라벨 600건 확보~~ *(v1.9 · 아래는 근거)*

**요청: 팀원 20명에게 각 30쌍, 1인당 5분.** W4 검수 스프린트와 같은 날에 묶는다.

#### 왜 필요한가

설계 5-2-2 의 가중치는 **검증되지 않은 추정치**이고, 그것이 이 프로젝트의 가장 약한
지점이다. R9 ablation 은 "바뀌는가"만 답하고 "나아지는가"는 답하지 못한다 — 정답이
없기 때문이다.

**쌍대비교가 정답을 만든다.** "A 와 B 중 어느 쪽을 만들겠는가"는 척도가 필요 없고,
우리가 배우려는 것이 정확히 순서이므로 라벨이 목적에 직접 맞는다.

#### 얼마나 필요한가 — 측정됨

```
    pairs   20명 기준     전체 11피처   5블록 축약
      200    10쌍/인        0.804        0.880
      600    30쌍/인        0.874       ★0.926      ← 목표
     3000   150쌍/인        0.936        0.964

    무작위 가중치 0.542 · 균등 0.670 · 정답 1.000  (NDCG@10)
```

#### 결정해야 할 것

| # | 질문 | 기본안 |
|---|---|---|
| 1 | 20명에게 5분을 요청할 수 있는가 | W4 검수 스프린트(25분)에 5분 추가 |
| 2 | 쌍을 어떻게 뽑는가 | 점수 차가 작은 쌍 우선. 🔑 *(v2.1)* **`f_expiring` 차분 0 인 쌍 비율을 사전 점검** — 높으면 임박소진 블록이 학습 불가 (01 5-2-6) |
| 3 | "둘 다 아니다" 를 허용하는가 | **허용.** 억지 선택은 노이즈다. 학습에서 제외 |
| 4 | 냉장고를 고정하는가 | **고정하되** 🔑 *(v2.1)* **시나리오 2~3개로 나누고, 각 시나리오에 D-3 임박 재료 2~3개 필수 포함** — 아니면 가중치 0.15 짜리 임박소진 블록이 통째로 식별 불가 (01 5-2-6) |
| 5 | 300쌍 미만이면 | 5블록도 포기, 직관값 유지 + 리포트에 "미검증" 명시 (04 컷라인) |
| 6 | 🔑 *(v2.1 신규)* 라벨 일관성을 재는가 | **잰다 — 쌍의 5% 를 중복 출제**해 같은 답이 나오는 비율을 잰다. v0→v1 전환 트리거가 "라벨 일관성 × 라벨 수"이므로(01 5-2-6) **이 값 없이는 전환 판단 자체가 불가능** |

**부수 효과:** 같은 화면이 I-8 의 온보딩(대표 레시피 선택형)이 되고,
W6 유저 모집이 실패해도 LightGBM 학습 데이터가 남는다.

---

### ✅ I-15. `flavor_vec` 산출 방법 — **확정 (2026-09-02)**

> **시드 결함 2건을 고쳤다.** 유저 모집 전이라 소급 비용이 없었다.
>
> | | 전 | 후 | 근거 |
> |---|---|---|---|
> | 식용유 기름짐 | `0.60` | **`1.00`** | 참기름 0.90 보다 낮을 이유가 없다. 참기름은 향·감칠맛이 더할 뿐이고 기름짐 축은 순수 유지가 최고다. LLM 교차검증도 1.00 |
> | 멸치 | 1종 뭉침 | **3종 분리** | 쓰임이 다르면 맛도 다르다 |
>
> ```
> 국물용멸치  짠맛 0.20 · 감칠맛 0.90   우려내고 건진다 — 짠맛은 국물에만
> 잔멸치      짠맛 0.80 · 감칠맛 0.75   그대로 먹는다 — 짠맛이 입에 직접
> 멸치        짠맛 0.45 · 감칠맛 0.85   총칭. 어느 쪽인지 모를 때
> ```
>
> 🔴 **총칭 `멸치` 가 레시피 원문에서 가장 흔하다.** 그래서 중간값을 뒀다 —
> 한쪽으로 밀면 절반이 틀린다. 사전(`ingredient.csv`)에는 3종이 이미 있었고
> **맛 벡터만 뭉쳐 있었다.**

### ~~🔴 I-15. `flavor_vec` 산출 방법~~ *(v2.5 신규 · 아래는 결정 과정)*

`recipe_feature.flavor_vec REAL[6] NOT NULL` 인데 **4.4만 레시피에 그 값을 어떻게 매기는지가
설계 어디에도 없다** (`grep flavor docs/*.md` → 산출 방법 0건).

`f_taste` 는 가중치 0.16 으로 두 번째로 큰 피처이고, `01` 2-5-1 ①의 측정은
**산출 노이즈가 축 수보다 훨씬 크게 성능을 좌우한다**고 말한다 — 노이즈 0.6 이면
축이 6이든 8이든 10이든 전부 0.08~0.16 으로 뭉친다.

| 후보 | 비용 | 예상 노이즈 | 비고 |
|---|---|---|---|
| **재료 기반 규칙** (고춧가루→매움) | 시드에 6축 추가 · 536종 | 중 | 사람이 536×6 을 매겨야 한다 |
| 제목·설명 키워드 | 낮음 | 높음 | "매콤" 표기가 없는 레시피가 대부분 |
| ko-SBERT → 회귀 | 라벨 필요 | 중 | 필요 라벨 수 미측정 |
| **LLM 배치 라벨링** | 재료 536종 · 55분 | — | ✅ **검증 완료** (아래) — 대체재가 아니라 **검수 플래그** |

#### ✅ LLM 교차검증 — 측정 완료 *(2026-08-31 · `scripts/bench/flavor_llm_agreement.py`)*

**Gemma 4 12B QAT** 를 로컬에서 돌려 수기 시드 `overrides` 33종 전량을 대조했다.
공정성을 위해 **같은 축 정의·같은 의미론**("통상 조리량 투입 시 그 축이 얼마나 서는가",
소금=[0,1,0,0,0,0] 예시 포함)을 프롬프트에 그대로 넣었다.

```
전체 MAE 0.044   중앙값 0.000   |차이|≤0.15  87.9%
```

일치도는 높다. **그러나 계통 편향이 있다:**

| | 매움 | 짠맛 | 단맛 | 신맛 | 감칠맛 | 기름짐 |
|---|---|---|---|---|---|---|
| 부호차 (LLM−수기) | **−0.011** | **+0.052** | **+0.048** | +0.020 | +0.023 | +0.023 |

전체 +0.026. LLM 이 더 높게 매긴 축 50 / 낮게 21 / 동일 127.
**짠맛·단맛을 일관되게 부풀린다** — 값을 그대로 채택하면 전 축이 올라가 변별력이 준다.
매움만 부호가 반대인데, LLM 이 "매움"을 캡사이신 열감으로 좁게 해석하기 때문이다
(후추: 수기 0.35 vs LLM 0.10).

> **판정: 대체재가 아니라 검수 플래그.** 일치하는 87.9% 구간은 새 정보가 없고,
> 어긋나는 12% 가 사람이 볼 지점이다. LLM 값을 시드에 직접 쓰지 않는다.

**실제로 걸러낸 결함 2건:**

- 🔴 **`식용유` 기름짐 0.60 → 수정 필요.** 참기름 0.90 보다 낮을 이유가 없다.
  참기름은 향과 감칠맛이 더할 뿐, **기름짐 축 자체는 순수 유지가 최고여야 한다.**
  LLM 은 1.00 을 줬고 그쪽이 맞다. 내부 일관성 오류다.
- 🟡 **`멸치` 짠맛 0.35 vs 0.80 — 명세 결함.** 국물용 마른멸치와 볶음용 잔멸치가
  한 항목에 뭉쳐 있어 값이 정해지지 않는다. **항목을 갈라야 한다.**

**비용은 제약이 아니다:** 건당 3.32s → 재료 1,000종 55분, 5,000종 4.6시간.

<details><summary>실행 환경 (재현용)</summary>

| 항목 | 값 |
|---|---|
| 모델 | `gemma4:12b-it-qat` (Gemma 4 12B Unified, QAT) |
| 라이선스 | **Apache 2.0** — 게이팅 없음 (Gemma 3 의 `gemma` 라이선스에서 변경) |
| 크기 | 6.66 GiB (VRAM 실사용 7.64 GB) |
| 런타임 | ollama **0.33.0 이상 필수** — 0.23.2 는 `gemma4` 매니페스트를 HTTP 412 로 거부 |
| 하드웨어 | M4 Pro 24GB (GPU 가용 16GB) |
| 출력 강제 | JSON Schema (`format`) → 33/33 파싱 성공 |
| 온도 | 0.0 (재현 가능) |

</details>

#### 결정해야 할 것

- [ ] 🔴 **`식용유` 기름짐 0.60 → 1.00 으로 고칠 것인가** (`f_taste` 에 직접 영향)
- [ ] 🟡 **`멸치` 를 국물용/볶음용으로 분리할 것인가** (P3 시드 사전도 같이 갈라야 함)
- [ ] 어느 방법으로 갈 것인가 (조합안 포함 — 규칙 초안 → 일부만 검수)
- [ ] `flavor_vec` 이 부정확하면 `f_taste` 가중치를 낮출 것인가
- [ ] 🔴 **순환 문제** — I-8 이 `taste_vec = 고른 레시피의 flavor_vec 평균` 이라
      `flavor_vec` 노이즈가 유저 쪽에도 그대로 전이된다. 두 노이즈가 독립이 아니다.
      척도 문항 3축(매움·단맛·짠맛)을 병행하면 그만큼은 독립 관측이 된다 (I-8 남은 결정과 연결)

> **시한:** Ranking v0(`04` P0 #3)의 전제다. **W4 착수 전에 정해야 한다.**

---

### 🟡 I-16. 추천 2트랙 분리 — 사유 정책 확정 *(v2.7 신규)*

`01` 5-6-1 에서 추천을 두 트랙으로 나눴다. 실측이 근거다 —
18 사유 = 450 출력토큰을 300ms 에 내려면 **1,500 tok/s** 가 필요한데
가장 빠른 로컬 모델이 90.9 tok/s 다 (16.5배 부족).

| | 트랙 1 (오늘의 추천) | 트랙 2 (잔여 재료) |
|---|---|---|
| 트리거 | 배치 1일 1회 | 실시간 |
| LLM 사유 | 생성 | 없음 |
| 응답 | ~30ms (조회) | ~58ms |

#### 결정해야 할 것

- [ ] 🔴 **트랙 2 에 템플릿 사유를 남길 것인가**
      사용자 초안은 "사유는 달지 않음"이었다. **템플릿은 유지하기를 권한다** —
      비용이 0ms 이고 이미 구현돼 있으며 구조적으로 거짓말을 못 한다.
      무엇보다 **설명가능성이 접근 A(순수형)의 핵심**이고 디버거·R9 ablation 이
      `reason_features` 에 걸려 있다. 빼면 그 둘이 같이 죽는다.
      빼는 것은 **LLM 사유뿐**이면 충분하다.
- [ ] **트랙 1 배치를 어디서 돌리는가** — 개발 노트북(0원, sleep 시 중단) vs 교내서버 GPU
- [ ] **G4 사람 검수를 누가·언제** 하는가. 게이트 통과분 잔여 오류 25% (`01` 5-5)
- [ ] **하루 중 stale 허용 범위** — 낮에 재료를 쓰면 사유가 어긋난다.
      "오늘의 추천"이라는 제품 성격으로 감당할 것인가, 냉장고 변경 시 재생성할 것인가
- [ ] 트랙 1 의 Top-N 을 몇으로 할 것인가 (현재 `top_k=20`, exploration 2 제외 시 18)

> **시한:** 트랙 2 는 지금 설계 그대로라 급하지 않다.
> 트랙 1 은 배치 cron 과 스키마(사유 저장 컬럼)가 걸려 있어 **W4 전에 정해야 한다.**

---

### 🔴 I-17. DB 분리 시 남은 미해결 2건 *(v2.7 신규)*

`06` 8-D 로 분리 사양을 정리하면서 **코드로 고칠 수 있는 것은 고쳤다**
(역할 단위 타임아웃 · `tcp_keepalives` · PG 설정 적용 · `apply_schema.sh` 경고).
협의가 필요한 둘이 남았다.

#### ① 인증 · 암호화 — 원격 전에 반드시

현재 상태는 **한 대 전제**다. 원격이 되면 그대로 노출된다.

| | 현재 | 필요 |
|---|---|---|
| DB 비밀번호 | `POSTGRES_PASSWORD:-reco` **기본값** | `.env` 분리 · 저장소에 안 올림 |
| 포트 바인딩 | 0.0.0.0 | AI 서버 IP 만 |
| 전송 암호화 | 지정 없음 (Grafana 는 `sslmode: disable`) | `sslmode=require` |
| Redis | `requirepass` 없음 | 설정 |

- [ ] 교내망 전용이면 어디까지 해야 하는가 (망 분리에 기대도 되는가)
- [ ] DB 관리자가 `scram-sha-256` 을 쓰는가

#### ② 🔴 DB 다운 시 폴백이 없다

한 대일 때 "DB 다운"과 "앱 다운"은 **같은 사건**이었다. 분리하면 독립 사건이 된다.

그런데 `01` 1-5 가 **PostgreSQL 을 SoT** 로, **Redis 를 순수 캐시**로 못 박았고
(`--save "" --appendonly no`), `retrieve_for_user()` 자체가 **PG 함수**다.
**DB 가 내려가면 추천이 통째로 죽는다.**

이것은 `1-4` 가 GPU 에 대해 한 말과 같은 구조다 —
*"온라인 경로가 GPU 에 의존하면 GPU 서버가 내려갔을 때 서비스 전체가 죽는다."*

- [ ] **감수할 것인가** — 부트캠프 프로젝트에서 가장 현실적인 답일 수 있다
- [ ] 감수한다면 **어떻게 보이게 할 것인가** (에러 화면 / 대시보드 알림)
- [ ] 트랙1(오늘의 추천)은 **사전계산이라 캐시로 버틸 수 있는가** —
      사유까지 미리 만들어 두므로 Redis 에 그날치를 얹으면 읽기만으로 뜬다.
      단 `1-5` 의 "Redis 에만 존재하는 데이터를 만들지 않는다"와 충돌하지 않는지 확인

> **시한:** ①은 원격 DB 를 쓰기로 정하는 시점. ②는 그 전에 결론만 내면 된다
> (감수/대응 중 택일).

---

### ⚪ I-14. 소비기한 데이터 출처 *(v1.9 신규)*

**현재 `seeds/ingredient_shelf_life.yaml` 의 값은 통념이지 실측이 아니다.**
카테고리 기본 58종 + 재료 예외 42종으로 536종 전량을 해소했지만,
근거는 "상추는 5일쯤 간다" 수준의 상식이다.

#### 왜 이슈인가

`f_expiring` 은 가중치 0.15 로 세 번째로 크고, **이 서비스의 차별화 그 자체**다.
발표에서 "소비기한은 어디서 왔나요"를 반드시 받는다.

#### 선택지

| # | 안 | 비용 | 방어력 |
|---|---|---|---|
| **1** | 지금 값 유지 + **"통념 기반 추정치"로 명시** | 0h | 정직하지만 약하다 |
| **2** | 식약처 **식품유형별 권장 소비기한** 공개자료 매핑 | 6~10h | 강하다. 출처를 댈 수 있다 |
| 3 | 유저 피드백으로 보정 ("아직 멀쩡해요" 버튼) | 8h + 유저 필요 | 데이터가 안 모인다 |

**권장: 1 로 시작하고 W5 에 2 를 시도한다.** 2 는 식품유형 분류와 우리 카테고리를
매핑해야 해서 생각보다 오래 걸린다. 커버리지가 부분적이어도 **덮은 항목만 교체**하고
나머지는 통념값을 유지하면 된다 — `note` 컬럼에 출처를 남긴다.

> 🔴 **어느 쪽이든 리포트에는 출처를 쓴다.** "실측 소비기한"이라고 쓰면 안 된다.

---

### ⚪ I-10. 중복 레시피 제거 기준 *(v1.1 신규)*

**이슈**
설계 6-4-4 — 임베딩 클러스터링으로 중복 레시피를 묶으면 **CF 밀도가 2배**가 된다
(아이템당 0.4 → 0.8). 다른 어떤 방법보다 저렴한 개선이다.

**결정 필요**
- 유사도 임계값 (너무 낮으면 다른 레시피가 합쳐진다)
- 대표 레시피 선정 기준 (조회수 / 이미지 유무 / 스텝 완결성)
- 합친 레시피를 삭제할 것인가 `status='duplicate'` 로 남길 것인가

**제안** — **삭제하지 않고 `recipe.status='duplicate'` + `canonical_id` 컬럼**으로 남긴다.
임계값이 잘못됐을 때 되돌릴 수 있어야 한다. 4-4-1 에서 임계값 판단이 틀렸던 전례가 있다.

**기한** — 크롤링 데이터 확보 후. 실제 중복 비율을 봐야 임계값을 정할 수 있다.

---

### ⚪ I-7. GPU 서버 접근 방법

**이슈** — 임베딩 생성·LTR 학습이 배치 전용으로 GPU 를 쓴다 (설계 0-3).

**필요한 결정** — 접근 방법 / 사용 가능 시간대 / 큐 정책 / 결과물을 서버로 옮기는 방법

**주의** — **온라인 추론 경로는 GPU 에 의존하지 않는다** (설계 1-4). GPU 서버가
내려가도 추천은 동작해야 한다.

**필요 사양은 크지 않다** *(v1.0 계산)* — ko-SBERT 임베딩 **추론**에만 쓰므로
**VRAM 2~4GB** 면 충분하고, 레시피 1만건에 약 2분이다. CPU 로도 가능하다(10~20배 느림).

> 🔴 **딥러닝 학습용 GPU 는 필요 없다.** 설계 6-2-4 에서 계산했듯 Two-Tower 파라미터
> 70만 개는 fp32 로 2.8MB 이고, **부족한 것은 VRAM 이 아니라 데이터**다
> (필요 유저 3.2만 명 vs 확보 가능 100명). GPU 서버를 "딥러닝 학습용"으로 신청하면
> 실제 용도와 어긋난다.

---

## 부록 — Docker 이미지 적재 타이밍

### 결론: **지금. 다만 `postgres` 하나만 먼저.**

**지금 해야 하는 이유 4가지**

1. **미검증 코드가 누적되고 있다.** 지금은 29테이블뿐이라 오류가 나도 원인이 명확하다.
   C(정규화 코드)·D(스켈레톤)를 더 올린 뒤 처음 실행하면 오류가 한꺼번에 터져
   **어느 층의 문제인지 분리하기 어려워진다.**

2. **`make smoke` 가 설계 자체를 검증한다.** 결정 2(staple) · 결정 3(배열 GIN) ·
   알러지 4경로가 실제로 동작하는지를 확인한다. **틀렸다면 설계를 되돌려야 하고,
   되돌리는 비용은 지금이 가장 싸다.**

3. **크롤링 완료가 크리티컬 패스다.** 데이터가 오는 순간 바로 적재해야 하는데
   그때 처음 Docker 를 설치하면 그 시간만큼 전체가 밀린다.

4. **팀 20명이 같은 환경을 쓰려면 누군가 먼저 검증해야 한다.** 각자 설치하다
   각자 다른 곳에서 막히면 그게 더 큰 비용이다.

### 비용은 작다

| | |
|---|---|
| 설치 | `brew install --cask orbstack` — 5분 |
| 이미지 | postgres 400MB · redis 40MB · grafana 400MB · mlflow 1GB |
| 첫 기동 | 1분 |

### 단계적으로 하면 더 싸다

```bash
# 1단계 — postgres + redis (약 440MB). 핵심 경로를 먼저 검증
make bootstrap          # up → seed → smoke

# 2단계 — 관측 도구. 대시보드 트랙 착수 시점에
make up-obs             # grafana + mlflow
```

**1단계만으로 스키마 · 시드 · Retrieval · 성능이 전부 검증된다.**

**MLflow 는 컨테이너가 아예 필요 없을 수도 있다.** 순수 Python 패키지이고 서버가 상태를
갖지 않으므로 `make mlflow-ui` 로 로컬에서 띄워도 같은 백엔드 DB(`mlflowdb`)를 보면
같은 실험 데이터가 보인다. 1GB 이미지를 받을 이유는 "항상 켜져 있는 공용 UI 주소"가
필요할 때뿐이다.

### 나중으로 미뤄도 되는 경우

디스크 여유가 2GB 미만이거나 교내망 정책상 이미지 pull 이 막힌 경우뿐이다.
그 경우 대안은 `brew install postgresql@16 pgvector` 후
`DATABASE_URL=... ./infra/apply_schema.sh` 인데, `pgvector` 설치가 번거로워
컨테이너 쪽이 훨씬 쉽다.
