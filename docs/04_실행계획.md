# 실행 계획 — ~~2명~~ **3명** · 8주

> 작성자: 박재우 · 작성일: 2026-09-03

| 항목 | 내용 |
|---|---|
| 작성 | 2026-08-27 |
| 갱신 | **2026-09-03** — 3인 전환 반영 · 09-02~03 DDL/계약/구현 확정분 · 실측 재대조 |
| **개발 인력** | ~~AI 파트 2명~~ → **AI 파트 3명** *(2026-09-02 전환)* (+ 데이터 정제는 팀원 도움 요청 가능) |
| 기간 | 8주 — **현재 5주차 종료. 남은 15영업일** |
| 기준 문서 | [`01_추천시스템_설계.md`](01_추천시스템_설계.md) ~~v1.9~~ **v3.0** |
| 진입점 | [`00_README.md`](../README.md) |
| 인프라 | [`06_인프라_사양.md`](06_인프라_사양.md) — **신청값 8 core · 32 GB · 100 GB** (현재 개발환경 실측 4 / 16 / 50) |
| 트랙 지시서 | [`draft/03_작업분담_공통.md`](draft/03_작업분담_공통.md) · [`01_결정사항`](draft/05_작업분담_결정사항.md) **D-1~D-19** |

> ⚠️ **설계 문서는 20명 전제로 쓰였다.** 이 문서가 그것을 ~~2명~~ **3명** 기준으로 잘라낸 결과이며,
> 충돌하면 **이 문서가 우선**한다. 설계 문서는 "무엇을 어떻게" 이고, 이것은 "그중 무엇을 실제로" 다.

> 🔴 **0·2·3 절의 시간 예산(520h · 720h · 495h)은 2인 8주 원안 기준이다.**
> 09-02 에 3인으로 바뀌면서 **남은 3주의 정본은 3-1 절**(3트랙 병렬)이다.
> 두 계산을 섞지 않는다 — 0·2 절은 "8주 전체를 어디에 썼나"의 기록이고,
> 3-1 은 "남은 15일에 무엇을 하는가"다.

---

> 🔑 **구현 순서는 3-1 절을 본다.** 이 문서의 4절이 *언제*를 정한다면
> 3-1 은 *무엇부터*를 정한다 — 설계가 끝나고 코드를 쓰기 시작하는 시점의 순서다.
> 착수 전에 발견된 공백 3건도 거기 있다 — ~~적재 코드 없음~~ ✅ · ~~`reco/` DB 커넥션 없음~~ ✅ ·
> 🔴 **`infra/app/` 없음(아직 그대로)**.

> ⚠️ **이 문서에 `B-1` 이 두 개 있다.** §0 표의 `B-1` 은 *쌍대비교 수집 + BT 학습*(v1.9 작업 번호)이고,
> 3-1 절의 `B-1` 은 *트랙 B 의 첫 작업 = mock → 라이터 연결*이다. **서로 무관하다.**
> 3-1 이후의 `A-*`·`B-*`·`C-*` 는 전부 [지시서](draft/03_작업분담_공통.md)의 트랙 작업 번호다.

# 0. 요약 — 한 장

> ⚠️ **아래는 2인 8주 원안의 예산 계산이다** (2026-08-27 작성).
> 3인 전환 후 남은 3주의 배분은 [3-1 절](#-3인-병렬로-전환-2026-09-02)에 있다.

| | |
|---|---|
| 가용 시간 | **약 520h** (2명 × 6.5주 × 40h) |
| 설계 전량 추정 | **약 720h** |
| **초과** | **약 38%** |
| 결론 | **P0 + P1 + P2 중 1개만** = **약 495h (버퍼 25h · 5%)** — 상세는 §2 |

## v1.9 + v2.0 반영 — 순증 **+15h** (v1.9 +5h · v2.0 우연성 +10h)

설계 검토에서 나온 결함 수정과 장치 추가. **시뮬레이터를 자르는 것으로 상쇄한다.**

| | 작업 | 시간 | 상태 |
|---|---|---|---|
| A-1 | 이유 생성 z-salience 전환 | 3h | ✅ **완료** |
| A-2 | `features` 원값 로깅으로 전환 | 3h | ✅ **완료** |
| A-3 | `f_cooccur` 재정의 · 가중치 재분배 | 0h | ✅ **완료** |
| B-2 | exploration 위치 무작위화 | 1h | ✅ **완료** — 🔴 단 **09-03 까지 슬롯 결과를 버리고 있었다** (아래 [09-02~03 확정분](#-09-0203-추가-확정--ddl계약구현) ⑧) |
| B-4 | 캘리브레이션 임계값 | 4h | ✅ **완료** |
| B-5 | 핵심어 구조 매칭 (P3 예산 내) | 0h | ✅ **완료** |
| C | 소비기한 시드 ~~525종~~ **536종** | 2h | ✅ **완료** |
| B-3 | Team-Draft Interleaving | 8h | 계약·구현 완료 · **W6 연동 남음** |
| **B-1** | **쌍대비교 수집 + BT 학습** | **14h** | ⬜ **W3** |
| **S-A** | **k-means 배치 (K≈50)** *(v2.0)* | **4h** | ⬜ W5 — **캡 대체가 여기 달려 있다. 즉시 효과** |
| **S-B** | `user_cluster_stat` 갱신 + Thompson *(v2.0)* | **6h** | ⬜ W6 — **컷라인 1순위.** 8주엔 효과가 미미하다 |
| | 시뮬레이터 삭제 | **−30h** | |
| | **순증** | **+15h** | |

### 🔑 시뮬레이터를 자르고 쌍대비교로 바꾼 이유

규칙 기반 시뮬레이터(아래 P1 #9)는 **판단 2 가 경고한 순환 논리에 가장 가깝다** —
선호 함수를 우리가 써놓고 그것을 학습한다. 진짜 사람의 쌍대비교 600건이
합성 유저 수천 명보다 방어 가능하고, **가중치가 추정치라는 최대 약점을 직접 해결한다**
(설계 5-2-5).

부수 효과로 **W6 유저 모집이 실패해도 LightGBM 학습 데이터가 이미 있다.**
컷라인의 위험이 줄어든다.

**가장 크게 잘린 것은 ⑥ ML 스택이다.** FM · item2vec · ALS 3용도 · 클러스터링을
전부 열어놨으나 **하나만 한다.**

---

# 1. 이미 완료

| 산출물 | 규모 | 검증 | 시점 |
|---|---|---|---|
| **A 시드 데이터** | 재료 ~~525~~ **536** · alias 245 · 단위환산 193 · 계층 ~~65~~ **66** | `make validate` | W1 |
| **B DB 부트스트랩** | DDL ~~27테이블~~ **29테이블** · 함수 **6** · 뷰 **2**(`v_real_*`) | **실행 검증** 스모크 13/13 · p95 30.2ms | W1 |
| **D 계약 + Mock** | 모델 20종+ · 엔드포인트 ~~7~~ **8**(온보딩 신설) | `make contract` ~~42~~ → **98** · Mock ~~15~~ **20캡처** | W1 |
| **크롤러 어댑터** | YAML 매핑 · probe 진단 | `make probe-all` 3종 | W1 |
| **C 정규화 P1·P2** | 코드 3 · fixture 74건 | `make normalize-test` | **W2** |
| **C 정규화 P3 캐스케이드** *(v2.2)* | L0~L2 + 구조 검증 + 커버리지 도구 | 회귀 23건 · 실데이터 mention ~~79.4%~~ → **80.8%** (전량 실측) | **W2** |
| **C 정규화 P4 역할 판정** *(v2.4)* | 주재료 가드 · 미매칭 보류 · 규칙별 집계 | 회귀 16건 · essential 51.9% | **W2** |
| **이유·탐색·우연성 모듈** *(v1.9~v2.0)* | `app/services/recommends/` 3종 — z-salience · 혼합 탐색 · interleave | `make contract` ~~42~~ → **98** | W2 |
| **캘리브레이션 임계값** *(v1.9)* | `app/services/eval/threshold.py` — Wilson 하한 · 필요표본 계산 | 계약 검증 | W2 |
| **소비기한 시드** *(v1.9)* | 기본 ~~58~~ **59** + 예외 42 · ~~525종~~ **536종** 전량 해소 | `make validate` | W2 |
| **판단 근거 시뮬레이션** *(v2.0~v2.1)* | `scripts/bench/` 5종 — 문서 인용 숫자를 재현 | `make bench-quick` (q3 는 --quick, 나머지 전량) | W2 |
| **S1 `app/db/`** *(신규 09-02)* | 풀 · `retrieve_for_user()` 래퍼 · S3 자리 | `make smoke-py` **19건** | W5 |
| **S2 `app/core/`** *(신규 09-02)* | 라이터 · 카운터 · 종단 테스트 | `make log-test` **48건** | W5 |
| **S6 크롤 적재** *(신규 09-02)* | `scripts/load_recipes.py` — 레시피 **46,353** · 원문 재료 **451,862** · 조리단계 362,677 · 후기 **624,422** | `make verify` · 재적재 24초 | W5 |
| **DDL 개정 + 온보딩 계약** *(신규 09-02~03)* | `event_log.source` · `session_id` 접두어 · `purchased_at` · 온보딩 원본 · `daily_recommendation` · 로더 멱등 제약 · TZ | `make ddl-test` **47건** · `make contract` **98건** | W5 |
| **환경 일원화** *(신규 09-03)* | `pyproject.toml` + `uv.lock` · `make install TRACK=A\|B\|C` (구 `requirements` 파일 2종 폐지) | `make doc-check` | W5 |
| **온보딩 제시 레시피** *(신규)* | `seeds/onboarding_recipes.yaml` — 제시 **20** + 예비 **31** · 6축 | `make validate` | W5 |
| 설계 문서 | 0·①~⑦ + 02~08 | `make doc-check` | |

## 실행에서만 드러난 버그 — 누적 ~~5건~~ **8건**

| # | 버그 | 어디서 |
|---|---|---|
| 1 | `locale=C` 가 한글 pg_trgm 을 전멸시킴 | DB 실행 |
| 2 | `ltree` 가 로케일에 따라 한글 거부 | DB 실행 |
| 3 | 빈 `essential_ids` 는 `&&` 로 영원히 안 잡힘 | 스모크 A1 |
| 4 | L3 임계값 0.6 에서 재현율 0% | 실측 |
| 5 | **복합 구분자 `,` `/` 가 분수·천단위를 파괴** | **P2 fixture** |
| **6** | 🔴 **로더가 멱등이 아니었다** — 2회 실행 시 원문 재료가 전량 2배 (451,862 → 903,724) | **재적재 실측 (09-02)** |
| **7** | 🔴 **탐색 슬롯을 계산해 놓고 버렸다** — `exploration_slots()` 결과를 안 써서 탐색 아이템이 점수 순서에 그대로 남았다 | **계약 검증 (09-03)** |
| **8** | 🔴 **DB 시간대가 UTC** — 한국 자정~오전 9시에 `current_date` 가 하루 전이라 `f_expiring` 임박 판정이 어긋난다. 컨테이너 `TZ`/`PGTZ` 는 **밖에서 붙는 psycopg2 에 적용되지 않는다** | **실측 (09-03)** |

**전부 정적 검토를 통과한 뒤 실행에서만 드러났다.** 이 패턴이 반복되므로
남은 단계도 **설계 → 즉시 구현 → 측정** 순서를 유지한다.

> 위 표는 **설계 단계에서 못 잡은 것**만 센다. S2 라이터 구현 직후의
> 적대적 검증 8건과 mock 결함 3건은 [3-1 절](#-적대적-검증에서-확정된-결함-8건--전부-수정)에 따로 있다.

---

# 2. 작업량 재산정

## P0 — 없으면 프로젝트가 성립하지 않는다

| # | 작업 | 추정 | 근거 |
|---|---|---|---|
| 1 | **C 정규화 파이프라인** P1~P5 + 테스트 | 70h | 크리티컬 패스. 이게 없으면 후보가 안 나온다 |
| 2 | Retrieval 실연동 (SQL 함수 ↔ API) | 20h | 함수는 이미 있음 |
| 3 | **Ranking v0** 선형 가중합 | 40h | 없으면 "필터링 엔진"이지 추천이 아니다 |
| 4 | **대시보드 — 추천 디버거** | 40h | 유일한 로그 유입구 + 발표 데모 |
| 5 | **대시보드 — 검수 큐** | 30h | 데이터 정제 도움을 받으려면 필수 |
| 6 | 평가 하네스 + 지표 | 40h | 리포트의 근거 |
| 7 | 리포트 | 60h | 산출물 |
| | **소계** | **300h** | |

## P1 — 있으면 프로젝트가 좋아진다

| # | 작업 | 추정 |
|---|---|---|
| 8 | Re-ranking (MMR + exploration **위치 무작위**) | 30h |
| **9'** | **쌍대비교 수집 + Bradley-Terry** *(v1.9 신규)* | **14h** |
| 13 | Team-Draft Interleaving 연동 | 8h |
| ~~9~~ | ~~규칙 기반 시뮬레이터~~ — **컷. 순환 논리라 B-1 로 대체** | ~~30h~~ |
| 10 | MLflow 실험 4개 | 30h |
| 11 | 대시보드 — 데이터 품질 | 20h |
| 12 | **LightGBM v1** | 40h |
| | **소계** | ~~150h~~ **142h** |

> 🔴 **소계 150h 는 산술 오류였다** (09-03 정정). 30+14+8+30+20+40 = **142h** 이고,
> 아래 "누적 442h"(= P0 300 + P1 142)와 §2 결론 495h 는 **142 를 전제로 계산돼 있었다.**
> 즉 틀린 것은 소계 칸 하나뿐이고 뒤 숫자는 전부 맞다.

**누적 442h.** 가용 520h 대비 여유 78h. *(시뮬레이터 −30h · B-1 +14h · interleaving +8h)*

## P2 — 하나만 고른다

| 후보 | 추정 | 유저 의존 | 크롤링만 있으면? |
|---|---|---|---|
| **임베딩 파이프라인** (ko-SBERT + 용도 5종) | 30h | **없음** | ✅ **가능** |
| item2vec (recipe×ingredient) | 30h | 없음 | ✅ |
| FM (선호도 × 속성) | 30h | 유저 100명 + 설문 확정 | ❌ |
| user×ingredient ALS | 35h | 유저 100명 | ❌ |

> ## 🔑 선택: **임베딩 파이프라인** *(2026-08-27 변경)*
>
> 이전 판은 `item2vec` 을 골랐으나, **자연어 레시피 검색을 하기로 결정**하면서 바뀌었다.
> 검색은 임베딩 외에 방법이 없고, 같은 인프라로 **네 가지가 따라온다** (설계 6-4-3).
>
> | 용도 | 임베딩 없이 |
> |---|---|
> | **자연어 검색** | ❌ 불가능 |
> | `cuisine` kNN 분류 | ⚠️ 부분적 |
> | 추천 이유 "○○와 비슷해요" | 자카드로도 |
> | 이상치 탐지 | ⚠️ 부분적 |
> | 중복 레시피 제거 | 자카드로도 |
>
> **`item2vec` 은 하차한다.** 주 용도였던 재료 대체가 **측정 후 판단**으로 바뀌면서
> (설계 6-4-6) `f_cooccur` 피처 하나만 남았다. 측정 결과가 좋으면 그때 W7 에 재검토한다.
>
> **가중 자카드**는 별도 비용이 아니다 — `freq_count` 컬럼과 배열 연산이 이미 있다.
>
> **누적 495h. 버퍼 25h (5%).** *(P2 30h + v1.9 완료분 13h + v2.0 10h — B-1 14h·B-3 8h·시뮬레이터 −30h 는 P1 누적 442h 에 기반영)*
>
> 🔴 버퍼가 5% 로 얇아졌다. **절삭 순서는 S-B → S-A 다.**
>
> | | 빼면 | 측정된 손실 |
> |---|---|---|
> | **S-B** (6h) | 탐색이 균등으로 폴백 | 20세션 기준 조리 −0.6 · 숨은취향 −0.53 — **사실상 없음** |
> | **S-A** (4h) | 분류축 캡으로 남음 | **점수 유지 98.6% → 89.0%** · 근중복 3.0 → 5.8 — **크다** |
>
> *(`scripts/bench/kmeans_worth_it.py` 재실행 09-03: ILD 0.965/0.952 · 근중복 3.0/5.8 · 점수 유지 98.6%/89.0% — 그대로다.
> `01` 5-3-2 의 다른 표에 있는 99.0%/89.3% 는 seed 가 다른 이전 실행이므로 이 숫자를 쓴다.)*
>
> **S-B 를 먼저 자른다.** S-A 는 4h 로 가장 값싼 개선이므로 마지막까지 지킨다.
>
> 개발 추정이 낙관적임을 감안하면 이 버퍼도 빠듯하다.

## 자르는 것

| 항목 | 절감 | 사유 |
|---|---|---|
| **Grafana 3보드** | 20h | Streamlit 으로 대체. 컨테이너도 안 띄운다 |
| **대시보드 개요 · 실험비교 화면** | 40h | MLflow UI 를 링크로 대체 |
| **FM · ALS** ~~· 클러스터링~~ | 90h | P2 에서 1개만. 유저 모집 성공 시 W7 에 재검토 |
| **MealPlan (④)** | 40h | 이미 스트레치 |
| **Feast** | — | 이미 스트레치 |
| **중복 레시피 제거** | 20h | ~~조건부~~ → **컷 확정 (09-03 실측).** 제목 완전일치 중복 잉여 **1,568건 = 3.4%** (46,353 → distinct 44,785). 3.4% 를 위해 20h 를 쓰지 않는다 |

> 🔴 **클러스터링은 컷이 아니다 (09-03 정정).** v2.0 에서 `S-A` (k-means K≈50 · 4h)로
> **되살아났고** 08 §5 에 확정으로 올라가 있다 — 분류축 캡을 대체해 점수 유지가
> 89.0% → 98.6% 가 되기 때문이다. 이 표의 90h 는 **FM · ALS 2종**을 뜻한다.
> 남은 것은 `S-B`(`user_cluster_stat` + Thompson · 6h)이고 **절삭 1순위**다.
| MLflow 실험 8개 → 4개 | 30h | `random` · `popularity` · `cf-als` · `ranker-v0` 우선 |

> **자른 것을 설계 문서에서 지우지 않는다.** "왜 안 했는가"가 아니라 **"어떻게 하면 되는지
> 알지만 인력 제약으로 우선순위를 낮췄다"** 가 되고, 리포트의 확장성 절에 그대로 쓴다.

---

# 3. ~~2명 역할 분담~~ *(W2~W5 의 기록. 남은 3주는 [3-1](#-3인-병렬로-전환-2026-09-02))*

> 🔴 **2026-09-02 에 3인 병렬로 대체됐다.** 아래 A/B 2열 표는 W2~W5 에 실제로
> 그렇게 일한 기록으로 남긴다. **W6~W8 의 정본은 3-1 절의 A·B·C 3트랙이다** —
> 여기의 "개발자 B"와 3-1 의 "트랙 B"는 **서로 다른 사람·다른 범위**이므로
> 두 표를 겹쳐 읽지 않는다.

Mock 서버(D)가 이미 있으므로 **W2 부터 완전 병렬**이다. 순차로 일하면 8주가 모자란다.

| | **개발자 A — 엔진** | **개발자 B — 대시보드 · 데이터** |
|---|---|---|
| W2 | C 정규화 P1·P2 (파싱) | 디버거 화면 (mock 기반) |
| W3 | C 정규화 P3~P5 + 커버리지 측정 | 검수 큐 화면 + `suggested` |
| W4 | Ranking v0 | **검수 스프린트 진행** + 데이터 품질 화면 |
| W5 | 임베딩 파이프라인 | 평가 하네스 + 지표 |
| W6 | Re-ranking (MMR · exploration) | **유저 모집 + 로그 수집** |
| W7 | LightGBM v1 + MLflow 실험 | 리포트 초안 + 시뮬레이터 |
| W8 | 안정화 · 성능 | 리포트 완성 · 발표 |

**접점은 API 계약 하나뿐이다.** B 는 W6 까지 mock 만으로 일할 수 있고,
A 의 엔진이 완성되는 대로 `RECO_API_URL` 만 바꿔 붙인다.

---

# 3-1. 🔑 구현 플로우 — 무엇부터 짜는가 *(v2.1 · 2026-09-01 · S1·S2 완료 09-02 · S6 적재 완료 09-02)*

주차 계획(4절)이 **언제**를 정한다면, 이 절은 **무엇부터**를 정한다.
설계는 끝났고 코드를 쓰기 시작하는 시점의 순서다.

```
S0 규약 동결 ──┬─→ S1 DB 액세스 ──→ S2 로그 쓰기 ──→ S3 피처12+스코어러 ──┐
               │                                                          ├─→ S8
               ├─→ S4 평가 하네스 ─────────────────────────────────────────┤
               └─→ S5 화면 2종 ────────────────────────────────────────────┘
                                    ↑
                        S6 로더+적재 ──→ S7 빌더+피처5   (크롤 도착 시 끼어든다)
```

> 🔴 **피처 17종부터 짜면 안 된다.** S1 이 없으면 전부 막힌다.
> 지금 `app/` 에는 **DB 커넥션이 없고**, `retrieve_for_user` 를 호출한 코드가 0개다.

| 단계 | 무엇을 끝내나 | 담당 | 공수 |
|---|---|---|---|
| **S0** | 규약 동결 5건 (아래) | A+B | 반나절 |
| **S1** ✅ | ~~`app/db/` — 파이썬이 Retrieval 을 실제로 호출한다~~ **완료 (09-02)** | A | ~~1.5일~~ 반나절 |
| **S2** ✅ | ~~`app/core/` — 요청 1건 → `recommendation_log` 1행~~ **완료 (09-02)** | A | ~~2일~~ 반나절 |
| **S3** | 피처 12종 + `app/services/recommends/scorer.py` | A | 4일 |
| **S4** | 평가 하네스 자체 검증 (골든 픽스처) | B | 2일 |
| **S5** | 화면 2종 (디버거 · 검수 큐) | B | 5일 |
| **S6** ◐ | ~~`scripts/load_recipes.py`~~ **적재 완료 (09-02)** · **정규화 배치 남음** | A+B | ~~4일~~ 잔여 |
| **S7** | `recipe_feature` 빌더 + 피처 5종 | A | 6일 |
| **S8** | Re-ranking · 유저 모집 · BT 가중치 교체 | A+B | 잔여 |

> 담당 열의 A·B 는 **2인 시절 표기**다. 3인 배분은 [아래 트랙 표](#-3인-병렬로-전환-2026-09-02)를 본다.

## 🔴 착수 전 발견 — 목록에 없던 공백 3건 *(3건 중 2건 해소)*

**① ~~적재 코드가 없다.~~ 해소 (09-02).** `scripts/load_recipes.py` 가 생겼고
실제로 **레시피 46,353 · 원문 재료 451,862 · 조리단계 362,677 · 후기 624,422** 를 넣었다.
아래는 발견 당시 기록이다. `app/services/ingest/` 에 어댑터만 있고
**`recipe_ingredient_raw` 에 INSERT 하는 코드가 0개다.**

> 🔴 **로더는 처음에 멱등이 아니었다.** 2회 실행하면 원문 재료가 전량 2배
> (451,862 → **903,724**)가 됐다. `recipe_ingredient_raw UNIQUE(recipe_id, position)` 과
> `recipe_review UNIQUE(recipe_id, author_hash, written_at) NULLS NOT DISTINCT` 로 막았다 —
> **`NULLS NOT DISTINCT` 가 없으면 `author_hash` 가 NULL 인 후기가 무한히 쌓인다.**

**② ~~`reco/` 에 DB 커넥션이 없다.~~ 해소 (09-02).** `app/db/` 가 풀·래퍼를 갖췄다.
아래는 발견 당시 기록이다. `psycopg` 를 쓰는 것은 `tests/smoke_test.py`·
`scripts/migrate.py` 뿐이다. SQL 함수는 검증됐지만 **엔진과 연결된 적이 없다.**

**③ 🔴 `infra/app/` 디렉터리가 없다 — 아직 그대로다 (09-03 확인).**
`docker-compose.yml` 이 `infra/app/Dockerfile.api` 와
`Dockerfile.dashboard` 를 빌드 대상으로 선언해 놓았는데 디렉터리가 비어 있다.
디버거 화면이 **코드 0줄**이고, 이것이 **C 트랙(S5)의 첫 작업**이다.

## ✅ S1 완료 기록 *(2026-09-02)*

`app/db/` 3개 파일. **필터 로직은 한 줄도 파이썬에 옮기지 않았다** — 전부 SQL 안에
있고 여기는 번역만 한다. 두 곳에 두면 반드시 어긋난다.

| 파일 | 역할 |
|---|---|
| `pool.py` | 커넥션 풀. `PG_MAX_CONN` 기본 10 — `max_connections=50` 을 앱·대시보드·배치가 나눠 쓴다 |
| `retrieval.py` | `retrieve_for_user()` → `list[Candidate]`. `retrieve_raw()` 는 벤치용 |
| `features.py` | S3 자리. 🔴 채울 때 후보 N개를 N번 조회하면 왕복 1회가 깨진다 |

**검증은 케이스를 복제하지 않았다.** `tests/smoke_test.py --via-python` 이 **같은 9건**을
`app.db.retrieve()` 로 다시 돌린다 (`make smoke-py`). 복제하면 두 벌이 서로 다르게 썩는다.
여기에 파이썬 경로에서만 깨질 수 있는 6건을 더해 **19건**.

- P1·P2 `missing_count`·`missing_ids` 전달 · P3 `coverage` 실수형
- **P4 `cluster_id`** — 계약에 있는데 SQL 이 안 넘겨주던 자리다 (`infra/init/04_functions.sql:122`). 회귀 방지
- P5 `missing_ids` NULL → `[]` 정규화
- P6 풀 재사용 — 20회 호출 후 커넥션 2→2

**계약 결함 1건 수정.** `RetrievalInput` 이 `retrieve_for_user()` 대응이라고
적혀 있었는데 실제로는 `pantry_ids`·`allergy_ids` 를 받으므로
`retrieve_candidates()` 대응이었다. **서빙 경로에 맞는 계약이 아예 없었다.**
`RetrievalRequest` 를 신설하고 계약 검증 6건을 추가했다 (당시 `make contract` 65건 → **현재 98건**).

> 🔴 **서빙 계약은 pantry 를 받지 않는다.** 파이썬이 먼저 조회해서 넘기면
> 왕복이 2회가 된다. 계약이 `extra="forbid"` 로 이걸 막는다.

**실데이터 확인 (46,353건).** `retrieve()` 8.2ms, 후보 0건 — `recipe_feature` 가
비어서 빌더 게이트(`n_total > 0`)에 전부 걸린 것으로 **정상**이다. 실후보는 S7 이후에 나온다.
*(09-03 재확인: `recipe_feature` 여전히 0행. A 트랙 S7 이 채운다.)*

> ⚠️ **재초기화는 크롤 적재분을 지운다.** S0 ⑤ 검증 때 46,353건·후기 **624,422건**이
> 날아갔다. 재적재 24초라 실害는 없지만, `REVIEW_SALT` 가 `.env` 에 없으면
> **닉네임 해시가 달라져 후기 동일인 판정이 깨진다.** salt 는 절대 재생성하지 않는다.
>
> *(627,140 은 낡은 수치였다 — 실적재는 **624,422**, 크롤 원문 합은 **627,605**,
> 차이 3,183 이 중복 제거분이다. 인기도는 원문 합 쪽인 `recipe.review_count` 를 쓴다 — D-9.)*

## ✅ S2 완료 기록 *(2026-09-02)*

`app/core/` 3개 파일. 동기 쓰기 · 단일 트랜잭션 · **실패는 삼키되 반드시 센다.**

| 파일 | 역할 |
|---|---|
| `writer.py` | 요청 1건 → `recommendation_log` 1행 + `impression` N행 |
| `counters.py` | 쓰기 계측. 🔴 `degraded` 에 얹지 않는다 (아래) |
| `test_writer.py` | mock 출력 → 실제 DB 종단 검증 **48건** (`make log-test`) |

### 🔴 착수 후 발견 — propensity 가 저장되지 않고 있었다

`keep_candidates()` 가 `list[ScoredCandidate]` 를 돌려주는데 **`propensity` 는
`RankedItem` 에만 있다.** 그대로 짰으면 저장되는 후보가 전부 ② 투영이라
propensity 가 로그에 **단 한 번도 안 남았다.** `is_exploration`·`team`·
`mmr_penalty`·`explore_source` 도 같다.

propensity 는 서빙 순간의 MC 값이 유일본이다 — `user_cluster_stat` 이 갱신되면
사후 재계산이 불가능하다. 없으면 **IPS 분모가 없어 off-policy 평가가 통째로 불가**하고,
하필 propensity ≠ 1.0 인 행은 탐색 슬롯뿐인데 그게 정확히 평가에 필요한 그 행이다.

`merge_served_detail()` 을 신설해 해소했다 — **DDL 변경 없이** 된다.
`RankedItem` 이 `ScoredCandidate` 의 서브클래스이고 `keep_candidates` 는
`recipe_id` 만 읽기 때문이다. 노출분은 16키, 미노출 후보는 8키로 저장된다.
계약 테스트 8건이 회귀를 막는다.

### impression 을 서버가 쓴다 — 그리고 나중에 바꿀 수 있다

프론트 합의가 어려워 **A안(서버가 응답 생성 시점에 기록)** 으로 간다.
그 대가로 `event_log.source` 를 신설했다.

```
served    서버가 응답에 담았다. 본 것이 아니라 보낸 것이다
viewport  클라이언트가 실제 화면 노출을 관측했다
client    사용자 행위 보고 (click·cook…)
```

> 🔴 **이 열이 없으면 두 시대가 섞여 둘 다 못 쓰게 된다.** A 시대 impression 은
> 영구히 "보냈다" 만 뜻하고, 사용자가 실제로 본 것은 어떤 백필로도 복원되지 않는다.
> 전환기에 둘을 동시에 쓰면 `r(p) = viewport(p)/served(p)` 로 환산계수가 나온다.
> 부분 유니크 인덱스에 `source` 를 넣어야 그 dual 기록이 공존한다.

`position` 도 CHECK 로 강제했다 — impression 에 없으면 '상위 k 만 잘라 보기'조차
안 되고 position bias 보정이 불가능하다. DDL 이 막지 않으면 라이터 버그 하나로
조용히 비어간다.

### 실패 정책 — 삼키되 센다

로그 쓰기 실패가 추천을 죽이면 안 된다. 그렇다고 조용히 삼키면 데이터가
비어가는 것을 아무도 모른다. `write_recommendation()` 은 **예외를 올리지 않고**
성공 여부만 돌려주며, 실패를 종류별로 센다.

> 🔴 **`stage_trace.totals.degraded` 에 얹지 않는다.** `degraded` 는 `stage_trace`
> 안에 있고 `stage_trace` 는 그 INSERT 로만 저장된다 — 쓰기가 실패하면
> `degraded=true` 를 담은 행도 같이 사라진다. **실패를 실패한 것 안에 기록할 수는
> 없다.** 게다가 `degraded` 는 폴백율 지표라 로깅 결함을 섞으면 두 신호가
> 한 칸에서 합쳐져 다시 못 나눈다.

계약 위반(동결 키 누락 등)은 행을 쓰되 `request_params.log_degraded` 에 표시한다.
마커는 **대체가 아니라 가산**이다 — 요청 원문은 그대로 보존된다.

> ⚠️ **DB 가 통째로 죽으면 tombstone 도 실패한다.** 실측에서 FK 위반 시
> `failed:ForeignKeyViolation` 과 `tombstone_failed` 가 함께 올랐다.
> 그때 남는 것은 카운터뿐이라, **카운터가 선택이 아니라 필수다.**

### 🔴 적대적 검증에서 확정된 결함 8건 — 전부 수정

구현 직후 4개 렌즈로 반증을 돌려 28건이 제기됐고, 재현 검증을 통과한 **8건**을 고쳤다.

**① 묘비가 정본을 영구히 차단했다 (critical).** `ON CONFLICT DO NOTHING` 때문에,
쓰기가 한 번 실패해 묘비가 들어가면 **그 뒤 어떤 재시도도 정본을 통째로 버리고
`True` 를 돌려줬다.** 카운터는 `written` 을 올려 대시보드는 성공으로 읽는다.

```
1차=False  2차=True   ← 성공했다는 뜻인데
DB 실제:  candidates=NULL  config_hash=NULL  latency=0
```

재시도 시점에는 propensity 가 **아직 메모리에 살아 있었다.** 즉 이건
**복구 가능한 문제를 복구 불가능한 문제로 바꾸는 거래**다 — `07:918` 이
event_log 멱등키에 대해 명시적으로 반려한 바로 그 안티패턴이 반대로 적용돼 있었다.

수정: 묘비에 표식(`log_degraded.tombstone`)을 남기고, **묘비 위에서만 승격**하는
가드된 UPSERT 로 바꿨다. 정본 위에는 절대 덮지 않는다 — 로그는 append-only 다.
`RETURNING (xmax = 0)` 으로 `written`·`promoted`·`duplicate` 를 구분해 센다.

**② `include_trace=false` 가 `stage_trace` 를 통째로 잃었다.** 그 플래그는
**응답 페이로드**를 줄이라는 뜻이지 로그를 비우라는 뜻이 아니다. 게다가
`serving_mode` 도 추적에서 읽으므로 `real` 로 잘못 떨어져 **절단 폭이 10 대신 50**
이 됐다. 실효 추적을 함수 첫머리에서 정하고 `trace=` 인자를 열었다.

**③ 점수 재현 필수값이 조용히 NULL 이 됐다.** `config_hash`·`warm_alpha`·
`stats_version` 을 호출자가 안 주면 표시 없이 NULL 이었다 — 나중에 "왜 이 추천이
나왔나" 에 답할 수 없는 행인데 겉보기엔 정상이다. `log_degraded.not_reproducible`
로 표시한다.

**④ 풀이 커넥션을 1개만 캐시했다.** psycopg2 는 `putconn` 시 `minconn` 을 넘는
커넥션을 **닫는다.** 기본 `minconn=1` 이라 동시 8요청이면 7개가 매 라운드 재접속했다.
원격 DB 면 그게 그대로 지연이다 (06 8-D ④). `minconn` 기본값을 `maxconn` 과 같게 뒀다.
실측 p50 32ms → **2.5ms**.

**⑤ 묘비가 실패를 유발한 값을 재사용했다.** 직렬화 불가한 `context` 나 너무 긴
문자열이 원인이면 **마지막 보루까지 같이 죽는다.** 묘비는 반드시 직렬화되는
최소 페이로드만 싣게 했다.

**⑥ 긴 `session_id` 하나로 요청 로그가 통째로 유실됐다.** DDL 이 `VARCHAR(64)` 인데
라이터가 자르지 않았다. 세션이 조금 뭉치는 것보다 행 유실이 훨씬 비싸므로 잘라서 남긴다.

**⑦ `impressions` 카운터가 시도 수를 셌다.** 삽입 수를 세도록 `RETURNING` 을 붙였다.

**⑧ 🔴 내가 넣은 CHECK 가 추천 로그를 삭제 불가로 만들었다.**
`CHECK (source <> 'served' OR request_id IS NOT NULL)` 이 FK 의 `ON DELETE SET NULL`
과 충돌한다 — `recommendation_log` 행을 지우면 `request_id` 가 NULL 이 되면서
CHECK 를 위반한다. 실측으로 `DELETE` 가 `CheckViolation` 으로 막혔다.
**보존기간 만료분 정리가 막히고, 그건 개인정보 파기 의무와 충돌한다.**
CHECK 를 걷어내고 이 불변식은 라이터가 지킨다.

> 🔑 **검증이 제 수정의 버그도 잡았다.** ② 를 고치면서 헬퍼 두 개가 여전히
> `resp.trace` 를 읽고 있었는데, 새로 쓴 테스트가 절단 폭 불일치로 그것을 잡았다.

### mock 결함 3건 수정 — 픽스처로 쓰려면 먼저 고쳐야 했다

| 결함 | 실측 | 조치 |
|---|---|---|
| 같은 요청 2회에 propensity 가 다름 | 0.325 → 0.265 | 클러스터 관측을 인라인 누적하지 않는다. 실제 구현도 배치가 갱신하므로 이쪽이 현실에 가깝다 |
| `final_rank` 가 점수 내림차순이 아님 | top_k=20 에서 **53/59명** 역전 | 정렬 후 부여. `mixed_exploration` 의 전제이기도 하다 |
| `degraded` 가 상수 20 과 비교 | top_k=8 이면 항상 참 | `req.top_k` 와 비교 |

## 🔑 3인 병렬로 전환 *(2026-09-02)*

AI 파트가 **2인 → 3인**이 되면서 순차 S3→S7 을 **3트랙 병렬**로 재편했다.

| 트랙 | 맡는 스텝 | 공수 *(지시서 `**기간**` 합 · 09-03 실측)* | 자기 폴더 |
|---|---|---|---|
| **A** 데이터 | S6 잔여 · S7 (정규화 배치 → `recipe_feature`) | ~~9일~~ **9.25일** (14작업) | `app/services/normalize/` |
| **B** 엔진 | S3 (피처·스코어러) + 🔴 로그 개통 | ~~10.5일~~ **11.75일** (19작업) | `app/services/recommends/` `app/db/` |
| **C** 관측 | S4 (평가 하네스) · S5 (화면 2종) | **10.5일** (14작업) | `infra/app/` `app/services/eval/` |

~~남은 15영업일 대비 버퍼 약 30%.~~ **최장 트랙(B 11.75일) 기준 버퍼 3.25일 — 약 22%.**
지시서는 [`docs/draft/작업분담_*.md`](draft/03_작업분담_공통.md) 이고,
**트랙 간에 부딪히는 것은 [`05_작업분담_결정사항.md`](draft/05_작업분담_결정사항.md) D-1~D-19 에서 이미 정했다.**

**병렬이 가능한 이유는 S0~S2 가 경계를 만들어 놨기 때문이다** — 스키마 **29종**,
계약 **98건**, API **8종**이 고정돼 있어 세 사람이 서로를 기다리지 않는다.

> 🔴 ~~**3주 계획에 DDL 변경이 없다** — 필요한 테이블 5종이 이미 전부 있다.~~
> **틀렸다 (09-03 정정).** 실제로는 09-02~03 에 DDL 이 여섯 군데 바뀌었다 —
> `event_log.source` · `session_id` 접두어 CHECK · `user_vector` 온보딩 원본 2컬럼 ·
> `pantry_item.purchased_at` · `daily_recommendation` 신설 · 로더 멱등 UNIQUE 2종
> (+ `ALTER DATABASE ... SET timezone`).
> **전부 소급 불가라 지금 박아야 하는 것들이었다** — 실유저 0명·로그 0행인 지금이
> 재초기화 비용이 24초인 유일한 구간이다. **이 뒤로는 D-8 대로 셋이 모여서 한다.**
>
> 남은 3주에 **새로 필요한 테이블은 없다** — `batch_run` · `normalization_queue` ·
> `data_quality_snapshot` · `feature_stats` · `scoring_config` 5종이 이미 있다.

> 🔴 **B-1(mock → 라이터 연결)이 최우선이다.** 추천 기록이 0행이라
> C 가 화면에 띄울 데이터가 없다. 그리고 **B-1 이 로그 규약이 잠기는 시점**이다 —
> 그 전까지는 규약을 공짜로 바꿀 수 있다.

### 소유권 — 겹치면 반드시 어긋난다 *(D-1 · D-5 · D-6)*

| 대상 | 소유 | 다른 트랙은 |
|---|---|---|
| `batch_run` (배치 1회 = 1행) | **A** | C 가 읽어서 "마지막 배치가 언제 무엇을" 을 화면에 |
| `data_quality_snapshot` (품질 시계열) | **C** | **A 는 쓰지 않는다** |
| `recommendation_log` · `event_log` | **B 만 쓴다** | C 는 읽기 전용 |
| `normalization_queue` · `normalization_audit` | C 가 검수 결과를 쓴다 | — |
| `app/schemas/` · `app/main.py` | **B** | 고치면 `make contract` 98건 확인 |
| `app/services/normalize/` | **A** | — |
| `infra/app/` | **C** | — |
| `infra/init/*.sql` | **아무도 혼자 안 고친다** | 셋이 모여서 |
| `Makefile` · `scripts/doc_check.py` | 공용 | 자기 타깃만 · 문서 수치를 고치면 등록 |

> 🔴 **`mention_total` 정의는 "원문 행수"(451,862)로 통일한다** (C 기준 · D-1).
> A 가 실제로 매칭한 수는 `batch_run.output_count` 에 있다.
> **같은 칸에 두 정의가 들어가면 A 배치 전후로 시계열이 조용히 꺾인다.**

> **배치를 돌리기 전에 알린다** (D-7). A 의 정규화는 **15분간 35만 행을 쓴다** —
> 그동안 B·C 가 응답시간을 재면 숫자가 틀린다.

> 🔴 **`.env` 의 `REVIEW_SALT` 는 절대 재생성하지 않는다** (D-8).
> 후기 624,422건의 작성자 해시가 이 값으로 만들어졌다. 바꾸면 재적재 때 중복으로 쌓이고
> "같은 사람이 쓴 후기" 판정이 과거와 어긋난다. **재적재(24초)를 해도 salt 만 같으면 재현된다.**

### 착수 전 고친 것 — `Dictionary.from_db()` 가 죽어 있었다

`ingredient` 와 `ingredient_category` 양쪽에 `id`·`name` 이 있는데 한정하지 않아
`AmbiguousColumn` 으로 죽었다. **A 트랙 첫 작업이 여기서 막혔을 것**이라 미리 고쳤다
(536종 · 별칭 245 · staple 39 로딩 확인).

## 🔑 09-02~03 추가 확정 — DDL·계약·구현

**전부 "지금 안 하면 소급 불가"에 해당한다.** 로그 0행 · 실유저 0명인 지금이
공짜로 바꿀 수 있는 마지막 구간이라 몰아서 했다. 근거는 D-15~D-19 에 있다.

| | 무엇 | 왜 지금 | 검증 |
|---|---|---|---|
| **①** | **`event_log.source`** — `served`/`viewport`/`client`. NOT NULL · DEFAULT 없음. impression 은 `position` 필수(CHECK) · 부분 유니크 `ux_ev_impression(request_id, recipe_id, source)` | 서버 기록(A안)과 클라이언트 관측이 섞이면 **두 시대를 둘 다 못 쓴다.** 열이 있으면 전환기에 `r(p)=viewport(p)/served(p)` 로 환산된다 | `ddl-test` |
| **②** | **`session_id` 접두어에 `d-` 추가** — `c-`실사용자 `g-`게스트 **`d-`개발·디버거·시딩**. `v_real_events`·`v_real_recommendations` 가 `d-` 와 `model_version LIKE 'mock-%'` 를 거른다 | `is_simulated` 는 '가상 유저'만 잡는다 — **개발자가 실유저 ID 로 디버거를 눌러본 것은 통과한다.** 나중에는 못 가른다 (D-15) | `ddl-test` |
| **③** | **`user_vector.onboarding_picks` · `onboarding_scales`** — 온보딩 원본 보존 | `taste_vec` 은 고른 레시피 `flavor_vec` 의 **평균**이라, 원본이 없으면 시드가 바뀔 때 재계산이 불가하고 **유저를 다시 모아야 한다.** 실제로 09-02 에 시드 2건을 고쳤다 (D-16) | `contract`·`ddl-test` |
| **④** | **`pantry_item.purchased_at`** — 소비기한 추정의 기준일. 미래 구매 CHECK. `effective_expiry` 가 `COALESCE(purchased_at, added_at::date)` 로 바뀜 | `added_at`(앱 등록)을 기준으로 잡으면 마트에서 사고 사흘 뒤에 등록한 만큼을 **공짜로 벌어줘** 임박 판정이 늦어진다 | `ddl-test` |
| **⑤** | **`daily_recommendation` 신설** — `user_id`·`rank` PK · `reason` · `reason_source`(`llm`/`template`/`fallback`) · **`pantry_fingerprint`** · `config_hash` · `stats_version` · `batch_run_id` | 트랙1을 **사전 계산**으로 정했다 (D-19). 배치 구현은 3주 계획 밖이지만 **빈 테이블의 비용은 0** 이고 나중에 만들면 전원 재초기화다 | 5건 (`ddl-test` 중) |
| **⑥** | **로더 멱등 제약** — `recipe_ingredient_raw UNIQUE(recipe_id, position)` · `recipe_review UNIQUE(recipe_id, author_hash, written_at) NULLS NOT DISTINCT` | 2회 실행 시 원문 재료가 전량 2배(451,862 → 903,724)가 됐다 | 재적재 실측 |
| **⑦** | 🔴 **DB 시간대 `Asia/Seoul`** (`01_extensions.sql` 의 `ALTER DATABASE`) | UTC 면 한국 자정~오전 9시에 `current_date` 가 하루 전이라 `f_expiring` 임박 판정이 어긋난다. **컨테이너 `TZ`/`PGTZ` 만으로는 부족하다** — 밖에서 붙는 psycopg2 에 적용되지 않는다 | `doc-check` |
| **⑧** | **탐색 슬롯을 실제로 꽂는다** | 이전에는 `exploration_slots()` 결과를 **버려서** 탐색 아이템이 점수 순서 그대로 남았다. propensity 는 남는데 위치가 안 퍼져 **IPS 의 위치별 검사확률 곡선이 안 나온다** | `contract` "탐색 아이템이 여러 위치에 퍼진다 (8종)" |
| **⑨** | **`retrieve_candidates` / `retrieve_for_user` 에 `p_include_test`** (기본 `FALSE`) — `feature_version` 이 `test-` 로 시작하면 조회에서 제외 | B·C 의 합성 `recipe_feature` 5만 건이 실추천에 그대로 섞였다. **규율이 아니라 구조로 막는다** (D-17) | 실증 09-03 |
| **⑩** | **온보딩 계약** — `POST /v1/onboarding/{user_id}` · `OnboardingIn`/`OnboardingOut`. `picks`(레시피 인덱스) · `scales`(3축 0~4) · `allergy_groups` · `allergy_ingredient_ids` · `avoid_ingredient_ids` · `household_size` | S0 ② 로 문항이 확정됐는데 **받을 계약이 없었다** | `contract` |
| **⑪** | `PantryItemIn.purchased_at` 추가 · `PantryIn.removed` 를 mock 이 더는 버리지 않는다 | ④ 와 S0 ⑤(소진/폐기 1비트)의 프론트 쪽 짝 | `contract` |
| **⑫** | `PENDING_DATA_FEATURES = {f_cuisine, f_season}` · `ACTIVE_WEIGHT_TODAY = 0.94` 신설 | `UNAVAILABLE`(수단 없음)과 `PENDING`(데이터 없음)은 **가중치 처리가 반대다** — 앞은 0 이어야 하고 뒤는 그대로 둔다 (D-4) | `contract` |
| **⑬** | `make seed-reset` 가드 · 시드 결함 2건 수정 | `TRUNCATE ... CASCADE` 가 `recipe_ingredient`·`pantry_item` 까지 지운다 — 행이 있으면 멈추고 `FORCE=1` 로만 강행. 시드는 식용유 기름짐 0.60→**1.00**, 멸치 3종 분리(국물용/잔멸치/총칭) | `validate` |
| **⑭** | **`pyproject.toml` + `uv.lock`** · 구 `db/requirements` · `reco/requirements` 파일 폐지 | 트랙별 extras 로 갈랐다 — `api`(fastapi·uvicorn) `dash`(streamlit) `obs`(mlflow) `ml`(numpy·scikit-learn) `rank-v1`(lightgbm). 🔴 **맨손 `uv sync` 금지** — 락에 없는 패키지를 지운다. 실제로 09-02 에 fastapi·numpy 가 사라져 mock 서버가 죽었다. 설치는 `make install TRACK=A\|B\|C` | `doc-check` |

> 🔴 **⑨ 는 함수 시그니처를 바꾼다.** `CREATE OR REPLACE` 만으로는 구버전이 남아
> **오버로드**가 되고, 기본 인자로 부를 때 어느 쪽이 잡힐지 모호해진다.
> `04_functions.sql` 맨 앞에 `DROP FUNCTION IF EXISTS` 를 넣었다 (실제로 09-03 에 겪었다).
> `doc-check` 가 "`retrieve_for_user` 가 한 벌만"으로 회귀를 막는다.

> 🔴 **온보딩 알러지는 `severity='allergy'` 로 저장해야 한다.** `avoid` 로 넣으면
> 그룹 확산(`allergen_group`)이 적용되지 않아 **4갈래 차단 중 한 갈래가 조용히 죽는다.**

### 검증 건수 — 09-03 실측

| 명령 | 건수 | 무엇 |
|---|---:|---|
| `make contract` | **98** | 스테이지·API 계약 (DB 불필요) |
| `make ddl-test` | **47** | 소급 불가 컬럼 왕복 |
| `make normalize-test` | **113** | 74(P1·P2) + 23(P3) + 16(P4) |
| `make smoke` | **13** | Retrieval 정확성·지연 |
| `make smoke-py` | **19** | 같은 9건을 `app.db.retrieve()` 로 + 파이썬 전용 6건 |
| `make log-test` | **48** | mock 출력 → 실제 DB 종단 |
| `make doc-check` | — | 문서 수치 ↔ DB·코드 대조. **건수는 검사가 늘 때마다 바뀌므로 여기 고정하지 않는다** |

## 왜 이 순서인가 — 핵심 셋

**S2(로그)가 S3(스코어러)보다 먼저다.** 순환처럼 보이지만 아니다 —
`app/main.py` 가 이미 `FEATURE_KEYS` 17키 전량과 `stage_trace` 를 만든다.
**mock 출력만 흘려도 라이터가 종단 검증된다.** 그리고 **로그는 소급이 안 된다** (07 E-3).
스코어러를 기다리다 로그 규약을 놓치면 그 기간의 데이터는 영원히 못 쓴다.

**S4(평가 하네스)는 로그가 필요 없다.** 입력이 손으로 쓴 배열이라 지금 바로 된다.
그리고 **테스트가 먼저여야 한다** — IDCG 를 top-K 안에서만 계산하는 버그는
편향이 +0.182 인데 재려는 효과크기(0.01~0.03)의 **6~18배**다 (07 D-1).

**S5(화면)가 가장 급하다.** A 의 스코어러가 아니라 **B 의 착수**가 병목이다.
검수 큐의 `suggested` 가 비면 반나절 스프린트가 3일이 된다 (6절).

## 크롤 없이 어디까지 가는가

피처 17종의 가중치를 데이터 의존으로 묶으면 이렇다.

| 무엇이 필요한가 | Σw | 피처 |
|---|---|---|
| Retrieval 이 이미 준다 | **0.29** | `f_coverage` · `f_missing` |
| pantry + 시드 | **0.15** | `f_expiring` |
| **온보딩 설문** | **0.31** | `f_taste` · `f_ing_pref` · `f_cuisine` · `f_dish_type` |
| cook 이력 | 0.10 | `f_cooccur` |
| **크롤** | 0.13 | `f_popularity` · `f_time_fit` · `f_skill_fit` |
| 그 밖 | 0.02 | `f_season`(시드 없음) |

> 🔑 **크롤이 여는 것은 0.13 뿐이고 온보딩이 0.31 을 연다.**
> 크롤을 기다리는 동안 온보딩 설문을 확정하는 편이 값어치가 크다.
> *(둘 다 끝났다 — 크롤은 09-02 적재 완료, 온보딩 문항은 09-01 확정 · 계약은 09-03.)*

> **오늘 실제로 켜지는 가중치 합은 `ACTIVE_WEIGHT_TODAY = 0.94` 다** (설계 의도 1.00).
> 빠진 0.06 은 `f_cuisine`(0.04) · `f_season`(0.02) — `PENDING_DATA_FEATURES` 다.
> **수단이 없는 게 아니라 데이터가 없는 것이므로 가중치를 0 으로 내리지 않는다** (D-4).
> 분모에서 같이 빠지는 나눗셈 정규화라 지금 값이 왜곡되지 않고, 데이터가 오면 그냥 켜진다.

~~크롤이 늦으면 S1~S5 로 채우고, 그 뒤엔 로더를 미리 짜서 fixture 3건으로 왕복 검증한다.~~
**크롤은 도착했고 적재도 끝났다 (09-02).** 위 문단은 대기 시점의 계획으로 남긴다.
크롤러 미확인 3항목 중 **리뷰 원문은 확보**(624,422건, S0 ④), `group_name` 과 카테고리 축은
**여전히 미확인**이다 — 아래 [7절](#7-미해결-의존성)을 본다.

## ✅ 온보딩 제시 레시피 선정 *(AI 파트 · v2.1 신규 · **완료**)*

**프론트가 화면을 만들고 우리가 데이터를 준다.** 그래서 "무엇을 보여줄 것인가"가
AI 파트 작업이 됐는데 기존 목록에 없었다. ~~2인 반나절.~~

`taste_vec = 고른 레시피의 flavor_vec 평균` 이므로 **제시 목록이 곧 측정 도구**다.
20개가 다 비슷한 맛이면 3개를 골라도 정보가 안 나온다.

산출물은 `seeds/onboarding_recipes.yaml` — ~~후보 50개~~ **제시 20 + 예비 31 = 51개**
(`presented` 20 · `reserve` 31 · `axes` **6축**). 크롤 데이터가 필요 없다.
선정 도구는 `scripts/bench/onboarding_pick20.py`.

> 🔴 **고른 결과만 저장하면 안 된다.** `taste_vec` 은 평균이라 되돌릴 수 없다 —
> `user_vector.onboarding_picks`(고른 인덱스) · `onboarding_scales`(척도 3축 원본)에
> **원본을 같이 남긴다** (D-16). 시드가 바뀌면 재계산 한 번으로 끝나고,
> 없으면 유저를 다시 모아야 한다.

> **축은 6축으로 저장하되 실제 점수에 몇 축을 쓸지는 W3 쌍대비교에서 정한다** (D-11).
> 뒤 3축(신맛·감칠맛·기름짐)은 척도 문항이 없어 체계 편향이 그대로 남는다.
> 넓게 저장했다가 슬라이스를 줄이는 것은 소급 가능하고, 반대는 불가능하다.

## ✅ S0 — 규약 동결 5건 *(09-01~02 에 5건 전부 닫힘)*

**이 다섯 말고는 결정을 기다릴 이유가 없다.** 나머지는 기본값으로 밀어도 된다 —
5-2-2-1 의 나눗셈 정규화 덕에 값 교체가 적용 순서와 무관하고,
`config_hash` + `scoring_config` 로 과거 점수가 복원되기 때문이다.

> 🔑 **"동결했다"는 결정을 내렸다는 뜻이지 데이터가 묶였다는 뜻이 아니다.**
> 09-03 실측으로 **추천 기록 0행 · 사용자 행동 0행 · 실유저 0명 · 온보딩 응답 0건**이다.
> 그래서 **지금은 로그 규약도 온보딩 문항도 가중치도 여전히 공짜로 바꿀 수 있다.**
> 진짜로 잠기는 시점은 트랙 B 의 `B-1`(mock → 라이터 연결)이고,
> **그 전이 규약을 다시 볼 마지막 기회다.** 이미 잠긴 것은 `REVIEW_SALT` 하나뿐이다.

| # | 무엇을 | 왜 지금 | 어디 |
|---|---|---|---|
| **①** ✅ | ~~**로그 규약**~~ **확정 (09-01)** — propensity 가 무엇의 확률인가 · `request_params` 동결 키 10종 · `served ⊆ candidates` | 서빙 순간에만 존재한다. 정의가 흔들린 로그는 사후 구분 불가 | 07 E-3 ①② |
| **②** ✅ | ~~**온보딩 문항 3건**~~ **확정 (09-01)** — 알러지·식이제약·조리시간 별도 문항 여부, 선호/기피 재료 개수 | 100명 모은 뒤 바꾸면 데이터가 반쯤 버려진다 | 02 I-8 |
| **③** ✅ | ~~**개인정보 동의 문구**~~ **초안 확정 (09-01)** — 파기일·연락처는 팀 | 모집 안내문에 들어가야 한다. 수료 후엔 재동의 대상자를 못 모은다 | 07 E-3 ⑩ |
| **④** ✅ | ~~**`reviews` 수집 범위**~~ **확정 (09-02)** — 전량+해시 | 유일한 실제 (유저,레시피) 상호작용인데 어댑터가 개수만 세고 버린다. 재크롤 불가 | 02 C-5 |
| **⑤** ✅ | ~~**pantry 소진/폐기 1비트**~~ **확정 (09-02)** — "다 썼어요 / 상해서 버렸어요" | 부호가 반대인 두 신호를 합치면 영원히 못 나눈다. DDL 은 갔고 **UI 가 병목** | 07 E-3 ③ |

---

# 4. 주차별 마일스톤

> ⚠️ **W2~W5 의 A/B 는 2인 시절 표기다.** 09-02 이후의 담당은 3-1 절의 A·B·C 트랙이고,
> **W6~W8 의 실제 배분은 [`draft/작업분담_*.md`](draft/03_작업분담_공통.md) 가 정본**이다.
> 이 절은 "언제까지 무엇이 되어 있어야 하는가"(완료 기준)를 남긴다.

## W2 — 크롤링 없이 할 수 있는 것

| | 산출물 | 완료 기준 | 상태 |
|---|---|---|---|
| A | `app/services/normalize/` P1 전처리 · P2 분해 | 설계 4-0 표현이 테스트 통과 | ✅ **74건 통과** |
| A | P3 매칭 캐스케이드 L0~L2 | 시드 사전으로 exact·alias·rule 매칭 | ✅ **완료** — mention ~~79.4%~~ **80.8%** |
| B | 디버거 화면 v1 | mock 응답으로 trace 시각화 · 피드백 버튼 | 🔴 **미착수** — `infra/app/` 이 아직 없다. C 트랙 S5 |

**P3 부터 DB 가 필요하다** (`ingredient` · `ingredient_alias` 조회).
시드는 이미 적재되어 있으므로 바로 이어갈 수 있다 — 크롤링은 여전히 불필요하다.

fixture 는 74건으로 설계에 나온 패턴은 전부 덮었다. 목표 200건은
**크롤링 데이터가 오면 실제 표현을 추가**해 채운다.
*(현재 `make normalize-test` 는 **113건** — 74(P1·P2) + 23(P3) + 16(P4).)*

## W3 — 🔴 크롤링 데이터 도착이 전제

| | 산출물 | 완료 기준 |
|---|---|---|
| A | ✅ ~~🔴 **W3 초 · DDL 일괄 개정 + 재초기화 1회** (4.5~5.5h)~~ **완료 (09-02~03)** — session_id 2테이블 · pantry tombstone/detail · scoring_config · policies/team · FK 제거 · consent (07 E-3·E-4) **+ 09-02~03 확정분 ①~⑦** | 스모크 13/13 재통과 ✅ · `ddl-test` 47건 ✅. ~~시한 = 크롤 데이터 적재 전~~ — **실제로는 적재 뒤에 했고 재초기화 + 재적재가 24초였다.** 지금 비싸지 않은 이유는 로그 0행·실유저 0명이기 때문이지 시한을 지켜서가 아니다 |
| A | ~~P3 매칭~~ ✅ · ~~P4 역할~~ ✅ · **P5 수량 환산** | **1차 커버리지 측정 완료** — 실데이터 mention **80.8%** |
| A | ~~쌍대비교 화면~~ **구글폼** + BT 학습 *(v1.9·v2.1 · **D-18 로 개정**)* | 시나리오 2~3개 · **각각 D-3 임박 재료 2~3개 필수** · `f_expiring` 차분 0 쌍 비율 점검 통과 · 5블록 가중치 산출. **쌍은 블록별 120쌍씩 층화 추출** (`scripts/bench/pair_export.py` → TSV → 구글폼) |
| B | 검수 큐 화면 + `suggested` 후보 5개 | 1건 15초에 처리 가능 |

> 🔴 **쌍대비교 화면은 만들지 않는다 (D-18 · 09-03).** C 트랙이 이미 11일치라 넘치고,
> 한 번 쓰고 끝나는 수집이라 **구글폼으로 간다 — 개발 0일.** 쌍 추출 스크립트가
> 폼에 붙여넣을 형태(630문항 = 600 + 5% 중복)로 바로 내보낸다.
> 그리고 **쌍은 `recipe_feature` 가 채워진 뒤(A-4 후)에야 실측할 수 있다** — 지금은 0행이다.

**W3 말 목표: mention coverage ≥ 0.55** (설계 4-8)

## W4 — 검수 스프린트 · 팀원 투입

| | 산출물 | 완료 기준 |
|---|---|---|
| — | **검수 스프린트** | **300종 = 75분** → 커버리지 89.4% *(v2.2 실측 정정)*. 단위는 **종**이지 건이 아니다 |
| A | Ranking v0 | 디버거에서 개인화된 순서가 나온다 |
| B | 데이터 품질 화면 | 커버리지 추이가 보인다 |
| B | **쌍대비교 수집** *(v1.9·v2.1 · D-18)* | **20명 × 30쌍 = 600건 + 5% 중복 출제** → 중복 쌍 재현율 = **라벨 일관성** 산출. 검수 스프린트와 **같은 날 +5분**. **블록별 120쌍 보장** · "둘 다 아니다" 선택지 포함(학습에서 제외) |

> 🔴 **블록별로 뽑지 않으면 블록이 죽는다** (D-18). BT 가 배우는 것은 5블록의 비율인데,
> **어떤 블록의 차분이 0인 쌍만 모이면 그 가중치는 식별되지 않고 우리는 그 사실조차 모른다.**
> 가장 위험한 것은 **임박소진**(`f_expiring`, w=0.15) — 피처가 하나뿐이라 대체할 것이 없고
> 냉장고를 고정하면 두 레시피의 값이 같아지기 쉽다. 그래서 시나리오 C(임박 없음 대조군)를 둔다.
> **뽑기 전 점검:** 각 블록에서 `|차분| > 0.1` 인 쌍이 120개 미만이면 그 블록은 600쌍으로 학습 불가다 —
> 쌍을 더 뽑아도 안 나오고, 냉장고 시나리오를 늘려야 한다.

**W4 말 목표: coverage ≥ 0.75 · 추천에 개인화가 들어감**

## W5 — 표현 학습

| | 산출물 | 완료 기준 |
|---|---|---|
| A | **임베딩 파이프라인** — ko-SBERT 배치 · 검색 · kNN 분류 | 자연어 검색 동작 · `cuisine` 자동 분류 |
| A | **k-means K≈50 (S-A)** *(v2.0)* | 캡 대체가 켜진다 — 점수 유지 89.0% → 98.6%. **`sklearn.KMeans`(`n_init=10` · `random_state` 고정) — 이미 설치돼 있다** |
| B | 평가 하네스 + 지표 | **정답 아는 데이터로 하네스 자체를 먼저 검증** |

> 🔴 **클러스터는 A-4 직후 한 번만 매긴다** (D-12). `recipe_feature.cluster_version` 은
> 있는데 **`recommendation_log` 에는 판 번호 칸이 없다** — 재클러스터링하면 과거 로그의
> `cluster_id` 가 무엇을 가리켰는지 복원 불가고, Thompson belief 와 탐색 분석이 조용히 오염된다.
> 굳이 2회차를 해야 하면 `cluster_version='v2-…'` 로 올리고 **그 시각을 `batch_run` 에 남긴다.**

> **μ(`flavor_mu`)는 A-5·A-13 두 번만 갱신한다** (D-13). 매칭률 높은 레시피군과 낮은 군 사이
> 차이가 **매움 +23.5% · 기름짐 −29.7%** 라, 검수로 커버리지가 80.8% → 89% 가면 μ 가 그만큼 움직인다.
> 갱신하면 `batch_run(job_name='flavor_stats')` 에 남기고 다른 트랙에 알린다.

**W5 말 목표: coverage ≥ 0.85 · 오프라인 지표 산출 가능**

## W6 — 유저 확보

| | 산출물 | 완료 기준 |
|---|---|---|
| A | Re-ranking (MMR · exploration 슬롯) | 상위 10개 재료 자카드 평균이 낮아진다 · **탐색 아이템이 실제로 여러 위치에 퍼진다**(계약 검증) |
| B | **유저 모집 + 로그 수집** | **누적 그룹 1,000+ (요청 건수)** |
| B | **트랙1 `daily_recommendation` 배치** *(D-19 · 여유 있으면)* | 사유 사전계산 + `pantry_fingerprint` 로 stale 판정. **테이블·계약은 이미 있다** |

> ⏳ **트랙1 배치 구현은 3주 계획에 없다.** 테이블과 계약만 준비했다 —
> 기능명세 `MAIN-000` 이 약속한 것이라 **안 만들면 프론트에 알려야 한다** (D-19).
> 그리고 배치를 짜는 시점에 ②~⑤(배치 위치 · LLM 출력 검수 · stale 허용 범위 · 트랙1 Top-N)를 정한다.

> **여기가 프로젝트의 분기점이다.** 유저 100명 × 20요청을 못 채우면 W7 의 LightGBM 이
> 성립하지 않는다 (설계 6-6).

## W7 — 학습과 리포트

| | 산출물 | 완료 기준 |
|---|---|---|
| A | LightGBM v1 + MLflow 실험 4개 | `baseline-cf-als` 실패를 숫자로 보인다 |
| A | **Interleaving 으로 v0 vs v1 비교** *(v1.9)* | A/B 는 검정력이 없다 (설계 5-7-2) |
| B | 리포트 초안 | 지표표 · 그래프 완성 |

## W8 — 안정화

| | 산출물 |
|---|---|
| A | 성능 점검 · 폴백 비율 확인 · 버그 |
| B | 리포트 완성 · 발표 자료 · 데모 시나리오 |

**W8 에 새 기능을 넣지 않는다.** 데모가 깨지는 가장 흔한 원인이다.

---

# 5. 컷라인 — 언제 무엇을 포기할지 미리 정한다

**결정을 미루면 늦게 포기하게 되고, 늦은 포기는 그때까지 쓴 시간을 전부 버린다.**

| 시점 | 조건 | 조치 |
|---|---|---|
| ~~**W3 말**~~ ✅ | ~~크롤링 데이터 없음~~ | ~~**Food.com 공개 데이터로 전환.**~~ **발동하지 않았다** — 09-02 에 46,353건 적재 완료 |
| **W4 말** | coverage < 0.60 | 검수 2차 스프린트. **알고리즘을 손대지 않는다** — 사전 부족이 원인이다 *(현재 80.8% — 여유 있다)* |
| **W5 말** | coverage < 0.70 | 임베딩 파이프라인(P2) 포기 → 정규화에 시간 재배분 |
| **W5 중** *(v2.0)* | 버퍼 < 15h | **S-B(Thompson) 부터 자른다.** S-A 는 지킨다 |
| **W4 말** *(v2.1)* | 쌍대비교 < 300쌍 **또는 중복 쌍 재현율 < 70%** (라벨이 시끄러움) | 5블록도 포기. 가중치는 직관값 그대로 두고 **리포트에 "미검증"으로 명시** |
| **W6 말** | **누적 그룹 < 500** | **LightGBM v1 포기 확정.** v0 선형으로 마무리하고, 리포트를 "콜드스타트 전용 추천"으로 재구성. *(v1.9)* 단 **쌍대비교 라벨이 있으면 그것으로 학습을 시도한다** |
| **W7 중** | LightGBM 이 v0 를 못 이김 | **그대로 보고한다.** 음성 결과도 결과다 |
| **W7 말** | 리포트 초안 없음 | 기능 개발 중단, 전원 리포트 |

> **W6 말 컷라인이 가장 중요하다.** 유저 50명이면 그룹 1,000개로 LambdaRank 과적합이
> 확실하고(설계 6-6), 억지로 학습해서 결과를 싣는 것이 **하지 않은 것보다 나쁘다.**

---

# 6. 데이터 정제 도움 — 딱 한 번, 반나절

**팀원 20명의 시간은 W4 검수 스프린트 한 번에 몰아 쓴다.**

## 준비물 (W3 까지 ~~B~~ **C 트랙**이 완성)

- [ ] 검수 큐 화면 — 후보 5개가 **미리 채워져 클릭만 하면 되는** 상태
- [ ] `freq_count DESC` 정렬 — 빈도 높은 것부터
- [ ] `assignee` 배타 배정 — 2명이 같은 건을 보지 않게
- [ ] `normalization_audit` 기록 — 되돌릴 수 있게
- [ ] **`cuisine` 라벨 200건을 같은 화면에 얹는다** (I-11 — 검수 스프린트에 2분)

> **`normalization_queue.suggested` 는 객체 형식이고 C 가 정한다** (D-2) —
> `{"candidates": [{"name", "score", "method"}], "blocked_by": null}`.
> 배열 `[["매실청", 0.62, "trgm"]]` 로 두면 **위치로 의미를 기억해야 하고 필드를 못 늘린다.**
> `candidates[].name` 은 **반드시 사전에 있는 표제어**여야 한다 — 없는 이름을 만들지 않는다.
> **읽는 쪽(화면)이 형식을 정하고 시더가 그 모양에 맞춘다.** 시더도 C 가 만들고, A 는 미매칭 목록만 넘긴다.

> 🔴 **`score` 가 아무리 높아도 자동 확정하지 않는다.** 임계 0.6 에서 재현율 **0%**,
> 정탐 중앙값 0.143 vs 오탐 0.118 로 겹친다. 사람이 누르기 전엔 반영하지 않는다.

## 실행 *(v2.2 — 실측으로 전면 정정)*

기존 계산 `20명 × 100건 = 2,000건`은 **두 군데가 틀렸다.**

**① 단위가 틀렸다.** 검수 대상은 재료 언급 *건수*가 아니라 **서로 다른 표현의 종수**다.
`밥`이 2,738번 나와도 검수는 **1종**이다. 한 번 판정하면 2,738건이 한꺼번에 해결된다.
건 단위로 세면 필요 인원을 수십 배로 잡게 된다.

**② 규모가 틀렸다.** 2,000종은 반나절에 안 끝난다 — 실측 **500분**이다.

크롤 46,353건 전체를 현재 사전으로 실측한 곡선이다. 기준 커버리지 **80.8%**.

| 검수 종수 | 미매칭 중 덮는 비율 | 도달 커버리지 | 소요(1종 15초) |
|---:|---:|---:|---:|
| 50종 | 25.5% | **85.7%** | 12분 |
| 100종 | 32.5% | 87.0% | 25분 |
| **300종** | 44.8% | **89.4%** | **75분** |
| 500종 | 51.1% | 90.6% | 125분 |
| 1,000종 | 59.3% | 92.2% | 250분 |
| 2,000종 | 66.7% | 93.6% | 500분 |

> 🔑 **50종 12분에 +4.9%p.** 수익이 앞쪽에 극단적으로 쏠려 있다 —
> 뒤 1,500종을 더 봐야 +1.4%p 다. **300종에서 끊고 나머지는 여유 있을 때 한다.**

**W5 목표 85% 는 50종으로 넘는다.** 검수는 병목이 아니다.

> 🔴 **검수는 소급 불가가 아니다.** `recipe_ingredient_raw` 가 불변이므로
> 사전을 고치면 언제든 재정규화된다 (원문 불변 원칙). 로그와 달리
> **늦게 해도 잃는 것이 없다** — 그래서 W4 를 막지 않는다.

> ⚠️ **시트는 사전을 고칠 때마다 낡는다.** `물` 을 등록한 뒤에도 시트 1위가
> `물`(11,229회) 이라 검수자가 이미 끝난 항목부터 보게 돼 있었다.
> `make unmatched` (10분) → `make review-sheet` 순으로 다시 뜬다.

> 🔴 **`suggested` 후보가 비어 있으면 이 계산이 무너진다.** 검수자가 타이핑을 해야 하면
> 1건당 2~3분이 걸리고, 반나절이 3일이 된다. **W3 까지 후보 생성이 반드시 되어 있어야 한다.**

## 2차 스프린트 (조건부)

W4 말 커버리지가 0.60 미만이면 W5 에 한 번 더. 그 이상은 하지 않는다 —
롱테일은 검수 효율이 급락한다.

---

# 7. 미해결 의존성

**우리가 통제할 수 없고 계획 전체를 밀 수 있는 것들.**

| # | 항목 | 필요 시점 | 현재 *(09-03)* | 없으면 |
|---|---|---|---|---|
| ~~1~~ | ~~**크롤링 데이터**~~ | ~~W3 초~~ | ✅ **도착·적재 완료 (09-02)** — 레시피 46,353 · 원문 재료 451,862 · 후기 624,422 | ~~전체가 밀린다~~ |
| **2** | 크롤러 `group_name` 수집 (C-1) | 즉시 | 🔴 **미수집 확정** — 정규화 회귀 테스트가 "R1·R2(`group_name`)는 발동하지 않는다"를 **실측 전제로 못박았다** | `role` 판별 정확도 급락. **P4 는 이미 이 전제 위에서 돌고 있다** |
| ~~2'~~ | ~~🔴 **크롤러 `reviews` 원문 수집 (C-5)**~~ *(v2.1)* | ~~즉시~~ | ✅ **확정·수집 완료 (S0 ④, 09-02)** — 전량 + 닉네임 해시. 624,422건 | ~~CF 기회 영영 소실~~ |
| **3** | 크롤러 카테고리 4축 (C-2) | 즉시 | 🔴 **요리 유형 축 0건 · 평점 0건 · 이미지 0건 실측** | `f_cuisine`(w=0.04)이 `PENDING_DATA_FEATURES` 로 남는다. **재크롤은 46,353건** |
| 4 | 팀원 20명 검수 시간 | W4 | 협의 필요 | 커버리지 목표 미달 |
| **5** | **유저 100명 (I-6)** | W6 | 🔴 **여전히 미결** — 설계 필요 | **LightGBM 포기** |
| ~~6~~ | ~~온보딩 설문 문항 확정 (I-8)~~ | ~~W6 전~~ | ✅ **확정 (09-01 · S0 ②)** — 5문항. 계약도 09-03 에 생겼다 | ~~데이터 반쯤 폐기~~ |
| 7 | **팀원 20명 쌍대비교 30쌍씩** *(v1.9 · D-18)* | W4 | 협의 필요 — **수단은 구글폼으로 확정** | 가중치가 끝까지 추정치로 남는다 |
| 8 | **소비기한 공개자료** (~~I-13~~ → **I-14**) *(v1.9)* | W5 | 시드는 통념 기반 (기본 59 + 예외 42) | 리포트에 "실측"이라 쓸 수 없다 |
| **9** | **`cuisine` 라벨 200건 (I-11)** | 검수 스프린트에 2분 | 미확보 | kNN 분류 불가 → `f_cuisine` 이 계속 꺼져 있다 |

> ~~**1·2·3 이 같은 사람에게 달려 있다.**~~ **1 과 2' 는 해소됐다.**
> 남은 크롤러 의존은 **2(`group_name`)와 3(카테고리 축)** 이고, 둘 다 **적재가 이미 끝난 뒤**라
> 확보하려면 46,353건 재크롤이다. 그래서 **없는 것을 전제로 계속 간다** —
> `f_cuisine`·`f_season` 은 `PENDING_DATA_FEATURES` 로 두고 가중치는 그대로 남긴다 (D-4).

> 🔴 **8 번의 이슈 번호는 `I-13` 이 아니라 `I-14` 다** (09-03 정정).
> `I-13` 은 쌍대비교 라벨 600건이고 **09-03 에 닫혔다.** 소비기한 데이터 출처는 `I-14` 다.

**닫힌 이슈 (09-03 기준):** `I-8`(온보딩 문항) · `I-13`(쌍대비교) · `I-15`(`flavor_vec` 산출).
**남은 미결:** `I-6`(유저 100명) · `I-11`(cuisine 라벨) · `I-14`(소비기한 출처) ·
`I-16`(트랙2 사유 정책) · `I-17`(인증·암호화 / DB 다운 폴백).

---

# 8. 설계 문서와의 차이

| 설계 문서 | 이 계획 | 사유 |
|---|---|---|
| 7트랙 병렬 (T1~T7) | ~~2명~~ **3트랙(A·B·C) + 1회 도움** | 인력 실제 *(09-02 3인 전환)* |
| ⑧ 팀 분할 8개 서브팀 | **작성하지 않음** | ~~2명이면~~ 3명이면 무의미. 이 문서와 `draft/작업분담_*` 이 대체 |
| ⑥ ML 스택 6종 | **임베딩 파이프라인 1개** | ~~485h~~ **495h** 제약 *(§2 결론과 일치시킴 — 485 는 오기였다)* |
| P1 규칙 기반 시뮬레이터 | **컷** | *(v1.9)* 순환 논리. 쌍대비교 600건으로 대체 |
| 대시보드 6화면 | **3화면** | 디버거 · 검수큐 · 데이터품질 |
| Grafana 3보드 | **제외** | Streamlit 대체 |
| MLflow 실험 8개 | **4개** | |
| ④ MealPlan · Feast | **제외** | 이미 스트레치 |

**설계 문서에서 잘라낸 것을 지우지 않는다.** 두 문서의 역할이 다르다 —
설계는 "이 시스템은 어떻게 만드는 것이 옳은가", 실행계획은 "그중 ~~2명~~ **3명**이 8주에 무엇을 하는가".
리포트에서는 이 차이 자체가 **범위 관리를 했다는 근거**가 된다.

> 🔴 ~~**설계 문서를 수정하지 않는다.**~~ **정정 (09-03).** 이 문장은 두 가지를 뭉쳐 놨었다.
> **범위 축소는 설계에 반영하지 않는다** — 그건 이 문서의 일이다.
> 하지만 **실측·결함 정정은 설계에도 반영한다** — 실제로 `01` 은 v1.9 → **v3.0** 으로
> 여러 번 개정됐다(레시피 4.4만 정정 · `event_log.source` · 요리 유형 축 0건 …).
> 틀린 숫자를 설계에 남겨 두면 그게 다시 인용되고, `make doc-check` 가 바로 그걸 막는다.
