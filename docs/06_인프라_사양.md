# 인프라 · 서버 사양

> 작성자: 박재우 · 작성일: 2026-09-03

| 항목 | 내용 |
|---|---|
| 작성 | 2026-08-27 |
| 기준 | [`01_추천시스템_설계.md`](01_추천시스템_설계.md) **v3.0** |
| 갱신 | **2026-09-03** — 🔴 **DB 시간대 Asia/Seoul** · 환경이 `pyproject.toml`+`uv.lock` 로 · 실측 재측정 · **2.5 GB / 7.8 GB / 1,013 MB 잔재 정정** · 시뮬 로그 전제 폐기 |
| 이전 | 2026-09-01 — 레시피 4.4만 정정 (DB 2.50→0.48GB · 벡터 지배 해소) · 트랙1 LLM · DB 분리 |
| 산출 방식 | **행당 크기는 전부 실측 또는 실측 파생치** (아래 2절) |
| 진입점 | [`00_README.md`](../README.md) |

---

# 0. 요약 — 이것만 보면 됨

| | 최소 (동작은 함) | **권장 (신청값)** | 여유 (있으면 좋음) |
|---|---|---|---|
| CPU | 2 core | **8 core** | 16 core |
| RAM | 8 GB | **32 GB** | 64 GB |
| 디스크 | 20 GB | **100 GB** | 200 GB |
| GPU | 불필요 | **없어도 됨** (온라인 기준) | **통합 16GB 또는 VRAM 8GB** — 트랙1 LLM 사유용 |

**필요량 실측치는 4코어 / 16GB / 50GB 다.** 신청값을 2배로 잡는 이유는 아래와 같다.

> 🔑 **이 표는 한 대에 다 올린다는 전제의 합산값이다.**
> DB 를 별도 장비로 분리한다면 **8절 D** 의 분할표를 쓴다 — 그대로 쓰면 안 된다.

### 🔑 왜 실측의 2배를 신청하는가 *(v2.6)*

이 문서는 **예산 산정과 실운영 테스트**에 쓰인다. 실측치를 그대로 신청하면
아래 항목들이 전부 여유분을 잡아먹는다 — **한 번 받은 서버는 늘리기 어렵다.**

| 여유가 필요한 곳 | 근거 |
|---|---|
| **죽은 공간(dead tuple)** | 🔴 실측: 스모크 테스트 뒤 `recipe_feature` 가 **행 3개인데 29 MB** 였다. `VACUUM FULL` 로 51 MB → 1.9 MB 로 줄었다. 재정규화가 TRUNCATE+전량재삽입이라(4-1) **이 현상이 반복된다** |
| **후기 저장** | 02 C-5 미확정. 레시피당 30건이면 **+0.83 GB** |
| **크롤 건수 변동** | ~~4.4만은 상한 추정~~ → **실적재 46,353** (09-02 완료). 재크롤 시 늘 수 있다 |
| **모델 실험** | MLflow 아티팩트 · LightGBM 모델 파일 · 임베딩 재계산본 |
| **동시 실행** | 크롤 적재 + 임베딩 배치 + 개발 서버가 겹치는 순간 |
| **8주 이후 로그 누적** | 월 350 MB (실유저 100명 기준) |

> **CPU 를 8 core 로 올리는 이유는 하나 더 있다** — 임베딩 배치가 CPU 3~6시간인데
> 코어를 2배로 하면 절반이 된다. GPU 없이 갈 것이라면 **코어가 곧 배치 시간**이다.

### 🔑 실측 대조 — 지금 실제로 쓰는 양 *(2026-09-03 재측정)*

크롤 **46,353건** 적재 + 시드 + 계약·정규화 검증까지 끝난 현재 상태다.

| | 실측 (09-03) | 이전 (08-31 / 09-02) | 신청 사양 대비 |
|---|---|---|---|
| `reco-postgres` RAM | **562 MiB** | 559 MiB | 32 GB 의 1.7% |
| `reco-redis` RAM | **5.4 MiB** | 11.9 MiB | — |
| `recodb` 디스크 | **507 MB** — 이 중 `reco` 스키마 **497 MB** | 552 / 542 MB | 100 GB 의 0.5% |
| `.venv` | **170 MB** — 트랙 B *(fastapi·numpy·scikit-learn 확인. streamlit·mlflow·lightgbm 없음)* | 152 MB | uv 관리. C 는 streamlit·mlflow 로 더 크다 |

**큰 테이블 순서** *(`pg_total_relation_size`, 인덱스 포함)*

| 테이블 | 크기 | 행 수 |
|---|---|---|
| `recipe_review` | **186 MB** | 624,422 |
| `recipe` | **160 MB** | 46,353 |
| `recipe_ingredient_raw` | **70 MB** | 451,862 |
| `recipe_step` | **67 MB** | 362,677 |
| `recipe_feature` | 12 MB | **0** — 🔴 **피처 산출 전이다** |

> 🔄 **09-02 표는 `recipe` 를 204 MB 로 적었다.** 재측정에서 160 MB 다 —
> 그 사이 `VACUUM` 이 돌았다. **1위는 `recipe` 가 아니라 `recipe_review` 다.**
>
> ⚠️ **DB 크기는 같은 날 안에서도 10~20 MB 씩 흔들린다** (VACUUM · WAL · 테스트 삽입).
> 그래서 `make doc-check` 는 **자릿수만** 대조한다 — 정확히 맞추려 들면
> 아무 잘못 없이 매번 빨개진다.
>
> 🔴 **`recipe_feature` 가 0행이라는 것이 중요하다.** 아래 1-0·2절의 임베딩·HNSW 수치는
> **전부 투영값이지 실측이 아니다.** 실측 표시가 붙은 것은 행당 크기(3,080 B)뿐이다.

8주 누적 로그를 더해도 **1 GB 미만**으로, 단일 PostgreSQL 이 여유롭게 감당한다 —
그래서 DuckDB·Parquet 를 도입하지 않았다 (`docs/draft/01_기술스택.md` Roadmap).

> 🔴 **`VACUUM FULL` 전에는 59 MB 였다.** 스모크 테스트(합성 5만~20만 건)가 남긴
> 죽은 공간이며 **레시피 건수와 무관하다** — 행 3개짜리 `recipe_feature` 가 29 MB 였다.
> ~~4.4만 건 산정은 1-0 의 샘플 실측 기반 **2.5 GB** 를 쓴다.~~
> 🔴 **정정 (09-03) — 그 2.5 GB 는 23만 건 전제의 잔재다.** v2.9 가 1-0 을 다시 계산해
> **0.48 GB** 를 냈는데 이 문장만 옛 값을 붙들고 있었다. **문서 안에서 쓰는 값은 0.48 GB 다.**
> 상세는 9절 autovacuum.

> ⚠️ **`torch` 는 아직 미설치다** *(09-03 확인 — `import torch` → `ModuleNotFoundError`)*.
> 현재 들어 있는 수치 계열은 `numpy 2.5.2` 뿐이며, ko-SBERT 를 실제로 돌리는 시점에
> 5-4 의 디스크 수치가 발생한다. `torch` 는 `ml` extra 가 아니라 **배치 이미지에만** 넣는다 (6절).

### 배치 목록 — "1회성" 이 아니다 *(v2.0~v2.6 누적)*

초판은 "배치는 1회성"을 전제했으나 **상시 배치가 여럿 생겼다.**
전부 CPU 수 분 이내라 **사양 산정은 바뀌지 않지만**, 운영 항목이므로 9절에 cron 으로 남긴다.

| 배치 | 주기 | 비용 | 근거 |
|---|---|---|---|
| ko-SBERT 임베딩 | 신규 레시피 발생 시 | 4.4만 건 CPU 3~6h / GPU·MPS 20~45분 | 6-4-3 |
| **k-means K≈50 (S-A)** | 주 1회 | 분 단위 | 5-3-5 · 04 |
| `user_cluster_stat` 갱신 (S-B) | 시간당 | 초 단위 | 5-3-5 |
| **`flavor_vec` 산출** *(v2.5)* | 신규 레시피 발생 시 | 재료 조회 + 집계, 초~분 | 2-5-1 ⑤ |
| **맛 코퍼스 평균 μ** *(v2.6)* | `flavor_vec` 재계산 시 | 초 | 2-5-1 ⑤ (중심화) |
| `freq_count` / IDF | 주 1회 | 분 단위 | 6-4-3-1 |
| 정규화 P1~P4 | 크롤 적재 시 | 300만 행 · 사전은 메모리 상주 | ④ |
| Bradley-Terry 가중치 학습 | 쌍대비교 수집 후 | **초 단위** (파라미터 5~12) | 5-2-5 |
| LightGBM v1 | 조건부 (라벨 확보 시) | 분 단위 | 5-2-6 |
| **트랙1 Top-N 사전계산** *(v3.0 · D-19)* | **매일 새벽** | 유저 100명 · 초 단위 | 9절 · `daily_recommendation` |
| **트랙1 LLM 사유 생성** *(v2.7)* | **매일 새벽** | 유저 100명 Top-18 · 약 30분 | 5-6 · 🔴 **RAM 7.64 GB 피크** |

> 🔴 **`flavor_vec` 과 μ 는 짝이다.** 레시피가 추가되면 μ 가 변하고, 그러면 과거 `f_taste`
> 값과 달라진다. **μ 를 로그에 함께 남겨야 재현된다** (07 E-3 ⑧ 와 같은 성격).

> 🔴 **GPU 는 없어도 된다.** 두 가지 이유가 겹친다.
>
> 1. **추천 서비스(실시간)는 GPU 를 쓸 일이 없다.** 연산량이 작아 GPU 로 보내는
>    전송 오버헤드가 계산보다 크다 — **CPU 가 실제로 더 빠르다** (5절).
> 2. **GPU 학습을 하지 않는다.** ko-SBERT 는 남이 학습시킨 것을 **추론**에만 쓴다.
>    *(v2.1 정정)* CPU 소규모 학습은 **한다** — Bradley-Terry(파라미터 11, 초 단위) ·
>    LightGBM(분 단위) · k-means K≈50 · (조건부) ALS. 전부 CPU 로 수 분 내 작업이라
>    GPU 판단은 바뀌지 않는다.
> 근거는 [`03_모델_선정_사유.md`](03_모델_선정_사유.md) 4절 —
> Two-Tower 파라미터 70만 개는 fp32 로 2.8MB 이고, **부족한 것은 VRAM 이 아니라 데이터**다
> (필요 유저 3.2만 명 vs 확보 가능 100명). GPU 를 "딥러닝 학습용"으로 신청하면
> 실제 용도와 어긋난다.

---

# 1. 측정 근거

## 1-0. 🔑 샘플 실측 기반 DB 산정 *(v2.6 · 크롤 샘플 3건)*

> ⚠️ **먼저 정정한다.** 개발 DB 가 한때 59 MB 였으나 그것은 **레시피 건수와 무관**했다 —
> 스모크 테스트(합성 5만~20만 건)가 남긴 **죽은 공간**이었고, `VACUUM FULL` 후
> **recodb 10 MB · reco 스키마 1.9 MB** 다. 시드만 든 상태의 실제 크기다.

크롤 샘플 3건(`tests/fixtures/real/`)에서 행당 크기를 실측해 4.4만 건으로 투영했다.

> ⚠️ **이 문서 전체에서 "4.4만"은 산정 당시의 추정 건수다. 실적재는 46,353건이다.**
> 차이 5.3% 만큼 아래 투영치가 **낮게 잡혀 있다** — 보수적인 방향이 아니라 낙관적인 방향이라
> 인용할 때 주의한다. 다시 계산하면 테이블 합계 0.34 → **0.36 GB**, 총계 0.48 → **0.51 GB** 다.
> **판단이 바뀌는 크기가 아니라 표를 다시 그리지 않았다.**
> 반면 **본문 크롤분은 추정이 아니라 실측 507 MB** 다 (0절) — 이쪽이 정본이다.

**샘플 실측** — JSON 평균 2,001 B · `raw_json` 압축 **1,876 B** · 재료 **11.3개** · 조리단계 3.3개

| 항목 | 4.4만 건 | 산출 |
|---|---|---|
| `recipe.raw_json` (원본 보존) | **0.08 GB** | 샘플 실측 1,876 B |
| 본문 컬럼 (title·description 등) | 0.02 GB | 추정 400 B |
| `recipe_ingredient_raw` | 0.05 GB | 재료 11.3개 × ~90 B |
| `recipe_ingredient` (정규화) | 0.03 GB | 재료 11.3개 × ~60 B |
| `recipe_feature` (임베딩 제외) | 0.03 GB | 배열 3개 + 스칼라 |
| `content_emb vector(768)` | **0.13 GB** | fp32 3,080 B |
| **테이블 합계** | **0.34 GB** | |
| + 인덱스 (~40%) | 0.14 GB | GIN·HNSW·B-tree |
| **= DB 총계** | **0.48 GB** | |

#### 🔴 여기에 얹힐 수 있는 것

| | 추가분 |
|---|---|
| 후기 저장 (02 C-5) — 레시피당 2건 | +0.01 GB |
| 〃 10건 | +0.05 GB |
| 〃 **30건** | **+0.16 GB** |
| 죽은 공간 (재정규화 반복) | **최대 테이블 크기만큼** — 실측으로 확인됨 |
| 로그 (실유저 100명 · 8주) | ~0.5 GB |

**DB 만 보수적으로 1.5 GB, 컨테이너 이미지 4.3 GB** — 이 **1.5 GB 가 문서 전체의 정본**이다
(0.48 GB 테이블+인덱스 + 후기 0.16 + 로그 0.5 + 여유). ~~2.5 GB~~ 는 23만 건 전제의 잔재다.
그래도 100 GB 를 신청하는 이유는
8주 이후 로그 증가·재크롤 여유·재정규화 중 일시 공간이지 데이터 크기가 아니다.
디스크는 이 프로젝트에서 **가장 싼 자원**이고 부족하면 가장 아프다.

---

## 1-1. 기존 측정 근거

추정이 아니라 **실제로 잰 값**에서 출발한다.

| 값 | 측정치 | 어디서 |
|---|---|---|
| `recipe_feature` 행당 | **340 B** (임베딩 제외) | 20만 건 적재 후 `pg_total_relation_size` |
| `vector(768)` 행당 | **3,080 B** | `768 × 4 + 8` |
| `raw_json` 건당 | **약 2,100 B** | 실제 크롤링 샘플 3건 평균 (2,088 / 1,953 / 1,961) |
| 레시피당 재료 | **12개** | 실측 3건 평균 11.3 |
| 레시피당 조리단계 | **4개** | 실측 3건 평균 3.3 |
| 쿼리 워킹셋 | **42 MB** | `EXPLAIN (BUFFERS)` shared hit 5,437 × 8KB |
| 정렬 메모리 | **58 kB** | `top-N heapsort Memory: 58kB` |
| 추천 p95 | **30.2 ms** | 20만 건 · 30회 측정 |
| `pgvector/pgvector:pg16` | **638 MB** | `docker images` |
| `redis:7-alpine` | **60 MB** | `docker images` |

---

# 2. 디스크

## 시나리오 A — 레시피 1만 (권장)

| 항목 | 크기 |
|---|---|
| **`event_log`** | **397 MB** |
| **`recommendation_log` (시뮬)** | **286 MB** |
| `recommendation_log` (실유저) | 46 MB |
| HNSW 인덱스 (벡터) | 44 MB |
| `recipe_feature.content_emb` | 29 MB |
| `recipe` (raw_json 포함) | 25 MB |
| 재료 사전 + 임베딩 | 16 MB |
| `recipe_ingredient` | 11 MB |
| `recipe_step` | 10 MB |
| `recipe_ingredient_raw` | 9 MB |
| 일반 인덱스 (GIN · B-tree) | 8 MB |
| `recipe_feature` (배열 · 스칼라) | 3 MB |
| **DB 순수 데이터** | **884 MB** |
| **+ WAL · vacuum · 여유 (×2)** | **1.7 GB** |

### 🔑 ~~의외의 1위는 로그다~~ → 🔴 **전제가 폐기됐다 (09-03)**

~~**로그가 전체의 77%** (683 MB / 884 MB) 를 차지한다.
시뮬레이션 5,000 유저 × 20 요청이 지배적이다.~~

**위 표의 `event_log` 397 MB · `recommendation_log`(시뮬) 286 MB 는
시뮬레이터 5,000 유저 전제다. 그 시뮬레이터는 `04 §2` 에서 컷됐다** (−30h, 순환 논리).

| | 시뮬 전제 (폐기) | **실유저 100명 (현재 계획)** |
|---|---|---|
| `event_log` | 397 MB | impression 20,000 + 기타 ≈ **22,000행 · 약 3 MB** |
| `recommendation_log` | 286 MB (시뮬) + 46 MB (실유저) | 요청 2,000 × ~12KB ≈ **24 MB** |
| **로그 소계** | 729 MB | **약 27 MB** |

**따라서 "로그가 디스크의 77%" 는 더 이상 사실이 아니다.**
`07` F-3 가 이 문서를 인용하지 말라고 지적한 항목이 이것이고, 여기서 닫는다.
DB 순수 데이터는 **884 MB → 약 180 MB** 가 된다.

> 🔴 **그래도 보존 정책 cron 은 건다** (9절). 이유가 바뀌었을 뿐이다 —
> 디스크가 아니라 **개인정보 보존기간** 때문이다.

> ~~설계 3-6 의 보존 정책을 **cron 으로 실제 걸면** 시뮬 로그가 286 MB → 약 30 MB 로 줄어든다.~~
> 🔴 **시뮬레이터가 컷돼서 이 절감분은 존재하지 않는다.** 아래 SQL 은 그대로 두되,
> **거는 이유는 디스크가 아니라 개인정보 보존기간**이다 (9절).
> ```sql
> -- 주 1회
> UPDATE recommendation_log r SET candidates = NULL
> FROM app_user u WHERE u.id = r.user_id AND u.is_simulated
>   AND r.created_at < now() - interval '7 days';
> ```
> **정책을 문서에만 두고 안 걸면 8주 뒤 로그가 데이터보다 커진다.**

## 시나리오 B — 레시피 4.4만 *(실적재 46,353. 아래 표는 4.4만 투영치)*

| 항목 | 크기 |
|---|---|
| **HNSW 인덱스 (벡터)** | **194 MB** |
| **`recipe_feature.content_emb`** | **129 MB** |
| `recipe` (raw_json 포함) | 109 MB |
| `event_log` | 397 MB |
| `recommendation_log` (시뮬) | 286 MB |
| `recipe_ingredient` | 50 MB |
| `recipe_step` | 42 MB |
| `recipe_ingredient_raw` | 40 MB |
| 일반 인덱스 | 37 MB |
| `recipe_feature` (배열 · 스칼라) | 14 MB |
| `recommendation_log` (실유저) | 46 MB |
| 재료 사전 + 임베딩 | 16 MB |
| **DB 순수 데이터** | **1.36 GB** |
| **+ WAL · vacuum · 여유 (×2)** | **2.7 GB** |

> 🔴 **v2.9 정정 — 벡터가 더 이상 지배하지 않는다.** 4.4만 건에서 임베딩(129MB) +
> HNSW(194MB) = **323 MB** 로, 총 1,357MB 의 24% 다.
> ~~**1위는 로그다** — `event_log` 397MB + `recommendation_log` 시뮬 286MB + 실유저 46MB
> = **729 MB · 약 54%**. 레시피가 줄어도 로그는 유저 수의 함수라 그대로이기 때문이다.~~
>
> 🔴 **v3.0 재정정 (09-03) — 로그도 1위가 아니다.** 위 729 MB 는 시뮬레이터 5,000 유저
> 전제인데 그 시뮬레이터가 `04 §2` 에서 컷됐다. 실유저 100명이면 **로그 소계 약 27 MB** 다.
>
> | | 시뮬 전제 (폐기) | **실유저 100명** |
> |---|---|---|
> | 로그 | 729 MB (54%) | **약 27 MB (4%)** |
> | 벡터 (임베딩+HNSW) | 323 MB (24%) | **323 MB (50%)** |
> | DB 순수 데이터 | 1,357 MB | **약 655 MB** |
>
> **즉 실제 1위는 다시 벡터다.** 다만 절대량이 323 MB 라 **어느 쪽도 사양을 좌우하지 않는다.**
> 우선순위는 벡터 튜닝도 로그 보존도 아니고, **9절 autovacuum 과 로더 멱등성**이다 —
> 실제로 디스크를 배로 늘린 사건은 그쪽에서 났다.

> [`01_추천시스템_설계.md`](01_추천시스템_설계.md) 6-7 에서 "레시피 1만이 유리"라고 한 근거가
> 여기서도 확인된다 — ~~디스크가 1.6배 차이~~ → **약 3.6배** (순수 데이터 180 MB vs 660 MB).
>
> 🔄 **v3.0 — 배수가 다시 벌어졌다.** v2.9 가 1.6배로 좁힌 이유는 "로그가 유저 수의 함수라
> 레시피가 줄어도 그대로"였는데, **그 로그가 시뮬 전제라 폐기됐다.** 로그를 빼면 격차는
> 레시피 수 비율(4.6배)에 가까워진다.
>
> 🔴 **그래도 인프라는 결정 근거가 아니다.** 660 MB 와 180 MB 의 차이는 100 GB 요청에서
> 아무 의미가 없다. **1만이 유리한 진짜 근거는 CF 밀도(4.6배)와 검수 부담**이고,
> 그건 `01` 6-7 과 `03 §2` 가 낸다. 이 절을 근거로 인용하지 말 것.

## 합계

> 🔴 **09-03 정정.** 아래 표의 `4.4만 → DB 7.8 GB` 는 **23만 건 전제의 잔재**였다.
> 같은 문서 바로 위 시나리오 B 가 `1.36 GB → ×2 = 2.7 GB` 라고 적고 있어 **모순**이었다.
> 시뮬 로그까지 걷어내면 더 줄어든다. **아래가 정본이다.**

| | 레시피 1만 | **레시피 46,353 (실적재)** |
|---|---|---|
| DB 순수 데이터 | ~~884 MB~~ → **약 180 MB** *(시뮬 로그 제외)* | ~~1.36 GB~~ → **약 0.66 GB** |
| DB (WAL·vacuum 여유 ×2) | **약 0.4 GB** | ~~7.8 GB~~ → **약 1.3 GB** |
| 컨테이너 이미지 (배치 포함) | 4.3 GB | 4.3 GB |
| **총 필요 디스크** | ~~약 6 GB~~ → **약 5 GB** | ~~약 12 GB~~ → **약 6 GB** |

**여전히 100 GB 를 신청한다.** 데이터가 6 GB 인데 100 GB 를 받는 이유는 데이터 크기가
아니기 때문이다 — 8주 이후 로그 증가 · 재크롤링 여유 · 인덱스 재빌드 중 임시 공간 ·
WAL 버스트 · `pg_dump` 사본이다. **디스크는 이 프로젝트에서 가장 싼 자원이고
부족하면 가장 아프다.**

---

# 3. RAM

| 컴포넌트 | 1만 | **46,353** | 근거 |
|---|---|---|---|
| PostgreSQL `shared_buffers` | 512 MB | 2 GB | 워킹셋 42MB/쿼리 · `recipe_feature` 전체 캐싱 목표 |
| PostgreSQL 커넥션 (50) | 500 MB | 500 MB | 커넥션당 약 10 MB |
| Redis (`maxmemory` 설정됨) | 512 MB | 512 MB | compose 에 명시됨 |
| reco-api (FastAPI) | 300 MB | 300 MB | |
| dashboard (Streamlit) | 400 MB | 400 MB | |
| **평시 소계** | **2.2 GB** | **3.7 GB** | |
| **+ 배치 실행 중 (torch)** | **4.2 GB** | **5.7 GB** | ko-SBERT 추론 peak |
| **+ 트랙1 LLM 배치** *(v2.7)* | **9.9 GB** | **11.3 GB** | Gemma 4 12B QAT **7.64 GB 실측** |

### 🔴 LLM 은 평시에 상주하지 않는다 *(v2.7)*

콜드 로드 **5.46s** · 웜 0.44s 실측. 30분 배치에서 5.46초는 **0.3%** 이므로
**배치 시작 때 올리고 끝나면 내린다.** 온라인 경로는 트랙2 = 템플릿이라
LLM 을 아예 쓰지 않는다 (`01` 5-6-1).

따라서 위 표의 마지막 행은 **평시가 아니라 새벽 배치 창의 피크**다.

> ⚠️ **ko-SBERT 배치와 LLM 배치가 겹치면 피크가 합산된다.**
> 겹치면 4.4만 기준 3.7 + 2.0 + 7.64 ≈ **13.3 GB** 가 된다.
> 9절 cron 에서 순서를 갈라 **11.3 GB 로 유지**한다.

## PostgreSQL 설정 권장

```
shared_buffers      = 512MB   (1만) / 2GB (4.4만)
work_mem            = 4MB              ← 실측 정렬 58kB. 넉넉하다
maintenance_work_mem= 1GB              ← HNSW 인덱스 생성용 (아래 정정)
max_connections     = 50
effective_cache_size= 4GB     (RAM 의 50~75%)
statement_timeout   = 5s      (reco_app 역할)      ← 원격 분리 시 필수
idle_in_transaction_session_timeout = 60s (reco_batch)
```

`work_mem` 을 크게 잡을 이유가 없다. 실측에서 정렬이 `top-N heapsort Memory: 58kB` 였다.

### 🔴 DB 시간대는 `Asia/Seoul` 이어야 한다 — 성능이 아니라 **정확성** 문제다 *(09-02 신설)*

```sql
-- infra/init/01_extensions.sql
ALTER DATABASE recodb SET timezone = 'Asia/Seoul';
```

```
$ docker exec reco-postgres psql -U reco -d recodb -c "SHOW TimeZone"
 Asia/Seoul          ← 09-03 확인
```

**UTC 로 두면 한국 자정~오전 9시 사이에 `current_date` 가 하루 전이 된다.**
`f_expiring` 이 `days_left = expiry - current_date` 로 임박을 판정하므로
**그 9시간 동안 임박 판정이 통째로 하루씩 어긋난다.**

| 왜 하필 그 창인가 | |
|---|---|
| 새벽 배치가 그 창에서 돈다 | 트랙1 사전계산 · LLM 사유 생성 (9절 cron) |
| 사유 문구가 날짜에 걸린다 | "오늘까지" / "내일까지" 가 하루 밀린다 |
| 조용히 틀린다 | 에러가 안 나고 값만 하루 다르다. **테스트가 낮에 돌면 안 잡힌다** |

> 🔴 **컨테이너 `TZ`·`PGTZ` 만으로는 부족하다.** 그건 컨테이너 안에서 실행되는
> 클라이언트(`psql` 등)에만 적용된다. **밖에서 TCP 로 붙는 `psycopg2` 세션에는 안 걸린다** —
> 배치도 API 도 전부 그 경로다. `docker-compose.yml` 에 `TZ: Asia/Seoul` 이 이미 있지만
> **그것과 별개로 `ALTER DATABASE` 가 필요하다.**
>
> `make doc-check` 가 `current_setting('TimeZone') = 'Asia/Seoul'` 을 매번 확인한다.
> **원격 DB(8절 D)로 옮길 때 이 설정이 따라가지 않으면 조용히 되돌아간다** —
> `apply_schema.sh` 가 `01_extensions.sql` 을 실행하는지 확인할 것.

### ✅ 이 권장값은 **v2.6 까지 적용되어 있지 않았다 — v2.7 에서 고쳤다**

`infra/docker-compose.yml` 의 `postgres` 서비스에 `command:` 도 `postgresql.conf`
마운트도 없어서, **문서에만 있고 실제로는 PG16 기본값으로 돌고 있었다.**

| 파라미터 | 문서 권장 | v2.6 실제 (기본값) | **v2.7 적용 후** |
|---|---|---|---|
| `shared_buffers` | 512 MB (1만) | **128 MB** | **512 MB** ✅ |
| `maintenance_work_mem` | 256 MB | **64 MB** | **256 MB** ✅ (+ 인덱스 생성 시 세션 1 GB) |
| `effective_cache_size` | 4 GB | 4 GB | 4 GB ✅ |
| `work_mem` | 4 MB | 4 MB | 4 MB ✅ |
| `max_connections` | 50 | **100** | **50** ✅ |
| `tcp_keepalives_idle` | (미기재) | **0** (OS 기본 7200s) | **60s** ✅ |

```yaml
# infra/docker-compose.yml — 값은 .env 로 덮어쓸 수 있다 (4.4만 적재 시 상향)
command:
  - "postgres"
  - "-c"
  - "shared_buffers=${PG_SHARED_BUFFERS:-512MB}"
  ...
```

> ✅ **p95 30.2ms 측정 자체는 유효했다.** 쿼리당 워킹셋이 42 MB 라 기본값
> 128 MB 안에 들어간다. **성능이 나빴다는 뜻이 아니라, 4.4만 건으로 늘렸을 때
> 쓰려던 여유가 실제로는 설정되어 있지 않았다**는 뜻이다.

#### `maintenance_work_mem` 은 전역이 아니라 세션에서 올린다

HNSW 인덱스는 그래프가 `maintenance_work_mem` 안에서 만들어진다.
전역을 크게 잡으면 **autovacuum 워커마다 물린다.**

> 🔄 **v2.9 — 근거가 약해졌다.** 23만 전제에서 인덱스가 1,013 MB 라 전역 256 MB 로는
> 턱없이 부족했다. 4.4만에서는 **194 MB** 라 전역 안에 들어간다.
> **그래도 세션 설정을 남겨 둔다** — 인덱스 빌드 중 피크 메모리가 최종 크기와 같은지가
> 문서에 없어 **미확인**이고, 확인 전에 안전장치를 걷어낼 이유가 없다.
그래서 전역은 256 MB 로 두고 `infra/init/post_index.sql` 이 세션 단위로만 올린다:

```sql
SET maintenance_work_mem = '1GB';
SET max_parallel_maintenance_workers = 3;   -- 리더 포함 4 프로세스 → CPU 4 core
CREATE INDEX ... USING hnsw ...;
```

#### 타임아웃은 ROLE 단위로 건다 — 원격 분리 대비

`infra/init/05_roles.sql` 에 넣었다. **DATABASE 수준으로 걸면 공유 DB 에서
백엔드팀에 영향을 준다** (같은 파일의 `search_path` 와 같은 이유).

| 역할 | `statement_timeout` | `idle_in_transaction` | `lock_timeout` |
|---|---|---|---|
| `reco_app` (온라인) | **5s** — 실측 p95 30ms 대비 관대 | 30s | — |
| `reco_batch` (배치) | **없음** — 적재·인덱스가 길다 | **60s** | 10s |
| `reco_ro` (대시보드) | 30s | 30s | — |

> 🔴 **왜 배치에 `idle_in_transaction` 이 핵심인가.** 트랜잭션을 열어둔 채 노는 세션의
> xmin 이 **인스턴스 전체 autovacuum 을 멈춘다.** 한 대일 때는 유닉스 소켓이라
> 클라이언트가 죽으면 커널이 즉시 소켓을 닫아 이 문제가 없었다.
> 원격 TCP 는 서버가 세션 사망을 모르고 OS 기본 **7200초**를 기다린다.
> `tcp_keepalives_idle=60` 과 이 설정이 함께 그 창을 막는다.

---

# 4. CPU

| 작업 | 요구 | 근거 |
|---|---|---|
| **온라인 추천** | **거의 없음** | p95 30ms × 4코어 = 초당 약 130 요청. 유저 100명엔 과잉 |
| 정규화 배치 | 1~2 core × 수십 분 | |
| **임베딩 생성 (CPU)** | **가장 무거움** — 4.4만 건 수 시간 | GPU 쓰면 40분 |
| LightGBM 학습 | 몇 초 | GPU 불필요 |
| ALS 학습 | 약 30초 | GPU 불필요 |

**서빙에는 4 core 면 충분**하다. **8 core 를 신청하는 이유는 배치다** —
텍스트 벡터화가 CPU 만으로 3~6시간이고 코어 수에 거의 비례해 줄어든다.

---

# 5. GPU — 온라인은 없어도 된다 *(v2.7 정정)*

> **v2.6 까지 이 절의 제목은 "GPU — 없어도 된다" 였다.**
> 추천이 2트랙으로 나뉘면서(`01` 5-6-1) **트랙 1 의 LLM 사유 생성이 GPU 를 쓴다.**
> 다만 **배치 전용**이고, 유저 100명 규모에서는 **개발 노트북으로 충분하다** — 5-6 참조.
> 온라인 추천 경로가 GPU 를 쓰지 않는다는 결론은 그대로다.

## 5-1. 온라인 추천은 CPU 가 **더 빠르다**

GPU 가 유리하려면 **두 조건**이 모두 필요하다.

| 조건 | 우리 온라인 경로 |
|---|---|
| ① 연산이 **행렬곱** 구조여야 한다 | ❌ 선형 가중합 · 트리 분기 — **신경망이 아니다** |
| ② 연산이 **대량**이어야 한다 | ❌ 후보 500 × 피처 17 ≈ **17,000 회** |

```
CPU 연산 시간              수 μs
GPU 로 데이터 보내는 시간만  수십~수백 μs   ← 계산보다 전송이 오래 걸린다
```

**LightGBM(v1) 도 마찬가지다.** 트리 모델은 조건 분기의 연속인데 GPU 는 분기에 약하다.
LightGBM 의 GPU 지원은 **학습**용이며, 추론은 CPU 가 빠르다 (500건 1ms 미만).

> 설계 1-4 는 **가용성** 때문에 "온라인 경로 GPU 의존 금지"를 정했다.
> 성능 관점에서도 같은 결론이 나온다.

## 5-2. 배치 임베딩은 GPU 가 10~20배 빠르다

여기는 조건 ①②가 모두 성립한다 — Transformer 는 행렬곱이고, 4.4만 건은 대량이다.

| | 연산량 (대략) |
|---|---|
| **ko-SBERT 문서 1건** | **약 44 GFLOPs** (파라미터 1억 × 토큰 200) |
| 우리 랭킹 500건 전체 | 약 17 KFLOPs |

**약 260만 배 차이**다.

| 레시피 수 | GPU | CPU |
|---|---|---|
| 1만 | 약 2분 | 20~40분 |
| 4.4만 | 약 40분 | 3~6시간 |

**그래도 없어도 된다** — 초기 1회성 작업이고, 밤에 CPU 로 돌려두면 된다.

## 5-3. 학습에는 아예 쓰지 않는다

| 작업 | GPU | 사유 |
|---|---|---|
| **Bradley-Terry 가중치 학습** *(v2.5)* | ❌ | 파라미터 5~12. **CPU 초 단위** (5-2-5) |
| LightGBM LambdaRank | ❌ | CPU 수 초 |
| **k-means K≈50** *(v2.0)* | ❌ | CPU 분 단위 |
| ALS | ❌ | CPU 30초 |
| ko-SBERT 파인튜닝 | ❌ | **하지 않는다.** 추론만 |
| Two-Tower · DeepFM · LightGCN · SASRec | ❌ | **채택 안 함. 데이터가 2~3 자릿수 부족** |

> **(v2.5 보강)** 부족한 것이 데이터라는 판단이 `01` 2-5-1 ④ 에서 정량화됐다 —
> **최적 파라미터 수는 라벨 수의 1/300~1/1000** 이다. 확보 예정 라벨 600쌍 기준
> 최적 모수는 1~3 개이고, DeepFM(20만 모수)은 **6,000만 라벨**이 필요하다.
> **GPU 로 살 수 있는 것이 아니다.**

### 🔴 LLM 배치 라벨링을 한다면 *(검토 중 · 02 I-15)*

`flavor_vec` 을 레시피에 직접 매기는 안이 열려 있다. 그 경우에만 사양이 달라진다.

| | |
|---|---|
| 모델 | 로컬 7B Q4 (~5 GB) — M4 24GB 급에서 여유 |
| 처리량 | 초당 5~10건 → 4.4만 건 **7~13시간** (하룻밤) |
| GPU 요건 | 통합메모리 16GB+ 또는 VRAM 8GB+ |
| 판정 | ⬜ **미확정.** 200건으로 규칙 대비 검증한 뒤 결정한다 |

**이것을 한다면 GPU(또는 통합메모리 큰 장비)가 처음으로 실질적 의미를 갖는다.**
다만 온라인 경로가 아니라 **1회성 오프라인 배치**이므로, 서버가 아니라
**개발 노트북에서 돌려도 된다** (8-A 참조).

> **4,000 샘플로 딥러닝 모델을 학습하면 몇 초 만에 끝난다. 그 자체가 데이터 부족의 증거다.**
> 상세는 [`03_모델_선정_사유.md`](03_모델_선정_사유.md).

## 5-4. 임베딩 배치가 확정되면서 달라진 것 *(2026-08-27)*

| | 임베딩 미사용 가정 | **현재 (확정)** | 🔴 09-03 정정 |
|---|---|---|---|
| `torch` | 미설치 | **설치 예정** (배치 컨테이너만) | **아직 미설치** — 실측 확인 |
| 배치 이미지 | 300 MB | **2.0 GB** | 그대로 |
| DB 디스크 (46,353) | 2.2 GB | ~~3.9 GB~~ | **약 0.66 GB** (2절) |
| 필요 디스크 (46,353) | 4.4 GB | ~~7.8 GB~~ | **약 6 GB** (이미지 4.3 포함) |
| GPU | 논의 불필요 | **여전히 선택** — 1회 배치, CPU 3~6시간 | 그대로 |

> 🔴 **`3.9 GB` · `7.8 GB` 는 23만 건 전제의 잔재였다.** 이 문서 안에서만
> 2절(1.36 GB)·1-0(0.48 GB)과 어긋나 있었다. **8절 D 의 근거 문단도 같은 값을 쓰고 있어
> 함께 고쳤다.**

**신청 사양(0절)은 바뀌지 않는다.** 1-0 의 실측 산정 **0.48 GB**(보수적으로 1.5 GB)와
정합적이며 100 GB 안에 여유 있게 들어간다.

## 5-5. 우리 모델의 계열

GPU 판단의 근거가 되므로 명확히 해둔다.

| 모델 | 계열 | 규모 | 우리가 하는 일 |
|---|---|---|---|
| v0 랭커 | **선형 가중합** — 신경망 아님 | 가중치 17개 | 손으로 정함 |
| v1 랭커 | **결정 트리 앙상블** (LightGBM) — 신경망 아님 | 트리 수백 개 | 학습 |
| ALS | 행렬 분해 — 신경망 아님 | — | baseline 학습 |
| ko-SBERT | **Transformer — 딥러닝** | 파라미터 1억 | **추론만. 학습 안 함** |

**"작은 신경망을 쓴다"가 아니라 "신경망을 쓰지 않는다"** 가 정확하다.
유일한 딥러닝은 ko-SBERT 인데 그것도 남이 학습시킨 것을 갖다 쓰기만 한다.

---

## 5-6. 🔑 트랙1 LLM 사유 생성 — 모델·용량·비용 *(v2.7 신규)*

`01` 5-6-1 에서 추천을 2트랙으로 나눴다. **트랙 1(오늘의 추천)만 LLM 을 쓴다.**
온라인 경로는 템플릿이라 GPU 를 안 쓴다.

### 모델 — Gemma 4 12B QAT 로 확정

| 항목 | 값 | 확인 |
|---|---|---|
| 모델 | `gemma4:12b-it-qat` (11.95B dense) | Ollama · HF |
| **라이선스** | **Apache 2.0** · 게이팅 없음 | HF `gated=False`, `license=apache-2.0`. 원문이 표준 Apache 2.0 전문 |
| 다운로드 | **인증·동의 불필요** | 레지스트리 매니페스트를 토큰 없이 취득 |
| 크기 | **6.66 GiB** (VRAM 실사용 7.64 GB) | 실측 |
| 런타임 | **ollama 0.33.0 이상 필수** | 0.23.2 는 `gemma4` 를 HTTP 412 로 거부 |

> Gemma 3 까지는 `license: gemma` + `gated: manual` 로 **약관 동의가 필요**했다.
> Gemma 4 에서 Apache 2.0 으로 바뀌었다. 재배포하지 않고 서버에서 돌리기만 하는
> 우리 용도에서는 **의무가 사실상 없다.**

#### 🔴 E4B 를 쓰지 않는 이유 — 12B 가 **더 작다**

```
e4b-it-q4_K_M   9.6 GB      ← 더 큼
12b-it-q4_K_M   7.04 GiB    ← 더 작고 더 똑똑함
12b-it-qat      6.66 GiB    ← 채택
```

E 시리즈는 레이어별 임베딩 테이블이 거대하고 **Q4 로 줄지 않는다.**
폰에서 플래시 스트리밍할 때나 이득이다. 통합메모리 환경에서는 12B dense 가 전면 우위다.

E2B(4.3GB)는 3.4배 빠르지만(90.9 tok/s) **숫자를 잃는다** — `D-3` → "임박".
`01` 5-6-1 의 비교표 참조.

### 배치 용량 — 실측 (`scripts/bench/reason_batch_capacity.py`)

유저당 Top-10 사유를 배치 1회로. 10명 연속, 스로틀링 없음.

```
유저당 중앙값 9.85s   생성 27.0 tok/s   프리필 136.8 tok/s
사유 1건 = 입력 104 tok / 출력 25 tok (n=8)
```

| 유저 | Top-10 | Top-18 | 4시간 창 |
|---|---|---|---|
| **100** | **16분** | 30분 | ✅ |
| 500 | 1.4h | 2.5h | ✅ |
| 1,000 | 2.7h | 4.9h | 🟡 |
| 5,000 | 13.7h | — | ❌ |

### 비용 — 세 갈래

전제: 유저 100명 · **1일 1회** · 사유 18건 = 1,800 사유/일.
월 입력 5.6M tok · 출력 1.35M tok.

| 경로 | 구성 | 유저 100 | 유저 1,000 | 비고 |
|---|---|---|---|---|
| **개발 노트북** *(권장)* | M4 Pro 24GB — **이미 있음** | **0원** | 0원 (4.9h/일 점유) | 새벽 30분. 노트북 sleep 이면 배치 중단 |
| 교내서버 GPU | VRAM 8GB+ 필요 | 신청값 내 | 신청값 내 | 있으면 안정적. **없어도 된다** |
| 클라우드 GPU (야간만) | 0.5h/일 → 15h/월 | **$5~7** | $47~59 | ⚠️ 단가 미확인 · 범위 추정 |
| 상용 API | Flash 급 $0.075/$0.30 per 1M | **약 $0.83** (1,200원) | 약 $8.3 | 🔴 냉장고 내용이 외부로 나간다 |
| (참고) 템플릿만 | — | **0원** | 0원 | 트랙 2 가 이것 |

> 🔴 **문서·발표에서 "비싸서 안 한다"고 쓰면 안 된다.** 월 1,200원짜리를 비용으로
> 기각했다고 하면 한 마디에 무너진다. **온라인에서 안 하는 이유는 지연(1,500 tok/s 필요)
> 하나이고, 배치에서는 실제로 한다.**

> ⚠️ 클라우드 GPU 시간당 단가는 2026-08 시점에 재확인하지 못했다. 범위 추정이다.
> 결론(비용은 결정 변수가 아니다)은 배수가 3배 틀려도 바뀌지 않는다.

### 운영 시 챙길 것

- 배치 cron 에 **사유 생성 단계 추가** (9절 표에 반영)
- `temperature` · `rng seed` **고정** — `01` 1-5 상 배치 재실행으로 복구 가능해야 한다
- 생성물은 **PostgreSQL 저장**. Redis 아님
- 게이트 통과율·폴백률 로깅 (`01` 5-5 G1~G4). **폴백률 절대값**을 본다

# 6. 컨테이너 이미지

| 이미지 | 크기 | 필요도 |
|---|---|---|
| `pgvector/pgvector:pg16` | **638 MB** | 필수 |
| `redis:7-alpine` | **60 MB** | 필수 |
| reco-api (python + fastapi) | 250 MB | 필수 |
| dashboard (streamlit) | 400 MB | 필수 |
| **batch (+ torch CPU)** | **2.0 GB** | 배치 전용 |
| grafana-oss | 400 MB | 선택 |
| mlflow (+psycopg2) | 1.0 GB | **선택** — 로컬 `pip` 로 대체 가능 |

| 조합 | 크기 |
|---|---|
| 필수만 (`make up` + 앱) | 1.3 GB |
| 배치 포함 | 4.3 GB |
| 전부 | 4.7 GB |

## ⚠️ `torch` 를 온라인 이미지에 넣지 말 것

**`torch` 가 이미지의 절반이다** (CPU 빌드 800MB~1GB, CUDA 빌드 2.5GB).

온라인 경로는 임베딩 추론을 하지 않으므로(설계 1-4) **`reco-api` 에는 필요 없다.**
배치 컨테이너에만 넣는다. 섞으면 배포 이미지가 8배로 커진다.

---

# 7. 네트워크 (원격 DB 사용 시)

| 항목 | 값 |
|---|---|
| 쿼리당 응답 | 약 25 KB (후보 500 × 50B) |
| 요청당 왕복 | **1회** — `retrieve_for_user()` 로 감쌈 |
| 8주 총 전송량 | 약 50 MB |
| 교내망 RTT | 0.5~2 ms 예상 |

**대역폭은 문제가 아니다.** 문제는 왕복 횟수다.

> 🔴 애플리케이션에서 스테이지마다 DB 를 따로 조회하도록 리팩터링하면
> **왕복 횟수만큼 RTT 가 곱해진다.** 원격 DB 에서는 "함수로 감싸 1왕복" 원칙(설계 1-7)이
> 로컬일 때보다 훨씬 중요하다.

## 🔑 그래서 분리해도 지연은 거의 그대로다 *(v2.7)*

```
in-DB retrieve_for_user()    13.2ms    ← DB 가 하는 일
Python 왕복 포함 p95         30.2ms
                             ─────
                             약 17ms    ← localhost 에서도 나는 오버헤드
```

**17ms 는 DB 가 같은 기계에 있어도 발생한다.** 원격이면 RTT 0.5~2ms 가 더해져
**약 32ms** 다. 목표 300ms 에서 무의미하다. 사양 분할은 **8절 D** 참조.

## 배치의 대량 전송은 다르다 *(v2.7)*

온라인은 요청당 25KB 지만 **배치는 다르다.**
임베딩 적재 시 ~~4.4만 × 3,080 B ≈ 690 MB~~ → **46,353 × 3,080 B ≈ 143 MB** 가 네트워크를 탄다.

> 🔴 **09-03 정정 — 690 MB 는 산수가 안 맞았다.** `44,000 × 3,080 B = 135 MB` 다.
> 690 MB 는 23만 건 전제(`230,000 × 3,080 = 708 MB`)의 잔재가 레시피 수만 바뀐 채 남은 것이다.

교내망에서 대역폭은 문제가 아니지만 `COPY` 성능이 로컬보다 떨어진다.
**적재는 크롤 시점 1회성**이므로 감수할 수 있다.

---

# 8. 시나리오별 권장

## A. 개발 노트북 (개인)

```
CPU 2 core · RAM 8 GB · 디스크 20 GB
make up          # postgres + redis 만. 440 MB
```

Grafana · MLflow 는 띄우지 않는다. MLflow UI 가 필요하면 `make mlflow-ui` 로
로컬 실행한다 (컨테이너 불필요).

**8 GB 로도 된다.** 배치를 돌릴 때만 빠듯하다.

### 🔴 파이썬 환경 — `pyproject.toml` + `uv.lock` *(09-02 신설)*

`db/` 와 `reco/` 아래 있던 **requirements 파일 2개는 폐지**했다. 단일 `pyproject.toml`
+ `uv.lock` 로 통합하고, 트랙별로 필요한 것만 설치한다.

```
make install TRACK=A     # 데이터   ml
make install TRACK=B     # 엔진     api + ml
make install TRACK=C     # 관측     dash + ml + obs
```

| extra | 내용 |
|---|---|
| `api` | fastapi · uvicorn |
| `dash` | streamlit |
| `obs` | mlflow |
| `ml` | numpy · scikit-learn |
| `rank-v1` | lightgbm *(v1 랭커 시점)* |

> 🔴 **맨손 `uv sync` 를 치지 말 것.** 락에 없는 패키지를 **지운다.**
> 09-02 에 실제로 `fastapi`·`numpy` 가 사라져 mock 서버가 죽었다.
> `make install` 은 `TRACK` 없이 실행하면 **아무것도 하지 않고** 안내만 출력한다 —
> 실수로 쳐도 남의 환경이 날아가지 않게 한 것이다.

**실측 `.venv` 170 MB (트랙 B).** `torch` 는 여기 들어가지 않는다 — 배치 이미지 전용(6절).
트랙 C 는 streamlit·mlflow 로 더 크다.

### 🔑 최신 노트북이면 서버 사양을 넘는다 *(2026-08-31 실측)*

| | 0절 권장 (교내 서버) | 실측 개발기 (M4 Pro) |
|---|---|---|
| CPU | 4 core | **12~14 core** |
| RAM | 16 GB | **24 GB** (통합메모리) |
| GPU | 없어도 됨 | **20-core GPU · MPS** |

**이 프로젝트 전체가 노트북 한 대에서 돈다.** ko-SBERT 4.4만 건 임베딩도
MPS 로 **20~45분**이면 끝나 5-2 의 "GPU 40분" 쪽에 해당한다.

> 🔴 **그렇다고 서버 신청이 불필요한 것은 아니다.** 서버가 필요한 이유는 **성능이 아니라**
> ① 백엔드팀과 공유하는 DB 인스턴스(설계 1-7) ② 발표 데모용 상시 가동 이다.
> **요청서에 그 이유를 쓰는 것이 정확하다** — "성능이 부족해서"라고 쓰면
> 실측과 어긋나고 심사에서 되받아친다.

> ⚠️ Docker 기본 할당이 물리 RAM 의 절반인 경우가 많다(실측 11.7 GB).
> 크롤 4.4만 건 적재 시에는 할당을 올려야 할 수 있다.

## B. 교내 서버 (공용) — 권장

```
신청  CPU 8 core · RAM 32 GB · 디스크 100 GB · GPU VRAM 8GB (선택)
최소  CPU 4 core · RAM 16 GB · 디스크  50 GB
```

- 전 컨테이너 + 배치 동시 실행 가능
- 레시피 46,353 시나리오 산정 ~~2.5 GB~~ → **0.48 GB** (1-0) · 보수적으로 **1.5 GB** + 여유
- GPU 는 임베딩 배치용. 없으면 CPU 로 몇 시간 — **코어 수가 곧 배치 시간**
- 🔴 **디스크를 넉넉히 잡는다.** 재정규화가 TRUNCATE+전량재삽입이라(4-1)
  죽은 공간이 반복 발생하고, 실측에서 행 3개 테이블이 29 MB 인 상태를 확인했다

## C. 공유 DB (백엔드팀과 같은 인스턴스)

DB 관리자에게 전달할 값 — **`reco` 스키마 몫만**이다.

| | 레시피 1만 | **레시피 46,353 (실적재)** |
|---|---|---|
| 스키마 크기 | ~~2 GB~~ → **1 GB** | ~~8 GB~~ → **2 GB** *(실측 497 MB + 임베딩·인덱스 투영 + 여유)* |
| 커넥션 | 최대 50 | 최대 50 |
| `shared_buffers` 요구 | +512 MB | +1 GB |

> 🔴 **09-03 정정.** 2 GB / 8 GB 는 23만 건 전제였다. 공유 DB 관리자에게 8 GB 를 요구해 놓고
> 실제로 500 MB 를 쓰면 다음 요청 때 신뢰를 잃는다. **실측 497 MB 를 근거로 2 GB 를 요청한다.**

설계 1-7 의 스키마 · 역할 분리를 함께 전달한다.

## D. DB · AI 서버 분리 *(v2.7 신규)*

C 는 "같은 인스턴스를 공유"하는 경우고, 여기는 **DB 가 물리적으로 다른 장비**인 경우다.
0절 표는 한 대에 다 올린다는 전제의 **합산값**이므로 그대로 쓰면 안 된다.

### 🔑 먼저 — 성능은 사실상 달라지지 않는다

Retrieval 30.2ms 의 내역이 이것을 말해준다 (2-12 실측):

```
in-DB retrieve_for_user()    13.2ms   ← DB 가 하는 일
Python 왕복 포함 p95         30.2ms
                             ─────
                             약 17ms   ← localhost 에서도 나는 드라이버·직렬화 오버헤드
```

**17ms 는 DB 가 같은 기계에 있어도 발생한다.** 원격이 되면 여기에 교내망 RTT
0.5~2ms 가 더해질 뿐이라 **약 32ms** 다. 목표 300ms 에서 무의미한 차이다.

> 🔴 **단, "요청당 왕복 1회" 원칙(1-7)이 지켜질 때만이다.**
> `retrieve_for_user()` 로 감싸 1회로 만들어 뒀는데, 나중에 스테이지마다 따로
> 조회하도록 리팩터링하면 **RTT 가 왕복 횟수만큼 곱해진다.**
> 로컬에서는 티가 안 나던 것이 원격에서는 바로 드러난다. 7절 참조.

### DB 서버 — 최소 · 권장 · 여유 *(레시피 46,353 기준)*

| | **최소** (동작은 함) | **권장** (신청값) | **여유** (있으면 좋음) |
|---|---|---|---|
| CPU | 2 core | **4 core** | 8 core |
| RAM | 4 GB | **8 GB** | 16 GB |
| 디스크 | 20 GB | **50 GB** | 100 GB |
| `shared_buffers` | 512 MB | **2 GB** | 4 GB |
| `maintenance_work_mem` (전역) | 128 MB | **256 MB** | 512 MB |
| 〃 (인덱스 생성 세션) | 512 MB | **1 GB** | 2 GB |
| `effective_cache_size` | 2 GB | **4 GB** | 12 GB |
| GPU | 불필요 | 불필요 | **불필요** (pgvector 는 CPU) |

레시피 **1만** 시나리오면 한 단계씩 내려도 된다 (디스크 1.7 GB · `shared_buffers` 512 MB).

#### 각 단계가 무엇을 의미하는가

> 🔴 **09-03 정정 — 아래 표의 1,013 MB · 676 MB 는 23만 건 전제의 잔재다.**
> 2절이 이미 46,353건에서 **임베딩 129 MB · HNSW 194 MB** 로 다시 계산했는데
> 이 표만 옛 값을 붙들고 있었다. 값이 5배 다르면 **권장 사양의 근거 자체가 달라진다.**

| 단계 | 무엇이 달라지는가 *(46,353건 기준)* |
|---|---|
| **최소** (`shared_buffers` 512 MB) | 돌아간다. 벡터 전량(임베딩 129 + HNSW 194 = **323 MB**)이 이미 들어가므로, ~~"HNSW 가 캐시에 다 안 올라간다"~~ 는 **더 이상 사실이 아니다.** 밀리는 것은 `recipe`·`recipe_review` 쪽이다 |
| **권장** (2 GB) | `recipe_feature` 전체 + 벡터 + 자주 읽는 `recipe` 까지 상주한다. 쿼리당 워킹셋 실측 42 MB 가 넉넉히 들어간다 |
| **여유** (4 GB) | `reco` 스키마 **전량(실측 497 MB + 임베딩 투영)** 이 메모리에 상주한다 |

> 🔑 **그래서 `shared_buffers` 권장을 2 GB 로 유지하는 근거가 바뀌었다.**
> 이전 근거는 "벡터 1.7 GB 를 담기 위해"였다. 지금 근거는 **"`recipe`(160 MB) +
> `recipe_review`(186 MB) + 벡터(323 MB) + 인덱스가 함께 도는 배치 창"** 이다.
> **1 GB 로 내려도 온라인 서빙은 문제없다** — 실측 워킹셋이 쿼리당 42 MB 다.

##### 🔴 CPU 2 core 로는 HNSW 인덱스 생성이 제 속도가 안 난다

pgvector 의 HNSW 병렬 빌드는 **리더 + `max_parallel_maintenance_workers`** 만큼 프로세스를 쓴다.
현재 실행 중인 DB 실측값이 `max_parallel_maintenance_workers = 2` 이므로 **3 프로세스**다.
2 core 면 기본값조차 못 채운다. **권장 4 core 는 여기서 나온 값이다.**

##### 🔴 `maintenance_work_mem` 은 전역이 아니라 세션에서 올린다

HNSW 그래프는 `maintenance_work_mem` 안에서 만들어지고, 넘치면 훨씬 느린 on-disk 경로로
간다. 필요량은 대략 **최종 인덱스 크기** = ~~1,013 MB~~ → **194 MB** (2절 · 46,353건 투영)다.

> 🔴 **그러면 전역 256 MB 로 충분한 것 아닌가?** 산술적으로는 그렇다.
> **그래도 세션 설정을 걷어내지 않는다** — 인덱스 **빌드 중 피크 메모리가 최종 크기와
> 같은지가 확인되지 않았고**(pgvector 문서에 없다), 확인 전에 안전장치를 빼면
> 실패가 조용히 on-disk 경로로 나타난다(느려질 뿐 에러가 안 난다).
> 3절의 같은 항목과 판단을 일치시킨 것이다.

그런데 **전역을 1 GB 로 잡으면 autovacuum 워커마다 물린다** (기본 3워커 = 최대 3 GB).
그래서 전역은 256 MB 로 두고 **`infra/init/post_index.sql` 이 세션 단위로만 올린다** — 반영 완료:

```sql
SET maintenance_work_mem = '1GB';
SET max_parallel_maintenance_workers = 3;   -- 리더 포함 4 프로세스
CREATE INDEX ... USING hnsw ...;
```

> 🔴 **디스크 최소 20 GB 는 빠듯하다.** 데이터가 ~~7.8 GB~~ → **약 1.3 GB** 로 줄었지만
> **권고는 바뀌지 않는다** — 얹히는 것이 데이터가 아니기 때문이다:
> 인덱스 재빌드 중 임시 공간 · WAL 버스트 · `pg_dump` 사본(압축 약 0.3 GB × 3일) ·
> 재크롤 시 이중 보유. **50 GB 를 권한다.**

### 🔑 임베딩 벡터는 어디에 있나 — 별도 벡터 DB 가 없다

**`reco` 스키마 안, PostgreSQL + `pgvector` 다.** Milvus·Qdrant 같은 별도 벡터 DB 를
쓰지 않는다. 따라서 **분리하면 벡터도 전부 DB 서버로 간다.**

| 위치 | 내용 | 46,353 기준 크기 | 인덱스 |
|---|---|---|---|
| `reco.recipe_feature.content_emb` | 레시피 임베딩 (제목+재료+카테고리) | ~~676 MB~~ → **136 MB** *(46,353 × 3,080 B)* | HNSW `idx_rf_emb` — ~~1,013 MB~~ → **약 200 MB** |
| `reco.ingredient.embedding` | 재료 임베딩 (정규화 `MatchMethod.EMBED` 용) | 재료 사전 포함 16 MB | HNSW `idx_ing_emb` |
| `reco.user_vector.content_emb` | 유저 임베딩 (좋아한 레시피 가중평균) | 유저 100명 = **0.3 MB** | **없음** — `user_id` PK 조회라 불필요 |

```
확장은 public, 테이블은 reco  (infra/init/01_extensions.sql · 02_schema.sql)
  CREATE EXTENSION vector SCHEMA public;
  SET search_path TO reco, public;
```

~~**벡터 합계 약 1.7 GB 로 DB 디스크의 절반 가까이를 차지한다**~~
→ **벡터 합계 약 0.34 GB** (46,353건 투영). DB 순수 데이터 0.66 GB 의 절반이다 —
**비율은 여전히 절반이지만 절대량이 5배 작다.**
`vector(768)` 행당 3,080 B (`768 × 4 + 8`) 이므로 fp32 다. **이 행당 크기만 실측이고
합계는 투영이다** — `recipe_feature` 는 09-03 현재 **0행**이다.

> 🔴 **HNSW 인덱스는 대량 적재 후에 만든다** (9절 · `infra/init/post_index.sql`).
> 빈 테이블에 만들면 INSERT 마다 느려진다. `make post-index` 로 분리해 뒀다.
> 생성에는 `maintenance_work_mem` 이 필요하니 **DB 서버 RAM 이 여기서도 쓰인다.**

> ⚠️ **분리 시 임베딩 적재가 네트워크를 탄다.** ko-SBERT 는 AI 서버(또는 GPU)에서
> 돌고 결과 **약 143 MB**(46,353 × 3,080 B)를 DB 로 보낸다. 크롤 시점 1회성이라
> 감수 가능하다 (7절). *(이전 판의 690 MB 는 23만 건 전제였다.)*

### AI 서버 — 최소 · 권장 · 여유

| | **최소** (동작은 함) | **권장** (신청값) | **여유** (있으면 좋음) |
|---|---|---|---|
| CPU | 4 core | **8 core** | 16 core |
| RAM | 8 GB | **16 GB** | 32 GB |
| 디스크 | 30 GB | **50 GB** | 100 GB |
| GPU | 없음 | 없어도 됨 | **통합 16GB / VRAM 8GB** |

디스크 내역(실측 필요 약 20.6 GB): 컨테이너 이미지 · **LLM 모델 blob 7.15 GB** ·
`/artifacts` 모델 버전 · venv · 크롤 원본 · 로그 · 빌드 캐시 · **`pg_dump` 사본**.
30 GB 로는 덤프 사본을 둘 자리가 빠듯하다.

#### 각 단계가 무엇을 의미하는가

| 단계 | 무엇이 달라지는가 |
|---|---|
| **최소** | 서빙과 경량 배치는 된다. 🔴 **트랙1 LLM 사유 생성이 안 된다** — 모델만 7.64 GB 라 8 GB 에 안 들어간다. 사유 생성은 개발 노트북에서 돌려야 한다 |
| **권장** | 배치 창 피크 11.3 GB 가 들어간다 (평시 1.2 + torch 2 + **LLM 7.64**). 단 **ko-SBERT 와 LLM 배치를 겹치지 않게** 스케줄해야 한다 |
| **여유** | 두 배치가 겹쳐도(13.3 GB) 버틴다. GPU 가 있으면 임베딩 3~6시간 → 40분, 사유 생성 30분 → 더 짧아진다 |

### 두 대 합산 — 한 대일 때와 비교

| | 한 대 (8절 B) | DB 서버 | AI 서버 | 분리 합계 |
|---|---|---|---|---|
| CPU | 8 core | 4 | 8 | **12 core** |
| RAM | 32 GB | 8 | 16 | **24 GB** |
| 디스크 | 100 GB | 50 | 50 | **100 GB** |

**CPU 는 늘고(8→12) RAM 은 줄며(32→24) 디스크는 같다(100).**
코어가 느는 것은 장비마다 OS 와 여유를 따로 가져야 하기 때문이고,
RAM 이 주는 것은 한 대일 때 잡아둔 공용 여유가 쪼개지기 때문이다.
디스크는 **`pg_dump` 사본을 양쪽에 두면서** 줄어들 여지가 없어진다.

#### 근거

**DB 서버**
- `shared_buffers` 2 GB + 커넥션 50×10MB 500 MB + OS page cache
- **쿼리당 워킹셋 실측 42 MB** (버퍼 5,437 × 8KB) — `shared_buffers` 2GB 에 넉넉히 담긴다
- 디스크: 데이터 ~~3.9 GB → 7.8 GB~~ → **0.66 GB → 여유 ×2 = 약 1.3 GB** (2절) + 인덱스 재빌드 임시공간 + `pg_dump` 사본
- CPU: 온라인 쿼리는 13.2ms 로 가볍다. **HNSW 인덱스 생성이 가장 무겁다**
- **GPU 를 넣지 말 것.** pgvector 연산은 CPU 다

**AI 서버**
- Redis 512 MB + reco-api 300 MB + dashboard 400 MB = 평시 1.2 GB
- **+ 배치 창 피크**: ko-SBERT(torch) 약 2 GB · **LLM 7.64 GB** (5-6)
- 디스크: 컨테이너 이미지 4.3 GB + **LLM 모델 6.66 GiB** + `/artifacts` + MLflow + 로그
- CPU: 8 core 를 신청하는 이유는 여전히 **배치**다 (4절). DB 분리로 줄지 않는다

### 🔴 LLM 은 상주시키지 않는다 — 배치 창에만 올린다

콜드 로드 **5.46s** · 웜 0.44s 실측. 30분 배치에서 5.46초는 **0.3%** 다.

따라서 7.64 GB 는 **평시 RAM 이 아니라 새벽 배치 창의 피크**다.
온라인 경로는 트랙2 = 템플릿이라 LLM 을 아예 쓰지 않는다 (`01` 5-6-1).

> ⚠️ **ko-SBERT 배치와 겹치면 피크가 합산된다** (2 + 7.64 ≈ 9.6 GB).
> 9절 cron 에서 **순서를 갈라야** RAM 신청값을 16 GB 로 유지할 수 있다.

### 분리하면 새로 생기는 것

한 대일 때는 없었던 항목만 적는다. **v2.7 에서 ①⑧ 은 고쳤다.**

| | 항목 | 왜 한 대일 때는 없었나 | 상태 |
|---|---|---|---|
| ① | **타임아웃 3종** | 유닉스 소켓은 클라이언트가 죽으면 **커널이 즉시 소켓을 닫는다.** TCP 는 서버가 세션 사망을 모르고 OS 기본 **7200초**를 기다린다. 그동안 `TRUNCATE` 의 ACCESS EXCLUSIVE 락이 유지되고, 열린 트랜잭션의 xmin 이 **인스턴스 전체 autovacuum 을 멈춘다** | ✅ **해결** — `05_roles.sql` 역할 단위 타임아웃 + `tcp_keepalives_idle=60` (3절) |
| ② | **인증 · 암호화 · 바인딩** | 한 대일 때는 전부 `localhost` 안이라 안 터졌다. 현재 `docker-compose.yml` 은 `POSTGRES_PASSWORD:-reco` **기본값**을 쓰고 포트를 0.0.0.0 에 연다. Redis 에 `requirepass` 가 없고, Grafana 데이터소스는 `sslmode: disable` 이다 | 🔴 **미해결** — 원격 전에 반드시. `.env` 분리 + `scram-sha-256` + `sslmode=require` + 출발지 IP 제한 |
| ③ | **DB 다운 시 폴백이 없다** | 한 대에서 "DB 다운"과 "앱 다운"은 **같은 사건**이었다. 분리하면 독립 사건이 되는데, `01` 1-5 상 PG 가 SoT 이고 Redis 는 순수 캐시라 대신할 수 없다. `retrieve_for_user()` 자체가 PG 함수다 | 🔴 **미해결** — 설계 결정 필요 (02 I-17) |
| ④ | **커넥션 풀링이 선택 → 필수** | 원격 접속 수립 = TCP 3-way + PG 스타트업 + SCRAM + TLS ≈ **커넥션당 5~10 ms**. 유닉스 소켓은 ~0.1 ms 였다. 7절 표는 **쿼리 왕복만 세고 접속 왕복을 안 셌다** | 🟡 원격 결정 시 |
| ⑤ | **적재는 `COPY (FORMAT BINARY)` 로** | 로컬에서는 포맷 차이가 안 보였다. `vector(768)` 을 **텍스트로 보내면 행당 약 9 KB → 46,353건 약 417 MB**, 바이너리면 3,080 B → **143 MB**. 🔴 *(09-03 — 이전 판의 2 GB/708 MB 는 23만 건 전제였다.)* 비용은 대역폭이 아니라 **양쪽 CPU 와 WAL** 이다 | 🟡 크롤 적재기 작성 시 |
| ⑥ | **"왕복 1회" 회귀를 탐지할 수단** | 로컬 소켓 RTT ≈ 0.05 ms 라 **1왕복이 30왕복으로 회귀해도 노트북에서 1.5 ms 늘 뿐 아무도 못 본다.** 원격에서만 드러난다 | 🟡 계약 테스트에 왕복 수 단언 |
| ⑦ | **백업의 낙하 지점** | 한 대일 때는 "어느 디스크"가 질문이 아니었다. 압축 덤프 약 1.2~1.5 GB × 3일을 어디에 둘지 정해야 한다 (AI 디스크 50 GB 의 근거) | 🟡 미해결 |
| ⑧ | **원격 스키마 적용 경로** | 컨테이너 `initdb.d` 자동 실행이 없다. `apply_schema.sh` 가 `00_databases.sql` 을 **조용히** 건너뛰어 `mlflowdb` 가 안 생기고, MLflow 가 테이블 15개를 `reco` 에 만든다 (`01` 1-6) | ✅ **완화** — 스크립트가 이제 경고하고 대안 2가지를 안내한다 |
| ⑨ | **트랙1 사유 저장 협의** | `01` 1-5 가 PostgreSQL 을 SoT 로 못 박아 사유가 DB 에 들어간다. 공유 DB 면 소유자와 스키마 협의 대상 | 🟡 02 I-16 |

### 개발 환경은 여전히 한 대다

시나리오 A(개발 노트북)는 `make up` 으로 postgres + redis 를 같이 띄운다.
**개발은 한 대, 운영은 두 대**가 되므로 접속 설정이 환경별로 갈린다 —
설계 1-7 의 스키마·역할 분리를 그대로 쓰되 **호스트만 환경변수로 분리**하면 된다.

---

---

# 9. 운영 시 챙길 것

## 🔴 로그 보존 정책을 cron 으로 걸 것 — **이유가 바뀌었다** *(09-03)*

~~시나리오 A 에서 **로그가 디스크의 77%** 다.~~
🔴 **그 전제는 시뮬레이터 5,000유저였고, 시뮬레이터는 `04 §2` 에서 컷됐다.**
실유저 100명이면 로그는 약 27 MB — **디스크는 이유가 되지 못한다.**

**그래도 건다. 이유는 개인정보 보존기간이다.**
`07` F-3 가 "삭제 요청은 `DELETE` 가 아니라 가명화로 처리한다"고 정했고,
`recommendation_log.candidates` 에는 **요청 시점 냉장고 내용이 통째로** 들어 있다.
서로 다 아는 지인 50~100명 규모에서 이것은 익명이 아니라 **가명처리**다.

```
주 1회   recommendation_log.candidates → NULL   (보존기간 경과분)
일 1회   부하테스트 로그 삭제                  (24시간 경과분)
```

## 배치 cron — 0절 표를 그대로 건다 *(v2.6 신규)*

```
시간당    user_cluster_stat 갱신 (S-B)              초 단위
매일 새벽  트랙1 "오늘의 추천" Top-N 사전계산          유저 100명 · 초 단위   → daily_recommendation
매일 새벽  트랙1 LLM 사유 생성 (v2.7)                유저 100명 Top-18 · 약 30분  ← GPU
주 1회    k-means K≈50 재배치 (S-A)                 분 단위
주 1회    freq_count / IDF 갱신                     분 단위
적재 시    ko-SBERT 임베딩 · flavor_vec · 맛 평균 μ    신규 레시피분만
```

### 트랙1 은 **사전 계산**이다 — 실시간이 아니다 *(D-19 확정, 09-03)*

산출물은 **`daily_recommendation` 테이블 [26]** 에 들어간다.

| 컬럼 | 왜 |
|---|---|
| `user_id` · `rank` (PK) · `recipe_id` · `score` | 목록 |
| `reason` · `reason_source` | `llm` / `template` / `fallback` — **폴백률 절대값을 본다** |
| 🔴 **`pantry_fingerprint`** | **stale 판정.** 새벽에 계산했는데 낮에 재료를 쓰면 사유가 어긋난다 — "두부가 상하기 전에 쓰세요"인데 두부를 이미 다 썼다. 지문이 없으면 **어긋났다는 사실조차 모른다** |
| `config_hash` · `stats_version` | 점수 재현 |
| `batch_run_id` · `computed_at` | 어느 배치가 만들었나 |

**행 수는 유저 100 × Top-20 = 2,000행**이라 디스크에 영향이 없다 (현재 실측 64 kB).
배치가 실패해도 **전날 행이 남아 화면은 뜬다** — 다만 stale 표시가 붙는다.

> 🔴 **배치를 어디서 돌릴지는 아직 미정**이다 (D-19 ②). 개발 노트북이면
> **노트북이 sleep 상태면 그날 추천이 안 만들어진다.** 서버로 옮기려면 8절 D 의
> AI 서버 권장(RAM 16 GB)이 필요하다 — LLM 만 7.64 GB 다.

> 🔴 **사유 생성은 Top-N 사전계산 뒤에 와야 한다.** z-salience 의 μ·σ 가
> 그 요청의 후보 500건에 의존하므로(`pipeline.py:116`), 후보가 확정돼야 이유가 정해진다.
> 순서가 뒤바뀌면 사유가 다른 후보집합 기준으로 만들어진다.

> 🔴 **`flavor_vec` 을 다시 계산하면 μ 도 다시 계산해야 하고,
> 그러면 과거 로그의 `f_taste` 와 값이 달라진다.** `cluster_version` 처럼
> **μ 에도 버전을 붙여 로그에 남긴다** — 안 그러면 재현이 깨진다 (07 E-3 ⑧).

## 🔴 8주 이후 — 유입이 끊기면 로드맵이 무너진다

디스크는 문제가 아니다. 문제는 **데이터가 8주에서 멈추는 것**이다.
`07` 부록 Y D-1 이 지적했고 `04` 의 W8 은 "리포트 완성·발표"로 끝난다.

`01` 2-5-1 ④ 의 측정대로 **모델을 키우려면 라벨이 필요하고, 라벨은 유입에서 나온다.**
월 9% 성장이면 2년 뒤 Two-Tower(유저 800명)가 열리지만, 그러려면 **서비스가 2년을 살아야 한다.**
**서버 사양보다 이쪽이 상위 질문이다.**

## 🔴 적재 멱등성 — **실제로 데이터가 2배가 됐다** *(09-02)*

```
제약 이전   적재 2회 → recipe_ingredient_raw  451,862 → 903,724   (전량 2배)
제약 이후   UNIQUE (recipe_id, position)                          ← 2회차가 충돌한다
            recipe_review UNIQUE NULLS NOT DISTINCT (recipe_id, author_hash, written_at)
```

**에러가 나지 않고 조용히 늘어난다.** 그리고 그 위에서 계산한 인기도(`review_count` 기반) ·
재료 커버리지 · `freq_count` 가 전부 틀리는데, 값이 그럴듯해서 아무도 눈치채지 못한다.

> 🔴 **`NULLS NOT DISTINCT` 가 없으면 후기 멱등이 깨진다.** PostgreSQL 의 기본 UNIQUE 는
> NULL 을 서로 다른 값으로 보므로, `author_hash` 나 `written_at` 이 NULL 인 후기는
> **몇 번을 넣어도 매번 새 행**이 된다. 크롤 후기의 상당수가 그 형태다.

## 🔴 `make seed-reset` 가드 — `CASCADE` 가 크롤 데이터를 함께 지운다 *(09-02)*

`scripts/migrate.py --reset` 은 `TRUNCATE ... RESTART IDENTITY CASCADE` 를 쓴다.
`ingredient` 를 비우면 **`recipe_ingredient` · `pantry_item` 이 CASCADE 로 함께 사라진다** —
크롤 46,353건을 정규화한 결과와 유저 냉장고가 같이 날아간다.

```
_CASCADE_VICTIMS  (scripts/migrate.py) — 여기 행이 있으면 reset 을 막는다
  recipe_ingredient       A 트랙 정규화 결과 (46,353건 배치 = 10분)
  pantry_item             유저 냉장고
  user_allergy            🔴 온보딩 재수집 불가
  user_ingredient_pref    🔴 온보딩 재수집 불가
  normalization_queue     사람이 판정한 검수 결과

강행은  make seed-reset FORCE=1  로만.
```

**3인 병렬에서 누가 무심코 치면 남의 하루가 사라진다.**
특히 `user_allergy`·`user_ingredient_pref` 는 **재수집이 불가능**하다 —
온보딩은 유저가 한 번만 한다.

## HNSW 인덱스는 대량 적재 후에

빈 테이블에 만들면 INSERT 마다 느려진다. `make post-index` 로 분리되어 있다.

## 🔴 `autovacuum` 확인 — 실제로 겪었다 *(v2.6)*

`recipe_ingredient` 는 재정규화마다 TRUNCATE + 전량 재삽입된다 (설계 4-1).

**실측 사례** — 스모크 테스트(합성 5만~20만 건) 뒤 개발 DB 상태:

```
VACUUM FULL 전   recodb 59 MB   recipe_feature 29 MB (행 3개)  recipe 20 MB (행 3개)
VACUUM FULL 후   recodb 10 MB   reco 스키마 1.9 MB
```

**행이 3개인 테이블이 29 MB 였다.** autovacuum 이 회수하지 못한 상태였고,
그 59 MB 를 "레시피 데이터 크기"로 오독할 뻔했다.

### 🔴 원인 정정 — `TRUNCATE` 가 아니었다 *(v2.7)*

v2.6 은 이 현상을 "TRUNCATE + 전량 재삽입이 dead tuple 을 쌓는다"고 설명했다. **틀렸다.**

**`TRUNCATE` 는 dead tuple 을 만들지 않는다.** 새 relfilenode 를 할당하고 커밋 시
구 파일을 통째로 drop 하므로 autovacuum 이 회수할 것 자체가 없다.

그리고 **위 실측이 그 설명과 맞지 않는다.** 부푼 것은 `recipe_feature`(29 MB) 와
`recipe`(20 MB) 인데, TRUNCATE 대상으로 지목된 `recipe_ingredient` 는 목록에 없다.

실제 원인은 **스모크 테스트를 반복 실행한 것**이다 — 실측으로 확인했다.

```
스모크 1회차 직후    recipe 0 dead · recipe_feature 0 dead    (전부 신규 INSERT)
스모크 2회차 직후    recipe 10,007 dead · recipe_feature 10,007 dead
                     recipe_ingredient 0 dead   ← TRUNCATE 대상인데 깨끗하다
```

`tests/smoke_test.py` 는 합성 레시피 1만 건을 `ON CONFLICT` 로 넣는다(`:76,85,203,212`).
두 번째 실행부터는 같은 `source_id` 와 충돌하는데, PostgreSQL 은 **투기적 삽입**을
하므로 튜플을 먼저 쓰고 충돌을 확인한 뒤 dead 로 표시한다. 그래서 **실행 1회당
약 1만 개**가 쌓인다. 원래 관측된 두 테이블이 정확히 이 두 개다.

> ✅ **따라서 이 현상은 운영에서 반복되지 않는다.** 개발 중 스모크 반복의 산물이다.
> 디스크 여유를 두는 이유는 여전히 유효하지만(인덱스 재빌드 임시공간·WAL 버스트·덤프),
> **"재정규화가 매번 테이블 크기만큼 죽은 공간을 만든다"는 근거는 철회한다.**

```sql
-- 대량 재적재 후에는 명시적으로 돌린다
VACUUM (ANALYZE) reco.recipe_ingredient, reco.recipe_feature;
```

> 🔴 **HNSW 인덱스가 생긴 뒤에는 `VACUUM FULL` 을 쓰지 말 것.**
> 회수할 것이 없는데도 **1 GB 인덱스를 ACCESS EXCLUSIVE 락 아래 단일 스레드로 재빌드**한다.
> 현재 `maintenance_work_mem` 실측값이 64 MB 라 그 재빌드는 디스크 경로로 떨어진다.
> 공유 DB(8-C)라면 남의 테이블까지 멈춘다. **`VACUUM (ANALYZE)` 로 충분하다.**

## 8주 이후

로그는 계속 늘어난다. 프로젝트가 연장되면 **월 약 350 MB** 씩 증가한다고 보면 된다
(실유저 100명 기준). 50 GB 면 수년치다. **디스크는 제약이 아니다.**

---

# 10. 서버 요청서 템플릿

> 🔴 **제출 전에 읽을 것 (v2.6).** 실측 결과 **최신 개발 노트북(M4 Pro 24GB)이 아래 요청
> 사양을 넘는다** (8-A). 그러므로 **"성능이 부족해서 서버가 필요하다"고 쓰면 안 된다** —
> 그렇게 쓰면 "노트북으로 하면 되지 않나"에 답할 수 없다.
>
> **정확한 이유는 둘이다.**
> ① **백엔드팀과 DB 인스턴스를 공유**해야 한다 (설계 1-7 — 스키마로 분리하되 인스턴스는 공용)
> ② **발표·데모용 상시 가동** — 노트북은 끄고 다닌다
>
> 아래 템플릿의 `용도` 항목이 그렇게 쓰여 있다.

```
[요청] 추천 시스템 개발 서버

용도   냉장고 기반 레시피 추천 시스템 (부트캠프 최종 프로젝트, 8주)
       ① 백엔드팀과 공유하는 DB 인스턴스 (스키마로 분리, 인스턴스는 공용)
       ② 발표·시연을 위한 상시 가동 환경
       ※ 개발·배치 자체는 개인 노트북에서도 가능합니다. 서버가 필요한 이유는
          성능이 아니라 '공유'와 '상시 가동'입니다.
구성   Docker Compose — PostgreSQL(pgvector) · Redis · Python 앱 2개 · 배치

사양
  CPU     8 core
  RAM     32 GB
  디스크   100 GB
  GPU     없어도 됩니다 (아래 참조)

근거 (실측 기반 산출 + 운영 여유)
  - DB 데이터 약 1.5 GB
      레시피 46,353건을 실제로 적재해 측정한 값입니다 (2026-09-03 기준 507 MB).
      여기에 아직 산출하지 않은 임베딩·벡터 인덱스 투영분 약 0.34 GB 가 더해집니다.
      (본문 크롤 0.51 GB 실측 + 임베딩 0.14 GB + 벡터 인덱스 0.20 GB + 여유)
  - 여기에 아래가 얹힙니다
      · 후기 데이터 +0.16 GB (레시피당 30건 기준)
      · 인덱스 재빌드 중 임시 공간 · WAL 버스트 · pg_dump 사본
      · 실험 로그·모델 파일, 8주 이후 누적분 (월 350 MB)
  - 컨테이너 이미지 4.3 GB
  - RAM 평시 3.7 GB, 텍스트 벡터화 배치 실행 중 5.7 GB
      추천 사유 생성 배치(로컬 LLM) 실행 중에는 11.3 GB 입니다.
      이 모델은 하루 한 번 새벽에만 올라가고 평시에는 내려갑니다
      (로드 5.5초 실측 — 상주시킬 이유가 없습니다).
      배치와 개발 서버가 겹칠 때를 감안해 여유를 둡니다.
  - 추천 응답 p95 30ms 측정 (20만 건 규모) — 서빙 자체는 CPU 여유가 충분합니다.
      8 core 를 요청하는 이유는 서빙이 아니라 배치입니다:
      텍스트 벡터화가 CPU 만으로 3~6시간인데 코어에 거의 비례해 줄어듭니다.

  ※ 최소 동작 사양은 4 core / 16 GB / 50 GB 입니다.
     위 값은 실운영 테스트와 예산 산정을 위한 여유를 포함한 수치입니다.
     실제 데이터는 현재 0.5 GB 이며, 100 GB 를 요청하는 이유는 데이터 크기가 아니라
     재크롤·인덱스 재빌드·백업 사본이 겹치는 순간의 여유입니다.

GPU 관련
  - 추천 서비스(실시간)는 CPU 만 씁니다.
    연산량이 작아 GPU 로 보내는 전송 오버헤드가 계산보다 커서,
    GPU 를 쓰면 오히려 느려집니다.
  - 텍스트 벡터화 배치가 있는데 (신규 레시피 발생 시)
    GPU 사용 시 약 40분, CPU 만 사용 시 3~6시간 걸립니다.
    자주 도는 작업이 아니라 CPU 로도 충분합니다.
  - 대규모 모델 학습은 하지 않습니다 (임베딩은 공개 모델 추론).
    소규모 CPU 학습만 있고 전부 수 분 내 끝납니다 —
    선형 랭커(파라미터 5~12) · LightGBM · k-means(K≈50).
    학습 데이터가 적어(라벨 수백~수천 건) 큰 모델을 쓸 수 없는 것이지
    장비가 부족한 것이 아닙니다.
  - 그 밖의 상시 배치(클러스터 통계·맛 벡터 산출)도 전부 CPU 수 분 이내입니다.
  - GPU 는 **온라인 경로에 불필요**합니다. 다만 '오늘의 추천' 사유 생성 배치에
    로컬 LLM(Gemma 4 12B QAT · **VRAM 7.64GB 실측**)을 씁니다.
    여유가 있다면 **VRAM 8GB 이상**을 주시면 그 배치를 서버로 옮길 수 있습니다.
    없어도 됩니다 — 현재는 개발 노트북에서 새벽 16분이면 끝납니다.

포트   5432(PG) 6379(Redis) 8000(API) 8501(대시보드) [3000 5000 선택]
접근   교내망 전용
```

---

## 10-1. DB 를 분리해서 신청하는 경우 *(v2.7 신규)*

위 템플릿은 **한 대에 다 올리는 경우**다. DB 가 별도 장비라면 두 장으로 나눠 낸다.
근거는 8절 D 를 그대로 쓴다.

```
[요청 ①] 추천 시스템 — DB 서버

용도   백엔드팀과 공유하는 PostgreSQL 인스턴스 (설계 1-7 · 스키마로 분리)
구성   PostgreSQL 16 + pgvector · intarray · pg_trgm · ltree

사양
  CPU     4 core
  RAM     8 GB
  디스크   50 GB
  GPU     불필요 (pgvector 연산은 CPU 입니다)

근거
  - 데이터 0.66 GB → WAL·vacuum 여유 포함 약 1.3 GB (레시피 46,353 적재 기준)
      2026-09-03 실측 507 MB + 아직 산출 전인 임베딩·벡터 인덱스 투영분
  - 쿼리당 워킹셋 실측 42 MB → shared_buffers 2 GB 로 충분합니다
  - 온라인 쿼리는 in-DB 13.2 ms 측정. 서빙 부하는 낮습니다
  - 디스크 50 GB 를 요청하는 이유는 데이터 크기가 아니라
    인덱스 재빌드 중 임시 공간 · WAL 버스트 · pg_dump 사본(3일치)입니다
  - CPU 4 core 중 가장 무거운 작업은 HNSW 벡터 인덱스 생성입니다
  - DB 시간대는 Asia/Seoul 로 설정해 주십시오 (소비기한 임박 판정이 날짜에 걸립니다)

접속   AI 서버(아래)에서만 5432 접근. 출발지 IP 제한 요청합니다
```

```
[요청 ②] 추천 시스템 — AI 서버

용도   추천 API · 대시보드 · 배치 (DB 는 별도 요청 ①)
구성   Docker Compose — Redis · FastAPI · Streamlit · 배치 워커

사양
  CPU     8 core
  RAM     16 GB
  디스크   50 GB
  GPU     없어도 됩니다. 있다면 VRAM 8GB 이상 (통합메모리 16GB 도 가능)

근거
  - 8 core 를 요청하는 이유는 서빙이 아니라 배치입니다.
    텍스트 벡터화가 CPU 만으로 3~6시간인데 코어에 거의 비례해 줄어듭니다.
    추천 응답 자체는 p95 30 ms 로 여유가 큽니다.
  - RAM 평시 1.2 GB. 배치 창에서 최대 11.3 GB
    (텍스트 벡터화 2 GB + 추천 사유 생성 LLM 7.6 GB — 실측)
    두 배치는 시간대를 갈라 피크가 겹치지 않게 운영합니다.
  - 디스크 = 컨테이너 이미지 + LLM 모델 7.2 GB + 모델 아티팩트 + 로그
    + DB 덤프 사본 (약 1.5 GB × 3일). 실측 필요 약 20.6 GB 에 여유를 더한 값입니다.

GPU 관련
  - 실시간 추천은 CPU 만 씁니다. GPU 가 있으면 두 배치가 빨라집니다:
    ① 텍스트 벡터화 3~6시간 → 40분
    ② 추천 사유 생성 (하루 1회 새벽, 유저 100명 기준 30분)
  - 없어도 운영 가능합니다. 사유 생성은 개발 노트북에서 돌릴 수 있습니다.
```

> 🔴 **분리해도 성능은 나빠지지 않는다.** Retrieval 30.2 ms 중 DB 내부는 13.2 ms 이고
> 나머지 17 ms 는 localhost 에서도 나는 왕복 오버헤드다. 원격이 되면 교내망 RTT
> 0.5~2 ms 가 더해져 약 32 ms 다 (8절 D).
>
> **단, "요청당 왕복 1회" 원칙(1-7)이 지켜질 때만이다.**
