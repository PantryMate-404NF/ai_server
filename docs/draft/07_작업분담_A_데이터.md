# 트랙 A — 데이터

> 작성자: 박재우 · 작성일: 2026-09-03

> 담당: ______  ·  총 **8.5일** (A-1 은 09-02 에 완료)  ·  남은 기간 15영업일
> 공통 규칙은 [03_작업분담_공통.md](03_작업분담_공통.md) 를 먼저 읽을 것.

정규화 배치를 돌려 **레시피 특성 46,353건**을 채운다. 지금 실데이터 추천이 0건인 이유가 이것이고, B의 가중치 **81%**가 여기에 묶여 있다.

**목표**  3주 뒤: recipe_feature 46,353행이 실데이터로 채워져 retrieve_for_user() 가 mock 아닌 실제 후보를 돌려준다. feature_stats 에 실측 코퍼스 평균 μ 가 들어가 f_taste 가 무작위보다 낫다는 것이 숫자로 증명된다. 그리고 그 전부가 `make feature-test` 명령 하나로 매번 재검증된다 — 조용한 유실이 4번째로 일어나지 않게.


## 다른 트랙과 주고받는 것

- **reco.recipe_feature 46,353행 → B·C** — 온라인 경로가 읽는 유일한 테이블. A 가 채우는 컬럼 계약: essential_ids(role='essential' AND NOT is_staple) · all_ids(도구 제외) · category_ids · n_essential · n_total · n_unmatched · flavor_vec(REAL[6] 원값) · popularity_score(0~1 백분위) · quality_score(0 — 평점 원천 없음) · cook_minutes · difficulty · cluster_id · cluster_version · feature_version. **NULL 로 남는 것과 그 이유**: content_emb(생산 코드 없음) · season_vec(원천 없음) · nutrition(원천 없음) · cuisine_family·dish_type(원본에 분류축 없음). B 의 스코어러는 이 4개를 **None 으로 받고 가중치는 그대로 둔다** — 🔴 **재분배가 아니다** (D-4). 나눗셈 정규화 `Σwᵢfᵢ / Σwᵢ` 가 None 인 피처를 분자·분모에서 함께 뺀다 (app/schemas/common.py:135-139).
- **reco.feature_stats.flavor_mu(길이 6) + stats_version → B** — 🔴 B 는 flavor_vec 과 유저 taste_vec **양쪽 모두**에서 같은 μ 를 뺀 뒤 코사인을 잰다. 한쪽만 빼면 좌표계가 어긋나 에러 없이 틀린다. 그리고 계산에 쓴 stats_version 을 recommendation_log.stats_version(계약 api.py:214 에 자리 있음)에 실어야 μ 갱신 후에도 과거 점수가 재현된다. 🔴 **μ 값을 여기 적지 않는다.** 표본에 따라 15~20% 움직이는 값이라
문서에 박으면 B 가 하드코딩할 위험이 있다 — 그러면 좌표계가 어긋난 채
에러 없이 돈다. **B 는 반드시 `feature_stats` 최신행에서 읽는다.**
- **app/services/normalize/fixtures/feature_golden.json → B·C** — DB 없이 쓰는 레시피 30건 골든 픽스처. 정상 20 + n_essential=0 케이스 5(전체의 7.2%) + n_unmatched 높은 것 5. B 의 scorer 단위테스트와 C 의 평가 하네스 입력. 이 파일이 바뀌면 recipe_feature 계약이 바뀐 것이다.
- **reco.recipe_ingredient 약 354,600행 → C** — 화면의 '부족 재료' 표시와 재료별 레시피 역조회. role · match_method · match_score 포함. C 는 match_method IN ('fuzzy','embed') 인 행이 나오면 규약 위반으로 신고해야 한다(자동확정 금지, 설계 4-4-1).
- **reco.batch_run · reco.data_quality_snapshot → C** — 관측 화면이 그리는 원천. A 가 S2 라이터의 첫 호출자가 된다(현재 호출 코드 0개). C 는 coverage_rate 시계열과 batch_run.status='failed' 알림을 여기서 뽑는다.
- **make feature-test → B·C 규약** — recipe_feature 스키마나 빌더를 건드린 사람은 이걸 돌린다. 8개 체크 중 하나라도 깨지면 exit 1. 특히 체크 ⑦은 **실배치 행(`feature_version` 이 `test-` 로 시작하지 않는 것)만** 센다. 합성 격리는 09-03 에 조회 함수로 옮겼다(D-17) — `retrieve_candidates` 가 `test-*` 를 자동 제외하므로 **B 가 테스트를 돌려도 A 가 빨개지지 않는다.**
- **C ← A 로 넘기는 것: 검수 큐** — C 가 검수 화면(S5)을 만들면 A 의 scripts/bench/out/review_sheet.tsv 대신 normalization_queue 테이블을 쓴다. 그때 A 는 시트 생성기를 normalization_queue INSERT 로 바꾼다(suggested JSONB 형식은 MatchResult.suggested 와 동일: [[재료명, 유사도, 사유]]). 그 전까지는 TSV 가 인터페이스다.
- **B ← A 통보 사항: 공급 불가 피처** — DEFAULT_WEIGHTS 에서 w>0 인 10개 중 A 가 공급하는 것은 f_coverage(essential_ids) · f_taste(flavor_vec+μ) · f_cooccur(all_ids) · f_popularity · f_missing(n_essential) · f_time_fit(cook_minutes) · f_expiring(all_ids, SQL 함수 이미 있음). **f_cuisine(w=0.04)은 공급 불가** — recipe.cuisine 이 46,353건 전부 NULL 이고 원본에 분류축이 없다. 🔴 **B 는 `w_cuisine=0.04` 를 유지한 채 값만 None 으로 내보낸다.** 0 으로 내리지도, 재분배하지도 않는다 (D-4). 나눗셈 정규화가 흡수하며 오늘의 실효 분모는 `ACTIVE_WEIGHT_TODAY = 0.94` 다 (f_season 0.02 까지 빠진 값 — app/schemas/common.py:143). 재분배하면 `config_hash` 로 점수를 되찾을 수 없고 빠지는 피처의 **순서에 따라 결과가 달라진다.**

## 작업 목록

| | 작업 | 기간 | 선행 |
|---|---|---:|---|
| A-1 | ~~Dictionary.from_db() 수정~~ **✅ 09-02 완료** | 0일 | — |
| A-2 | 정규화 배치 러너 — app/services/normalize/batch.py | 1일 | A-1 |
| A-3 | 전량 정규화 1회차 실행 | 0.5일 | A-2 |
| A-4 | recipe_feature 빌더 — 배열 · n_* (essential 규칙 확정) | 1일 | A-3 |
| A-5 | flavor_vec + 코퍼스 평균 μ + 중심화가 실제로 낫다는 증명 | 1일 | A-4 |
| A-6 | popularity_score · quality_score — 분포를 보고 정하기 | 0.5일 | A-4 |
| A-7 | 배치 관측 — batch_run · data_quality_snapshot 기록 | 0.5일 | A-2 |
| A-8 | 회귀 게이트 — make feature-test | 0.5일 | A-6 |
| A-9 | B·C 트랙용 골든 픽스처 — DB 없이 돌게 | 0.5일 | A-6 |
| A-10 | 검수 시트 발주 — 커버리지 80.8% → 89% 의 유일한 경로 | 0.5일 | 없음 |
| A-11 | 재정규화 원커맨드 — make renormalize | 0.5일 | A-4, A-10 |
| A-12 | k-means cluster_id — 미루지 않는다. 단 content_emb 로는 못 한다 | 1일 | A-4 |
| A-14 | 🔴 ingredient.freq_count 채우기 — B 의 f_cooccur 가 기다린다 | 0.25일 | A-3 |
| A-13 | 2회차 재정규화 + 인수인계 | 1일 | A-11, A-12 |

---


## A-1 — Dictionary.from_db() 수정  ✅ **09-02 완료**

**기간** 0.5일

**무엇을**

app/services/normalize/p3_match.py:160 의 SQL. `SELECT id, name, is_staple, ...FROM ingredient i LEFT JOIN ingredient_category c` 는 i·c 양쪽에 id/name 이 있어 AmbiguousColumn 으로 죽는다. 실측: psycopg2.errors.AmbiguousColumn: column reference "id" is ambiguous. `i.id, i.name, i.is_staple, i.is_seasoning` 로 한정한다(수정본 실행 확인함 — 536행 반환, category_path NULL 0건). app/services/normalize/tests/test_p3.py 에 from_db 스모크 추가 (DB 없으면 skip).

**완료 기준**

.venv/bin/python -c "import sys,psycopg2; sys.path.insert(0,'.'); from app.services.normalize import Dictionary; d=Dictionary.from_db(psycopg2.connect('postgresql://reco:reco@localhost:5432/recodb')); print(len(d.names), len(d.aliases), sum(1 for m in d.meta.values() if m['is_staple']))"  →  `536 245 39` 출력. 그리고 `make normalize-test` 가 **113건(74+23+16)** 통과. 16건은 test_p4 하나의 수다.

**상태** ✅ 완료. `p3_match.py:162` 가 `i.` 로 한정한다. 아래는 회귀 확인용

**막히면** 막힐 수 없다(4줄 수정). 다만 DB 가 안 뜨면 make up 먼저.

> ⚠️ **조용히 틀리는 방식**
>
> 고치지 않고 from_seeds() 로 배치를 돌리는 것. seeds 행순서 id 와 DB serial id 가 **지금은** 정확히 일치한다(실측 불일치 0/536) — 그래서 배치가 잘 도는 것처럼 보인다. 그런데 A-10 검수에서 NEW 재료를 ingredient.csv 중간에 한 줄 넣는 순간 그 아래 전부가 1씩 밀린다. ingredient_id 는 여전히 유효한 FK 라 INSERT 가 성공하고, 추천은 '돼지고기' 자리에 '닭고기'를 넣은 채 에러 없이 계속 돈다.


## A-2 — 정규화 배치 러너 — app/services/normalize/batch.py

**기간** 1일

**무엇을**

recipe_ingredient_raw → P1·P2 → P3 match → P4 judge → recipe_ingredient. coverage.py 를 원형으로 하되 입력이 파일이 아니라 DB. 옵션: --limit N / --truncate / --dry-run. 커밋은 레시피 2,000건 단위(로더의 BATCH 관례). execute_values 는 **page_size 를 배치 크기로 명시** — 로더가 후기 627,610→29,035 를 잃은 그 버그다.

🔴 재개(resume) 기능은 만들지 않는다. 근거: 실측으로 2,000 레시피(원문 18,936행) = CPU 22.6초(P1P2 20.0 · P3 2.6 · P4 0.01). 451,862행 전량 = 약 525초 ≈ 9분. coverage.py 실측(8,000건/102초 = 12.75ms/건 × 46,353 = 9.9분)과 일치. 재개 로직의 버그 비용이 9분보다 비싸다. 대신 ① 2,000건마다 진행 출력 ② --truncate 로 언제든 전량 재생성.

**완료 기준**

.venv/bin/python -m app.services.normalize.batch --limit 2000 --truncate 를 **두 번** 돌리고: psql -c "SELECT count(*) FROM recipe_ingredient" 가 두 번 같은 수. 그리고 --dry-run 이 DB 를 안 건드림(count 불변).

**선행** A-1

**막히면** A-1 이 늦으면 from_seeds() 로 --dry-run 경로만 먼저 만들고 쓰기는 붙이지 않는다.

> ⚠️ **조용히 틀리는 방식**
>
> recipe_ingredient PK 가 (recipe_id, ingredient_id) 라 **한 레시피에 같은 재료가 두 번 나오면 한 행으로 접힌다.** 실측 2,941 레시피에서 577건 충돌(레시피의 19.6%), 그중 3건은 role 이 서로 다르다. ON CONFLICT DO NOTHING 이면 '먼저 온 행'이 이기는데, '간장'이 재료칸(→essential)과 양념칸(→seasoning)에 둘 다 있으면 position 순서에 따라 결과가 바뀐다. 에러 없이, 실행마다 다를 수도 있게. 파이썬에서 먼저 역할 우선순위(essential > seasoning > garnish > optional)로 합쳐 넣어야 한다.


## A-3 — 전량 정규화 1회차 실행

**기간** 0.5일

**무엇을**

make normalize-batch (Makefile 타깃 추가) 로 451,862행 전량. 실행 후 검증 쿼리 세트를 app/services/normalize/batch_verify.sql 로 남긴다: 행 수 · match_method 분포 · role 분포 · 레시피당 재료 수 분포.

**완료 기준**

make normalize-batch 후 psql: recipe_ingredient count 가 340,000~370,000 사이(실측 기대 46,353 × 7.65 = 354,600). match_method 에 exact·alias·rule 세 값만 존재하고 fuzzy/embed 가 0건(자동확정 금지 규약). **role 은 3종(essential·seasoning·optional)만 등장하고 garnish 는 0건인 것이 정상이다** —
`p4_role.py:57 GARNISH_BY_POSITION=False` 이고 원문 `group_name` 이 451,862행 전부
'기본재료' 라 R1·R2 도 안 돈다. 🔴 garnish 가 0 이 아니면 훅이 켜진 것이므로 **실패로 본다**:
`SELECT count(*) FROM recipe_ingredient WHERE role='garnish'` = 0. 소요 15분 이내.

**선행** A-2

**막히면** --limit 10000 으로 부분 적재하고 A-4 를 먼저 진행. 부분 적재 상태를 batch_run.params 에 남겨 착각을 막는다.

> ⚠️ **조용히 틀리는 방식**
>
> 행 수만 보고 넘어가는 것. 354,600 은 **매칭된 것만**의 수다. 미매칭 87,000건은 recipe_ingredient 에 아예 안 들어가므로 '깔끔하게' 보인다 — 20%를 버렸다는 사실이 어느 카운트에도 안 나타난다. A-4 의 n_unmatched 를 채워야만 소급이 된다(스키마 주석이 경고한 그것).


## A-4 — recipe_feature 빌더 — 배열 · n_* (essential 규칙 확정)

> ✅ **결정됨 ([D-10](05_작업분담_결정사항.md)).** 필수재료 0개(7.2%)는
> `n_unmatched / (n_total + n_unmatched) > 0.3` 이면 **정규화 실패로 보고 제외**한다.
> 임계값 0.3 은 임시값 — 실제 분포를 보고 조정한다(소급 가능).
> 🔴 품질 보고 시 **정상과 실패를 따로 센다.**

**기간** 1일

**무엇을**

app/services/normalize/feature_build.py. recipe_ingredient + ingredient + recipe 를 읽어 recipe_feature 를 채운다.

🔴 is_staple 39종 규칙 — 확인 결과: P4 가 이미 R3_is_staple 로 staple 을 SEASONING 에 보낸다. 그런데 39종 중 24종만 is_seasoning 과 겹치고(간장·소금·설탕류), **15종은 is_staple 로만 걸린다**: 쌀·밀가루·전분·참깨·물·생수·정수물·수돗물·찬물·냉수·뜨거운물·온수·끓는물·미지근한물·얼음. 그래서 **두 겹으로 건다** — SQL 에서도 `role='essential' AND NOT i.is_staple`. P4 만 믿으면 검수 alias 나 manual override 가 role 을 바꿨을 때 물·쌀이 essential_ids 에 들어간다.

- essential_ids: role='essential' AND NOT is_staple
- all_ids: 전부. 🔴 단 is_non_ingredient(도마 ×2,259 · 냄비 등, 실측 파싱분의 5.8%)는 제외 — 알러지 하드컷(`all_ids && allergy_ids`)의 분모에 조리도구가 섞이면 안 된다
- category_ids: DISTINCT ingredient.category_id
- n_essential / n_total / **n_unmatched**(P3 미매칭 수, 반드시 채운다) / cook_minutes · difficulty 복사
- recipe.status 를 'raw' → 'normalized' 로 (지금 46,353건 전부 'raw')

**완료 기준**

make feature-build 후 SQL 6줄이 전부 통과: ① count(*)=46,353 ② `SELECT count(*) FROM recipe_feature rf WHERE EXISTS(SELECT 1 FROM ingredient i WHERE i.is_staple AND i.id = ANY(rf.essential_ids))` = **0** ③ n_total = cardinality(all_ids) 전행 일치 ④ n_essential ≤ n_total 전행 ⑤ sum(n_unmatched) > 0 (0이면 안 채운 것) ⑥ avg(n_essential) 이 2.7~3.3 (실측 2.99 · 중앙값 3).

**선행** A-3

**막히면** A-3 부분 적재분으로 빌더를 만들고 전량은 나중에 다시 돌린다 — 빌더는 멱등이라 비용이 없다.

> ⚠️ **조용히 틀리는 방식**
>
> **n_essential=0 이 7.2%(약 3,340건)** 다(실측). retrieve_candidates 는 이걸 `CASE WHEN n_essential=0 THEN 1.0` 로 coverage 만점을 주고, `cardinality(essential_ids)=0` 전용 인덱스로 항상 후보에 넣는다. 여기엔 정상(간장계란밥)과 **정규화 실패**가 섞여 있는데 구분할 방법이 없다 — Top-20 이 전부 이것으로 채워져도 에러가 안 난다. n_unmatched/(n_total+n_unmatched) 로 갈라 두 숫자를 따로 보고해야 한다. 그리고 n_total=0 인 90건(0.2%)은 ⓪ 게이트가 잡지만, 12개 중 11개를 놓친 레시피는 통과한다 — 스키마 주석이 정확히 경고한 상황이다.


## A-5 — flavor_vec + 코퍼스 평균 μ + 중심화가 실제로 낫다는 증명

**기간** 1일

**무엇을**

p5_flavor.aggregate(mode='role_w') × p5_intensity.intensity() 로 6축을 만들어 recipe_feature.flavor_vec 에. 코퍼스 평균을 feature_stats 에 새 stats_version 으로 INSERT (bootstrap 영벡터 행은 남겨둔다).

μ 는 **전량 46,353건**으로 계산해 `feature_stats` 에 넣는다.
⚠️ 표본이 작으면 값이 15~20% 움직인다 — 부분 표본으로 확정하지 말 것.

🔴 flavor_vec 에는 **원값**을 저장한다. μ 는 feature_stats 에만 둔다. 빼기는 B 의 스코어러가 flavor_vec 과 user taste_vec **양쪽에** 같은 μ 로 한다.

검증기 app/services/normalize/flavor_check.py 를 함께 만든다 — 세 숫자를 낸다:
① 원본 코사인 평균 vs 무작위 대조군 (실측 0.722 vs 0.681 — 차이 0.041, 사실상 구분 안 됨)
② 중심화 후 표준편차 (실측 0.441 vs 무작위 0.409)
③ **판별력**: 제목 라벨(매움/단맛/신선) 그룹의 within−between 격차

**완료 기준**

.venv/bin/python -m app.services.normalize.flavor_check 가 exit 0. 게이트 조건은 ③ 이다 — 매움·단맛 두 라벨 모두에서 `중심화 격차 > 원본 격차` 여야 한다. 실측 기준선: 매움 0.096→0.187(×1.9) · 단맛 0.038→0.354(×9.3). 미달이면 exit 1 + 두 숫자를 나란히 출력. (신선 라벨은 0.061→0.046 으로 오히려 나빠진다 — 라벨이 노이즈라 게이트에 넣지 않고 참고로만 찍는다. 이걸 게이트에 넣으면 멀쩡한 배치가 빨간불이 된다.)
그리고 psql: `SELECT array_length(flavor_mu,1), n_recipes FROM feature_stats ORDER BY stats_version DESC LIMIT 1` → `6 | 46353`.

**선행** A-4

**막히면** flavor_check 는 DB 없이 시드+원문만으로도 돈다(실제로 그렇게 측정했다). A-4 가 늦으면 검증기부터 만들어 둔다.

> ⚠️ **조용히 틀리는 방식**
>
> μ 를 flavor_vec 에 **미리 빼서** 저장하는 것. CHECK(길이 6)는 통과하고 값도 그럴듯하며 배치도 초록이다. 그런데 유저 taste_vec 은 원좌표계라 f_taste 가 좌표계 어긋난 채로 조용히 돈다 — 게다가 B 가 나중에 규약대로 μ 를 한 번 더 빼면 두 번 빠진다. 두 번째: stats_version 을 recommendation_log 에 안 실으면(계약 api.py:214 에 자리가 있다) μ 를 갱신하는 순간 과거 요청의 점수 재현이 끊긴다 — 소급 불가다.


## A-6 — popularity_score · quality_score — 분포를 보고 정하기

> ✅ **결정됨 ([D-9](05_작업분담_결정사항.md)).** 인기도는 **`recipe.review_count`**
> (크롤 원문 개수 627,605) 로 계산한다. 실적재 행수(624,422)를 쓰면 후기 파싱
> 규칙이 바뀔 때마다 인기도가 흔들린다. `log1p` 후 **순위 / N**. 🔴 `percent_rank` 가 아니라
> `row_number() OVER (ORDER BY ln(1+review_count), id)` 다 — `percent_rank` 는
> 동점 블록이 8,470건이라 3분위가 비고, 후보 500컷이 비결정적이 되어
> **propensity 재현이 깨진다.**

**기간** 0.5일

**무엇을**

실측 review_count 분포(46,353건): min 0 · max 1,079 · 평균 13.54 · p50 **4** · p90 25 · p99 181 · 0건인 레시피 단 1개.
→ 꼬리가 극단적이다(p50 4 vs max 1,079). min-max 정규화를 쓰면 상위 1%가 나머지 전부를 0.02 근처로 눌러 버려 popularity 가 사실상 이진 피처가 된다. **log1p 후 백분위 순위(rank/N)** 를 쓴다 — 0~1 균등이 보장되고 f_popularity(w=0.10)가 순위를 실제로 만든다.

🔴 quality_score 는 **재료가 없다.** 실측: rating_avg NOT NULL 0건 · rating_count>0 0건 · view_count>0 0건. 크롤에 평점이 아예 없다. DEFAULT_WEIGHTS 에서 f_quality 는 w=0.0 이므로 **0 으로 두고 그 이유를 컬럼 COMMENT 와 배치 로그에 남긴다.** 후기 개수로 만들면 popularity 와 상관계수 1.0 인 가짜 축이 하나 늘 뿐이다.

**완료 기준**

psql 한 줄: `SELECT min(popularity_score), max(popularity_score), corr(popularity_score, ln(1+r.review_count)) FROM recipe_feature f JOIN recipe r USING(recipe_id)` → min≈0 · max≈1 · **Pearson corr ≥ 0.92**.
🔴 0.99 를 요구하면 어떤 구현도 통과 못 한다 — 실측 percent_rank 0.9264 ·
cume_dist 0.9347 · row_number 0.9242 · min-max 0.6532. 백분위 순위는 원값을
등간격으로 펴므로 로그값과의 **선형** 상관이 1 이 될 수 없다. 순위상관을 보려면
`corr` 대신 순위끼리 재야 하고 그때는 0.99 를 넘는다. 그리고 10분위 히스토그램이 각 구간 4,600±500 (백분위 순위이므로 균등해야 한다). quality_score 는 `SELECT count(*) FROM recipe_feature WHERE quality_score <> 0` = 0.

**선행** A-4

**막히면** 없음 — recipe.review_count 만 있으면 되므로 A-4 와 병행 가능.

> ⚠️ **조용히 틀리는 방식**
>
> **두 개의 review_count 가 있다.** recipe.review_count 는 로더가 `len(d["reviews"])` 로 넣은 원문 개수(627,610 기준)이고, recipe_review 실적재는 624,422 건이다(파싱 99.14%, 본문 빈 것 제외). 어느 쪽으로 정규화했는지 안 적으면 나중에 아무도 재현 못 한다 — 값은 둘 다 그럴듯해서 틀린 걸 알아채지 못한다. 그리고 백분위 순위는 **코퍼스가 늘면 전부 바뀐다.** 크롤을 추가하면 과거 점수가 재현 불가가 되므로 popularity 계산 시점의 n_recipes 를 batch_run.params 에 남긴다.


## A-7 — 배치 관측 — batch_run · data_quality_snapshot 기록

> 🔴 **결정됨 ([D-1](05_작업분담_결정사항.md)).** `data_quality_snapshot` 은 **C 전담**이다.
> A 는 `batch_run` 에만 쓴다 — 그 테이블이 정확히 배치 기록용이고 이미 있다.
> 같은 컬럼에 두 정의(A "파싱 언급" vs C "원문 행수")가 섞이면 시계열이 조용히 꺾인다.

**기간** 0.5일

**무엇을**

app/core/writer.py 에는 write_recommendation 하나뿐이다(S2 라이터를 부르는 코드가 0개인 상태). app/core/batch.py 를 추가해 컨텍스트 매니저 하나로 batch_run 행을 running→success/failed 로 닫는다. 배치가 끝나면 data_quality_snapshot 한 행 — 컬럼이 이미 정확히 맞게 설계돼 있다: mention_total · mention_matched · coverage_rate · type_coverage_rate · match_method_dist · avg_essential_per_recipe · pct_recipes_no_cooktime(실측 2,072/46,353 = 4.47%) · n_recipes_normalized.

**완료 기준**

배치 3종을 돌린 뒤 `SELECT job_name, status, input_count, output_count, finished_at IS NOT NULL FROM batch_run ORDER BY id DESC LIMIT 3` 에 normalize·feature·flavor 3행이 전부 success + finished_at 채워짐. 그리고 **일부러 실패시키는 테스트**: 잘못된 DSN 으로 돌려 status='failed' + error_msg 가 남는지 확인.

**선행** A-2

**막히면** 없음.

> ⚠️ **조용히 틀리는 방식**
>
> 예외를 except 로 삼키고 status='success' 로 닫는 것. 이 프로젝트의 조용한 유실 3건 중 커버리지 게이트가 정확히 이 형태였다 — coverage.py 가 --min 없으면 항상 0 을 반환해 CI 가 계속 초록이었다. 실패 경로를 **일부러 한 번 밟아보지 않으면** 실패 기록 코드가 작동하는지 알 수 없다.


## A-8 — 회귀 게이트 — make feature-test

**기간** 0.5일

**무엇을**

A-4·A-5·A-6 의 검증 SQL 을 한 명령으로 묶는다. app/services/normalize/feature_test.py — 실패하면 exit 1. 체크 목록: ① recipe_feature 행 수 ② essential_ids 에 staple 0건 ③ n_total=cardinality(all_ids) ④ flavor_vec 길이 6 전행 ⑤ feature_stats 최신 μ 길이 6 · n_recipes>0 ⑥ popularity 0~1 균등 ⑦ **실배치 행에 `test-` 접두어가 없다** (합성은 D-17 로 격리됨) ⑧ 중심화 판별력(A-5 게이트 재실행).

⑦ 이 필요한 이유: tests/smoke_test.py 가 같은 recipe_feature 테이블에 합성 행을 넣는다(feature_version='smoke'). 정상 종료 시 cleanup 이 지우지만 crash·Ctrl-C 면 남고, make smoke-big 은 50,000행이다. retrieve_candidates 에는 source·status 필터가 없으므로 **합성 레시피가 실유저에게 서빙된다.** idx_rf_version 인덱스가 이미 있으니 확인 비용은 0 이다.

**완료 기준**

make feature-test 가 8개 체크 전부 초록 + exit 0. 그리고 `psql -c "UPDATE recipe_feature SET n_total=99 WHERE recipe_id=(SELECT min(recipe_id) FROM recipe_feature)"` 로 일부러 깬 뒤 make feature-test 가 **exit 1** 인지 확인 — 게이트가 진짜 잡는지.

**선행** A-6

**막히면** 없음.

> ⚠️ **조용히 틀리는 방식**
>
> 게이트를 만들었는데 아무도 안 돌리는 것. make validate 나 CI 훅에 걸지 않으면 존재만 하고 일하지 않는다 — 이 프로젝트에 이미 그런 게이트가 있었다. B·C 트랙에게 '스키마를 건드렸으면 make feature-test' 를 규약으로 통보한다.


## A-9 — B·C 트랙용 골든 픽스처 — DB 없이 돌게

**기간** 0.5일

**무엇을**

app/services/normalize/fixtures/feature_golden.json — 실제 recipe_feature 에서 뽑은 30건(정상 20 · n_essential=0 인 것 5 · n_unmatched 높은 것 5)과 flavor_mu 를 담는다. B 의 scorer.py 는 이걸로 DB 없이 단위테스트를 쓸 수 있고, C 의 평가 하네스도 마찬가지. **A 가 recipe_feature 계약을 바꾸면 이 파일도 같이 바뀐다** — 그게 계약이 바뀌었다는 신호다.

**완료 기준**

픽스처 파일이 존재하고, `.venv/bin/python -m app.services.normalize.fixtures.check` 가 30건 전부 recipe_feature 의 현재 컬럼 집합과 키가 일치함을 확인 + n_essential=0 케이스가 5건 포함됨을 확인. B 에게 파일 경로와 로딩 예시 3줄을 전달.

**선행** A-6

**막히면** 없음.

> ⚠️ **조용히 틀리는 방식**
>
> 정상 케이스 30건만 넣는 것. B 의 스코어러가 n_essential=0(7.2%)·flavor_vec 전부 0(0.3%)·cook_minutes NULL(4.47%) 을 만나면 ZeroDivision 이 아니라 **NaN 이나 1.0 만점**을 조용히 낸다. 픽스처에 경계 케이스가 없으면 그 버그는 실데이터에서만 나타나고, 그때는 원인이 A 인지 B 인지 아무도 모른다.


## A-10 — 검수 시트 발주 — 커버리지 80.8% → 89% 의 유일한 경로

> 🔴 **결정됨 ([D-2](05_작업분담_결정사항.md)).** `suggested` 는 **객체 형식**이고
> **시더는 C 가 만든다**(`scripts/bench/queue_seed.py`). A 는 미매칭 목록만 넘긴다.

**기간** 0.5일

**무엇을**

실측: 미매칭 고유 표현 24,701종 / 언급 86,846건. 상위 300종이 미매칭 언급의 **44.8%** 를 덮는다 → mention 커버리지 80.8% → **89.4%**. 상위 500종이면 51.1% → 90.6% 로 W5 목표(0.85)를 넘긴다. 검수 1건 15초 규약이면 300종 = 75분이다.

현재 scripts/bench/out/review_sheet.tsv 는 2,000행 있지만 `결정` 칸 **0건 기입** — 검수가 시작되지 않았다.
상위 4종은 사전 누락이 확실하니 시트에 미리 표시한다: 밥(2,738) · 깨소금(2,419) · 매실액(1,723) · 참치액(962).
make review-sheet TOP=300 으로 다시 뽑고 담당자에게 넘긴다.

**완료 기준**

scripts/bench/out/review_sheet.tsv 가 300행 + 안내 헤더로 재생성되고, 상위 20종에 후보 3개가 전부 채워져 있음(빈 후보가 있으면 15초 규약이 깨진다). 전달 사실을 docs/09_CHANGELOG.md 에 기록. 기입 자체는 사람 몫이므로 A 의 DoD 에 넣지 않는다.

**선행** 없음 — A-3 과 병행. 다만 A-3 이후에 unmatched 를 다시 뽑으면 빈도가 정확해진다

**막히면** 사람이 시트를 안 채우면 **기다리지 않는다.** 상위 50종만 내가 직접 판정해 seeds/ingredient_alias.csv·ingredient.csv 에 넣는다 — 50종이면 미매칭 언급의 약 24%(커버리지 80.8→85.4%)라 W5 목표를 혼자서도 넘긴다. 애매한 것(참치액이 새 재료인지 액젓 alias 인지)만 남겨 둔다.

> ⚠️ **조용히 틀리는 방식**
>
> 시트를 채웠는데 `make seed` 를 안 돌려 DB 사전이 그대로인 것. 재정규화가 옛 사전으로 돌아 커버리지가 안 오른다 — 에러가 아니라 '검수했는데 별로 안 올랐네' 로 보인다. A-11 의 원커맨드가 이걸 구조적으로 막는다.


## A-11 — 재정규화 원커맨드 — make renormalize

**기간** 0.5일

**무엇을**

검수 결과가 반영되는 경로를 한 줄로 고정한다: review_apply --write → seeds/validate.py → scripts/migrate.py → normalize batch --truncate → feature-build → flavor μ 재계산 → feature-test. 각 단계 사이에 이전 값을 캐시해 두고 **마지막에 전/후 커버리지를 나란히 출력**한다.

검수 결과의 흐름은 이미 설계돼 있다 — 시드 파일이 SoT(migrate.py docstring), review_apply 가 alias/non_ingredient/new_ingredients 세 곳으로 나눈다. A 는 그것들을 **끊기지 않게 잇기만** 한다.

**완료 기준**

make renormalize 가 한 번에 완주하고 마지막에 `mention 0.808 → 0.894` 형식으로 전후를 출력. 커버리지가 **떨어지면 exit 1** (검수가 잘못됐다는 뜻이다). 그리고 아무것도 안 채운 시트로 돌리면 커버리지 불변 + exit 0.

**선행** A-4, A-10

**막히면** 없음.

> ⚠️ **조용히 틀리는 방식**
>
> recipe_ingredient 를 TRUNCATE 하고 재생성했는데 recipe_feature 를 다시 안 만드는 것. recipe_feature 는 온라인 경로가 읽는 **유일한** 테이블이라 화면은 옛날 배열로 멀쩡히 돌고, 두 테이블이 어긋난 사실은 어떤 쿼리도 알려주지 않는다. 그래서 renormalize 를 두 단계로 쪼개지 않고 한 타깃으로 묶는다 — 사람이 반만 실행할 수 없게.


## A-12 — k-means cluster_id — 미루지 않는다. 단 content_emb 로는 못 한다

**기간** 1일

**무엇을**

**미뤄도 되는가에 대한 답: 안 된다.** 근거 둘.
① content_emb 를 만드는 코드가 저장소 어디에도 없다 — sentence-transformers·sklearn 미설치(numpy 2.5.2 만 있음), ko-SBERT 배치는 3주에 안 들어간다. 그래서 설계대로의 k-means 는 **불가능**하다.
② 대체 다양성 축인 분류축도 죽어 있다 — recipe.dish_type/cuisine/main_ing_cat 실측 NOT NULL **0건**이고, raw_json.categories 는 46,353건에 고유 태그 45,529종(사실상 레시피당 유일한 SEO 키워드: '미소라면','챠슈라멘')이라 분류축이 아니다. 25.8%(11,956건)는 태그가 아예 없다.
→ cluster_id 가 없으면 ③ 재랭킹에 다양성 축이 **하나도** 없다. scripts/bench/kmeans_worth_it 이 비교 대상으로 삼은 '분류축 캡' 폴백 자체가 존재하지 않는다.

그래서 **싼 버전으로 한다**: essential_ids 멀티핫(536차) → TF-IDF → numpy SVD 48차 ⊕ 중심화 flavor_vec(6) → Lloyd K=50, 고정 시드. sklearn 없이 30줄. cluster_version='v1-tfidf-svd48' 로 기록해 나중에 content_emb 판과 구분되게 한다.

**완료 기준**

psql: `SELECT count(*) FROM recipe_feature WHERE cluster_id IS NULL` = 0. `SELECT max(c)::real/46353 FROM (SELECT count(*) c FROM recipe_feature GROUP BY cluster_id) t` < 0.15 (한 클러스터가 15% 넘으면 편중). 빈 클러스터 0개(count(DISTINCT cluster_id)=50). 같은 시드로 두 번 돌려 배정 100% 일치. **그리고 클러스터 10개를 골라 제목 5개씩 눈으로 본다** — 출력에 포함.

**선행** A-4 (essential_ids 필요)

**막히면** A-4 가 늦으면 recipe_ingredient 만으로 프로토타입(멀티핫은 recipe_ingredient 에서 바로 만든다). 정 안 되면 cluster_id NULL 로 두고 B 에 통보 — retrieval.py 가 이미 NULL→균등탐색 폴백을 하므로 깨지지는 않는다. 다만 ③ 의 다양성이 0 이 된다는 사실을 명시적으로 넘긴다.

> ⚠️ **조용히 틀리는 방식**
>
> 클러스터가 **'재료 개수'로 갈리는 것.** TF-IDF 의 L2 정규화를 빼면 재료 12개짜리 레시피끼리 한 클러스터가 된다. 균형 지표(최대 비중 <15%, 빈 클러스터 0)는 오히려 좋아 보이고 ILD 도 올라간다 — 다양성 지표가 전부 초록인데 의미는 없다. 클러스터별 대표 제목을 눈으로 보는 것이 유일한 방어다. 두 번째: cluster_version 을 안 남기고 재클러스터링하면 과거 로그의 cluster_id 가 다른 것을 가리키게 되어 Thompson belief 가 조용히 오염된다.


## A-14 — 🔴 `ingredient.freq_count` 채우기

**기간** 0.25일

**무엇을**

`ingredient.freq_count` 가 **536종 전량 0** 이다. 그런데 **B 지시서가 A 를 선행으로
지목한다** — `f_cooccur`(w=0.10)의 IDF 분모와 `00` 문서의 MMR IDF 가 둘 다 여기 묶여 있다.

정규화가 끝나면 한 문장이면 된다.

```sql
UPDATE ingredient i SET freq_count = c.n
FROM (SELECT ingredient_id, count(DISTINCT recipe_id) n
      FROM recipe_ingredient GROUP BY 1) c
WHERE c.ingredient_id = i.id;
```

**완료 기준**

`SELECT count(*) FILTER (WHERE freq_count > 0), count(*) FROM ingredient`
→ 앞 값이 400 이상. 그리고 상위 5종이 상식과 맞는가 눈으로 확인
(소금·간장·설탕 류가 위에 와야 한다).

**선행** A-3 (정규화 결과가 있어야 센다)

**막히면** B 에게 **지금 0 이라는 사실을 알린다.** B 가 모르면 가중 자카드가
그냥 자카드로 조용히 퇴화한 채 돈다.

> ⚠️ **조용히 틀리는 방식**
>
> IDF 는 `log(N / freq_count)` 인데 `freq_count = 0` 이면 0 나눗셈이거나
> (가드가 있으면) IDF 가 상수가 된다. 후자면 **모든 재료의 가중치가 같아져**
> 가중 자카드가 그냥 자카드가 된다 — 예외도 안 나고 점수도 정상 범위다.
> "흔한 재료(소금)와 희귀한 재료(트러플)를 구분한다"는 설계 의도만 사라진다.

## A-13 — 2회차 재정규화 + 인수인계

**기간** 1일

**무엇을**

검수 결과가 들어온 뒤 make renormalize 전량 1회. 그리고 A 가 만든 것을 B·C 가 계속 돌릴 수 있게 정리: Makefile 타깃 6개(normalize-batch · feature-build · flavor-check · feature-test · renormalize · review-sheet)를 make help 에 노출, README 에 '레시피가 늘거나 사전이 바뀌면 make renormalize' 한 줄.

🔴 **명시적으로 자르는 것과 그 근거를 문서에 남긴다**: content_emb(생산 코드 없음) · cuisine_family/dish_type(원본에 분류축이 없음 — 태그 45,529종) · season_vec(월별 데이터 원천 없음, f_season w=0.02) · nutrition(원본에 영양정보 없음) · quality_score(평점 0건). 자른 것을 안 적으면 3주 뒤 누군가 '왜 NULL 이지' 로 하루를 쓴다.

**완료 기준**

make renormalize 완주 + make feature-test 초록. `SELECT count(*) FROM recipe_feature WHERE flavor_vec IS NOT NULL AND cluster_id IS NOT NULL` = 46,353. make help 에 6개 타깃 표시. docs/09_CHANGELOG.md 에 최종 커버리지 수치와 잘라낸 5개 항목의 근거.

**선행** A-11, A-12

**막히면** 검수가 안 끝났으면 A-10 의 대안(상위 50종 직접 판정)으로 돌리고 2회차를 확정한다. 3주차에 사람을 기다리지 않는다.

> ⚠️ **조용히 틀리는 방식**
>
> 1회차와 2회차 사이에 feature_version 을 안 올리는 것. recipe_feature 는 UPSERT 라 조용히 덮이고, 두 회차 결과를 비교할 방법이 사라진다 — '커버리지가 올랐는데 추천이 나빠졌다' 를 진단할 근거가 없어진다. idx_rf_version 인덱스가 부분 재계산용으로 이미 준비돼 있다.
