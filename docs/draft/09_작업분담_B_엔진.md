# 트랙 B — 엔진

> 작성자: 박재우 · 작성일: 2026-09-03

> 담당: ______  ·  총 **10.5일**  ·  남은 기간 15영업일
> 공통 규칙은 [03_작업분담_공통.md](03_작업분담_공통.md) 를 먼저 읽을 것.

mock이 만드는 **난수를 실제 점수로** 바꾸고, 추천 기록이 실제로 쌓이게 한다. B-1이 끝나야 C가 화면에 띄울 데이터가 생긴다.

**목표**  3주 뒤 /v1/recommend 가 난수를 한 줄도 쓰지 않는다. 실제 recipe_feature·recipe·user 테이블에서 17피처를 계산하고, A={i: wᵢ>0 ∧ fᵢ≠None} 위에서 raw=(Σwᵢfᵢ)/Σwᵢ 로 정규화하고, 곱셈 페널티 3종을 곱해 점수를 낸다. 매 요청이 recommendation_log 1행 + impression N행으로 DB 에 남고, config_hash 하나로 그 점수의 가중치·페널티 계수를 되찾을 수 있다. 추천 이유는 후보 집합 대비 z-salience 로 실제 DB 값에서 만들어진다.


## 다른 트랙과 주고받는 것

- ← A 에게서 받는다 (없으면 B-6·B-7·B-8·B-15 가 막힌다): recipe_feature 46,353행 — essential_ids · all_ids · n_essential · n_total · n_unmatched · flavor_vec(6) · season_vec(12) · popularity_score · quality_score · cook_minutes · difficulty · cluster_id. B 는 이 컬럼명만 읽고 채우는 방법에는 관여하지 않는다
- ← A 에게서 받는다: ingredient.freq_count 실값. 🔴 현재 536종 **전량 0** 이라 f_cooccur(w=0.10) 의 IDF 분모가 없다. 0 을 그대로 쓰면 전 재료가 같은 IDF 가 되어 가중 자카드가 조용히 그냥 자카드로 퇴화한다
- ← A 에게서 받는다: feature_stats 신규 행 (flavor_mu 실측 μ · n_recipes>0). 현재는 부트스트랩 영벡터라 f_taste 중심화가 꺼져 있고, 그 상태에서는 무작위 벡터끼리도 코사인 0.767 이 나온다. B 는 최신 stats_version 을 읽어 recommendation_log 에 싣는 것까지만 한다
- ← A 에게 넘긴다: f_popularity 의 정규화 정의. popularity_score = review_count 의 코퍼스 백분위(실측 중앙값 4 · p99 181 · max 1079). A 의 빌더가 max 나눗셈을 쓰면 99% 가 [0,0.17] 로 뭉쳐 가중치 0.10 이 0.02 처럼 작동한다 — B 는 recipe.review_count 에서 직접 계산하는 폴백을 들고 A 를 기다리지 않는다
- → C 에게 준다 (B-1 부터, 즉 1일차부터): recommendation_log · event_log 에 실제로 쌓이는 행. `make serve-smoke` 로 C 가 화면을 붙이기 전에 데이터 존재를 확인한다
- → C 에게 준다: POST /v1/recommend 응답 — items[].features 17키 전수(None 포함) · score · penalty · propensity · reason · reason_features. 계약(app/schemas/api.py)은 바뀌지 않으므로 C 는 지금 mock 응답에 붙여도 되고 B-13 에서 값만 실제로 바뀐다
- → C 에게 준다: GET /v1/recommendations/{id} (DB 백엔드 · 프로세스 재시작 후에도 조회) + GET /v1/recommendations/{id}/candidates (RecommendationLogOut 에 candidates 필드가 없어 별도 엔드포인트로 낸다)
- → C 에게 준다: scoring_config 1행 — config_hash → base_weights 17키 + penalty_spec + n_warm. C 의 디버거가 w·f 막대그래프를 그리는 유일한 근거다(contrib 은 저장하지 않고 되계산한다)
- → C 에게 준다: app.core.counters.counters() 스냅샷을 /health 에 노출. written/promoted/duplicate/failed/tombstoned/impressions/contract_violation/degraded/event_no_position. 🔴 프로세스 메모리라 재시작하면 0 이 된다 — C 가 주기적으로 긁어 시계열로 쌓아야 유실을 누적으로 볼 수 있다
- → C 에게 준다: demo 유저 12명 (username LIKE 'demo_%', is_simulated=TRUE). 🔴 v_real_events · v_real_recommendations 뷰가 이들을 전량 걸러낸다 — C 의 화면은 뷰가 아니라 원본 테이블을 읽어야 한다
- → C 에게 준다: app/services/eval/feature_report.py — 피처별 (min/p50/max/None 비율) + 레시피 매칭률 n_total/(n_total+n_unmatched) 분포. C 의 평가 하네스가 '피처가 죽었는지'를 판정하는 입력
- B 가 건드리지 않는 것 (경계): app/schemas/ 는 읽기 전용이다 — DEFAULT_WEIGHTS · FEATURE_KEYS · UNAVAILABLE_FEATURES 를 수정하지 않는다. f_cuisine 은 가중치 0.04 를 유지한 채 값만 None 으로 내보내고, W_A=0.94 나눗셈이 흡수한다

## 작업 목록

| | 작업 | 기간 | 선행 |
|---|---|---:|---|
| B-0 | 데모 유저 픽스처 — 로그 쓰기의 FK 전제 (0단계 전반) | 0.5일 | 없음 |
| B-1 | 🔴 0단계: mock → write_recommendation 연결 | 0.5일 | B-0 |
| B-2 | 0단계: /v1/events → event_log INSERT | 0.5일 | B-1 |
| B-3 | 0단계: 로그 조회를 메모리에서 DB 로 | 0.5일 | B-1 |
| B-4 | 피처 로더 골격 + 지금 바로 되는 5종 | 1일 | 없음 |
| B-5 | f_expiring (w=0.15) 연결 — 함수는 이미 있다 | 0.5일 | B-4 |
| B-6 | f_taste (w=0.16) + 중심화 + stats_version | 1일 | A |
| B-7 | f_ing_pref (0.11) · f_cooccur (0.10) · f_pantry_use  | 1일 | A |
| B-8 | f_season(0.02) + 🔴 f_cuisine(0.04) 판단 + 나머지 결측 확정 | 0.5일 | A |
| B-9 | 🔴 스코어러 본체 — 결측은 재분배가 아니라 나눗셈 | 1일 | 없음 |
| B-10 | 곱셈 페널티 3종 | 0.5일 | B-9 |
| B-11 | scoring_config 등록 — config_hash 로 가중치를 되찾는다 | 0.5일 | B-10 |
| B-12 | 이유 생성(z-salience) 연결 — 남은 것은 ctx 하나다 | 0.5일 | B-4 |
| B-13 | 🔴 스코어러 ↔ 라이터 교체 — 난수 제거 | 1일 | B-9, B-10, B-12 |
| B-14 | 폴백 — 추천은 절대 실패하지 않는다 | 0.5일 | B-13 |
| B-17 | 온보딩 저장 — mock → DB (계약은 09-02 신설됨) | 0.5일 | B-0 |
| B-18 | 팬트리 쓰기 — mock → DB · tombstone | 0.5일 | B-0 |
| B-16 | 🔴 백엔드 인터페이스 확정 · 전달 | 0.25일 | B-1, B-2 |
| B-15 | 종단 검증 + A 산출물 교체 리허설 | 0.5일 | A |

---


## B-0 — 데모 유저 픽스처 — 로그 쓰기의 FK 전제 (0단계 전반)

**기간** 0.5일

**무엇을**

app/db/fixture.py 신설 + Makefile 타깃 `demo-users`. tests/smoke_test.py:96 의 add_user() 패턴을 그대로 재사용한다. app_user 12명(username 'demo_01'~'demo_12', is_simulated=TRUE) + user_preference 12행(spicy/sweet/salty/skill_level/max_cook_minutes/pref_cuisines) + pantry_item 유저당 5~12행(일부는 added_at 을 과거로 밀어 shelf_life_days 로 임박 상태가 만들어지게 한다) + user_ingredient_pref 유저당 5행(양수 3 · 음수 2 — p_avoid 검증용). 전부 ON CONFLICT 로 멱등.

**완료 기준**

`make demo-users && make demo-users && make psql` 후 한 쿼리: SELECT (SELECT count(*) FROM app_user WHERE username LIKE 'demo_%'), (SELECT count(*) FROM user_preference), (SELECT count(*) FROM user_ingredient_pref WHERE score<0), (SELECT count(*) FROM effective_expiry((SELECT id FROM app_user WHERE username='demo_01')) WHERE days_left<=3) → 12 / 12 / >0 / >0 이고 두 번 실행 후에도 행 수가 같다

> ⚠️ **조용히 틀리는 방식**
>
> is_simulated=TRUE 로 넣으면 04_functions.sql 의 v_real_events·v_real_recommendations 뷰가 데모 12명을 전량 걸러낸다. 로그는 정상으로 쌓이는데 C 트랙 화면만 0건이 되고, 원인이 라이터로 오인된다. FALSE 로 넣으면 반대로 실유저 지표가 데모로 오염되고 되돌릴 수 없다. 두 번째로, pantry_item.added_at 을 now() 기본값으로 두면 shelf_life_days 가 짧은 재료조차 D-3 안에 안 들어와 f_expiring 이 전 후보 NULL 이 된다 — 가중치 0.15 짜리 최대 차별화 피처가 죽은 채로 모든 테스트가 통과한다.


## B-1 — 🔴 0단계: mock → write_recommendation 연결

> 🔴 **`model_version` 을 `mock-` 으로 시작하게 유지하라** (09-03 조사에서 발견).
>
> 0단계 동안 쌓이는 행은 **점수가 난수**다. 지표에 섞이면 아무 의미가 없는데
> 겉보기엔 정상적인 추천 로그다. 걸러내는 장치는 `v_real_recommendations` 뷰의
> `model_version NOT LIKE 'mock-%'` **한 줄뿐**이라, 쓰는 쪽이 접두어를 안 붙이면
> 필터가 통째로 헛돈다.
>
> 실측 — `'mock_linear_v0'`(하이픈 대신 밑줄)로 넣으면 실지표 뷰에 그대로 잡힌다.
>
> **막는 방법 둘.** 하나만 해도 되고 둘 다 하면 확실하다.
> - `mock_server.py` 가 요청값을 그대로 쓰지 않고 항상 `mock-` 을 앞에 붙인다
>   (지금은 `req.model_version or "mock-linear-v0"` 라 요청이 아무 이름이나 넣을 수 있다)
> - DDL 에 `CHECK (model_version ~ '^[a-z0-9]+(-[a-z0-9.]+)*$')` 를 걸고
>   실엔진 이름 규칙을 정한다
>
> 계약 테스트 1건을 같이 넣어라 — **"난수 행이 실지표 뷰에 안 잡힌다"**.

**기간** 0.5일

**무엇을**

app/main.py:recommend() 에서 `_LOGS[rid] = RecommendationLogOut(...)` 직전에 app.core.write_recommendation() 호출을 추가한다. 인자: req, resp, scored=scored, pantry_ids=user_pantry_ids(uid) 조회값, allergy_ids=expand_user_allergens(uid), pantry_detail=pantry_item 5컬럼, config_hash=_config_hash(weights), warm_alpha=0.0, stats_version=(SELECT max(stats_version) FROM feature_stats), trace=trace(include_trace=false 여도 반드시 넘긴다). _trace() 의 serving_mode 를 "sim"→"real" 로 바꾼다 — CANDIDATE_KEEP 이 10→50 이 되어야 C 가 미노출 후보를 볼 수 있다. Makefile 에 `serve-smoke` 추가.

**완료 기준**

`make demo-users && make serve-smoke` — TestClient 로 demo 12명 × 1요청 후: SELECT count(*) FROM recommendation_log = 12 · SELECT count(*) FROM event_log WHERE event_type='impression' = 12*top_k · SELECT count(*) FROM recommendation_log WHERE request_params ? 'log_degraded' = 0 · app.core.counters() 에서 failed=0 · tombstoned=0 · contract_violation=0

**선행** B-0

**막히면** 해당 없음

> ⚠️ **조용히 틀리는 방식**
>
> user_id 가 app_user 에 없으면 FK 위반이 나는데 write_recommendation 은 예외를 삼키고 False 만 돌려주며 _tombstone() 도 같은 FK 로 실패한다. HTTP 응답은 200 이고 화면도 정상인데 DB 만 0행이다 — counters() 를 안 보면 영원히 모른다. 후기 60만 건 유실과 같은 형태다. 두 번째로, scored= 를 안 넘기면 writer 가 `pool = list(resp.items)` 로 폴백해 candidates 가 노출 20건뿐이 되는데, served ⊆ candidates 는 그대로 성립해서 log-test 48개가 전부 통과한다 — 미노출 후보 정보가 0 이 된 사실을 어떤 검증도 잡지 못한다.


## B-2 — 0단계: /v1/events → event_log INSERT

> 🔴 **`source` 컬럼이 09-02 에 신설됐다.** `event_log` 에 `NOT NULL` 로 붙었고
> **기본값이 없다** — 넣지 않으면 INSERT 가 실패한다.
>
> ```
> served    서버가 응답에 담았다 (impression)
> viewport  클라이언트가 실제 노출을 관측했다 (아직 안 쓴다)
> client    사용자 행위 보고 (click·cook·save…)
> ```
>
> 그리고 impression 은 `position` 이 **CHECK 로 필수**다. 부분 유니크 인덱스
> `(request_id, recipe_id, source)` 가 리플레이 멱등을 보장한다.

**기간** 0.5일

**무엇을**

app/core/events.py 신설 — write_events(batch: EventBatchIn) -> (accepted, rejected, errors). execute_values 에 page_size=len(rows) 를 반드시 준다(loader 유실 전례). impression 은 ux_ev_impression 부분 유니크로 DO NOTHING, click/save/cook 등은 중복 허용. request_id 가 recommendation_log 에 없는 행은 그것만 걸러내고 나머지는 넣는다(배치 전량 실패 금지). mock_server.events() 의 검증-만-하는 본문을 이 호출로 교체. position 이 없는 행은 counters.bump('event_no_position').

**완료 기준**

`make event-test` — recommend 1회 후 click·save·cook 각 1건을 같은 request_id·position 과 함께 POST → SELECT event_type, count(*) FROM event_log WHERE request_id=%s GROUP BY 1 이 impression=top_k · click=1 · save=1 · cook=1; 같은 배치 재전송 후 impression 은 여전히 top_k; counters()['event_no_position']==0; 임의 UUID 로 바꾼 배치는 rejected=3 이고 event_log 총행 불변

**선행** B-1

**막히면** 해당 없음

> ⚠️ **조용히 틀리는 방식**
>
> EventIn.position 이 optional 이라 클라이언트가 빼먹어도 accepted 로 집계된다. position 이 없는 시대의 로그는 position bias 보정이 영구 불가이고 '상위 k 만 잘라 보기'조차 안 된다 — 소급이 안 되는데 에러가 안 난다. 두 번째로, execute_values 에 page_size 를 안 주면 RETURNING 이 마지막 청크만 돌아와 accepted 가 실제보다 작게 보고되고, 반대로 fetch 없이 cur.rowcount 를 믿으면 DO NOTHING 으로 건너뛴 중복이 성공으로 세어진다. 두 방향 다 조용하다.


## B-3 — 0단계: 로그 조회를 메모리에서 DB 로

**기간** 0.5일

**무엇을**

app/db/logs.py 신설 — read_log(request_id) -> RecommendationLogOut|None, list_logs(user_id=None, limit=50). mock_server.get_log() 의 _LOGS 딕셔너리 조회를 교체하고 _LOGS 자체를 삭제한다. 🔴 RecommendationLogOut(app/schemas/api.py)에 candidates 필드가 없다 — C 의 디버거가 후보를 보려면 GET /v1/recommendations/{id}/candidates 를 따로 낸다(응답 모델 필드 추가는 additive 라 계약을 안 깬다).

**완료 기준**

`make log-read-test` — recommend → 프로세스를 새로 띄운 뒤 GET /v1/recommendations/{id} 가 200 이고 served·stage_trace·config_hash 가 응답과 일치; GET .../candidates 의 직렬화 결과 문자열에 '"f_cuisine": null' 이 포함된다(None 이 0.0 으로 안 바뀌었다)

**선행** B-1

**막히면** 해당 없음

> ⚠️ **조용히 틀리는 방식**
>
> JSONB 왕복에서 features 의 None 이 0.0 으로 복원되면 '계산 불가'가 '계산했더니 0'으로 뒤집힌다. pydantic 의 float|None 은 null 을 지키지만 중간에 `or 0.0` 을 한 번 태우거나 pandas 로 읽으면 조용히 메워지고, 값은 전부 [0,1] 안이라 어떤 범위 검증도 안 걸린다. 두 번째로 _LOGS 를 지우지 않고 DB 조회를 추가만 하면 같은 request_id 에 메모리본과 DB본이 공존해 C 가 어느 쪽을 보는지 모른 채 서로 다른 값을 본다.


## B-4 — 피처 로더 골격 + 지금 바로 되는 5종

**기간** 1일

**무엇을**

app/db/features.py 를 채운다(현재 __all__ 만 있는 8줄 스텁). load_features(user_id, candidates) -> dict[recipe_id, dict[str, float|None]]. 🔴 `WHERE rf.recipe_id = ANY(%s)` 한 쿼리 — 후보를 하나씩 조회하면 안 된다(파일 docstring 의 경고). 이번에 채우는 5종: f_coverage(=Candidate.coverage 그대로) · f_missing(=1/(1+missing_count)) · f_popularity(recipe.review_count 의 코퍼스 백분위 — 실측 중앙값 4 · p99 181 · max 1079) · f_time_fit(cook_minutes NULL→0.5 중립, 상한 이내 1.0, 초과 감쇠) · f_skill_fit(1−|difficulty−skill_level|/4 — w=0 이지만 difficulty 45,300건이 이미 있어 공짜이고, 지금 안 남기면 나중에 켤 때 로그에 값이 없다).

**완료 기준**

`make feature-test` — 후보 200건에 대해 (a) 5종 전부 [0,1] 또는 None (b) cook_minutes IS NULL 인 레시피의 f_time_fit == 0.5 정확히 (0 도 1 도 아니다) (c) review_count 상위 1% 레시피가 f_popularity > 0.98 이고 중앙값(4건) 레시피가 0.4~0.6 (d) 요청 1회의 DB 왕복 횟수 == 2 (retrieve 1 + features 1) — 커서 실행 횟수를 세어 assert

**선행** 없음 — recipe 테이블만 읽는다. 단 후보를 뽑으려면 recipe_feature 가 필요하다

**막히면** recipe_feature 가 0행이라 retrieve() 가 후보 0건이면, tests/smoke_test.py:283 의 합성 recipe_feature INSERT 를 재사용해 3,000건짜리 개발 픽스처를 만들고 그 위에서 개발한다. A 의 실데이터가 오면 픽스처만 끄면 되고 코드는 안 바뀐다.

> ⚠️ **조용히 틀리는 방식**
>
> f_popularity 를 review_count 원값이나 max 나눗셈(÷1079)으로 만들면 0~1 축에는 들어가지만 중앙값이 4 라 99% 의 레시피가 [0, 0.17] 에 뭉친다 — 가중치 0.10 이 사실상 0.02 처럼 작동하고, Σw=1.00 검증도 [0,1] 범위 검증도 통과한다. 두 번째로 로더가 후보를 for 루프로 하나씩 조회하면 로컬 유닉스소켓(~0.1ms)에서는 안 보이다가 원격 DB(5~10ms)로 옮기는 순간 500×RTT 로 드러난다.


## B-5 — f_expiring (w=0.15) 연결 — 함수는 이미 있다

**기간** 0.5일

**무엇을**

DB 함수 f_expiring(uid, all_ids, 3) 은 infra/init/04_functions.sql 에 이미 완성돼 있다(분모 캡 3 · 임박 0건이면 NULL). 남은 것은 후보 500건을 한 쿼리로 부르는 것뿐이다 — B-4 의 로더 쿼리에 `f_expiring(%s, rf.all_ids, 3)` 컬럼을 합친다. SQL 의 NULL 을 파이썬 None 으로 그대로 통과시킨다.

**완료 기준**

`make feature-test` — (a) demo_01(임박 재료 2건) 기준 그 재료를 쓰는 레시피의 f_expiring 이 0.5 또는 1.0 (b) 임박 재료를 안 쓰는 레시피는 0.0 (c) 임박 0건인 demo 유저의 전 후보에서 f_expiring is None 이고 0.0 이 하나도 없다 (d) B-4 에서 잰 왕복 횟수 2 가 그대로다(늘지 않았다)

**선행** B-4 (같은 쿼리에 합치므로). all_ids 실값은 A 의 recipe_feature 를 기다리지만, 합성 픽스처로 지금 개발 가능

**막히면** 합성 recipe_feature 의 all_ids 로 검증한다. f_expiring 은 SQL 안에 있어 A 데이터가 오면 코드 변경 0줄이다.

> ⚠️ **조용히 틀리는 방식**
>
> SQL 의 NULL 을 `float(row[k])` 로 캐스팅하거나 `row[k] or 0.0` 으로 받으면 '임박 재료가 하나도 없다'가 '이 레시피는 임박 재료를 하나도 안 쓴다'로 바뀐다. 그러면 W_A 가 0.15 만큼 커져 전 후보 점수가 일괄로 눌리는데 **순위는 그대로**라 화면상 아무 차이가 없다. 임계값·degraded 판정·C 의 평가 하네스만 조용히 틀린다. 이것이 나눗셈 정규화가 존재하는 이유 자체를 무력화하는 가장 흔한 방식이다.


## B-6 — f_taste (w=0.16) + 중심화 + stats_version

**기간** 1일

**무엇을**

app/services/recommends/taste.py — f_taste = cos(taste_vec − μ, flavor_vec − μ). μ 는 feature_stats 최신행(현재 부트스트랩 영벡터 · n_recipes=0 — 즉 중심화가 꺼져 있다). user_vector 가 0행이므로 taste_vec 은 user_preference 의 spicy/sweet/salty 3축에서 유도한다. 🔴 설문에 없는 신맛·감칠맛·기름짐 3축은 0.5 중립이 아니라 **코퍼스 평균 μ 를 그대로 넣어 중심화 후 0 이 되게** 한다 — 0.5 를 넣으면 μ 차감 후 부호가 생겨 '없는 취향'이 신호가 된다. 읽은 stats_version 을 write_recommendation(stats_version=) 으로 넘긴다.

**완료 기준**

`make feature-test` — (a) μ=0 일 때 비음수 무작위 벡터쌍 1,000개의 코사인 평균이 0.75±0.05 (scripts/bench/flavor_scale.py 실측 0.767 재현) (b) μ 차감 후 같은 쌍의 평균이 |0.1| 미만 (c) taste_vec 과 flavor_vec 에서 뺀 μ 가 같은 객체임을 assert (d) SELECT stats_version FROM recommendation_log ORDER BY created_at DESC LIMIT 1 이 NULL 이 아니고 feature_stats 최신값과 같다

**선행** A — recipe_feature.flavor_vec (현재 0행) · feature_stats 의 실제 μ (현재 영벡터)

**막히면** seeds/ingredient_flavor.yaml + recipe_ingredient_raw 로 레시피 1,000건만 임시 flavor_vec 을 계산해 코드를 검증한다. 정본은 A 가 채우고, B 는 읽는 소스만 교체하면 되므로 f_taste 코드는 그대로 산다.

> ⚠️ **조용히 틀리는 방식**
>
> μ 를 flavor_vec 에서만 빼고 taste_vec 에서는 안 빼면 두 벡터가 다른 좌표계에 놓이는데, 코사인은 여전히 [-1,1] 이라 값이 그럴듯하고 예외도 안 난다. 두 번째로 더 위험한 것 — μ 가 아직 영벡터라 중심화가 사실상 꺼져 있다는 사실을 잊으면, 문서 실측대로 **무작위 벡터끼리도 0.767 이 나오는 상태**로 가중치 0.16 이 잡음에 배정된 채 서빙된다. 값은 정상 범위이고 순위도 그럴듯하다. 세 번째로 stats_version 을 안 실으면 μ 가 갱신되는 순간 과거 f_taste 를 영원히 재현할 수 없다.


## B-7 — f_ing_pref (0.11) · f_cooccur (0.10) · f_pantry_use (w=0)

> 🔴 **결정됨 ([D-3](05_작업분담_결정사항.md)).** `ingredient.freq_count` 는 지금 **536종 전량 0** 이고
> A-3 직후 A 가 채운다. **기다리되 멈추지 않는다** — 먼저 할 수 있는 피처가 54% 있다.
> 🔴 `freq_count = 0` 을 그냥 쓰면 가중 자카드가 **그냥 자카드로 조용히 퇴화**한다.
> 0 이면 `f_cooccur` 를 **`None` 으로 내보낸다.**

**기간** 1일

**무엇을**

f_ing_pref = all_ids ∩ user_ingredient_pref 의 score 평균 → (x+1)/2 로 [0,1] 사상, 교집합 0 이면 None. f_cooccur = 유저가 최근 cook 한 레시피들과의 IDF 가중 자카드 **최댓값**, cook 0건이면 None. 🔴 f_cooccur 를 UNAVAILABLE_FEATURES 에 넣지 않는다 — 저기는 영구히 수단이 없는 것(ALS·KMeans)이고 f_cooccur 는 유저가 한 번만 요리하면 살아난다. IDF 분모는 ingredient.freq_count 인데 **실측 536종 전량이 0** 이다. f_pantry_use = |all_ids ∩ pantry| / |pantry|.

**완료 기준**

`make feature-test` — (a) demo 유저에 그 레시피 자신을 cook 이벤트로 주입하면 f_cooccur == 1.0, 재료 교집합 0 인 레시피는 0.0 (b) cook 0건 유저는 전 후보 f_cooccur is None 이고 0.0 이 단 하나도 없다 (c) 기피 재료(score<0)만 든 레시피의 f_ing_pref < 0.5 이고 [0,1] 을 안 벗어난다 (d) freq_count=0 인 재료가 섞여도 예외 없이 값이 나오고 IDF 가 전부 같은 값이 되지 않는다

**선행** A — recipe_ingredient(0행)에서 유도되는 ingredient.freq_count. 현재 536종 전량 0 이라 IDF 분모가 없다

**막히면** IDF 를 recipe_ingredient_raw(451,862행)의 raw_text 빈도로 임시 계산하거나, freq_count=0 인 재료는 IDF=코퍼스 중앙값으로 대체하는 폴백을 둔다. 자카드 계산 자체는 IDF 없이도 검증되므로 A 를 기다리지 않는다.

> ⚠️ **조용히 틀리는 방식**
>
> 콜드 유저에게 f_cooccur=0.0 을 채우면 W_A 에 0.10 이 들어가 모든 콜드 후보가 일괄 감점되는데, 전 후보가 같이 내려가므로 순위가 안 바뀌어 보이지 않는다. 두 번째로 freq_count=0 을 IDF 분모에 그대로 넣으면 ZeroDivisionError 가 아니라 log(N/(1+0))=log(N) 이 되어 **전 재료가 동일한 최대 IDF** 를 갖는다 — 가중 자카드가 그냥 자카드로 조용히 퇴화하고, 값은 여전히 [0,1] 이라 테스트가 전부 통과한다. 세 번째로 f_ing_pref 의 (x+1)/2 사상을 빼먹으면 음수 선호가 -1 로 들어가 raw 가 [0,1] 을 벗어나는데, 이건 시끄럽게 틀리므로 오히려 안전하다.


## B-8 — f_season·f_cuisine 처리 ✅ **결정 완료 (09-02)** + 나머지 결측 확정

> 🔴 **판단할 것이 없다. 09-02 에 결정됐다.** 이 작업은 결정을 *적용*하는 일이다.
>
> `app/schemas/common.py` 에 `PENDING_DATA_FEATURES = {f_cuisine, f_season}` 가
> 신설됐다. `UNAVAILABLE_FEATURES = {f_ing_cf, f_group_pref}` 와 뜻이 다르다.
>
> | | 뜻 | 되살리려면 | 가중치 |
> |---|---|---|---|
> | `UNAVAILABLE_FEATURES` | **수단이 없다** | 그 방법을 구현 | 0 이어야 한다 |
> | `PENDING_DATA_FEATURES` | **데이터가 없다** | 데이터만 오면 됨 | **그대로 둔다** |
>
> **가중치를 0 으로 내리지 않고 재분배도 하지 않는다.** 나눗셈 정규화가
> `raw = Σwᵢfᵢ / Σwᵢ` 에서 `fᵢ=None` 을 분자·분모에서 함께 빼므로
> **비례 재분배와 수학적으로 같다.** 데이터가 오면 코드 변경 없이 켜진다.
>
> 문서에 쓰는 숫자는 `ACTIVE_WEIGHT_TODAY = 0.94` 상수다 (설계 의도는 1.00).
> B 가 할 일은 **스코어러가 이 둘을 None 으로 내보내는지 확인**하는 것뿐이다.

**기간** 0.5일

**무엇을**

f_season = recipe_feature.season_vec[현재 월], NULL 이면 None. **f_cuisine 은 None 으로 고정한다.** 근거: recipe.cuisine_family 가 46,353건 전량 NULL 이고, raw_json.categories 는 구조화된 분류축이 아니라 자유 태그다(실측 `["닭고기요리"]` · `["미소라면","일본식라면","미소라멘","챠슈라면"]`). 3주 안에 15종 taxonomy 분류기를 만들 근거가 없다. 🔴 **가중치 0.04 를 0 으로 내리지 않는다** — 나눗셈이 흡수해 W_A=**0.94** 가 되고(f_season 0.02 까지 빠진다 — `ACTIVE_WEIGHT_TODAY`) raw 는 여전히 [0,1] 이다(08_확정설계 139행 '맞추려 하지 않는다'). 내리면 config_hash 가 바뀌고, cuisine_family 가 나중에 채워졌을 때 되돌려야 하며, 그 사이 로그의 실효 가중치가 두 벌이 된다. 🔴 **UNAVAILABLE_FEATURES 에도 넣지 않는다** — 저기는 영구히 수단이 없는 것이고 f_cuisine 은 데이터만 없다. 같은 판단으로 f_dish_type(dish_type 0건) · f_quality(rating_avg 전량 NULL) · f_content(user content_emb 없음) 도 None, f_ing_cf·f_group_pref 는 규약대로 None.

**완료 기준**

`make feature-test && make contract` — (a) 전 후보에서 features['f_cuisine'] is None 이고 0.0 이 하나도 없다 (b) features 가 FEATURE_KEYS 17키 전수(ScoredCandidate 검증기가 강제) (c) make contract **98건** 전부 통과 — 특히 test_contract.py:51 'w>0 인 피처는 전부 계산 가능하다' 가 계속 통과한다 (d) `git diff --exit-code app/schemas/common.py` 가 0 을 돌려준다(DEFAULT_WEIGHTS 를 한 글자도 안 건드렸다)

**선행** 없음 — A 가 season_vec 을 잘랐으므로 `f_season` 은 영구 None 이다

**막히면** season_vec 이 NULL 이면 f_season 은 None 으로 나가고 W_A 가 0.02 더 줄 뿐이다. 나눗셈이 흡수하므로 기다릴 이유가 없다.

> ⚠️ **조용히 틀리는 방식**
>
> f_cuisine 을 0.0 으로 채우면 pref_cuisines 에 한식을 넣은 유저에게 '이건 한식이 아니다'라는 틀린 신호가 되고 W_A 에 0.04 가 들어간다 — 조용하다. 반대로 UNAVAILABLE_FEATURES 에 넣으면 test_contract.py:51 이 깨져 make contract 가 실패하므로 이쪽은 시끄럽게 틀린다(안전). 진짜 위험은 세 번째다 — 급하다고 0.04 를 0 으로 내리고 나머지 9종에 비례 재분배하는 것. Σw=1.00 검증을 통과하면서 5-2-2-1 이 명시적으로 금지한 '적용 순서에 따라 값이 달라지는' 상태로 되돌아가고, 재분배가 웜 전환·결측 두 경로에서 겹치는 순간 같은 요청이 두 값을 낸다.


## B-9 — 🔴 스코어러 본체 — 결측은 재분배가 아니라 나눗셈

**기간** 1일

**무엇을**

app/services/recommends/scorer.py 신설(현재 파일 자체가 없다). ① 웜 전환: α=min(1, n_events/n_warm), w_content ← 0.08α, w_taste ← 0.16−0.05α, 나머지 불변. **Σw 가 1+0.03α 로 1 을 벗어나는 것을 맞추지 않는다.** ② A = {i : wᵢ>0 ∧ fᵢ≠None}, W_A = Σ_A wᵢ, raw = (Σ_A wᵢfᵢ)/W_A. **W_A==0 → raw=0.0** (예외도 None 도 아니다 — 전 후보가 동점이 되어 ① 의 정렬 missing_count ASC · popularity_score DESC 가 그대로 살아나고 5-6 '추천은 절대 실패하지 않는다'가 성립한다).
왜 나눗셈인가 (확인함): 재분배는 웜 전환 · 임베딩 NULL · f_cooccur NULL · 설문 결측 **네 경로에서 각각** 일어나 적용 순서에 따라 값이 달라진다. 나눗셈은 순서와 무관하고, 복원에 컬럼을 하나도 안 늘린다 — config_hash 로 기준 가중치를, warm_alpha 로 ①을, features 의 None 패턴으로 A 를 알면 W_A 가 계산되기 때문이다. 이것이 v2.9 가 RecommendationLogOut 에서 weights 컬럼을 아예 뺀 이유다.

**완료 기준**

`make scorer-test` (신설 · DB 불필요) — (a) 전 피처 f=1.0 이면 결측 개수와 무관하게 raw==1.0: f_cuisine 만 None 인 경우와 w>0 10종 중 9종이 None 인 경우 모두 (b) 나머지가 같고 f_expiring 만 None 인 후보의 raw 가, f_expiring 에 나머지 피처의 가중평균값을 채운 후보의 raw 와 같다 (재분배 구현이면 이 둘 중 하나가 반드시 깨진다) (c) 순서 무관: (웜 전환→결측 제거) 와 (결측 제거→웜 전환) 이 1e-12 이내로 같다 (d) W_A==0 → raw==0.0 이고 예외 없고 math.isnan(raw) 가 False (e) 무작위 10,000 케이스에서 0.0 ≤ raw ≤ 1.0 전수

**선행** 없음 — 순수 함수다. 피처 값은 테스트가 만든다

**막히면** 해당 없음. A·C 와 무관하게 지금 끝낼 수 있고, 실데이터가 오면 입력만 바뀐다

> ⚠️ **조용히 틀리는 방식**
>
> app/schemas/pipeline.py 의 contrib() 가 `(self.features.get(k) or 0.0)` 로 None 을 0 으로 흘린다 — **디버거 막대그래프 전용**인데 이 한 줄을 스코어러에 복붙하면 결측이 0 으로 대입되어 나눗셈 정규화가 통째로 무의미해진다. 결과는 여전히 [0,1] 안이고 예외도 없어서 (e) 로는 안 잡힌다. (a)(b) 가 유일한 방어다. 두 번째: W_A 가 부동소수 누적으로 1e-17 이 되면 raw 가 1e16 으로 폭발하고 정렬이 뒤집힌다 → W_A < 1e-9 를 0 으로 취급한다. 세 번째: f=0.0 과 f=None 을 `if not f` 로 구분하면 '계산했더니 0'인 피처가 A 에서 빠져 W_A 가 줄고, 그 후보만 점수가 올라간다 — 부정 신호가 가점으로 뒤집히는데 값은 전부 정상 범위다.


## B-10 — 곱셈 페널티 3종

**기간** 0.5일

**무엇을**

app/services/recommends/penalty.py — p_recent = 0.7 (최근 7일 impression 있으면) 아니면 1.0 · p_cooked = 0.5 (최근 14일 cook 있으면) 아니면 1.0 · p_avoid = min(avoid_cap, avoid_slope × |avoid ∩ all_ids| / |all_ids|), 기본 avoid_cap=0.8 · avoid_slope=2.0, avoid 집합은 user_ingredient_pref.score < 0. penalty = p_recent × p_cooked × (1−p_avoid) 를 ScoredCandidate.penalty(ge=0, le=1)에 담고 score = raw × penalty. **알러지는 여기 없다 — ① 의 하드 컷이다.** 후보 500건을 한 쿼리로: event_log 를 recipe_id 로 집계해 (last_impression_at, last_cook_at) 을 받는다.

**완료 기준**

`make scorer-test` — (a) 이벤트 0건 유저는 전 후보 penalty==1.0 정확히 (b) cook 을 주입한 레시피만 penalty==0.5, 15일 전 cook 은 1.0 (c) 기피 재료 비율 100% 레시피의 p_avoid==0.8(캡) 이고 penalty==0.2, 20% 면 p_avoid==0.4 (d) 전 후보에서 penalty == p_recent*p_cooked*(1-p_avoid) 를 1e-9 이내로 assert (e) all_ids 가 빈 배열인 레시피에서 예외가 안 난다

**선행** B-9

**막히면** 해당 없음

> ⚠️ **조용히 틀리는 방식**
>
> 페널티를 더하면(score = raw − penalty) 점수 높은 항목이 페널티를 이겨서 '어제 만든 김치찌개'가 계속 1위에 남는데, 점수 자체는 그럴듯하고 아무 에러도 없다. 두 번째: user_allergy 의 severity='avoid' 를 p_avoid 에 섞으면 ① 의 하드컷과 이중이 되고, 알러지가 페널티로 내려앉는 순간 **점수가 충분히 높으면 통과한다** — 안전 제약이 조용히 확률 문제로 바뀐다. 세 번째: |all_ids| 분모에 양념·고명이 다 들어 있어 기피 재료 1개가 20개 중 1개면 p_avoid 가 0.1 에 그친다. 의도가 아니면 분모를 essential_ids 로 바꿔야 하는데, **바꾼 사실이 config_hash 에 안 들어가면** 과거 점수가 영원히 재현되지 않는다.


## B-11 — scoring_config 등록 — config_hash 로 가중치를 되찾는다

> 🔴 **`base_weights` 에 17키를 전부 넣어라. `w=0` 인 것도.** (09-03 조사에서 발견)
>
> 이 표가 없으면 과거 점수를 재현할 수 없다. 그런데 지금은 **몇 개를 넣든 통과**한다 —
> `jsonb NOT NULL` 뿐이고 키 개수를 보는 것이 없다.
>
> 실측 — `f_coverage`·`f_taste` **2키만** 넣어도 등록이 성공한다. 그러면 빠진
> `f_expiring`(0.15)이 재현 코드에서 `.get(k, 0.0)` 으로 읽혀 **0 으로 조용히
> 재현**된다. 재현 점수는 그럴듯한 숫자로 나오는데 실제와 다르고, **해시는
> 단방향이라 원본과 대조할 수도 없다.**
>
> ⚠️ `tests/ddl_test.py` 가 지금 2키만 넣고 통과시키고 있다 — 그 테스트도 같이 고쳐라.
> 규약 위반 행을 테스트가 앞장서서 만들고 있는 셈이다.
>
> **막는 방법.** 등록 함수가 `FEATURE_KEYS` 전수를 확인하고, 빠진 키가 있으면
> 거부한다. `ScoredCandidate.features` 가 이미 같은 검사를 하므로
> (`pipeline.py` 의 17키 전수·미지키 금지) 그 방식을 그대로 가져오면 된다.

**기간** 0.5일

**무엇을**

app/services/recommends/config.py — config_hash(base_weights, penalty_spec, n_warm) -> str(32). 🔴 현재 mock_server._config_hash() 는 **weights 만** 해시한다 — penalty_spec 이나 n_warm 을 바꿔도 같은 해시가 나와 scoring_config 1행이 서로 다른 두 설정을 가리키게 된다. 세 입력을 모두 해시에 넣는다(5-2-2-1 이 '세 계수는 penalty_spec 에 담고 config_hash 에 포함시킨다'고 명시). 서버 기동 시 `INSERT ... ON CONFLICT (config_hash) DO NOTHING` 으로 1행 등록 — DO UPDATE 를 쓰면 안 된다(같은 해시는 정의상 같은 내용이어야 한다). base_weights 는 FEATURE_KEYS 17키 전수, w=0 도 명시(DDL 주석 요구). scoring_config 에 FK 가 없으므로 등록 실패가 로깅을 막지는 않는다 — 대신 아래 (b) 가 강제한다.

**완료 기준**

`make config-test` — (a) SELECT count(*) FROM jsonb_object_keys((SELECT base_weights FROM scoring_config LIMIT 1)) == 17 (b) SELECT count(*) FROM recommendation_log r LEFT JOIN scoring_config c USING (config_hash) WHERE c.config_hash IS NULL == 0 — 되찾을 수 없는 행이 0 (c) penalty_spec 의 avoid_cap 을 0.8→0.7 로 바꾸면 해시가 달라진다 (d) 같은 입력이 프로세스를 새로 띄워도 같은 해시를 낸다

**선행** B-10

**막히면** 해당 없음

> ⚠️ **조용히 틀리는 방식**
>
> 해시 입력이 dict 순회 순서나 str(float) 에 의존하면 같은 설정이 두 해시로 갈라져 레지스트리에 쌍둥이 행이 생긴다 — mock 의 sorted(weights) + f'{v:.6f}' 패턴을 유지하면 막힌다. 반대 방향이 훨씬 나쁘다: penalty 계수를 바꿨는데 해시가 그대로면, 나중에 과거 요청을 **새 계수로 '재현'** 해 틀린 답을 자신 있게 내놓는다. 아무것도 실패하지 않고 숫자만 다르다. 세 번째: ON CONFLICT DO UPDATE 로 쓰면 같은 해시의 내용이 조용히 덮여 그 해시를 참조하는 **과거 로그 전체**의 근거가 소급으로 바뀐다.


## B-12 — 이유 생성(z-salience) 연결 — 남은 것은 ctx 하나다

**기간** 0.5일

**무엇을**

reason.py 의 REASON_TEMPLATES · build_reason · 조사 처리와 pipeline.py 의 salience/top_reasons/SIGMA_FLOOR 는 **이미 완성돼 있고 make contract 가 검증한다.** 남은 것은 템플릿이 요구하는 ctx 값을 실제 DB 에서 만드는 것뿐이다 — mock 의 _CTX 는 요청과 무관한 하드코딩이다('두부','애호박','매운맛','김치볶음밥'). 후보별 ctx 를 만든다: expiring_name/expiring_days(effective_expiry 결과 ∩ all_ids 중 최소 days_left) · missing_name(missing_ids[0] → ingredient.name) · pref_ing(user_ingredient_pref 최고점 ∩ all_ids) · taste_axis(6축 중 유저 최대축 한국어명) · similar_title(f_cooccur 최댓값을 준 레시피 제목) · pantry_used · **cook_minutes**. cuisine·dish_type 은 데이터가 없어 자동 스킵된다.
🔴 실측 결함: REASON_TEMPLATES['f_time_fit'] 이 {cook_minutes} 를 요구하는데 mock _CTX 에 그 키가 **없다** → _fill() 이 KeyError 를 삼켜 None 을 돌려주므로 f_time_fit 이유는 지금까지 단 한 번도 렌더된 적이 없다.

**완료 기준**

`make reason-test` — demo 12명 × top_k 20 = 240 아이템에서 (a) reason 이 폴백 문구('추천 목록에 포함됐어요')인 비율 < 10% (b) reason_features 최빈 피처의 점유율 < 60% — 문서가 기록한 붕괴(contrib argmax 100%, salience 85%) 재현 방지 (c) 출력 문자열에 '{' 또는 '}' 가 0건 (d) f_time_fit 이 top_reasons 상위 2 에 든 아이템에서 '분이면 완성' 문구가 실제로 나온다 = cook_minutes 키가 살아 있다 (e) make contract 의 조사·템플릿 검사 3종 계속 통과

**선행** B-4(cook_minutes) · B-5(expiring_name) · B-7(pref_ing·similar_title)

**막히면** 해당 피처가 아직 없으면 그 ctx 키만 빠지고 그 이유가 자동 스킵된다 — 나머지 이유로 문장은 나온다. (a) 의 폴백 비율만 일시적으로 올라가므로 임계값을 완화해 두고 피처가 붙을 때마다 조인다.

> ⚠️ **조용히 틀리는 방식**
>
> _fill() 이 KeyError·IndexError 를 삼키고 None 을 돌려주는 설계라, ctx 키를 하나 빠뜨리면 그 이유가 **조용히 사라지고 로그에도 흔적이 없다.** 지금 f_time_fit 이 정확히 그 상태이고 make contract 98건이 전부 통과한다. 두 번째: 요청당 ctx 하나를 20개 아이템이 공유하면(mock 이 그렇다) 모든 카드에 같은 재료명이 박혀 **그럴듯하게 전부 틀린다** — 유저는 '두부'가 실제로 그 레시피에 있는지 확인할 방법이 없고 우리도 로그로는 못 잡는다. 세 번째: 후보 집합이 1건이면 σ 가 SIGMA_FLOOR 로 떨어지고 salience 가 전부 0 이 되어 이유가 사전순으로 뽑힌다.


## B-13 — 🔴 스코어러 ↔ 라이터 교체 — 난수 제거

**기간** 1일

**무엇을**

reco/api/serve.py 로 실서빙 경로를 만든다: ① app.db.retrieve() → ② load_features + score_candidates + penalty → 점수 내림차순 정렬 → ③ feature_stats + top_reasons + build_reason + mixed_exploration → write_recommendation. mock_server.recommend() 를 이 경로로 갈아끼우고 **rng.random() 으로 feats 를 채우는 for 문과 _RECIPES 하드코딩 8건을 삭제한다.** _trace() 의 하드코딩 filters(no_overlap:227999 · allergy_cut:12 · missing_gt_k:1502)와 score_stats(p50:0.41 등)도 지우고 실측으로 채운다 — **못 세는 값은 넣지 않는다.** serving_mode='real', REQUIRED_TRACE_PARAMS 10종 전수.

**완료 기준**

`make serve-smoke` 확장 — (a) 같은 요청 2회의 items 순서·score 가 완전히 동일(난수 제거 확인) (b) SELECT count(*) FROM recommendation_log WHERE request_params ? 'log_degraded' == 0 (c) stage_trace 의 ranking.score_stats.p50 이 응답 items 점수의 중앙값과 1e-6 이내 일치 (d) SELECT jsonb_array_length(candidates) FROM recommendation_log ORDER BY created_at DESC LIMIT 1 >= min(50, 후보수) (e) `git grep -c 'rng.random()' app/main.py` 가 0

**선행** B-9, B-10, B-12

**막히면** 해당 없음

> ⚠️ **조용히 틀리는 방식**
>
> 가장 위험한 것은 _trace() 의 하드코딩 숫자를 안 지우는 것이다 — no_overlap: 227999 는 레시피가 46,353건인 지금 **나올 수 없는 값**인데 C 트랙 대시보드에 실측처럼 그려지고, 에러도 없고 그럴듯해서 아무도 의심하지 않는다. 두 번째: mock 경로를 지우지 않고 새 경로를 추가만 하면 C 가 어느 쪽을 보는지 모른 채 난수 화면을 계속 붙든다. 세 번째: score 를 새로 계산하면서 mock 의 base 값을 어딘가에 남겨두면 items 순서는 새 점수인데 score 필드는 옛 값이 되어, 로그의 features 와 score 가 서로 모순된 상태로 쌓인다 — 라벨을 만들 때에야 드러나고 그때는 그 기간 로그를 통째로 버려야 한다.


## B-14 — 폴백 — 추천은 절대 실패하지 않는다

**기간** 0.5일

**무엇을**

후보 0건일 때(recipe_feature 미충전 · 빈 냉장고 · staple 만 보유) max_missing 완화 사다리 요청값 → +1 → +2 를 타고, 그래도 0 이면 popularity_score 상위 N 을 돌려준다. totals.degraded=True 로 표시하고, **실제로 쓴 값**을 max_missing_final 에 싣는다(REQUIRED_TRACE_PARAMS). counters.bump('degraded').

**완료 기준**

`make fallback-test` — (a) 냉장고 0건 유저 요청이 200 이고 len(items)==top_k (b) trace.totals.degraded == true (c) SELECT stage_trace->'stages'->2->'params'->>'max_missing_final' 이 요청값보다 크다 (d) recommendation_log 1행이 그대로 남는다 (e) recipe_feature 를 0행으로 비운 상태에서도 500 이 안 나고 로그가 남는다

**선행** B-13

**막히면** 해당 없음

> ⚠️ **조용히 틀리는 방식**
>
> 폴백이 잘 동작할수록 **recipe_feature 가 0행이어도 화면이 정상으로 보인다** — 지금 상태가 정확히 그렇게 감춰진다. degraded 비율은 stage_trace JSONB 안에 있어서 패널을 안 만들면 아무도 안 본다(TraceTotals 주석이 '이 비율이 조용히 오르는 것이 가장 위험한 실패 양상'이라고 적어둔 그것). 두 번째: max_missing 을 완화한 채 max_missing_final 을 안 갱신하면 로그상 2 인데 실제는 4 라, 나중에 같은 조건으로 재현하면 다른 결과가 나오고 원인을 못 찾는다.


## B-17 — 온보딩 저장을 DB 로

**기간** 0.5일

**무엇을**

계약(`OnboardingIn`·`OnboardingOut`)과 mock 엔드포인트 `POST /v1/onboarding/{user_id}` 는
**09-02 에 만들어 뒀다.** 남은 것은 mock 저장소(`_ONBOARDING` dict)를 DB 로 바꾸는 것.

```
user_vector       taste_vec · onboarding_picks · onboarding_scales · computed_from
user_preference   spicy/salty/sweet_level · household_size
user_allergy      🔴 severity='allergy' 를 **명시**한다
user_ingredient_pref  기피 재료 score=-0.8 · source='onboarding'
```

**완료 기준**

`POST /v1/onboarding/1` 후 psql — 네 테이블에 행이 생기고,
`SELECT severity FROM user_allergy WHERE user_id=1` 이 전부 `'allergy'`.
그리고 `expand_user_allergens(1)` 이 **그룹 확산까지** 포함한 배열을 돌려준다.

**선행** B-0

> ⚠️ **조용히 틀리는 방식**
>
> `severity` 를 생략하면 DB 기본값 `'avoid'` 가 들어간다. 그러면 직접 지정한
> 재료는 막히는데 **같은 알러젠 그룹으로 확산되지 않는다** — 아몬드만 등록한
> 사람에게 호두·잣이 그대로 추천된다. 에러가 안 나고 본인이 적은 재료는 잘
> 걸러지므로 **겉보기엔 정상이다.**

## B-18 — 팬트리 쓰기를 DB 로

**기간** 0.5일

**무엇을**

`PUT /v1/users/{id}/pantry` 가 지금 메모리 dict 에만 쓴다.
`f_expiring`(w=0.15)이 `expires_at` 을 읽는데 **저장 경로가 없다.**

🔴 `removed` 는 **행 삭제가 아니라 tombstone** 이다 — `removed_at`·`removed_reason`.
행을 지우면 "무엇을 얼마나 버렸나" 를 영원히 못 센다.

**완료 기준**

`PUT` 후 `SELECT count(*) FROM pantry_item WHERE user_id=1 AND removed_at IS NULL`
이 보낸 개수와 같고, `removed` 로 보낸 것은 `removed_reason` 이 채워진 채 남는다.
그리고 `user_pantry_ids(1)` 이 제거분을 빼고 돌려준다.

**선행** B-0

> ⚠️ **조용히 틀리는 방식**
>
> `expires_at` 을 안 받거나 NULL 로 두면 `f_expiring` 이 **항상 0** 이 된다.
> 가중치 0.15 가 통째로 죽는데 점수는 정상 범위로 나온다.
> `expires_at_source` 로 유저 입력인지 시스템 추정인지 구분해야 한다.

## B-16 — 🔴 백엔드 인터페이스 확정 · 전달

**기간** 0.25일

**무엇을**

**AI 파트는 프론트와 직접 말하지 않는다.** 백엔드가 유일한 호출자다
(기능명세 초안 `COMMON-005` "백엔드에서만 호출"). 주고받는 것을 문서로 확정해
백엔드 담당자에게 넘긴다.

```
① BE → AI   추천 요청     user_id · 🔴 session_id(c-/g-/d-) · top_k · max_missing · max_minutes
② AI → BE   추천 20건     레시피 · 부족재료 · 이유 · 🔴 request_id · position
③ BE → AI   행동 이벤트   🔴 request_id · position 을 그대로 되돌려 실어야 한다
```

**완료 기준**

`docs/draft/18_기능명세_AI파트.tsv` 의 `COMMON-021`·`COMMON-022` 행이
위 세 줄을 담고 있고, 백엔드 담당자가 **읽었다고 회신**한 기록이 있다.

**선행** B-1, B-2

**막히면** 회신이 늦어도 우리 쪽 구현은 진행한다. 다만 ③ 이 안 지켜지면
학습이 불가능하므로 **늦어도 유저 모집 전에는 확인**해야 한다.

> ⚠️ **조용히 틀리는 방식**
>
> 백엔드가 `request_id` 를 안 실어 보내도 `/v1/events` 는 **200 을 돌려준다** —
> 계약상 선택 필드이기 때문이다. 🔴 **응답은 200 이지만 `rejected` 로 집계되고
> 그 행은 저장되지 않는다** — 상태코드만 보고 성공 처리하면 클릭·조리 이벤트가
> 통째로 사라진다 (05 '🔴 소급 불가 필드').
> 그런데 추천과 이을 수 없어서 **"3번째로 보여준 그것을 눌렀다"가 복원되지 않는다.**
> 클릭 기록만 남으면 우리가 위에 올린 것이 인기 있어서 눌린 것처럼 보이고,
> 그 데이터로 학습한 모델은 자기 편향을 재생산한다. **몇 주 뒤에야 드러난다.**

## B-15 — 종단 검증 + A 산출물 교체 리허설

**기간** 0.5일

**무엇을**

A 가 recipe_feature 를 채우면 개발용 합성 픽스처를 끄고 같은 명령을 전부 다시 돌린다. app/services/eval/feature_report.py 를 만들어 피처별 (min, p50, max, None 비율)과 레시피 매칭률 = n_total/(n_total+n_unmatched) 분포를 덤프하고 C 의 평가 하네스에 넘긴다.

**완료 기준**

한 줄: `make validate contract normalize-test ddl-test smoke smoke-py log-test feature-test scorer-test config-test reason-test serve-smoke fallback-test` 가 전부 통과하고, SELECT count(*) FROM recommendation_log > 100 · SELECT count(DISTINCT config_hash) FROM recommendation_log == 1 · 피처 리포트에서 w>0 인 10종 중 None 비율 100% 인 것이 `f_cuisine`·`f_season` 둘뿐이다(= `PENDING_DATA_FEATURES`)

**선행** A — recipe_feature 충전

**막히면** 합성 픽스처 위에서 전 명령을 먼저 통과시켜 둔다. A 데이터가 오면 픽스처만 끄고 재실행하며 코드 변경은 0줄이다.

> ⚠️ **조용히 틀리는 방식**
>
> 합성 픽스처에서 통과한 검증이 실데이터에서 조용히 무의미해진다. 픽스처는 재료 5~10개짜리 깨끗한 레시피지만 실데이터는 n_unmatched 가 큰 레시피가 섞이고, 02_schema.sql 이 경고하듯 `n_total > 0` 게이트는 **전멸만 잡고 12개 중 11개를 놓쳐도 통과한다.** 그러면 f_coverage 가 1.0 인데 실제로는 재료 대부분이 안 붙은 레시피가 상위를 채우고, 모든 지표가 정상으로 보인다. 매칭률을 피처 리포트에 반드시 넣어야 이걸 본다.
