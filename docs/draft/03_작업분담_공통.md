# AI 파트 3인 병렬 — 공통 규칙

> 작성자: 박재우 · 작성일: 2026-09-03

> 8주 중 **5주차** · 남은 3주(영업일 15일) · 트랙당 8.5~10.5일

| 트랙 | 담당 | 목표 | 자기 폴더 |
|---|---|---|---|
| **A** 데이터 | | 레시피 특성 46,353건을 채워 **실데이터 추천을 가능하게** · **8.5일** | `app/services/normalize/` |
| **B** 엔진 | | mock 난수를 **실제 점수로** 바꾸고 기록이 쌓이게 · **10.5일** | `app/services/recommends/` `app/db/` |
| **C** 관측 | | 화면 4종 + 평가 도구 · **10.5일** | `infra/app/` `app/services/eval/` |

각자 문서 — [A](07_작업분담_A_데이터.md) · [B](09_작업분담_B_엔진.md) · [C](11_작업분담_C_관측.md)

> 🔴 **먼저 [결정사항](05_작업분담_결정사항.md) 을 읽는다.** 트랙 간에 부딪힐 것을
> 미리 정해 뒀다 — 소유권 · 데이터 형식 · 순서. **거기 있는 것은 각자 정하지 않는다.**

---

# 🔴 "절대 바꾸면 안 되는 것"은 무엇인가

**스키마가 아니다.** 스키마는 바꿀 수 있다 — 비용이 조율일 뿐 데이터 손실이 아니다.
진짜로 못 되돌리는 것은 따로 있고, **지금은 대부분 아직 잠기지 않았다.**

## 층 1 — 지금 바꿔도 공짜다

실측(2026-09-02): 아래가 전부 **0행**이다.

```
추천 기록      0
사용자 행동    0
실유저         0
온보딩 응답    0
```

그래서 **지금은 로그 규약도 온보딩 문항도 가중치도 자유롭게 바꿀 수 있다.**
"S0에서 동결했다"는 것은 *결정을 내렸다*는 뜻이지 아직 데이터가 묶였다는 뜻이 아니다.

## 층 2 — 바꾸면 조율 비용 (손실은 아니다)

| 무엇 | 비용 | 규칙 |
|---|---|---|
| **DB 스키마** | 재초기화 + 재적재 **약 1분**. 전원이 다시 받아야 한다 | 혼자 하지 말고 셋이 모여서 |
| **`app/schemas/` 계약** | 세 트랙이 동시에 영향받는다 | 고치면 `make contract` 98건 확인 |
| **`Makefile`** | 충돌 나기 쉽다 | 자기 타깃만 추가 |
| **`app/main.py`** | B가 소유. C가 읽는다 | B만 고친다 |

> 스키마 변경이 무서운 이유는 "되돌릴 수 없어서"가 아니라
> **다른 두 사람의 작업 중이던 결과가 날아가서**다. 시간을 맞춰서 하면 된다.

## 층 3 — 곧 잠긴다. 그 시점 이후로 소급 불가

| 무엇 | 언제 잠기나 | 잠긴 뒤 바꾸면 |
|---|---|---|
| **로그 규약** (propensity 의미·동결 키 10종) | **B-1 연결하는 순간** | 그 전후 데이터를 같이 못 쓴다 |
| **노출 기록 방식** (`source='served'`) | **B-1 이후** | 두 시대가 섞인다 |
| **이벤트 8종** | B-2 이후 | 추가는 되지만 **변경·삭제 불가** |
| **온보딩 문항** | **유저 모집 시작** | 두 집단이 갈려 학습에 같이 못 쓴다 |
| **맛 척도 3축** | 〃 | 〃 |

> 🔑 **B-1 전에 로그 규약을 다시 볼 마지막 기회다.** 지금 보면 공짜고,
> 연결한 뒤엔 그때까지 쌓인 것을 버려야 한다.

## 이미 잠긴 것 — 하나

**`REVIEW_SALT`** (`.env`). 후기 624,422건의 작성자 해시가 이 값으로 만들어졌다.
바꾸면 해시가 전부 달라져 **"같은 사람이 쓴 후기"** 판정이 과거 분석과 어긋난다.

> ⚠️ **절대 재생성하지 않는다.** `.env` 를 지우거나 새로 만들지 말 것.
> 재적재(24초)를 해도 salt 만 같으면 해시가 그대로 재현된다.

---

# 🔴 백엔드와의 인터페이스 — 세 트랙 전부 영향받는다

**AI 파트는 프론트와 직접 말하지 않는다.** 백엔드가 유일한 호출자다
(기능명세 초안 `COMMON-005` "백엔드에서만 호출").

```
① BE → AI   추천 요청     user_id · 🔴 session_id(c-/g-/d-) · top_k · max_missing · max_minutes
② AI → BE   추천 20건     레시피 · 부족재료 · 이유 · 🔴 request_id · position
③ BE → AI   행동 이벤트   🔴 request_id · position 을 그대로 되돌려 실어야 한다
```

> 🔴 **③ 이 안 지켜지면 학습이 성립하지 않는다.** 백엔드가 `request_id` 를
> 빼먹어도 `/v1/events` 는 **200 을 돌려준다** — 계약상 선택 필드다.
> 🔴 **응답은 200 이지만 `rejected` 로 집계되고 그 행은 저장되지 않는다.**
> 상태코드만 보고 성공 처리하면 그 이벤트는 사라진다. 추천과 이을 수 없어
> **"3번째로 보여준 그것을 눌렀다"가 복원되지 않는다.** 클릭 기록만 남으면
> 우리가 위에 올린 것이 인기 있어서 눌린 것처럼 보이고, 그 데이터로 학습한
> 모델은 자기 편향을 재생산한다. **몇 주 뒤에야 드러난다.**

담당은 **B-16**. 다만 C 는 화면에서 이 연결이 끊긴 요청을 볼 수 있어야 하고
(고아 이벤트 비율), A 는 영향이 없다.

# 첫날 규칙 다섯

**① 스키마를 바꾸지 않는다.** 필요한 테이블 5종이 **이미 전부 있다** —
`batch_run` · `normalization_queue` · `data_quality_snapshot` · `feature_stats` ·
`scoring_config`. 3주 계획에 DDL 변경이 없다.

**② 대량 배치 전에 알린다.** A의 정규화는 15분간 35만 행을 쓴다.
그동안 B·C가 응답시간을 재면 숫자가 틀린다.

**③ 공용 파일은 말하고 고친다.** 위 층 2 표를 볼 것.
문서의 수치를 고쳤으면 `make doc-check` 도 같이 돌린다.

**④ 로더·배치는 몇 번 돌려도 안전하다 — 단 조건이 있다.** 09-02 에 멱등성 제약을
넣어서 `scripts.load_recipes` 를 두 번 돌려도 행 수가 그대로다(실증). 그전에는 재료 원문이
**전량 2배**가 됐고 에러도 안 났다.
🔴 **`.env` 의 `REVIEW_SALT` 를 지우거나 새로 만들면 그 보장이 깨진다** —
후기 작성자 해시가 전부 달라져 재적재 때 중복으로 쌓인다.

**⑤ 완료는 명령으로 증명한다.** 이 프로젝트는 **에러 없이 조용히 틀린 적이 열한 번**
있다 — 후기 60만 건 유실 · 커버리지 게이트 무력화 · propensity 미저장 ·
재료 원문 2배 적재 · 묘비가 정본 차단 · 커넥션 풀 1개 캐시 · 사전 로딩 사망 등.
**전부 화면상으론 정상이었다.** 눈으로 본 것은 완료가 아니다.

---

# 환경 준비 — 각자 자기 트랙만 깐다

`pyproject.toml` + `uv.lock` 이 정본이다 (옛 `db/requirements.txt`·`reco/requirements.txt`
2벌은 09-02 에 폐지). uv 를 못 쓰는 환경을 위해 `requirements/*.txt` 사본을 함께 둔다 —
사본은 `make requirements` 로만 만든다.

```bash
brew install uv                     # 없으면
brew install --cask orbstack        # 없으면 (Docker 대체)
./setup.sh --track A                # 자기 트랙: A 데이터 · B 엔진 · C 관측
```

`setup.sh` 가 `.venv` 생성 → 의존성 → 컨테이너 → 시드 → 검증까지 한다.
**여러 번 돌려도 안전하다** — 이미 있는 것은 건드리지 않고, 지우는 명령은
하나도 부르지 않는다. 진단만 하려면 `./setup.sh --check`.

손으로 하려면 아래와 같다(스크립트가 하는 일과 같다):

```bash
uv venv
make install TRACK=A
make bootstrap
```

| 트랙 | 추가로 깔리는 것 |
|---|---|
| **A** 데이터 | `ml` — numpy · scikit-learn |
| **B** 엔진 | `api` — fastapi · uvicorn  ＋ `ml` |
| **C** 관측 | `dash` — streamlit  ＋ `ml` ＋ `obs` — mlflow |

공통(전 트랙): pydantic · pyyaml · psycopg2-binary ／ dev: httpx2

> 🔴 **`uv sync` 를 맨손으로 치지 마세요.** 락에 없는 패키지를 **지웁니다.**
> 09-02 에 실제로 fastapi·numpy 가 사라져 mock 서버가 죽었다.
> 반드시 `make install TRACK=?` 로 하세요 — TRACK 없이 실행하면
> 아무것도 설치하지 않고 안내만 나옵니다.

> ⬜ **`lightgbm` 은 아직 안 깐다.** 랭킹 v1 용이고 W6 말 컷라인(누적 그룹 ≥ 500)을
> 통과해야 시작한다. 그때 `make install TRACK=B EXTRA=rank-v1` 로 추가한다.
> 🔴 EXTRA 는 **그때 한 번만이 아니다.** 다음번 `make install` 에서 빼먹으면
> `uv sync` 가 lightgbm 을 도로 지운다 — 얹었으면 계속 붙이세요.

> ⬜ **`sentence-transformers`(embed) 는 이번 3주에 안 깐다.** A-12 가 임베딩 없는
> TF-IDF→SVD 판으로 확정돼서 부를 코드가 없고, torch 2GB 를 끌고 온다.
> 나중에 content_emb 판으로 갈 때 `EXTRA=embed` 로 얹는다.

**uv 가 없는 환경(순정 pip · 코랩 · 채점 서버)** 이면 사본을 쓴다:

```bash
pip install -r requirements/B.txt     # base A B C all 중 자기 트랙
```

> 🔴 `requirements/*.txt` 를 **손으로 고치지 마세요.** `pyproject.toml` 을 고치고
> `make requirements` 로 다시 뽑습니다. 손으로 고치면 락과 조용히 갈라지고,
> 갈라진 걸 아무도 모릅니다. `make requirements-check` 가 그걸 잡습니다.

# 문서에 검증 건수를 적을 때

`make doc-check` 가 `docs/` 아래 모든 `.md` 를 훑어서 **문서가 적은 건수 vs 실제로
돌려본 건수**를 대조한다. 검사기가 읽을 수 있게 두 모양으로만 쓴다.

| 무엇 | 어떻게 쓰나 |
|---|---|
| **전체** 건수 | 명령이 먼저, 수가 뒤 — `` `make contract` 98건 `` · `` `make ddl-test` **47건** `` |
| **부분** 건수 (그 항목이 더한 몫) | 수가 먼저, 명령이 뒤 — `` 8건 (`make contract` 중) `` |

옛 값을 일부러 남길 때는 취소선을 치거나 화살표를 쓴다 — `~~84~~ **98**` · `65건 → 98건`.
검사기가 그건 건너뛴다.

> 🔴 09-03 에 `contract` 가 84→98, `ddl-test` 가 41→47 로 늘면서 **8개 파일 21곳이
> 한꺼번에 낡았다.** 그때 이 검사가 파일 하나만 보고 있어서 하나도 못 잡았다.
> 지금은 전부 본다 — 건수를 적었으면 `make doc-check` 를 돌리고 커밋하세요.

# 첫날 착수 — 셋이 동시에 시작한다

서로를 기다리는 첫 작업이 없다.

| 트랙 | 첫 작업 | 왜 지금 되나 |
|---|---|---|
| A | **A-2** 배치 러너 | A-1(사전 로딩 버그)은 **이미 고쳐 뒀다** |
| B | **B-0 → B-1** 데모 유저 + mock 로그 연결 | 의존 없음 |
| C | **C-1** 개발환경 · **C-2** 평가 골든 | compose 선언은 있고 파일만 없다 |

> 🔑 **B-1이 가장 급하다.** 지금 추천 기록이 0행이라 **C가 화면에 띄울 데이터가
> 없다.** B-1(반나절)이 끝나야 C가 실데이터로 붙는다.

---

# 지금 돌아가는 검증 명령

각자 자기 것을 고친 뒤 **전부** 돌려서 남의 것을 깨지 않았는지 본다.

```
make validate         시드 정합성
make contract         계약 98건
make normalize-test   정규화 113건 (74+23+16)
make ddl-test         스키마 47건
make smoke            Retrieval 13건
make smoke-py         파이썬 경로 19건
make log-test         로그 라이터 48건
make doc-check        🔴 문서 수치·단언이 DB·코드와 맞는가 94건
```

> 🔑 **`make doc-check` 가 09-02 에 새로 생겼다.** 문서는 조용히 낡는다 —
> 코드는 테스트가 깨져서 알려주는데 문서는 아무 일도 안 일어난다.
> 실제로 `00` 한 문서에만 14건이 나왔고 절반이 같은 날 고치며 새로 만든 오류였다.
> **문서에 수치를 쓰면 여기에 등록**한다.

---

# 09-02 에 바뀐 것 — 작업 시작 전에 읽을 것

| | 무엇 | 트랙 영향 |
|---|---|---|
| 🔴 | **`Dictionary.from_db()` 가 죽어 있어 고쳤다** | A-1 은 이미 끝났다. **A-2 부터 시작** |
| 🔴 | 계약 테스트가 **계산 불가 피처 0.06 을 통과**시키고 있었다 | B — `PENDING_DATA_FEATURES` 신설. `f_cuisine`·`f_season` 은 데이터 대기 |
| | `ACTIVE_WEIGHT_TODAY = 0.94` 상수 추가 | B — 문서에 쓰는 가중치는 이 값 |
| | `make doc-check` (94건) | 전원 — 문서 수치를 고치면 같이 돌린다 |
| | `01` v3.0 · `00` 실측 반영 | 전원 — 이제 두 문서 다 공유 가능 |

# 지금 상태 요약

```
레시피          46,353건      재료 원문   451,862
조리 순서      362,677단계    후기        624,422
재료 사전          536종      커버리지      80.8%

정규화 결과          0행  ← A가 채운다
레시피 특성          0행  ← A가 채운다
추천 기록            0행  ← B가 흐르게 한다
화면                0줄  ← C가 만든다
```
