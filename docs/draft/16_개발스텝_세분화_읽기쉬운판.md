# 개발 스텝 (세분화) — AI 파트

> 작성자: 박재우 · 작성일: 2026-09-03

우리는 **냉장고에 있는 재료로 만들 수 있는 요리**를 추천하는 시스템을 만들고 있습니다.
그중 AI 부분을 세 사람(A 데이터 · B 엔진 · C 화면·평가)이 나눠서 진행합니다.

전체 그림이 *무엇부터 하나*를 정한다면, 이 문서는 **하루 단위로 무엇을 하나**를 정합니다.
AI 파트 내부용 작업 지시라 파일 이름과 명령을 그대로 적어 뒀습니다.

> **다 됐다는 증거는 전부 "명령 하나로 확인된다"로 잡았습니다.**
> 확인할 수 없는 완료는 완료가 아닙니다 — 이 프로젝트에서 **에러 없이 조용히 틀린 적이
> 열한 번** 있었습니다(후기 60만 건이 사라짐 · 재료 원문이 두 배로 쌓임 ·
> 접속을 1개만 재사용 · 사전 로딩이 죽음 등). **전부 화면상으로는 정상이었습니다.**

표시 — ✅ 완료 · 🔨 진행 중 · ⬜ 대기 · 🔴 늦으면 그 기간 데이터를 잃는다

담당과 소유 폴더 — 겹치지 않게 나눠 두었습니다.

| | 맡는 단계 | 소유 폴더 |
|---|---|---|
| **A** 데이터 | S6 · S7 | `app/services/normalize/` |
| **B** 엔진 | S3 | `app/services/recommends/` · `app/db/` |
| **C** 화면·평가 | S4 · S5 | `infra/app/` · `app/services/eval/` |

> 🔴 **S3 의 하위 번호는 다시 매기지 않습니다.** 다른 문서들이 이미 이 번호로 특정 스텝을
> 가리키고 있습니다. 09-03 에 빠져 있던 항목 2종을 넣을 때도 **뒤에 붙였습니다**(S3-17 · S3-18).

---

## S0 — 규약 못박기  ✅ 2026-09-01~02

나중에 바꾸면 그때까지 모은 데이터를 버려야 하는 것들입니다.

| 스텝 | 내용 | 다 됐다는 증거 | 상태 |
|---|---|---|---|
| S0-1 | 🔴 기록 규약 — 노출 확률의 의미 · 바꾸지 않을 항목 10가지 · **보여준 것은 반드시 후보 안에 있을 것** | `make contract` | ✅ |
| S0-2 | 🔴 가입 설문 문항 — 알레르기(계열 이름) · 맛 3축 · 기피 재료 · 가구원 수 | 결정 기록 + `make ddl-test` (응답 원본이 그대로 왕복) | ✅ |
| S0-3 | 🔴 개인정보 동의 문구 초안 | 팀이 파기일·연락처를 채운다 | ✅ |
| S0-4 | 🔴 후기 수집 규약 — 닉네임을 되돌릴 수 없게 변환 | `make ddl-test` | ✅ |
| S0-5 | 🔴 다 썼는지 버렸는지 구분 — `removed_reason` 3값 | `make contract` | ✅ |

> **"못박았다"는 결정을 내렸다는 뜻이지 데이터가 묶였다는 뜻이 아닙니다.**
> 09-02 실측으로 추천 기록·행동·실사용자·설문 응답이 **전부 0행**입니다 — 지금 고치면 공짜입니다.
> **진짜로 잠기는 시점은 기록 개통(S3-15)과 사용자 모집(S8-3)** 입니다.
> 이미 잠긴 것은 `.env` 의 `REVIEW_SALT` 하나뿐입니다 — 후기 624,422건의 작성자 정보가
> 이 값으로 변환돼 있습니다.

**노출 확률**이란, 추천 목록의 그 자리에 그 후보가 뽑힐 확률입니다. 나중에 "위에 있어서 눌린 것"과
"좋아서 눌린 것"을 갈라내는 데 씁니다. 이 문서에서 계속 나옵니다.

---

## S1 — DB 읽기 계층  ✅ 2026-09-02  (실제로 반나절)

| 스텝 | 내용 | 다 됐다는 증거 | 상태 |
|---|---|---|---|
| S1-1 | `app/db/pool.py` — 접속을 모아 두고 재사용 | 20회 호출해도 접속이 늘지 않는다 | ✅ |
| S1-2 | `app/db/retrieval.py` — `retrieve_for_user()` 가 후보 목록을 돌려준다 | `make smoke-py` | ✅ |
| S1-3 | 조회 요청 형식 신설 | `make contract` — 경계값이 거부된다 | ✅ |
| S1-4 | 기존 검사를 파이썬 경로로 다시 통과 | `make smoke-py` | ✅ |
| S1-5 | 🔴 `p_include_test` 인자 — 시험용으로 만든 레시피 특성을 조회에서 제외 *(09-03)* | `make ddl-test` | ✅ |

> 🔴 **S1-5 로 `retrieve_candidates` · `retrieve_for_user` 의 인자가 바뀌었습니다.**
> PostgreSQL 은 인자가 다르면 **둘 다 다른 함수로 남겨 둡니다** — 옛 정의가 살아 있으면
> 호출이 조용히 그쪽으로 갈 수 있습니다. 그래서 새 정의 앞에 `DROP FUNCTION IF EXISTS` 를
> 붙였습니다. `make doc-check` 가 "`retrieve_for_user` 가 한 벌만 있는가"를 검사합니다.

---

## S2 — 기록 쓰는 길  ✅ 2026-09-02  (실제로 반나절)

| 스텝 | 내용 | 다 됐다는 증거 | 상태 |
|---|---|---|---|
| S2-1 | 🔴 `event_log.source` 신설 — 노출 기록의 출처를 남긴다 | `make ddl-test` — 두 방식이 함께 있어도 섞이지 않는다 | ✅ |
| S2-2 | 🔴 `merge_served_detail()` — 노출 확률이 저장되는 길 | `make contract` | ✅ |
| S2-3 | `app/core/writer.py` — 요약 1행 + 상세 N행을, 한 번에 묶어서 | `make log-test` | ✅ |
| S2-4 | `app/core/counters.py` — 실패를 삼키되 **센다** | 실패 횟수가 카운터에 잡힌다 | ✅ |
| S2-5 | 삭제 표시가 진짜 기록을 막지 않게 — 재시도해도 정본이 살아남는다 | `make log-test` | ✅ |
| S2-6 | mock 결함 3건 수정 (정렬 · 같은 입력에 같은 결과 · 저하 처리) | 점수 역전 0건 | ✅ |
| S2-7 | 🔴 탐색용 자리를 **실제로 꽂는다** *(09-02)* | `make contract` — 탐색 항목이 여러 위치에 퍼진다 | ✅ |
| S2-8 | 🔴 세션 번호 앞에 `d-` 추가 — 개발·디버깅·시딩 | `make ddl-test` · 실사용자 집계가 `d-` 와 `mock-` 을 거른다 | ✅ |

> 🔴 **S2-7 은 계산해 놓고 안 쓰던 결과였습니다.** 탐색 자리를 뽑는 함수의 결과를 버려서,
> 탐색 항목이 **점수 순서 그대로** 남았습니다 — 즉 늘 비슷한 자리에 왔습니다. 그러면 위치별
> 클릭률만 봐서는 "자리 때문에 눌린 것"과 "좋아서 눌린 것"을 갈라낼 수 없어, **자리 편향을
> 걷어내는 보정이 성립하지 않습니다.** 지금은 8개 위치에 고르게 퍼집니다.
>
> 🔴 **S2-8 은 나중에 고칠 수 없습니다.** 접두어 없이 쌓인 개발 트래픽은 나중에 실사용자와
> 갈라낼 방법이 없습니다. `c-` 실사용자 · `g-` 게스트 · `d-` 개발.

---

## S3 — 점수 항목 + 점수 계산기  ⬜ 다음  (4일 · B 담당)

> **지금 상태**: 가중치 표는 `common.py:100` 에 있는데 **계산 코드가 없습니다.**
> 소비기한 임박만 SQL 함수로 존재합니다(`04_functions.sql:245`).
> `app/services/recommends/` 에는 `explore.py` · `reason.py` · `serendipity.py` 만 있고
> **`scorer.py` 가 없습니다.** mock 은 난수를 채웁니다 — **지금 추천에는 개인화가 없습니다.**

> 🔴 **점수 항목은 전체 17종이고, 그중 비중이 0보다 큰 것이 10종입니다.**
> 아래 표가 오래도록 "함께 쓰는 재료"(10%)와 "제철"(2%)을 빠뜨려서 **비중 합이 88%** 였습니다.
> 09-03 에 S3-17 · S3-18 로 채웠습니다.
>
> | 구분 | 개수 | 비중 합 |
> |---|---|---|
> | 오늘 데이터로 계산 **가능**한 것 (= S3 에서 짤 것) | 8 | **94%** ← 다 짜면 이만큼으로 돈다 |
> | 방법은 있는데 데이터가 없는 것 (요리 계열 · 제철) | 2 | 6% |
> | 켜지 않는 것 (비중 0) | 4 | 0 |
> | 계산할 방법 자체가 없는 것 | 3 | 0 |
> | **합계** | **17** | **100%** (설계 의도) |

### S3-A. 조회가 이미 주는 것 — 여기부터 (비중 합 29%)

| 스텝 | 항목 | 비중 | 계산 근거 | 다 됐다는 증거 |
|---|---|---|---|---|
| S3-1 | 재료 충족도 | **24%** | 조회가 주는 충족도를 그대로 | 단위 테스트 |
| S3-2 | 부족 재료 수 | 5% | 부족 개수가 적을수록 높게 | 단위 테스트 |

> **재료 충족도 하나가 전체의 24% 입니다.** 여기까지만 해도 mock 난수보다 낫습니다.

### S3-B. 냉장고 + 기준값으로 되는 것 (비중 합 15%)

| 스텝 | 항목 | 비중 | 계산 근거 | 다 됐다는 증거 |
|---|---|---|---|---|
| S3-3 | 소비기한 임박 | **15%** | SQL 함수가 이미 있다 — 파이썬에 연결만 | 임박 재료가 있는 사용자에서 점수가 오른다 |
| S3-4 | 보유 재료 소진율 | 0% | 가진 재료를 얼마나 쓰나 | 켜지 않음(비교 실험용) |

> 🔴 **기준일이 09-03 에 바뀌었습니다.** `effective_expiry` 가 구매일을 먼저 보고, 없으면
> 앱에 넣은 날을 봅니다 — 즉 **구매일 기준**입니다. 구매일을 채워 주는 쪽이 **B-18**(냉장고
> 쓰기)이라, 그게 없으면 이 항목의 기준일 자체가 없습니다.
>
> 🔴 **DB 시간대가 `Asia/Seoul` 이어야 합니다.** UTC 면 한국 시간으로 자정~오전 9시 사이에
> 날짜가 **하루 전**이 되어 임박 판정이 어긋납니다. 컨테이너의 시간대 설정만으로는 부족합니다 —
> 밖에서 붙는 파이썬 접속에는 적용되지 않습니다. `01_extensions.sql` 의 `ALTER DATABASE` 가
> 정본이고, `make doc-check` 가 그 값을 검사합니다.

### S3-C. 가입 설문이 여는 것 (비중 합 27%)

> ⚠️ **설문 응답이 있어야 계산됩니다.** 사용자 모집(S8) 전에는 가상 데이터로만 확인합니다.
> 요리 계열(4%)은 여기가 아닙니다 — 설문이 아니라 **레시피 쪽 데이터**가 없어서 못 켭니다.
> 아래 S3-E 로 옮겼습니다.

| 스텝 | 항목 | 비중 | 계산 근거 | 다 됐다는 증거 |
|---|---|---|---|---|
| S3-5 | 맛 선호 | **16%** | 고른 레시피들의 맛 값 평균과, 각 레시피 맛 값이 얼마나 비슷한가 | 전체 평균을 빼서 기준을 맞춘 뒤 계산한다 |
| S3-6 | 선호·기피 재료 | 11% | 좋아하는/싫어하는 재료 | 기피 재료가 점수를 내린다 |
| S3-8 | 음식 유형 | 0% | — | 켜지 않음 |

### S3-D. 수집 + 재료 정리가 여는 것 (비중 합 23%)

| 스텝 | 항목 | 비중 | 계산 근거 | 다 됐다는 증거 |
|---|---|---|---|---|
| S3-9 | 인기도 | 10% | 후기 수를 순위 백분위로 (`row_number` + `recipe_id` 로 동점을 가른다) | 세 등분했을 때 한 칸이 비지 않는다 |
| S3-17 | 함께 쓰는 재료 | **10%** | 내 재료끼리 얼마나 자주 같이 쓰이나. 재료별 등장 횟수(`freq_count`)가 필요 | 🔴 **09-03 신설** — 표에 없었다 |
| S3-10 | 조리시간 적합 | 3% | 조리시간이 사용자가 낸 시간 안인가. **95.5% 만 값이 있다** | 값 없는 레시피가 버려지지 않는다 |
| S3-11 | 난이도 맞춤 | 0% | 난이도 5단계 | 켜지 않음 |
| S3-12 | 품질 | 0% | — | 켜지 않음 |

> 🔴 **인기도는 `percent_rank` 로 매기지 않습니다.** `percent_rank` 는 세 등분했을 때 한 칸이
> 비고 **8,470건이 동점**이 되어, 후보를 500개로 자르는 지점이 매번 달라집니다 — 그러면
> 노출 확률을 재현할 수 없습니다. `row_number` 와 `recipe_id` 로 동점을 가릅니다.
>
> 🔴 **S3-17 은 A 트랙이 재료별 등장 횟수를 채워야 계산됩니다.** 값이 없으면 0 으로 메우지
> 말고 "없음"으로 냅니다 — 0 으로 채우면 "계산했더니 0" 과 구별되지 않습니다.

### S3-E. 방법은 있는데 데이터가 없는 것 (비중 합 6% · 오늘은 계산 안 됨)

| 스텝 | 항목 | 비중 | 왜 못 켜나 | 다 됐다는 증거 |
|---|---|---|---|---|
| S3-7 | 요리 계열 | 4% | 레시피의 요리 유형이 **46,353건 전수 비어 있다.** 수집할 때 안 넘어왔고, 대신 있는 카테고리는 자유롭게 적은 태그 45,529종이다 | 태그를 유형으로 옮기는 작업(약 12시간)을 해 보고 판단 — B 담당 |
| S3-18 | 제철 | 2% | 제철 기준 데이터가 없다. 넣을 칸(`season_vec`)만 있다 | 🔴 **09-03 신설** — 표에 없었다 |

> **비중을 0 으로 내리지 않습니다.** 값이 없는 항목은 분자·분모에서 **함께** 빠지는 방식이라,
> 항상 값이 없는 항목은 계산에 아예 참여하지 않습니다 — 남은 항목끼리 비율을 다시 나눠 준 것과
> 수학적으로 같습니다. 그래서 **지금 나오는 점수는 이미 옳고**, 데이터가 들어오면 가중치를
> 다시 정하지 않고 그대로 켜집니다. 문서에 쓰는 오늘의 숫자는 **94%** 입니다.

### S3-F. 점수 계산기 본체

| 스텝 | 내용 | 다 됐다는 증거 |
|---|---|---|
| S3-13 | `app/services/recommends/scorer.py` — 비중을 곱해 더하되, **값이 있는 항목끼리만 나눈다** | 점수 = (값 있는 항목의 비중×값 합) ÷ (값 있는 항목의 비중 합) |
| S3-14 | `scoring_config` 등록 — 가중치 한 벌마다 지문을 붙인다 | 지문으로 그때 가중치를 되찾을 수 있다 |
| S3-15 | 🔴 기록 쓰는 코드를 실서빙에 연결 | `recommendation_log` 에 실제 행이 쌓인다 |
| S3-16 | 추천 이유 문구 연결 | 이유가 점수에 가장 크게 기여한 상위 2개 항목을 가리킨다 |

> 🔴 **S3-15 가 S2 의 실제 개통 지점입니다.** 지금은 기록 함수를 호출하는 프로덕션 코드가
> **0개**입니다 — mock 은 메모리에만 담습니다.

### S3-G. 저장 경로 — 가입 설문 · 냉장고  *(09-03 추가 · B-17 · B-18)*

> 🔴 **위의 27% 와 15% 를 넣을 곳이 없었습니다.** 주고받는 형식은 09-02 에 정해졌지만,
> mock 은 메모리에 담고 **DB 에 넣는 코드가 아직 없습니다.**

| 스텝 | 내용 | 다 됐다는 증거 |
|---|---|---|
| B-17 | 설문 저장 — `user_vector` · `user_ingredient_pref` · `user_allergy` | `make contract` 에서 설문 저장 호출이 성공한다 |
| B-18 | 냉장고 쓰기 — 구매일 · 다 씀/버림 표시 | 지운 재료의 사유가 남는다 |

- 🔴 **원본을 남깁니다** — 고른 레시피 번호와 답한 척도를 `user_vector` 에 그대로 저장합니다.
  취향 값은 고른 레시피들의 **평균**이라, 결과만 저장하면 기준 레시피의 맛 값을 고쳤을 때
  **다시 계산할 수 없습니다.** 실제로 09-02 에 맛 기준값 2건을 고쳤습니다
  (식용유 기름짐 0.60→1.00 · 멸치 3종 분리). 원본이 없으면 **사용자를 다시 모아야 합니다.**
- 🔴 **알레르기는 '차단' 등급으로 저장합니다.** DB 기본값인 '회피'에 맡기면 같은 계열로
  넓히는 처리가 **조용히 꺼져서** 본인이 적은 재료만 막힙니다 — 아몬드만 등록한 사람에게
  호두·잣이 그대로 추천됩니다. **에러가 안 나서 더 위험합니다.**
- 냉장고에서 지운 재료 정보를 **버리지 않습니다**(mock 도 09-03 부터 보관합니다).

---

## S4 — 평가 도구  ⬜  (2일 · **C 담당** · S3 와 동시 진행 가능)

| 스텝 | 내용 | 다 됐다는 증거 |
|---|---|---|
| S4-1 | 정답을 손으로 적어 둔 예제 만들기 | DB 없이도 돈다 |
| S4-2 | 🔴 순위 품질 점수 — **만점 기준을 상위 K개 안에서만 계산하지 않는다** | 편향 +0.182 를 재현하는 테스트가 있다 |
| S4-3 | 상위 K개 재현율 · 커버리지 · 다양성 | 손으로 적은 예제에서 기대값과 맞는다 |
| S4-4 | 자리 편향을 걷어내는 평가 — 노출 확률로 나눈다 | 노출 확률이 0 이면 예외를 낸다 |

> **평가 도구가 먼저입니다.** 만점 기준을 잘못 잡으면 점수가 +0.182 만큼 부풀려지는데,
> 우리가 재려는 효과는 0.01~0.03 입니다. **오차가 재려는 것보다 6~18배 큽니다.**
> 도구가 틀리면 모든 실험이 틀립니다.

---

## S5 — 화면 2종  ⬜  (5일 · **C 담당** · **가장 급하다**)

> `infra/app/` 디렉터리가 **아예 없습니다**(09-03 실측). `docker-compose.yml` 은 이미 이 폴더를
> 빌드 대상으로 적어 놓았으므로 **지금 `make up-all` 은 뜨지 않습니다.**
> **B 의 점수 계산기가 아니라 C 의 착수가 병목입니다.**
>
> 🔴 검수 목록 표(`normalization_queue`)도 **0행**입니다 — 화면(S5-4)보다 **채워 넣는 일이
> 먼저**입니다. 채울 재료는 이미 있습니다: `scripts/bench/out/review_sheet.tsv` 2,000종 ·
> `unmatched.tsv` 24,701종.

| 스텝 | 내용 | 다 됐다는 증거 |
|---|---|---|
| S5-1 | `infra/app/Dockerfile.dashboard` | `make up-all` 이 뜬다 |
| S5-2 | 추천 디버거 — 요청 1건의 항목별 점수와 이유 | mock 으로 먼저 붙인다 |
| S5-3 | 단계별 후보 감소 보기 (①→②→③) | `stage_trace` 를 읽는다 |
| S5-4 | 🔴 검수 화면 — 후보가 **미리 채워져** 클릭만 하면 되게 | 비어 있으면 반나절이 3일 된다 |
| S5-5 | 데이터 품질 화면 — 재료 인식률 추이 | `data_quality_snapshot` 을 읽는다 |
| S5-6 | 기록 실패 횟수 보여주기 | 카운터를 읽는다 |

---

## S6 — 데이터 적재 + 재료 정리 배치  ✅ 부분 완료  (A 담당)

| 스텝 | 내용 | 다 됐다는 증거 | 상태 |
|---|---|---|---|
| S6-1 | `scripts/load_recipes.py` — 레시피 · 재료 원문 · 후기 적재 | 46,353건 적재 | ✅ |
| S6-2 | 조리순서 적재 | 362,677단계 (97.4%) | ✅ |
| S6-3 | 조리도구 걸러내기 · 미매칭 사전 | 재료 인식률 69.2% → **80.8%** | ✅ |
| S6-4 | 검수 시트 2,000종 | `make review-sheet` | ✅ |
| S6-5 | ⬜ 300종 검수하기 (사람이 함) | 인식률 **89.4%** 목표 | ⬜ |
| S6-6 | ⬜ 검수 결과 반영 | `make review-apply WRITE=1` 뒤 인식률 재측정 | ⬜ |
| S6-7 | 🔴 몇 번 돌려도 안전하게 *(09-02)* | 두 번 돌려도 행 수가 그대로 | ✅ |

> **검수는 병목이 아닙니다.** 재료 원문(`recipe_ingredient_raw`)을 그대로 두기 때문에
> 사전을 고치면 언제든 다시 정리됩니다. 기록과 달리 늦어도 잃는 것이 없습니다.
>
> 🔴 **S6-7 전에는 적재를 2회 돌리면 전부 2배가 됐습니다** — 재료 원문이 451,862행에서
> 903,724행이 됐는데 **에러도 안 났습니다.** 지금은 레시피별 재료 위치에 중복 금지를 걸어
> 막았고, 후기 쪽도 같은 방식으로 막았습니다.
> 🔴 단, `.env` 의 `REVIEW_SALT` 를 새로 만들면 후기 작성자 값이 전부 달라져서 **그 보장이
> 깨집니다** — 지우거나 다시 만들지 마세요.
>
> ⚠️ `scripts/bench/out/review_sheet.tsv` 머리말이 아직 "우리 사전(525종)" 이라고 적습니다.
> 실제는 **536종**입니다. 문구를 만드는 곳은 `scripts/bench/review_sheet.py` — A 트랙 소관입니다.

---

## S7 — 레시피 재료 정리 + 특성 5종  ⬜  (6일 · A 담당)

> 🔴 **지금 레시피 특성(`recipe_feature`)이 0행이라 실데이터 추천이 0건입니다.**
> 만드는 단계에서 "재료가 하나라도 있나"에 전부 걸립니다. S3 를 다 짜도 여기가 없으면
> 실제 레시피가 한 건도 안 나옵니다.
> 실측(09-03): `recipe_feature` 0행 · `recipe_ingredient` 0행 · 원문만 451,862행.

| 스텝 | 내용 | 다 됐다는 증거 |
|---|---|---|
| S7-1 | 전처리·파싱·사전매칭·역할판정을 46,353건에 배치 적용 | `recipe_ingredient` 가 채워진다 |
| S7-2 | 레시피별 필수 재료 / 전체 재료 목록(`essential_ids` / `all_ids`) 만들기 | 배열 색인이 실제로 쓰인다 |
| S7-3 | 기본 양념 제외 규칙 | 양념 39종이 필수 재료에서 빠진다 |
| S7-4 | 맛 값(`flavor_vec`) 계산 + 전체 평균 구하기 | 평균값이 채워진다 |
| S7-5 | 비슷한 레시피를 약 50 묶음으로 나눠 `cluster_id` 부여 | 우연성 추천이 균등 뽑기에서 벗어난다 |
| S7-6 | 실데이터로 스모크 재통과 | 후보가 0건이 아니다 |

- 🔴 **맛의 전체 평균은 정해진 두 시점에만 갱신합니다.** 갱신하면 B·C 에게 알립니다 —
  기준이 바뀌면 이미 계산해 둔 맛 점수와 섞입니다.
- 🔴 **묶음(S7-5)은 딱 한 번만 매깁니다.** 기록에 "몇 판째 묶음인지" 적는 칸이 **없어서**,
  다시 매기면 옛 기록의 묶음 번호가 무엇을 가리켰는지 복원할 수 없습니다.
- 맛 값은 **6축으로 저장**합니다. 실제로 쓸 축은 나중에 정합니다.
- 필수 재료가 0개인 레시피(7.2% · 약 3,340건)는 **SQL 함수 쪽에서** 미매칭 비율 0.3 을
  기준으로 가릅니다.

---

## S8 — 재정렬 · 사용자 · 가중치 교체  ⬜  (남은 것)

| 스텝 | 내용 | 다 됐다는 증거 |
|---|---|---|
| S8-1 | 비슷한 것만 나오지 않게 흩뜨리는 재정렬 | 거의 같은 레시피가 줄어든다 |
| S8-2 | 혼합 탐색(무작위 + 성적 기반) + **위치 분산** | 노출 확률이 0 이 아니고, 탐색 항목이 여러 위치에 |
| S8-3 | 🔴 사용자 모집 + 가입 설문 실시 | 실사용자 50명 (받는 쪽은 B-17) |
| S8-4 | 둘 중 나은 쪽 고르기 데이터 수집 — **항목별로 120쌍씩 골고루** (20명 × 30쌍 = 600쌍 + 5% 중복) | 항목별 차이 분포를 그려 본다 |
| S8-5 | 그 결과로 가중치를 학습해 교체 | 항목을 하나씩 빼 보는 비교 실험으로 검증 |
| S8-6 | 최종 리포트 | — |
| S8-7 | ⏳ 오늘의 추천 미리 계산해 두기 | **3주 계획 밖** — 저장할 표만 있다 |

> 🔴 **S8-4 는 항목별로 골고루 뽑아야 합니다.** 두 후보의 차이가 0 인 쌍만 모이면 그 항목의
> 가중치를 **알아낼 수 없고, 알아내지 못했다는 사실조차 모릅니다.** 가장 위험한 것이
> 소비기한 임박입니다 — 비중이 15% 인데 대신할 항목이 하나도 없습니다. 그래서 냉장고
> 시나리오에 **임박한 재료를 일부러 넣습니다.** 수집은 구글폼으로 합니다(개발 0일).
> 뽑기 전에, 차이가 0.1 을 넘는 쌍이 항목별로 120개 있는지 **미리 확인**합니다.
>
> ⏳ **S8-7 은 만들기로 정하기만 했습니다.** 저장할 표(`daily_recommendation`)는 있지만
> **조회 방법도 미리 계산하는 작업도 없습니다** — `reco/` 안에 이 표를 읽고 쓰는 코드가
> 0개입니다. 프론트에 이미 약속한 기능이라 **못 만들면 알려야 합니다.**
> 실시간이 아니라 미리 계산하는 이유는 속도입니다 — 이유 문구 10건을 만들려면 초당 833 토큰이
> 필요한데 가장 빠른 로컬 모델이 90.9 입니다. 미리 돌려 두면 시간 제약이 사라집니다.
> 아직 안 정한 것: 어디서 돌릴지 · 생성된 문구를 어떻게 검수할지 · 낮에 어긋난 이유를
> 표시만 할지 다시 만들지 · 몇 개를 보여줄지.

---

## 무엇이 무엇을 막는가

```
S0 ✅ ─┬─ S1 ✅ ─ S2 ✅ ─ S3 ⬜ ─┐
       ├─ S4 ⬜ ────────────────┼─ S8
       └─ S5 ⬜ ────────────────┘
                    ↑
   S6 ✅ ─ S7 ⬜ ───┘   (실데이터가 흐르려면 S7 이 있어야 한다)
```

**지금 병목 셋**

1. **S5 (C 착수)** — `infra/app/` 이 아직 없습니다. 5일짜리인데 아직 시작 못 했습니다.
2. **S7 (A)** — 없으면 S3 를 다 짜도 실제 레시피가 0건입니다.
3. **S8-3 (사용자 모집)** — 비중 **27%** 가 가입 설문에 묶여 있습니다.

**병목이 아닌 것**: 검수(S6-5). 재료 원문이 그대로 있어서 언제 해도 됩니다.
