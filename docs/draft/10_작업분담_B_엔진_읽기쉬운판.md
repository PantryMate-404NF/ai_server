# 트랙 B — 엔진

> 작성자: 박재우 · 작성일: 2026-09-03

> 담당: ______  ·  일감 10.5일치  ·  남은 기간 15영업일

지금 추천 점수는 **난수**입니다. 서버가 요청을 받으면 아무 숫자나 만들어 순위를 매기고,
화면에는 멀쩡해 보이는 목록이 나옵니다.

B가 할 일은 이 숫자를 **진짜 점수**로 바꾸고, 추천할 때마다 **기록이 남게** 하는 것입니다.

이 문서 하나만 읽으면 시작할 수 있게 썼습니다.

---

## 3주 뒤에 이렇게 되어 있어야 합니다

- 추천 점수에 **난수가 한 줄도 없다.** 전부 데이터베이스의 실제 값에서 계산된다.
- 추천할 때마다 **기록이 남는다.** 무엇을 몇 번째 자리에 보여줬는지까지.
- 나중에 "그때 그 점수는 왜 그랬지"를 **되짚을 수 있다.**
- 추천 이유 문구가 그 레시피의 실제 값에서 만들어진다.
  (지금은 '두부', '애호박' 같은 말이 요청과 상관없이 박혀 있습니다.)

---

## 점수를 어떻게 내나

### 1단계 — 레시피마다 17가지를 재서 0~1 점수로 만든다

무엇을 재는지는 바로 다음 절 "재는 17가지"에 있습니다.

### 2단계 — 중요도를 곱해 평균 낸다

특성마다 중요도가 정해져 있습니다. 제일 무거운 것은 입맛 궁합 0.16,
곧 상할 재료 0.15, 좋아하는 재료 0.11 순입니다.

**여기서 제일 중요한 규칙 — 잴 수 없는 특성은 빼고 계산합니다.**

냉장고에 곧 상할 재료가 하나도 없으면 "곧 상할 재료를 쓰나" 점수는 **잴 수가 없습니다.**
이때 0점을 주면 안 됩니다. 0점은 "쟀더니 0"이라는 뜻이고, 이건 "못 쟀다"이기 때문입니다.
그래서 못 잰 특성은 **분자에서도 분모에서도 뺍니다.**

```
점수 =   잴 수 있었던 특성의 (중요도 × 점수) 를 다 더한 값
        ──────────────────────────────────────────────
         잴 수 있었던 특성의  중요도  를 다 더한 값
```

잴 수 있는 게 하나도 없으면 점수를 **0.0** 으로 둡니다. 오류를 내지 않습니다.
그러면 모든 후보가 동점이 되어 앞 단계의 "부족한 재료가 적은 순 → 인기 순" 정렬이
그대로 살아납니다. **추천은 어떤 경우에도 실패하지 않습니다.**

> **남은 특성에 중요도를 나눠주면 안 되나?** 안 됩니다. 특성이 빠지는 경로가 네 군데
> (유저가 익어갈 때 · 임베딩이 비었을 때 · 최근 요리 기록이 없을 때 · 설문을 덜 했을 때)라
> **어느 것을 먼저 적용하느냐에 따라 값이 달라집니다.** 나눗셈은 순서와 상관없이 같은 값을
> 내고, 되짚을 때 저장할 것도 늘지 않습니다.

### 3단계 — 감점을 곱한다

| 언제 | 얼마나 |
|---|---|
| 최근 7일 안에 이미 보여준 레시피 | ×0.7 |
| 최근 14일 안에 이미 만든 레시피 | ×0.5 |
| 싫어하는 재료가 들어 있음 | 재료 비율 × 2.0, 최대 0.8까지 깎음 |

**빼지 말고 곱합니다.** 빼기로 하면 점수 높은 항목이 감점을 이겨서
"어제 만든 김치찌개"가 계속 1위에 남습니다. 에러는 안 납니다.

**알러지는 여기 없습니다.** 감점이 아니라 후보를 뽑는 단계에서 아예 잘라냅니다.
감점으로 내리면 **점수가 충분히 높을 때 통과하게 되고**, 안전 문제가 확률 문제로 바뀝니다.

---

## 재는 17가지

**지금 바로 잴 수 있는 것**

| 이름 | 무슨 뜻인가 | 중요도 |
|---|---|---|
| `f_coverage` | 냉장고 재료로 레시피 재료가 얼마나 채워지나 (재료 충족도) | |
| `f_missing` | 부족한 재료가 적을수록 높다 | |
| `f_popularity` | 후기가 많이 달린 레시피인가 | 0.10 |
| `f_time_fit` | 원하는 시간 안에 만들 수 있나 | |
| `f_skill_fit` | 난이도가 내 실력과 맞나 | 0 |

**A의 데이터가 와야 제대로 재는 것**

| 이름 | 무슨 뜻인가 | 중요도 |
|---|---|---|
| `f_taste` | 6가지 맛(맵기·단맛·짠맛·신맛·감칠맛·기름짐)으로 본 입맛 궁합 | 0.16 |
| `f_expiring` | 곧 상할 재료를 쓰는 레시피인가 | 0.15 |
| `f_ing_pref` | 좋아한다고 표시한 재료가 들어 있나 | 0.11 |
| `f_cooccur` | 최근에 만든 요리와 재료가 겹치나 | 0.10 |
| `f_season` | 지금 달에 어울리는 제철 요리인가 | 0.02 |
| `f_pantry_use` | 냉장고에 있는 것을 얼마나 많이 쓰나 | 0 |

**당분간 못 재는 것 — 항상 '없음'으로 내보냅니다**

| 이름 | 무슨 뜻인가 | 중요도 | 왜 못 재나 |
|---|---|---|---|
| `f_cuisine` | 한식·일식 같은 요리 종류가 취향에 맞나 | 0.04 | 분류 칸이 4만 6천 건 전량 비어 있음 |
| `f_dish_type` | 요리 형태 | | 데이터 0건 |
| `f_quality` | 레시피 평점 | | 평점이 전량 비어 있음 |
| `f_content` | 레시피 내용 유사도 | 0.08 (익은 유저) | 유저 쪽 데이터가 없음 |
| `f_ing_cf` · `f_group_pref` | 비슷한 사람들의 취향 | 0 | **만들 방법 자체가 없음** |

> 중요도 빈 칸은 이 문서에 안 적혀 있습니다. 코드의 기본 중요도 표(`app/schemas/common.py`)에
> 있고, **이 파일은 읽기만 합니다.**

**유저가 익으면 중요도가 조금 옮겨갑니다.** 행동 기록이 기준치에 다다르는 만큼(0~1 비율로)
설문에서 받은 입맛을 0.16 → 0.11로 내리고, 행동에서 나오는 내용 유사도를 0 → 0.08로 올립니다.
이때 중요도 합이 1을 살짝 넘는데(최대 1.03) **맞추지 않습니다** — 어차피 분모로 나눕니다.

---

## 지금 어디까지 와 있나

**이미 완성돼 있어 손댈 필요 없는 것**

- 임박 재료를 계산하는 데이터베이스 함수 (`infra/init/04_functions.sql`) — 부르기만 하면 됩니다
- 추천 이유 문구 템플릿과 조사(을/를) 처리 — 남은 건 문구에 끼울 값을 만드는 것뿐
- 입출력 약속 파일 (`app/schemas/`) — 읽기만 하고 고치지 않습니다

**아직 없는 것 — B가 만듭니다**

```
app/services/recommends/scorer.py     점수 계산기 본체    파일 자체가 없음
app/services/recommends/penalty.py    감점                없음
app/services/recommends/config.py     설정 지문           없음
app/db/features.py     특성 읽어오기       8줄짜리 껍데기만
reco/api/serve.py       실서빙 경로         없음
```

**A가 채워줄 데이터 — 지금 전부 비어 있습니다** (자세한 건 다음 절)

```
레시피별 재료 정리 결과      0행
재료가 얼마나 흔한지         536종 전량 0
맛 6축의 코퍼스 평균         전부 0
```

**3주 안에 못 채우는 것 — 그래서 '없음'으로 고정합니다**

```
요리 종류 분류 (한식/일식…)  4만 6천 건 전량 비어 있음
레시피 평점                  전량 비어 있음
요리 형태                    0건
```

---

## A · C 와 주고받는 것

### A 에게서 받습니다

**① 레시피별 재료 정리 결과 4만 6천 행** — 필수/전체 재료 목록, 재료 개수, 못 붙은 재료 개수,
맛 6축, 제철 12개월, 인기·품질 점수, 조리 시간, 난이도, 묶음 번호.
B는 **칸 이름만 읽고 채우는 방법에는 관여하지 않습니다.** 없으면 B-6·B-7·B-8·B-15 가 막힙니다.

**② 재료가 얼마나 흔한지 세어둔 값** — 지금 536종 **전량 0**. 0을 그대로 쓰면
"흔한 재료(소금·간장)는 덜 치고 드문 재료는 더 친다"가 조용히 **"전부 똑같이 친다"로 퇴화합니다.**

**③ 맛 6축의 코퍼스 평균값** — 지금 전부 0이라 중심 맞추기가 꺼져 있고, 그 상태에서는
**아무 벡터 둘을 골라도 유사도가 0.767** 이 나옵니다.

### A 에게 넘겨줍니다

**인기 점수를 0~1로 만드는 방법.** 후기 수를 최댓값(1079)으로 나누면 안 됩니다. 중앙값이
4건이라 **레시피의 99%가 0~0.17에 뭉치고**, 중요도 0.10이 사실상 0.02처럼 작동합니다.
**백분위**로 만들어야 합니다. B는 기다리지 않고 `recipe.review_count` 에서 직접 계산합니다.

### C 에게 줍니다

- **1일차부터 — 실제로 쌓이는 추천 기록과 이벤트 기록.** `make serve-smoke` 로 확인
- **추천 응답** — 특성 17개 전수(못 잰 것은 '없음' 그대로) · 점수 · 감점 · 이유 문구.
  약속은 안 바뀌므로 C는 지금 가짜 서버 응답에 붙여도 되고, B-13에서 **값만** 진짜로 바뀝니다
- **기록 조회 API 두 개** — 보여준 것 / 안 보여준 후보까지
- **점수 설정 1행** — 그때의 중요도와 감점 계수를 되찾는 근거. C의 디버거가 막대그래프를
  그리는 유일한 재료입니다
- **카운터를 `/health` 에 노출** — 몇 건 썼나·실패했나·완화됐나.
  🔴 서버를 재시작하면 0이 되므로 C가 주기적으로 긁어 쌓아야 유실이 보입니다
- **데모 유저 12명.** 🔴 실유저만 걸러 보는 뷰가 이 12명을 **전량 걸러냅니다** —
  C의 화면은 뷰가 아니라 원본 표를 읽어야 합니다
- **특성별 리포트** (`app/services/eval/feature_report.py`) — 특성마다 최솟값·중앙값·최댓값·'못 잼'
  비율과 레시피 매칭률. C가 "이 특성이 죽었나"를 판정하는 입력입니다

---

## 무엇부터 하나

| | 작업 | 기간 | 선행 |
|---|---|---:|---|
| B-0 | 데모 유저 12명 만들기 | 0.5일 | 없음 |
| B-1 | 🔴 추천 기록이 실제로 쌓이게 연결 | 0.5일 | B-0 |
| B-2 | 행동 이벤트를 기록에 넣기 | 0.5일 | B-1 |
| B-3 | 기록 조회를 메모리에서 DB로 | 0.5일 | B-1 |
| B-4 | 특성 읽어오기 골격 + 지금 되는 5가지 | 1일 | 없음 |
| B-5 | 임박 재료 특성 연결 | 0.5일 | B-4 |
| B-6 | 입맛 궁합 + 중심 맞추기 | 1일 | A |
| B-7 | 재료 취향 · 최근 요리와의 겹침 · 냉장고 소진 | 1일 | A |
| B-8 | 제철·요리 종류 처리와 나머지 '못 재는 것' 확정 | 0.5일 | A |
| B-9 | 🔴 점수 계산기 본체 | 1일 | 없음 |
| B-10 | 감점 3가지 | 0.5일 | B-9 |
| B-11 | 점수 설정 등록 (설정 지문) | 0.5일 | B-10 |
| B-12 | 추천 이유 문구 연결 | 0.5일 | B-4 |
| B-13 | 🔴 진짜 점수로 갈아끼우기 — 난수 제거 | 1일 | B-9·B-10·B-12 |
| B-14 | 폴백 — 추천은 절대 실패하지 않는다 | 0.5일 | B-13 |
| B-17 | 온보딩 답변을 DB에 저장 | 0.5일 | B-0 |
| B-18 | 냉장고 수정을 DB에 저장 | 0.5일 | B-0 |
| B-16 | 🔴 백엔드와 주고받을 것 확정·전달 | 0.25일 | B-1·B-2 |
| B-15 | 전체 검증 + A 데이터로 갈아끼우기 | 0.5일 | A |

**첫날은 B-0 → B-1.** B-1이 팀 전체에서 제일 급합니다. 지금 추천 기록이 하나도 없어서
**C가 화면에 띄울 것이 없기 때문입니다.** 둘 다 반나절씩입니다.

**A를 기다리지 않고 지금 끝낼 수 있는 것** — B-0·B-1·B-2·B-3·B-4·B-9·B-10·B-11·B-17·B-18.
절반이 넘습니다. A가 있어야 하는 것도 **가짜 데이터로 코드를 먼저 완성**해 둘 수 있고,
실데이터가 오면 가짜 데이터만 끄면 됩니다.

---

# 작업별로

## B-0 — 데모 유저 12명 만들기

기록을 쓰려면 그 유저가 데이터베이스에 **실제로 있어야 합니다.** 없으면 한 줄도 안 남습니다.

**무엇을** — `app/db/fixture.py` 신설 + `make demo-users`.
`tests/smoke_test.py` 의 유저 추가 패턴을 그대로 재사용합니다.

- 유저 12명(`demo_01`~`demo_12`) + 각자 취향 설정 1행
  (맵기·단맛·짠맛·실력·최대 조리 시간·좋아하는 요리 종류)
- 냉장고 재료 5~12개 — **일부는 넣은 날짜를 과거로 밀어** 임박 상태가 실제로 만들어지게 합니다
- 좋아하는/싫어하는 재료 각 5개 — **양수 3, 음수 2.** 감점을 검증하려면 음수가 필요합니다

전부 **몇 번 돌려도 안전하게** 씁니다(이미 있으면 넘어감).

**확인** — `make demo-users` 를 **두 번** 돌려도 유저 12명·취향 12행 그대로.
싫어하는 재료가 있고, `demo_01` 의 3일 이내 임박 재료가 있다.

> ⚠️ **조용히 틀리는 방식**
>
> 데모 유저를 '시뮬레이션' 표시로 넣으면 **실유저만 보는 뷰가 12명을 전량 걸러냅니다.**
> 기록은 정상으로 쌓이는데 C의 화면만 0건이 되고, 원인이 기록 쓰는 쪽으로 오인됩니다.
> 반대로 표시를 안 하면 실유저 지표가 데모로 오염되고 되돌릴 수 없습니다.
>
> 냉장고 재료를 전부 '오늘 넣음'으로 두면 임박 재료가 안 생깁니다. **중요도 0.15짜리,
> 후보를 가장 크게 갈라놓는 특성이 죽은 채로 모든 테스트가 통과합니다.**

## B-1 — 🔴 추천 기록이 실제로 쌓이게 연결

**무엇을** — `app/main.py` 의 추천 함수에서 메모리에 넣기 **직전에** 기록 쓰기 함수를
부릅니다. 같이 넘길 것: 요청 · 응답 · **점수 매긴 후보 전체** · 냉장고 재료와 상세 ·
알러지 확장 결과 · 설정 지문 · 기준값 버전 · 추적 정보(응답에 안 실을 때도 **반드시** 넘깁니다).

서빙 모드를 '시뮬레이션'에서 '실제'로 바꿉니다 — 그래야 저장하는 후보가 10건에서 50건으로
늘어 **C가 안 보여준 후보까지 볼 수 있습니다.** `Makefile` 에 `serve-smoke` 추가.

**확인** — `make demo-users && make serve-smoke`
데모 12명이 한 번씩 요청하면 추천 기록 12행, 노출 기록 12×(보여준 개수)행.
'완화됨' 표시 0행, 실패·되돌림·계약위반 카운터 전부 0.

> ⚠️ **조용히 틀리는 방식**
>
> 유저가 데이터베이스에 없으면 기록 쓰기가 실패하는데 **예외를 삼키고 실패만 돌려줍니다.**
> 응답은 200이고 화면도 정상인데 데이터베이스만 0행입니다. 카운터를 안 보면 영원히 모릅니다.
> **후기 60만 건이 사라졌는데 아무 일도 없어 보였던 것과 같은 형태입니다.**
>
> 점수 매긴 후보 전체를 안 넘기면 **응답에 담긴 20건만** 후보로 저장됩니다. "보여준 것은
> 후보에 포함된다"는 여전히 성립해서 **검증이 전부 통과하고**, 안 보여준 후보 정보가
> 0이 된 것을 아무도 못 잡습니다.

## B-2 — 행동 이벤트를 기록에 넣기

> 🔴 **`source` 칸이 새로 생겼습니다.** 비워두면 저장이 **실패합니다.**
>
> ```
> served    서버가 응답에 담았다 (노출)
> viewport  화면에 실제로 보였다 (아직 안 씁니다)
> client    사용자가 한 행동 (클릭 · 저장 · 요리함)
> ```
>
> 그리고 노출은 **몇 번째 자리였는지가 필수**입니다.

**무엇을** — `app/core/events.py` 신설. 이벤트 묶음을 받아 (받아들인 수, 거절한 수, 오류)를 냅니다.

- 같은 노출이 두 번 들어오면 무시. 클릭·저장·요리는 중복 허용
- **모르는 요청 번호가 섞이면 그 행만 걸러내고 나머지는 넣습니다** — 묶음 전체를 실패시키지 않습니다
- 자리 번호가 없는 행은 카운터에 표시
- 여러 행을 한 번에 넣을 때 **페이지 크기를 행 수만큼 반드시 지정합니다** — 예전에 이것 때문에
  데이터가 유실됐습니다

가짜 서버의 '검증만 하고 마는' 본문을 이 호출로 바꿉니다.

**확인** — `make event-test`
추천 1회 뒤 클릭·저장·요리 각 1건을 같은 요청 번호·자리 번호로 보내면 노출은 보여준 개수,
나머지는 각 1건. 같은 묶음을 다시 보내도 노출 수가 안 늘고, 없는 요청 번호는 전부 거절되며
총 행 수는 그대로다.

> ⚠️ **조용히 틀리는 방식**
>
> 자리 번호가 선택 항목이라 클라이언트가 빼먹어도 정상 처리됩니다. **자리 번호가 없는 시대의
> 기록은 나중에 절대 못 고칩니다** — "위에 있어서 눌린 것"과 "좋아서 눌린 것"을 영원히
> 구분할 수 없습니다. 에러는 안 납니다.
>
> 페이지 크기를 안 주면 마지막 묶음의 결과만 돌아와 받아들인 수가 실제보다 적게 보고됩니다.
> 반대로 "몇 행 건드렸나"를 그냥 믿으면 중복이라 건너뛴 것이 성공으로 세어집니다. 양쪽 다 조용합니다.

## B-3 — 기록 조회를 메모리에서 DB로

**무엇을** — `app/db/logs.py` 신설. 가짜 서버의 메모리 딕셔너리 조회를 데이터베이스 조회로
바꾸고 **딕셔너리 자체를 지웁니다.** 응답 모델에 후보 목록 칸이 없으므로, C의 디버거가 후보를
보려면 `GET /v1/recommendations/{id}/candidates` 를 따로 냅니다.
(응답에 칸을 **더하는** 것은 약속을 깨지 않습니다.)

**확인** — `make log-read-test`
추천한 뒤 **서버를 새로 띄우고** 조회하면 200이고 값이 응답과 일치.
후보 조회 결과 문자열에 `"f_cuisine": null` 이 들어 있다 — 못 잰 것이 0.0으로 안 바뀌었다는 뜻입니다.

> ⚠️ **조용히 틀리는 방식**
>
> JSON으로 저장했다 읽어오는 사이에 '못 잼'이 0.0으로 복원되면 **"못 쟀다"가 "쟀더니 0"으로
> 뒤집힙니다.** 중간에 `or 0.0` 을 한 번 태우거나 pandas로 읽으면 조용히 메워지고,
> 값이 전부 0~1 안이라 어떤 범위 검사도 안 걸립니다.
>
> 메모리 딕셔너리를 안 지우고 조회를 **추가만** 하면 같은 요청 번호에 두 벌이 공존해서
> C가 어느 쪽을 보는지 모른 채 서로 다른 값을 봅니다.

## B-4 — 특성 읽어오기 골격 + 지금 되는 5가지

**무엇을** — `app/db/features.py`(지금 8줄 껍데기)를 채웁니다. 유저와 후보 목록을 받아
레시피별 특성 값을 돌려줍니다. 🔴 **후보 전체를 한 쿼리로** — 하나씩 조회하면 안 됩니다.

| 이번에 채우는 5가지 | 어떻게 |
|---|---|
| `f_coverage` | 후보를 뽑을 때 이미 계산된 값을 그대로 |
| `f_missing` | 1 ÷ (1 + 부족한 재료 수) |
| `f_popularity` | 후기 수의 **백분위** — 중앙값 4건 · 상위 1% 181건 · 최대 1079건 |
| `f_time_fit` | 상한 이내면 1.0, 넘으면 감쇠, **시간을 모르면 0.5(중립)** |
| `f_skill_fit` | 1 − ㅣ난이도 − 내 실력ㅣ ÷ 4 |

난이도 맞춤은 중요도가 0이지만 넣습니다. 데이터가 4만 5천 건 이미 있어서 공짜이고,
**지금 안 남기면 나중에 켤 때 과거 기록에 값이 없습니다.**

**확인** — `make feature-test` · 후보 200건에서
① 조리 시간을 모르는 레시피가 **정확히 0.5** — 0도 1도 아니다
② 후기 상위 1% 레시피의 인기가 0.98 초과, 중앙값(4건) 레시피가 0.4~0.6
③ 요청 1회의 데이터베이스 왕복이 **2회** — 후보 뽑기 1 + 특성 읽기 1

**막히면** 재료 정리 결과가 0행이라 후보가 안 나오면, `tests/smoke_test.py` 의 합성 데이터 삽입을
재사용해 **3,000건짜리 개발용 가짜 데이터** 위에서 개발합니다.

> ⚠️ **조용히 틀리는 방식**
>
> 인기를 후기 수 원값이나 최댓값 나눗셈(÷1079)으로 만들면 0~1 안에는 들어가지만 중앙값이 4라
> **99%가 0~0.17에 뭉칩니다.** 중요도 0.10이 사실상 0.02처럼 작동하는데, 중요도 합 검사도
> 범위 검사도 전부 통과합니다.
>
> 후보를 하나씩 조회하면 로컬(0.1밀리초)에서는 안 보이다가 **원격 데이터베이스(5~10밀리초)로
> 옮기는 순간 500배로 드러납니다.**

## B-5 — 임박 재료 특성 연결 (중요도 0.15)

**무엇을** — 계산 함수는 데이터베이스 안에 **이미 완성돼 있습니다**
(`infra/init/04_functions.sql` · 임박 재료가 0건이면 '없음'을 돌려줍니다).
남은 건 후보 500건을 한 번에 부르는 것뿐 — B-4의 쿼리에 칸 하나를 합칩니다.
**SQL의 '없음'을 파이썬의 '없음'으로 그대로 통과시킵니다.**

**확인** — `make feature-test`
임박 재료 2건인 `demo_01` 기준, 그 재료를 쓰는 레시피가 0.5 또는 1.0이고 안 쓰면 0.0.
임박 재료가 0건인 유저는 **전 후보가 '없음'이고 0.0이 하나도 없다.** 왕복 횟수 2 그대로.

**막히면** 가짜 데이터의 재료 목록으로 검증합니다. 계산이 SQL 안에 있어서 A의 데이터가 오면
코드 변경이 0줄입니다.

> ⚠️ **조용히 틀리는 방식**
>
> SQL의 '없음'을 `float(...)` 으로 바꾸거나 `or 0.0` 으로 받으면 **"임박 재료가 아예 없다"가
> "이 레시피는 임박 재료를 안 쓴다"로 바뀝니다.** 그러면 분모가 0.15만큼 커져 전 후보 점수가
> 일괄로 눌리는데 **순위는 그대로**라 화면상 아무 차이가 없습니다. 임계값과 C의 평가만 조용히
> 틀립니다. **나눗셈이 존재하는 이유 자체를 무력화하는 가장 흔한 방식입니다.**

## B-6 — 입맛 궁합 (중요도 0.16)

**무엇을** — `app/services/recommends/taste.py`. 유저의 맛 벡터와 레시피의 맛 벡터가 얼마나 같은 방향인지
잽니다. 단 **둘 다에서 코퍼스 평균을 뺀 다음** 잽니다(중심 맞추기).

유저 맛 벡터는 설문의 맵기·단맛·짠맛 3축에서 유도합니다. 🔴 **설문에 없는 신맛·감칠맛·기름짐
3축은 0.5(중립)가 아니라 코퍼스 평균을 그대로 넣습니다** — 평균을 빼고 나면 정확히 0이 되게
하려는 겁니다. 0.5를 넣으면 부호가 생겨서 **'없는 취향'이 신호가 됩니다.**

읽어온 기준값 버전을 추천 기록에 같이 싣습니다. (기준값 버전 = 코퍼스 평균이 갱신될 때마다
올라가는 번호. 안 남기면 평균이 바뀌는 순간 **과거 점수를 영원히 재현할 수 없습니다.**)

**확인** — `make feature-test`
① 평균이 0일 때 무작위 벡터 1,000쌍의 유사도 평균이 0.75±0.05 (실측 0.767 재현)
② 평균을 뺀 뒤에는 같은 쌍의 평균이 0.1 미만
③ 유저 쪽과 레시피 쪽에서 뺀 평균이 **같은 값**이다
④ 추천 기록의 기준값 버전이 비어 있지 않고 최신값과 같다

**막히면** 재료-맛 사전(`seeds/ingredient_flavor.yaml`)과 원본 재료 목록으로 레시피 1,000건만
임시 맛 벡터를 만들어 검증합니다. 정본은 A가 채우고 B는 **읽는 곳만 바꾸면** 됩니다.

> ⚠️ **조용히 틀리는 방식**
>
> 평균을 레시피 쪽에서만 빼고 유저 쪽에서 안 빼면 두 벡터가 다른 좌표계에 놓입니다.
> 그래도 유사도는 −1~1 안이라 값이 그럴듯하고 예외도 안 납니다.
>
> 더 위험한 것 — **지금은 평균이 전부 0이라 중심 맞추기가 사실상 꺼져 있습니다.** 이걸 잊으면
> **중요도 0.16이 통째로 잡음에 배정된 채** 서빙됩니다. 값은 정상 범위이고 순위도 그럴듯합니다.

## B-7 — 재료 취향 · 최근 요리와의 겹침 · 냉장고 소진

> 🔴 **결정 완료.** 흔함 값이 지금 **536종 전량 0** 이고 A가 곧 채웁니다. **기다리되 멈추지
> 않습니다.** 그리고 **0을 그대로 쓰면 안 됩니다** — 0이면 겹침 점수를 **'없음'으로 내보냅니다.**

**무엇을**

- `f_ing_pref` — 레시피 재료 중 유저가 좋다/싫다 표시한 것들의 평균 점수를 0~1로 옮깁니다
  (−1~1 을 `(x+1)/2` 로). 겹치는 재료가 없으면 '없음'
- `f_cooccur` — 최근에 만든 레시피들과 재료가 얼마나 겹치는지, 그중 **최댓값**. 흔한 재료는
  덜 치고 드문 재료는 더 칩니다. 만든 요리가 0건이면 '없음'
- `f_pantry_use` — 냉장고 재료 중 몇 %를 쓰는 레시피인가

🔴 겹침 점수를 **"영구히 못 만드는 것" 목록에 넣지 마세요.** 그 목록은 만들 방법 자체가
없는 것들이고, 겹침 점수는 **유저가 한 번만 요리하면 살아납니다.**

**확인** — `make feature-test`
① 그 레시피 자신을 '요리함'으로 넣으면 겹침 1.0, 재료가 안 겹치면 0.0
② 요리 기록 0건인 유저는 전 후보가 '없음'이고 **0.0이 단 하나도 없다**
③ 싫어하는 재료만 든 레시피의 재료 취향이 0.5 미만
④ 흔함 값이 0인 재료가 섞여도 예외 없이 값이 나오고 **가중치가 전부 같아지지 않는다**

**막히면** 흔함 값을 원본 재료 목록 45만 줄의 등장 빈도로 임시 계산하거나, 0인 재료는 중앙값으로
대체합니다. 겹침 계산 자체는 흔함 값 없이도 검증됩니다.

> ⚠️ **조용히 틀리는 방식**
>
> 요리 기록이 없는 유저에게 0.0을 채우면 분모에 0.10이 들어가 **모든 신규 유저의 후보가 일괄로
> 감점**되는데, 전부 같이 내려가므로 순위가 안 바뀌어 보이지 않습니다.
>
> 흔함 값 0을 그대로 나눗셈에 넣으면 오류가 아니라 **모든 재료가 똑같은 최대 가중치**를 갖게
> 됩니다. "흔한 재료는 덜 친다"가 조용히 사라지고, 값은 여전히 0~1이라 테스트가 전부 통과합니다.

## B-8 — 제철·요리 종류 처리와 나머지 '못 재는 것' 확정

> 🔴 **판단할 것이 없습니다. 이미 결정됐습니다.** 이 작업은 결정을 *적용*하는 일입니다.
>
> '못 재는 것'에는 두 종류가 있고, 코드에서도 다른 목록으로 나뉩니다.
>
> | | 뜻 | 되살리려면 | 중요도 |
> |---|---|---|---|
> | **만들 방법이 없다** | 수단 자체가 없음 | 그 방법을 구현해야 함 | 0이어야 한다 |
> | **데이터가 없다** | 방법은 있는데 값이 없음 | 데이터만 오면 됨 | **그대로 둔다** |
>
> **중요도를 0으로 내리지도, 남은 것에 나눠주지도 않습니다.** 나눗셈이 못 잰 특성을 분자·분모에서
> 함께 빼므로 **나눠주기와 수학적으로 같습니다.** 데이터가 오면 코드 변경 없이 켜집니다.
> B가 할 일은 **점수 계산기가 이 둘을 '없음'으로 내보내는지 확인**하는 것뿐입니다.

**무엇을** — 제철은 레시피의 12개월 값에서 이번 달 것을 꺼냅니다. 비어 있으면 '없음'.

**요리 종류는 '없음'으로 고정합니다.** 분류 칸이 4만 6천 건 **전량 비어 있고**, 대신 있는 원본
태그는 분류축이 아니라 자유 태그입니다 — 실제로 `["닭고기요리"]`,
`["미소라면","일본식라면","미소라멘","챠슈라면"]` 같은 식입니다.
3주 안에 15종 분류기를 만들 근거가 없습니다.

🔴 **중요도 0.04를 0으로 내리지 않습니다.** 나눗셈이 흡수해서 분모가 0.96이 되고 점수는 여전히
0~1입니다. 내리면 설정 지문이 바뀌고, 나중에 분류 데이터가 채워졌을 때 되돌려야 하며,
**그 사이 기록의 실효 중요도가 두 벌이 됩니다.**

같은 판단으로 요리 형태 · 평점 · 내용 유사도 · 비슷한 사람들의 취향도 '없음'으로 나갑니다.

**확인** — `make feature-test && make contract`
전 후보에서 요리 종류가 '없음'이고 0.0이 하나도 없다. 특성 목록이 **17개 전수**.
"중요도가 0보다 큰 특성은 만들 방법이 있어야 한다" 검사가 계속 통과한다.
`git diff --exit-code app/schemas/common.py` 가 0 — **기본 중요도 표를 한 글자도 안 건드렸다.**

**막히면** 제철 데이터가 비어 있으면 '없음'으로 나가고 분모가 0.02 줄 뿐입니다.
나눗셈이 흡수하므로 **기다릴 이유가 없습니다.**

> ⚠️ **조용히 틀리는 방식**
>
> 요리 종류를 0.0으로 채우면 한식을 좋아한다고 한 유저에게 **"이건 한식이 아니다"라는 틀린
> 신호**가 되고 분모에 0.04가 들어갑니다. 조용합니다. 반대로 "영구히 못 만드는 것" 목록에
> 넣으면 `make contract` 가 실패합니다 — 이쪽은 **시끄럽게 틀리므로 안전합니다.**
>
> 진짜 위험은 세 번째입니다. 급하다고 0.04를 0으로 내리고 남은 것들에 비례해서 나눠주는 것.
> 중요도 합 1.00 검사는 통과하면서 **적용 순서에 따라 값이 달라지는** 상태로 되돌아가고,
> 나눠주기가 두 경로에서 겹치는 순간 **같은 요청이 두 값을 냅니다.**

## B-9 — 🔴 점수 계산기 본체

**무엇을** — `app/services/recommends/scorer.py` 신설. 지금 **파일 자체가 없습니다.**
위 "점수를 어떻게 내나"의 익음 반영과 나눗셈을 그대로 구현합니다.
잴 수 있는 게 하나도 없으면 **0.0** — 예외도 '없음'도 아닙니다.

**확인** — `make scorer-test` (신설 · 데이터베이스 필요 없음)

① 모든 특성이 1.0이면 **못 잰 개수와 무관하게** 점수가 정확히 1.0 — 하나만 못 쟀을 때와
10개 중 9개를 못 쟀을 때 **둘 다**
② 나머지가 같고 임박 재료만 '없음'인 후보의 점수가, 임박 재료 자리에 나머지 특성의 가중평균값을
채운 후보의 점수와 **같다** — 나눠주기로 구현했으면 ①이나 ② 중 하나가 반드시 깨집니다
③ (익음 반영 → 결측 제거)와 (결측 제거 → 익음 반영)이 소수점 12자리까지 같다
④ 잴 수 있는 게 하나도 없을 때 0.0이고, 예외 없고, NaN이 아니다
⑤ 무작위 1만 케이스에서 점수가 전부 0~1

**선행** 없음 — 순수 계산이라 특성 값은 테스트가 만듭니다. 지금 바로 끝낼 수 있습니다.

> ⚠️ **조용히 틀리는 방식**
>
> 디버거용 코드에 `(값 or 0.0)` 한 줄이 있습니다. **막대그래프 전용**인데 이 한 줄을 점수
> 계산기에 복사해 넣으면 못 잰 것이 0으로 대입되어 **나눗셈이 통째로 무의미해집니다.** 결과는
> 여전히 0~1이고 예외도 없어서 ⑤로는 안 잡힙니다. **①과 ②가 유일한 방어입니다.**
>
> 분모가 소수점 누적 오차로 아주 작아지면(1e-17) 점수가 폭발해 순위가 뒤집힙니다
> → 분모가 1e-9보다 작으면 0으로 취급합니다.
>
> 0.0과 '없음'을 `if not f` 로 구분하면 **"쟀더니 0"인 특성이 분모에서 빠져** 그 후보만 점수가
> 올라갑니다. 부정 신호가 가점으로 뒤집히는데 값은 전부 정상 범위입니다.

## B-10 — 감점 3가지

**무엇을** — `app/services/recommends/penalty.py`. 위 3단계 표대로 만들고 **최종 점수 = 원점수 × 감점**.
후보 500건을 **한 쿼리로** — 이벤트 기록을 레시피별로 묶어 마지막 노출 시각과 마지막 요리 시각을
받아옵니다.

**확인** — `make scorer-test`
이벤트 0건 유저는 전 후보 감점이 **정확히 1.0**. 요리한 레시피만 0.5, 15일 전 요리는 1.0.
싫어하는 재료 비율 100%면 상한에 걸려 최종 감점 0.2, 20%면 0.4만큼 깎임.
재료 목록이 빈 레시피에서 예외가 안 난다.

> ⚠️ **조용히 틀리는 방식**
>
> 알러지를 여기 섞으면 후보 뽑기 단계의 차단과 이중이 되고, **점수가 충분히 높으면 통과**하게 됩니다.
>
> 분모로 쓰는 재료 목록에 양념·고명이 다 들어 있어서, 싫어하는 재료 1개가 20개 중 1개면
> 10%밖에 안 깎입니다. 의도가 아니면 분모를 **필수 재료만**으로 바꿔야 하는데, **바꾼 사실이
> 설정 지문에 안 들어가면 과거 점수를 영원히 재현할 수 없습니다.**

## B-11 — 점수 설정 등록 (설정 지문)

**설정 지문**은 그 점수를 낼 때 쓴 설정 — 기준 중요도 17개 + 감점 계수 + 익음 기준 — 을 짧은
문자열로 압축한 것입니다. 추천 기록에 이 지문을 남기고, 지문 → 설정 표를 따로 1행 저장해 둡니다.
몇 달 뒤 "이 추천은 어떤 설정으로 나왔지"를 이걸로 되찾습니다.

**무엇을** — `app/services/recommends/config.py`.
🔴 **지금 가짜 서버는 중요도만 지문에 넣습니다.** 감점 계수나 익음 기준을 바꿔도 같은 지문이
나와서 **한 지문이 서로 다른 두 설정을 가리키게 됩니다.** 세 가지를 모두 넣습니다.

서버가 뜰 때 **'있으면 넘어가기'로 1행 등록**합니다. 덮어쓰기를 쓰면 안 됩니다 — 같은 지문은
정의상 같은 내용이어야 합니다. 중요도는 17개 전수, **0인 것도 명시**합니다.

**확인** — `make config-test`
저장된 중요도 표의 칸이 17개. **되찾을 수 없는 추천 기록이 0행.**
감점 상한을 0.8 → 0.7로 바꾸면 지문이 달라지고, 같은 입력이면 서버를 새로 띄워도 같은 지문이 나온다.

> ⚠️ **조용히 틀리는 방식**
>
> 지문 계산이 딕셔너리 순회 순서나 소수 표기에 의존하면 같은 설정이 두 지문으로 갈라져 쌍둥이
> 행이 생깁니다 — 키를 정렬하고 소수를 자릿수 고정으로 찍는 지금 방식을 유지하면 막힙니다.
>
> **반대 방향이 훨씬 나쁩니다.** 감점 계수를 바꿨는데 지문이 그대로면, 나중에 과거 요청을
> **새 계수로 '재현'해서 틀린 답을 자신 있게** 내놓습니다. 아무것도 실패하지 않고 숫자만 다릅니다.
>
> 덮어쓰기로 등록하면 같은 지문의 내용이 조용히 바뀌어, 그 지문을 쓰는 **과거 기록 전체**의
> 근거가 소급으로 달라집니다.

## B-12 — 추천 이유 문구 연결

**무엇을** — 문구 템플릿, 조사(을/를) 처리, "어떤 특성이 유난히 두드러지나"를 고르는 계산은
**이미 완성돼 있고 계약 검사가 지켜줍니다.** 남은 건 문구에 끼울 값을 실제 데이터베이스에서
만드는 것뿐입니다 — 지금 가짜 서버에는 '두부', '애호박', '매운맛', '김치볶음밥'이 요청과
상관없이 박혀 있습니다.

후보마다 만들 값: 임박 재료 이름과 남은 날짜 · 부족한 재료 이름 · 유저가 좋아하는 재료 중 이
레시피에 든 것 · 유저의 가장 강한 맛 축 이름 · 재료가 제일 많이 겹친 레시피 제목 · 쓰는 냉장고
재료 · **조리 시간**. 요리 종류와 형태는 데이터가 없어 자동으로 건너뜁니다.

🔴 **지금 발견된 결함** — 조리 시간 문구가 `{cook_minutes}` 값을 요구하는데 가짜 서버에 그 값이
**없습니다.** 문구 만드는 함수가 오류를 삼키고 빈 값을 돌려주기 때문에, **조리 시간 이유는
지금까지 단 한 번도 화면에 나온 적이 없습니다.**

"두드러진 특성"은 그 후보의 값을 **같은 요청의 다른 후보들과 비교해서** 고릅니다.
그래서 후보가 1건뿐이면 비교 대상이 없어 이유가 사전순으로 뽑힙니다.

**확인** — `make reason-test` · 데모 12명 × 20개 = 240개에서
① 폴백 문구('추천 목록에 포함됐어요') 비율이 10% 미만
② 한 특성이 이유의 60%를 넘게 차지하지 않는다 — 예전에 한 특성이 100%를 먹은 적이 있습니다
③ 출력 문자열에 `{` 나 `}` 가 0건
④ 조리 시간이 상위 이유에 든 항목에서 **'분이면 완성' 문구가 실제로 나온다**

**막히면** 아직 없는 특성은 그 이유만 자동으로 건너뛰고 나머지 이유로 문장은 나옵니다.
폴백 비율만 일시적으로 오르니 임계값을 느슨하게 두고 나중에 조입니다.

> ⚠️ **조용히 틀리는 방식**
>
> 문구 만드는 함수가 오류를 삼키는 설계라, 값 하나를 빠뜨리면 그 이유가 **조용히 사라지고
> 기록에도 흔적이 없습니다.** 지금 조리 시간이 정확히 그 상태이고 계약 검사는 전부 통과합니다.
>
> 요청당 값 한 벌을 20개 항목이 나눠 쓰면(지금 가짜 서버가 그렇습니다) 모든 카드에 같은 재료
> 이름이 박혀 **그럴듯하게 전부 틀립니다.** 유저는 '두부'가 실제로 그 레시피에 있는지 확인할
> 방법이 없고, 우리도 기록으로는 못 잡습니다.

## B-13 — 🔴 진짜 점수로 갈아끼우기 — 난수 제거

**무엇을** — `reco/api/serve.py` 로 실서빙 경로를 만듭니다.

```
① 후보 뽑기
② 특성 읽기 → 점수 → 감점 → 내림차순 정렬
③ 이유 문구 만들기 → 섞기 → 기록 쓰기
```

그리고 가짜 서버에서 **난수로 특성을 채우는 for 문과 하드코딩된 레시피 8건을 삭제합니다.**
추적 정보에 박혀 있는 하드코딩 숫자(걸러낸 건수 227,999 등, 점수 중앙값 0.41 등)도 지우고
실측으로 채웁니다 — **셀 수 없는 값은 아예 넣지 않습니다.**

**확인** — `make serve-smoke` 확장
① 같은 요청 2회의 순서와 점수가 **완전히 동일** — 난수 제거 확인
② '완화됨' 표시가 붙은 기록이 0행
③ 추적 정보의 점수 중앙값이 실제 응답 점수의 중앙값과 일치
④ 저장된 후보 수가 50건 이상(후보가 그보다 적으면 후보 전체)
⑤ `git grep -c 'rng.random()' app/main.py` 가 0

> ⚠️ **조용히 틀리는 방식**
>
> 가장 위험한 건 하드코딩 숫자를 안 지우는 것입니다. **레시피가 4만 6천 건인데 "걸러낸 것
> 227,999건"은 나올 수 없는 값**인데, C의 대시보드에 실측처럼 그려지고 에러도 없고 그럴듯해서
> 아무도 의심하지 않습니다.
>
> 가짜 경로를 지우지 않고 새 경로를 **추가만** 하면 C가 어느 쪽을 보는지 모른 채 난수 화면을
> 계속 붙듭니다.
>
> 점수를 새로 계산하면서 옛 점수를 어딘가에 남겨두면 **순서는 새 점수인데 점수 칸은 옛 값**이
> 되어, 기록의 특성과 점수가 서로 모순된 채 쌓입니다. 학습용 라벨을 만들 때에야 드러나고,
> **그때는 그 기간 기록을 통째로 버려야 합니다.**

## B-14 — 폴백 — 추천은 절대 실패하지 않는다

**무엇을** — 후보가 0건일 때(레시피 정리 결과가 아직 없거나, 냉장고가 비었거나, 기본 양념만
있을 때) 부족 재료 허용치를 **요청값 → +1 → +2** 로 완화합니다. 그래도 0이면 인기 상위 N을
돌려줍니다. **'완화됨'으로 표시하고, 실제로 쓴 허용치를 기록에 싣습니다.**

**확인** — `make fallback-test`
냉장고 0건 유저의 요청이 200이고 개수가 요청한 만큼 나온다. '완화됨' 표시가 있고, 기록의 최종
허용치가 요청값보다 크다. 레시피 정리 결과를 0행으로 비워도 500이 안 나고 기록이 남는다.

> ⚠️ **조용히 틀리는 방식**
>
> **폴백이 잘 동작할수록 데이터가 하나도 없어도 화면이 정상으로 보입니다** — 지금 상태가 정확히
> 그렇게 감춰집니다. 완화 비율은 기록 안쪽에 들어 있어서 패널을 안 만들면 아무도 안 봅니다.
> 코드 주석이 **"이 비율이 조용히 오르는 것이 가장 위험한 실패 양상"**이라고 적어둔 그것입니다.
>
> 허용치를 완화해 놓고 기록의 최종값을 안 갱신하면 기록에는 2인데 실제는 4라, 나중에 같은
> 조건으로 재현하면 다른 결과가 나오고 원인을 못 찾습니다.

## B-17 — 온보딩 답변을 DB에 저장

**무엇을** — 약속과 엔드포인트(`POST /v1/onboarding/{user_id}`)는 **이미 만들어져 있습니다.**
남은 건 메모리 딕셔너리를 데이터베이스로 바꾸는 것.

```
user_vector             맛 벡터 · 고른 것 · 척도 답 · 어디서 나왔나
user_preference         맵기/짠맛/단맛 · 가구 인원
user_allergy            🔴 심각도를 'allergy' 로 명시한다
user_ingredient_pref    기피 재료 −0.8 · 출처 'onboarding'
```

**확인** — 요청 후 네 표에 행이 생기고, 알러지 심각도가 전부 `'allergy'`.
알러지 확장 함수가 **같은 그룹까지 퍼뜨린** 목록을 돌려준다.

> ⚠️ **조용히 틀리는 방식**
>
> 심각도를 생략하면 기본값 `'avoid'` 가 들어갑니다. 그러면 직접 적은 재료는 막히는데 **같은
> 알러젠 그룹으로 퍼지지 않습니다** — 아몬드만 등록한 사람에게 호두·잣이 그대로 추천됩니다.
> 에러가 안 나고 본인이 적은 재료는 잘 걸러지므로 **겉보기엔 정상입니다.**

## B-18 — 냉장고 수정을 DB에 저장

**무엇을** — 냉장고 갱신 API(`PUT /v1/users/{id}/pantry`)가 지금 **메모리에만 씁니다.**
중요도 0.15짜리 임박 재료 특성이 유통기한을 읽는데 **저장 경로가 없습니다.**

🔴 '뺐다'는 **행 삭제가 아니라 표시**입니다 — 뺀 시각과 이유를 남깁니다.
행을 지우면 **"무엇을 얼마나 버렸나"를 영원히 못 셉니다.**

**확인** — 갱신 후 안 뺀 재료 수가 보낸 개수와 같고, 뺀 것은 이유가 채워진 채 남아 있다.
냉장고 재료 조회 함수가 뺀 것을 빼고 돌려준다.

> ⚠️ **조용히 틀리는 방식**
>
> 유통기한을 안 받거나 비워 두면 **임박 재료 특성이 항상 0**이 됩니다. 중요도 0.15가 통째로
> 죽는데 **점수는 정상 범위로 나옵니다.** 유저가 직접 적은 날짜인지 시스템이 추정한 날짜인지도
> 구분해서 저장해야 합니다.

## B-16 — 🔴 백엔드와 주고받을 것 확정 · 전달

**무엇을** — **AI 파트는 프론트와 직접 말하지 않습니다. 백엔드가 유일한 호출자입니다.**
주고받는 것을 문서로 확정해 백엔드 담당자에게 넘깁니다.

```
① 백엔드 → 우리   추천 요청     유저 · 개수 · 부족 재료 허용치 · 최대 시간
② 우리 → 백엔드   추천 20건     레시피 · 부족 재료 · 이유 · 🔴 요청 번호 · 몇 번째 자리
③ 백엔드 → 우리   행동 이벤트   🔴 요청 번호와 자리를 그대로 되실어 보내야 한다
```

**확인** — `docs/draft/18_기능명세_AI파트.tsv` 의 해당 행이 위 세 줄을 담고 있고,
**백엔드 담당자가 읽었다고 회신한 기록**이 있다.

**막히면** 회신이 늦어도 우리 쪽 구현은 진행합니다. 다만 ③이 안 지켜지면 학습이 불가능하므로
**늦어도 유저 모집 전에는** 확인해야 합니다.

> ⚠️ **조용히 틀리는 방식**
>
> 백엔드가 요청 번호를 안 실어 보내도 이벤트 API는 **200을 돌려줍니다** — 약속상 선택 항목이기
> 때문입니다. ⚠️ **응답은 200 이지만 그 행은 저장되지 않습니다** — "거부됨"으로
> 세어질 뿐입니다. 추천과 이을 수 없어서
> **"3번째로 보여준 그것을 눌렀다"가 복원되지 않습니다.**
>
> 클릭 기록만 남으면 우리가 위에 올린 것이 인기 있어서 눌린 것처럼 보이고, 그 데이터로 학습한
> 모델은 자기 편향을 재생산합니다. **몇 주 뒤에야 드러납니다.**

## B-15 — 전체 검증 + A 데이터로 갈아끼우기

**무엇을** — A가 레시피 정리 결과를 채우면 **개발용 가짜 데이터를 끄고 같은 명령을 전부 다시
돌립니다.** `app/services/eval/feature_report.py` 를 만들어 특성별 (최솟값·중앙값·최댓값·'못 잼' 비율)과
레시피 매칭률 분포를 뽑아 C에게 넘깁니다.

**확인** — 한 줄로

```
make validate contract normalize-test ddl-test smoke smoke-py log-test \
     feature-test scorer-test config-test reason-test serve-smoke fallback-test
```

이 전부 통과하고, 추천 기록이 100행을 넘고, 기록에 쓰인 **설정 지문이 한 종류**이고,
중요도가 0보다 큰 10가지 중 '못 잼' 비율 100%인 것이 **요리 종류 하나뿐**이다.

**막히면** 가짜 데이터 위에서 전 명령을 먼저 통과시켜 둡니다. **코드 변경은 0줄**입니다.

> ⚠️ **조용히 틀리는 방식**
>
> **가짜 데이터에서 통과한 검증이 실데이터에서 조용히 무의미해집니다.** 가짜 데이터는 재료
> 5~10개짜리 깨끗한 레시피지만, 실데이터에는 재료가 제대로 안 붙은 레시피가 섞입니다.
> "붙은 재료가 1개 이상이면 통과"라는 관문은 **전멸만 잡고 12개 중 11개가 안 붙어도 통과시킵니다.**
>
> 그러면 재료 충족도가 1.0인데 실제로는 재료 대부분이 안 붙은 레시피가 상위를 채우고, 모든 지표가
> 정상으로 보입니다. **매칭률을 리포트에 반드시 넣어야 이게 보입니다.**

---

## 마지막으로 — 셋만 기억하면 됩니다

**1. '못 쟀다'를 0으로 바꾸지 않는다.**
이 문서의 조용한 실패 중 절반이 이겁니다. 0은 "쟀더니 0"이라는 **다른 뜻**입니다.

**2. `app/schemas/` 는 읽기 전용이다.**
기본 중요도 표를 고치면 셋 다 영향받고, 과거 기록의 근거가 두 벌이 됩니다.

**3. "됐습니다"가 아니라 명령으로 증명한다.**
이 문서의 경고는 전부 **에러가 안 나는데 틀린** 경우입니다. 화면상으로는 정상입니다.
