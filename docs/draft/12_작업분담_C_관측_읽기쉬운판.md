# 트랙 C — 화면과 평가 도구

> 작성자: 박재우 · 작성일: 2026-09-03

우리는 **냉장고에 있는 재료로 만들 수 있는 요리**를 추천하는 시스템을 만들고 있습니다.
AI 부분은 세 사람이 나눠서 동시에 만듭니다.

- **A** — 레시피 4만 6천 건의 재료를 정리해서, 추천이 실제로 돌아가게 만듭니다.
- **B** — 지금 아무 숫자나 내놓는 점수를 진짜 점수로 바꿉니다.
- **C** — **결과를 눈으로 볼 수 있게 하고, 잘 되고 있는지 재는 도구를 만듭니다.** ← 이 문서

분량은 **10.5일**, 남은 기간은 15영업일입니다.
셋 중 **가장 큰 덩어리이고, 아직 한 줄도 없습니다.**

---

## 무엇을 만드나

### 화면 4종

| 화면 | 무엇을 보는 곳 |
|---|---|
| **홈** | 지금 무엇이 살아 있나 — API·DB 상태, 주요 표의 행 수, 그리고 **지금 보고 있는 게 진짜 엔진인지 가짜 서버인지** |
| **추천 디버거** | 이 사람에게 왜 이게 추천됐나 — 점수가 무엇으로 이뤄졌고, 후보가 어디서 걸러졌나 |
| **검수 큐** | 사람이 재료명을 판단하는 곳 — "다진마늘"은 "마늘"인가? |
| **데이터 품질** | 데이터가 얼마나 채워졌나, 기록이 새고 있지 않나 |

`make up-all` 한 줄로 이 넷이 다 떠야 합니다.

### 평가 도구 — 추천이 좋아졌는지 재는 자

세 가지를 잽니다. K는 "상위 몇 개까지 볼 것인가"이고, 보통 10입니다.

| 이름 | 무엇을 재나 |
|---|---|
| **nDCG@K** | 좋아할 만한 것이 **위쪽에** 있나. 같은 것을 추천해도 아래에 놓으면 점수가 깎입니다. |
| **Recall@K** | 좋아할 만한 것 **전체 중 몇 개를** 상위 K개 안에 담았나. |
| **IPS** | 과거 추천 기록만 가지고 **"다르게 추천했으면 더 나았을까"** 를 되짚어보는 계산. |

**이 자가 틀리면 앞으로 하는 모든 실험이 무의미해집니다.**
그래서 C-2가 이 트랙에서 제일 중요한 작업입니다.

---

## 지금 어디까지 와 있나

비어 있는 것들입니다.

```
infra/app/ 폴더          없음    ← make up-all 이 지금 빌드 단계에서 실패한다
평가 코드             없음    ← app/services/eval/ 에 threshold.py 하나뿐
검수 큐               0행     ← 여기에 넣는 코드가 리포지토리 전체에 없다
데이터 품질 기록      0행     ← 마찬가지로 쓰는 코드가 없다
추천 기록             0행     ← B가 연결해야 채워진다
재료 정리 결과        0행     ← A의 배치가 채운다
```

하나 더 있습니다. 로그 쓰기가 실패한 횟수를 세는 카운터가 `app/core/counters.py` 에 있는데,
**그 값을 읽는 코드가 하나도 없습니다.** 세어만 놓고 아무도 안 봅니다.

도구도 안 깔려 있습니다 — 지금 `.venv` 에는 streamlit·scikit-learn·pandas가 없습니다.

### 그래도 아무도 기다릴 필요가 없습니다

가짜 서버(`app/main.py`)가 이미 **진짜와 같은 모양의 응답**을 돌려줍니다.
특성값 17종, 점수, 최종 순위, 추천 이유, 후보가 단계별로 얼마나 걸러졌는지까지 전부 채워서 옵니다.
평가 도구 쪽도 합성 데이터만으로 전부 검증됩니다.

그래서 **화면도 평가 도구도 지금 전부 만들 수 있습니다.**
B가 실제 기록을 연결하는 날, `make eval-smoke` 한 줄만 추가로 통과시키면 끝납니다.

---

## 순서

| | 작업 | 기간 | 먼저 끝나야 하는 것 |
|---|---|---:|---|
| C-1 | 개발 환경 개통 — `make up-all` 이 실제로 뜬다 | 0.5일 | 없음 |
| C-2 | nDCG·Recall 정답 고정 + 틀린 구현을 잡는 테스트 | 1일 | 없음 |
| C-3 | 성질 검사 — 누출 검사와 바깥 기준 대조 | 0.5일 | C-2 |
| C-4 | 나누기·신뢰구간·검출력 — 매 실행마다 찍는다 | 0.5일 | C-2 |
| C-5 | IPS — 지금은 쓸 수 없다는 것을 숫자로 돌려준다 | 0.5일 | C-2 |
| C-6 | 추천 디버거 — 가짜 서버에 바로 붙인다 | 1일 | C-1 |
| C-7 | 후보가 어디서 줄었나 + 가중치 바꿔보기 | 1일 | C-6 |
| C-8 | 🔴 검수 큐 채우기 — 0행을 미리 채운다 | 0.5일 | 없음 |
| C-9 | 🔴 검수 큐 화면 — 후보가 버튼으로 놓여 있다 | 1일 | C-1, C-8 |
| C-10 | 큐 → 사전 왕복 — 사전이 둘로 갈라지는 것을 막는다 | 0.5일 | C-9 |
| C-11 | 기록 → 평가용 표 변환 + 합성 기록 만들기 | 1일 | 🔴 실데이터는 B가 연결해야 |
| C-12 | 데이터 품질 기록기 + 🔴 실패 카운터를 시계열로 | 0.5일 | 없음 |
| C-13 | 데이터 품질 화면 + 실패 카운터 노출 | 1일 | C-1, C-12 |
| C-14 | 처음부터 끝까지 다시 밟기 | 1일 | C-7, C-9, C-13 |

**첫날 오전에 C-1을 끝냅니다.** 트랙 C 전체가 여기에 걸려 있습니다.
**C-2는 DB도 필요 없는 순수 파이썬**이라 바로 시작할 수 있습니다. 1주차 최우선입니다.
**C-8을 C-9보다 먼저** 끝냅니다. 화면부터 만들면 빈 큐가 떠서, 반나절이면 될 일이 3일이 됩니다.

---

## C-1 — 개발 환경 개통 · 0.5일

**무엇을**

`infra/docker-compose.yml`(루트가 아닙니다)이 이미 API와 화면 컨테이너를 선언해 두었고,
`infra/app/Dockerfile.api`·`infra/app/Dockerfile.dashboard` 를 빌드 대상으로 지정해 놨습니다.
그런데 **`infra/app/` 폴더 자체가 없어서 `make up-all` 이 지금 빌드에서 실패합니다.**
화면 도구는 Streamlit으로 확정입니다(포트 8501, 코드 주석에도 그렇게 적혀 있습니다).

만들 것:

- `infra/app/Dockerfile.api` — 기본은 가짜 서버를 띄우고, B가 완성하면 환경변수 하나로 진짜 서버로 바꿉니다. 워커는 **1개로 못 박습니다**(이유는 C-13에).
- `infra/app/Dockerfile.dashboard`, `infra/app/requirements.dashboard.txt`
- `infra/app/Home.py` — API 상태 · DB 상태 · 주요 표의 행 수 · **지금 가짜 서버인지 진짜 엔진인지 배지**
- `.dockerignore` — 빌드 대상 폴더가 상위 폴더라서, 없으면 `.venv` 와 원본 데이터가 통째로 딸려갑니다
- `Makefile` 에 `up-app`·`app-test` 추가

**완료 기준**

`make down-v && make bootstrap && make up-all` 후
`curl -fsS localhost:8000/health` 와 `curl -fsS localhost:8501/_stcore/health` 둘 다 성공.

**막히면** 해당 없음. 이게 트랙 C 전체의 시작점이므로 첫날 오전에 끝냅니다.

> ⚠️ **조용히 틀리는 방식**
>
> Dockerfile이 기본으로 가짜 서버를 띄우는데 화면에 표시가 없으면
> **난수 점수를 진짜 추천이라고 시연하게 됩니다.** 가짜 서버는 무작위 값으로
> 점수를 채우기 때문에 그럴듯한 숫자가 나오고, 아무도 눈치채지 못합니다.
> 홈 화면에 모델 버전과 데이터 출처 배지를 박아야 합니다.
> 곁다리로, `.dockerignore` 가 없으면 수백 MB가 빌드에 끌려 들어가서
> 에러 없이 빌드만 몇 분씩 길어집니다.

---

## C-2 — nDCG·Recall 정답 고정 + 틀린 구현을 잡는 테스트 · 1일

**무엇을**

`app/services/eval/metrics.py` 를 새로 만듭니다 — nDCG, Recall, MAP, MRR, 쌍 비교 정확도.

라벨을 어떻게 다룰지 **첫 커밋에 못 박습니다.**
nDCG는 음수 라벨을 0으로 깎아서 쓰고, 쌍 비교는 원래 값 그대로 씁니다.
별점을 라벨로 바꾸는 것은 반드시 기존 변환 함수 하나만 거칩니다 —
따로 상수 표를 만들면 **별점 5와 별점 1이 둘 다 0이 되는** 사고가 납니다.

그리고 정답 숫자를 **손으로 박습니다.** 같은 파일에 **고의로 틀린 구현**을 나란히 둡니다.

| | 제대로 만든 것 | 흔히 저지르는 실수 |
|---|---|---|
| nDCG@10 | 0.319147 | 0.501266 |
| Recall@10 | 0.583333 | 0.700000 |

nDCG 쪽 실수는 **0.18만큼 후하게** 나옵니다. 값이 0~1 안에 얌전히 들어 있어서
눈으로는 절대 구분할 수 없습니다. 그래서 테스트가 이 틀린 구현을
**실제로 잡아내는지까지** 확인합니다.

성질 검사 세 가지도 함께 넣습니다.

1. 점수가 같은 것끼리 자리를 바꿔도 값이 변하지 않는다
2. 상위 K 밖에 좋아할 만한 것을 하나 더 붙이면 nDCG@K가 **반드시 내려간다**
3. K 안에서 좋은 것을 위로 올릴 때만 값이 올라간다

**완료 기준**

`make eval-test` 가 위 네 숫자와 성질 세 가지를 전부 확인.

**막히면** 해당 없음. 추천 기록이 0행인 것과 무관하므로 1주차 최우선입니다.

> ⚠️ **조용히 틀리는 방식**
>
> 정답 숫자를 구현으로 만들어내면 **자기가 자기를 채점하는 꼴**이 됩니다 —
> 버그가 들어오면 기대값도 같이 틀려서 초록불이 뜹니다. 그래서 손으로 옮겨 적고,
> 틀린 구현을 옆에 둡니다.
>
> 쓸모없는 테스트도 두 종류 있습니다. 로그의 밑을 2에서 e로 잘못 쓰는 실수는
> 분자와 분모에서 상쇄되어 값이 사실상 안 바뀝니다(차이가 소수점 열여섯째 자리).
> 그걸 잡겠다는 테스트는 **항상 통과하는 빈 테스트**입니다.
> "상관없는 항목을 추가해도 값이 안 변한다"도 정답판과 버그판 둘 다 변화가 0이라
> 아무것도 판별하지 못합니다. 반드시 위 2번(K 밖에 관련 있는 것 추가)이어야 합니다.

---

## C-3 — 성질 검사 · 0.5일

**무엇을**

성질 검사 다섯 가지 중 남은 둘입니다.

- **누출 검사** — 학습용과 평가용에 **같은 사용자가 겹치지 않는지.**
  (요청 번호가 겹치는지는 정의상 항상 안 겹치므로 검사하지 않습니다.)
- **바깥 기준 대조** — 널리 쓰이는 계산 도구(scikit-learn)와 값이 일치하는지.
  단 조건이 맞을 때만 비교합니다 — 라벨이 0/1이고, 그룹이 하나이고, K가 전체 길이일 때.

`scikit-learn` 을 `.venv` 와 `infra/app/requirements.dashboard.txt` 에 추가합니다.

**완료 기준**

`make eval-test` 에 대조가 포함된 채로 통과.
그리고 scikit-learn이 없는 환경에서 한 번 돌려서,
**건너뛰는 게 아니라 실패하는지** 확인합니다.

**막히면** 해당 없음.

> ⚠️ **조용히 틀리는 방식**
>
> import 실패를 조용히 넘기거나 테스트를 건너뛰게 만들면,
> **바깥 기준이 영원히 안 도는데 화면은 초록입니다.**
> 우리 계산이 맞는지 확인해 줄 유일한 외부 잣대가 조용히 죽습니다.
>
> 반대로 비교 조건을 넓히면(그룹이 여럿이거나 K가 전체보다 작으면)
> 그 도구의 평균 내는 방식이 우리 정의와 달라서
> **우리가 맞는데 테스트가 틀립니다.** 조건을 함수 정의에 박고, 벗어나면 거부합니다.

---

## C-4 — 나누기·신뢰구간·검출력 · 0.5일

**무엇을**

`app/services/eval/split.py` 를 새로 만듭니다.

- **나누는 방식 두 가지** — 시간으로 자르기, 사람으로 자르기
- **신뢰구간** — **사람 단위로** 복원추출하고, 뽑힌 사람의 요청은 통째로 가져갑니다
- **검출력** — "얼마나 큰 차이라야 알아볼 수 있나"를 매 실행마다 찍습니다.
  가정하지 말고, 그때그때 실제로 잰 흩어짐(sd)과 상관(rho)으로 계산합니다.

  ```
  MDE = 2.8016 × sd × √(2×(1−rho)) / √n
  ```

시간으로 자른 결과에는 코드가 **주의 문구를 강제로 붙이게** 합니다(아래 참조).

**완료 기준**

`make eval-test` 가 다음을 확인 —
사람으로 자르면 양쪽에 겹치는 사람이 없다 /
시간으로 자르면 평가 데이터가 전부 학습 데이터보다 뒤다 /
`sd=0.30, rho=0.7, n=200` 일 때 값이 `0.046` ±0.001 /
실행 출력에 sd·rho·MDE 세 값이 실제로 찍힌다.

**막히면** 해당 없음 — 합성 데이터로 전부 검증됩니다.

> ⚠️ **조용히 틀리는 방식**
>
> 요청 하나하나를 단위로 복원추출하면, 같은 사람의 요청들은 같은 냉장고와
> 같은 취향을 공유해서 서로 독립이 아닌데도 **신뢰구간이 좁게 나옵니다.**
> 자릿수가 그럴듯해서 "차이가 의미 있다"는 결론이 조용히 뒤집힙니다.
>
> 그리고 시간으로 자르는 것은 지금 실사용자 기록이 한 주에만 몰려 있어서,
> 자르는 선이 **시간에 따른 변화가 아니라 가입 시기 차이**를 자릅니다.
> 검사는 통과하지만 그 검사가 보장하는 게 없습니다.
> 주의 문구 없이 대표 숫자로 쓰면 틀린 결론이 리포트에 그대로 남습니다.

---

## C-5 — IPS · 0.5일

**무엇을**

`app/services/eval/ope.py` 를 새로 만듭니다.
IPS는 과거 기록으로 "다르게 추천했으면 어땠을까"를 되짚는 계산인데,
**결론부터 말하면 지금은 못 씁니다.**
이 작업의 목적은 **못 쓴다는 것을 숫자로 돌려주는 것**입니다.

돌려줄 것: 추정값, 실제로 쓸 수 있는 표본 수, 가중치가 얼마나 흩어졌나,
잘라낸 건수와 비중, 신뢰구간 폭, 그리고 **쓸 수 있나 없나.**

쓸 수 있다고 판정하는 기준은 **실제로 쓸 수 있는 표본 1,800건 이상**입니다.
이건 "클릭률 10%에서 2%p 차이를 알아보려면"이라는 조건에서 계산해 나온 값입니다 —
눈대중으로 잡은 200 같은 숫자가 아닙니다.

추천 20칸 중 **18칸은 탐색이 아니라 확정적으로** 채워집니다.
그 칸들은 뽑힐 확률이 1.0이고, 이걸 계산에 그대로 넣으면 가중치가 전부 1이 됩니다.
그래서 확정 칸은 가중에서 빼고 따로 셉니다.
확률이 0인 건이 섞여 들어오면 계산 자체가 성립하지 않으므로 에러를 냅니다.

**완료 기준**

`make eval-test` 가 다음을 확인 —
확률 0이 섞이면 에러 /
탐색이 하나도 없는 합성 기록에서 "쓸 수 있는 표본 수 = 전체 건수"로 무너지는 것 /
표본이 모자라도 **추정값 자체는 그대로 돌려준다** — 예외를 던져서 리포트를 막지 않습니다.

**막히면** 기다리지 않습니다. 합성 기록만으로 완성됩니다.
실제 기록은 지금 0행이고, 몇 주 뒤에도 기준을 못 넘는 것이 예상되는 결과입니다.

> ⚠️ **조용히 틀리는 방식**
>
> 확정 칸의 확률 1.0을 그대로 넣으면 가중치가 전부 1이 되어 **그냥 산술평균**이 나옵니다.
> 숫자도 나오고 범위도 그럴듯해서 "되짚어봤다"고 착각하지만,
> 실제로는 **지금 방식의 클릭률을 다시 잰 것**일 뿐입니다.
> 코드에 이미 적혀 있는 경고 — "편향이 없다는 뜻이 아니라
> 이 기록으로는 보정할 수 없다는 뜻" — 이 사라지는 지점이 정확히 여기입니다.

---

## C-6 — 추천 디버거 · 1일

**무엇을**

**가짜 서버로 지금 전부 붙습니다. B를 기다릴 이유가 하나도 없습니다.**
가짜 서버가 이미 특성 17종(그중 일부는 "계산 불가" — 지금 3종), 점수, 최종 순위, 추천 이유,
뽑힐 확률, 탐색 여부, 클러스터, 빠진 재료 수, 커버리지를 다 채워서 돌려줍니다.
가중치도 실제로 적용된 값으로 함께 옵니다.

`infra/app/pages/1_추천디버거.py` 를 만듭니다.
입력 폼(사용자·개수·허용할 빠진 재료 수·최대 조리시간·모델 버전 등)으로 추천을 요청하고,
목록은 **반드시 최종 순위 그대로** 그립니다.

항목마다 보여줄 것 — 특성 17종의 원래 값, 각 특성이 점수에 얼마나 기여했는지 막대,
눈에 띄는 정도 막대, 추천 이유, 뽑힐 확률과 탐색 출처 배지.

**"계산 못 함"과 "계산했더니 0"은 절대 같게 그리지 않습니다.**
앞의 것은 회색 "계산불가" 배지, 뒤의 것은 숫자 0.0입니다.

**완료 기준**

`make app-test` — 특성 행이 17개 / "계산불가" 배지가 **값이 비어 있는 항목 전부에**
붙는다(개수를 고정하지 않습니다 — 지금은 3개, 나중에 5개가 됩니다) /
표시 순서가 응답의 최종 순위와 같다 /
기여 막대 개수가 가중치 0보다 큰 특성의 개수와 같다.

**막히면** 컨테이너가 안 뜨면 `make mock` 과 로컬 streamlit으로 같은 코드를 붙입니다.
트랙 B 의존은 처음부터 없습니다.

> ⚠️ **조용히 틀리는 방식**
>
> 기여도 계산 코드가 **"계산 못 함"을 0.0으로 접어버립니다.**
> 막대만 그리면 "계산 못 함"과 "계산했더니 0"이 똑같이 높이 0인 막대가 되어,
> 둘을 굳이 구분해 둔 이유가 화면에서 소멸합니다.
> 나중에 그 특성을 켜도, 과거 화면 판독이 틀렸다는 걸 아무도 모릅니다.
>
> 그리고 목록을 **점수 순으로 정렬하면** 지금은 가짜 서버가 점수 순 = 최종 순위라서
> **증상이 안 보입니다.** 그런데 B가 다양성 조정과 탐색 칸을 넣는 순간부터
> 조용히 틀린 순서를 보여주게 됩니다.

---

## C-7 — 후보가 어디서 줄었나 + 가중치 바꿔보기 · 1일

**무엇을**

같은 페이지에 **후보가 단계마다 얼마나 줄었는지**를 그립니다.
가짜 서버가 실제로 내놓는 값은 이렇습니다.

```
검색      231,042 → 487
거르기    겹치는 재료 없음 227,999 · 알레르기 12 · 빠진 재료 초과 1,502 · 조리시간 초과 1,042
점수      487 → 487
재정렬    487 → 최종 개수     빠진 것: 다양성 31 · 최근 본 것 8 · 카테고리 상한 5
```

쌓아 올린 막대로 그리고, **맞아떨어지지 않는 나머지를 담을 칸을 따로 둡니다**
(들어온 수 − 거른 합 − 나간 수).
추적 정보에 꼭 있어야 할 값 10종이 다 있는지 검사해서, 빠지면 빨간 배지를 띄웁니다.

그리고 특성 가중치 슬라이더를 붙여서, 값을 바꿔 다시 요청하고
**원래 순위와 바뀐 순위를 나란히 비교**합니다. 어떤 특성이 순위를 얼마나 움직이는지 보는 것입니다.

**완료 기준**

`make app-test` — 231,042 − (227,999+12+1,502+1,042) = 487 이고 나머지 칸이 0을 표시한다 /
필수 값 하나를 지운 추적을 넣으면 경고가 뜬다 /
`f_coverage` 가중치를 0으로 두고 다시 요청하면 순위 이동이 1건 이상 생긴다.

**막히면** 해당 없음.

> ⚠️ **조용히 틀리는 방식**
>
> 합이 안 맞아도 **그림은 그냥 그려집니다.**
> 가짜 서버는 합이 딱 맞지만, 진짜 엔진은 한 레시피가 알레르기와 빠진 재료 양쪽에
> 걸려서 두 번 세어질 수 있습니다. 그러면 화면은 "후보가 어디서 죽었는지"를
> 틀리게 말하면서도 멀쩡해 보이고, 그걸 믿고 엉뚱한 필터를 손보게 됩니다.
> 나머지를 항상 별도 칸으로 드러내야 이게 보입니다.

---

## C-8 — 🔴 검수 큐 채우기 · 0.5일

> ✅ **이미 정해졌습니다 — 후보를 적는 형식은 C가 정합니다.** 화면이 읽으니까요.
>
> A는 `[[재료명, 유사도, 사유]]` 배열로, C는
> `{candidates:[{name,score,method}], prefill, blocked_by}` 객체로 적어 놨었습니다.
> **객체 형식으로 통일합니다.** 안 그러면 채우는 쪽과 읽는 쪽이 서로 못 읽습니다.
> 정했으니 A에게 알립니다.

**무엇을**

검수 큐 표가 0행인데, **여기에 넣는 코드가 리포지토리 전체에 없습니다.**
인덱스는 이미 만들어져 있습니다. 화면부터 만들면 빈 큐가 뜹니다.

`scripts/bench/queue_seed.py` 를 새로 만듭니다.
아직 사전에 매칭 안 된 표현 목록(`scripts/bench/out/unmatched.tsv`, 24,701행)을 읽어,
기존 검수 시트를 만드는 코드와 **똑같은 방식으로** 후보를 3개까지 뽑아 넣습니다.

같은 표현이 이미 있으면 **빈도와 후보만 갱신**하고,
**사람이 내린 판단(결정·상태·연결한 재료)은 절대 덮지 않습니다.**

**완료 기준**

`.venv/bin/python scripts/bench/queue_seed.py --top 2000` 을 **두 번** 돌려도 여전히 2,000행
(= 몇 번 돌려도 안전) / 후보가 하나 이상 붙은 행이 1,500행 이상.

**막히면** 해당 없음. 화면(C-9)보다 먼저 끝내야
"큐가 비어서 반나절이 3일 되는" 사고를 막습니다.

> ⚠️ **조용히 틀리는 방식**
>
> 갱신 범위를 넓게 잡아 상태와 결정까지 덮으면,
> **다시 채울 때 사람이 끝낸 검수가 조용히 되돌아갑니다.**
> 에러 없이 큐만 다시 길어지고, 검수자는 이미 판단한 항목을 또 보게 됩니다.
>
> 두 번째: 채우는 시점의 사전으로 후보를 굳혀 놓으면, A가 나중에 별칭을 추가해도
> **이미 매칭되는 표현이 큐에 계속 남습니다.**
> 큐 길이가 실제 남은 양보다 크게 나오고 진척률이 틀립니다.
> 다시 채울 때 매칭된 항목을 건너뜀 상태로 내리는 경로가 필요합니다.

---

## C-9 — 🔴 검수 큐 화면 · 1일

**무엇을**

`infra/app/pages/3_검수큐.py`. 대기 중인 것을 **빈도가 높은 순으로** 보여줍니다
(이미 있는 인덱스를 그대로 탑니다).

한 줄에 — 표현 + 빈도 + 누적 비율 + **후보 3개가 눌리는 버튼**
+ [재료 아님] + [신규] + [보류] + 자유입력(사전 536종 자동완성, DB에서 직접 읽습니다).

버튼 한 번 누르면 **트랜잭션 한 번으로** 큐 행을 확정하고
누가 언제 무엇을 무엇으로 바꿨는지 감사 기록을 남깁니다. 되돌리기도 감사 기록으로 합니다.

**기존 TSV 시트와의 관계 — 대체가 아니라 "입력하는 자리"만 가져오는 것입니다.**
시트 내보내기는 스냅샷으로 계속 남깁니다.
화면이 나은 점은 감사 기록·담당자 표시·공유 파일 충돌이 없다는 것이고,
시트가 나은 점은 대량 일괄 편집입니다. 그래서 둘 다 남깁니다.

**완료 기준**

`make app-test` — 20행을 넣고 확정을 20번 호출 →
확정 20행 + 감사 기록 20행 + 양쪽의 "연결한 재료"가 전건 일치.
같은 행을 두 번 확정하면 감사 기록은 2행, 큐는 1행(마지막 값).

**막히면** C-8이 늦어지면 화면 코드는 그대로 두고 데이터만
이미 있는 `scripts/bench/out/review_sheet.tsv`(2,007행)에서 읽습니다 —
읽는 모양이 (표현, 빈도, 후보) 셋뿐이라 바꾸는 비용이 거의 0입니다.

> ⚠️ **조용히 틀리는 방식**
>
> 데이터베이스 구조가 "연결한 재료"를 비워둘 수 있게 되어 있습니다.
> 그래서 사전에 없는 이름을 자유입력으로 받으면
> **"이 재료로 연결"이라고 적혀 있는데 연결된 재료는 비어 있는 채로 '완료'** 가 됩니다.
> 큐 길이는 줄어서 진척처럼 보이는데, 실제로 반영될 게 없는 행이 쌓입니다 —
> 나중에 사전에 반영하는 단계에서 "사전에 없는 재료명" 오류가 날 때야 발견됩니다.
> 앱에서 이 조합을 막고, 화면에 "반영 대기 N건"을 따로 세어 보여줍니다.

---

## C-10 — 큐 → 사전 왕복 · 0.5일

**무엇을**

`scripts/bench/queue_export.py` 를 새로 만듭니다.
확정된 행을 **기존 검수 시트와 완전히 같은 컬럼**(순위·빈도·누적%·표현·결정·후보1~3·비고)으로
내보냅니다. 그러면 기존 `make review-apply WRITE=1` 이 그대로 받아먹고,
`seeds/` 에 쓰는 경로는 **여전히 `scripts/bench/review_apply.py` 하나뿐**입니다.
**화면은 `seeds/` 를 절대 직접 건드리지 않습니다.**

**완료 기준**

왕복 한 번이 한 줄로 통과합니다.

```bash
python scripts/bench/queue_seed.py --top 20 \
 && psql -c "UPDATE normalization_queue SET action='map', resolved_id=(SELECT id FROM ingredient LIMIT 1), status='resolved'" \
 && python scripts/bench/queue_export.py \
 && python scripts/bench/review_apply.py --write \
 && make validate
```

그리고 새로 만든 별칭이 사전에 실제로 들어왔는지 파이썬으로 한 번 확인합니다.

**막히면** 해당 없음.

> ⚠️ **조용히 틀리는 방식**
>
> 화면이 편하다고 `seeds/ingredient_alias.csv` 에 직접 쓰기 시작하면
> **사전이 둘이 됩니다.** DB판과 파일판이 갈라지고,
> "커버리지 80.8%"라는 숫자가 어느 쪽에 물었느냐에 따라 달라집니다.
> **에러는 한 번도 안 납니다.**
> 이 프로젝트에서 이미 세 번 일어난 조용한 유실과 정확히 같은 모양입니다.
>
> 곁다리: 검수 시트의 헤더가 바뀌면 내보내기가 컬럼이 어긋난 채 통과하므로,
> 헤더 문자열 자체를 검사합니다.

---

## C-11 — 기록 → 평가용 표 변환 + 합성 기록 만들기 · 1일

**무엇을**

`app/services/eval/loader.py` 를 새로 만듭니다.
추천 기록과 이벤트 기록을 평가에 쓸 수 있는 표 모양
(요청 번호, 사용자, 추천 순서, 라벨, 시각, 서빙 모드)으로 바꿉니다.

**"어디까지를 분모로 볼 것인가"를 두 가지로 나눠 둡니다** —
실제로 화면에 보인 20개인가, 기록에 남은 50개인가.

그리고 `app/services/eval/fixtures.py` —
**B의 기록 저장 코드가 실제로 쓰는 JSON 모양 그대로** 만드는 합성 기록 생성기입니다.
이 픽스처가 **B의 작업을 받아들이는 테스트도 겸합니다.**

**완료 기준**

`make eval-test` — 합성 기록으로 만든 표에 C-2의 지표를 적용한 nDCG가
손계산 값과 소수점 아홉째 자리까지 일치.

그리고 `make eval-smoke` 를 미리 추가해 둡니다.
추천 기록이 0행이면 **"실로그 없음 — 합성만 검증됨"을 명시적으로 출력하고 정상 종료**,
1행이라도 있으면 실제 행으로 같은 경로를 통과시킵니다.

**먼저 끝나야 하는 것** 🔴 **실데이터는 B가 기록을 실서빙에 연결해야 합니다.**
지금 추천 기록 0행 · 이벤트 기록 0행 · 사용자 0행이고,
기록 저장 함수를 부르는 코드가 **하나도 없습니다.**
사용자가 없으면 기록을 한 줄도 못 넣습니다 — 기록이 사용자를 참조하게 되어 있어서입니다.

**막히면** **기다리지 않습니다.**
합성 픽스처 경로만으로 변환기·지표·성질 검사가 전부 돕니다.
픽스처 모양을 B에게 먼저 넘겨 저장 코드와 대조시켜 두면,
B가 연결하는 날 `make eval-smoke` 가 저절로 초록이 됩니다.

> ⚠️ **조용히 틀리는 방식**
>
> 기록에 남기는 후보 수는 **잘려서** 저장됩니다 —
> 실서빙은 50개, 시뮬레이션은 10개, 부하 테스트는 0개.
> 서빙 모드를 안 보고 전부 50개라고 가정하면, 시뮬레이션 기록에서
> 분모가 실제로는 10인데 50인 줄 알고 **Recall이 5배로 나옵니다.**
> 값이 0~1 범위 안에 있어서 그럴듯하고, 두 종류가 섞이면 평균만 이상해져서
> 원인을 못 찾습니다. 반드시 서빙 모드를 함께 읽고, 안 맞으면 거부합니다.

---

## C-12 — 데이터 품질 기록기 + 🔴 실패 카운터를 시계열로 · 0.5일

> ✅ **이미 정해졌습니다 — 데이터 품질 기록 표는 C 전담입니다.**
> A는 배치 실행 기록 표에 씁니다. 다만 A도 이 표에 배치 결과를 넣으므로
> **칸별로 주인을 나눠 둡니다.**
>
> | 칸 | 누가 | 뜻 |
> |---|---|---|
> | 레시피 수 · 언급 수 · 매칭 방법 분포 | **A** | 배치가 실제로 처리한 결과 |
> | 커버리지 · 종류 커버리지 | **C** | 파일에서 잰 값 (배치 전에도 잽니다) |
> | 로그 실패 카운터 | **C** | 로그 쓰기 실패 계측 |
>
> ⚠️ **"언급 수"의 정의가 서로 다르면 시계열이 A의 배치 전후로 꺾입니다** —
> A는 "도구를 뺀 파싱 언급", C는 "원문 행 수 451,862"입니다.
> **같은 칸에 두 정의를 섞지 않습니다.** 정의를 합의하고, 어느 쪽 값인지 함께 남깁니다.

**무엇을**

데이터 품질 기록 표가 0행이고, **여기에 넣는 코드도 없습니다**(인덱스만 있습니다).
`app/services/eval/dq_snapshot.py` 를 새로 만들어 한 번에 한 행씩 남깁니다.

DB에서 세는 것 — 레시피 46,353 · 재료 언급 원문 451,862 ·
매칭된 것(**지금 0**) · 재료 사전 536 · 큐 대기·완료 수
파일에서 재는 것 — 커버리지와 종류 커버리지, 매칭 방법 분포 (A의 재료 정리 코드를 그대로 재사용)

표본 크기를 담을 칸이 따로 없으므로 분포 칸에 함께 실어 둡니다.
`Makefile` 에 `dq-snapshot` 을 추가합니다.

### 🔴 함께 할 것 — 로그 쓰기 실패 카운터를 시계열로 남긴다

이 카운터는 **프로세스 메모리 안에만** 있습니다.
서버를 재시작하면 0이 되어서 **"어제 실패가 몇 건이었나"를 나중에 물어볼 수 없습니다.**

하필 이게 가장 중요한 운영 지표입니다.
**실패한 요청은 DB에 흔적 자체가 없기 때문에**, 카운터가 사라지면
기록이 비어가고 있다는 사실이 영영 안 보입니다.
다른 지표(응답시간·단계별 지연·폴백율)는 요청마다 추천 기록에 남으니
나중에 언제든 다시 집계할 수 있는데, **이것만 안 남습니다.**

그래서 품질 기록을 남길 때 카운터를 **함께 적재합니다.**
기존 JSON 칸에 얹으면 되므로 **데이터베이스 구조를 바꿀 필요가 없습니다.**
나중에 운영 화면을 붙일 때, 이 시계열이 있느냐 없느냐로
**"그 기간이 통째로 비어 있다"가 갈립니다.**

⚠️ 카운터는 프로세스마다 따로입니다. 워커가 여럿이면 합계가 아니라
**찍은 프로세스 하나의 값**이라는 걸 함께 남깁니다.
지금은 프로세스가 하나라 괜찮지만 나중에 착각하기 쉽습니다.

**완료 기준**

`make dq-snapshot` 을 두 번 돌리고 psql로 확인 — 2행이 나오고,
커버리지가 0.80 부근(지금 실측 80.8%)이고, 매칭 수는 0(A의 배치 전이므로).

그리고 `make log-test` 로 카운터를 올린 직후 `make dq-snapshot` 을 찍어서,
저장된 카운터에 `written`·`failed` 가 담겨 있어야 합니다.
빈 객체면 연결이 안 된 것입니다.

**막히면** 해당 없음 — 커버리지는 파일에서 읽고,
DB 카운트는 이미 채워진 표만 셉니다.

> ⚠️ **조용히 틀리는 방식**
>
> 지금 재료 정리 결과가 0행이라 DB 기준 매칭률은 0입니다.
> 그 값을 커버리지 칸에 넣으면 화면이 **커버리지 0%** 를 그리는데,
> 실제로는 배치가 아직 안 돈 것이지 품질이 나쁜 게 아닙니다.
> 반대로 파일 기준 80.8%만 넣으면 **배치가 영원히 안 돌아도 초록**입니다.
> 두 값을 서로 다른 칸에 넣고, 그 격차를 "아직 반영 안 된 배치"로 명시해야 합니다.
>
> 두 번째: 표본 수를 바꾸면 커버리지가 표본 수에 따라 오르내려서
> **배치가 나아진 게 없는데 추이선이 움직입니다.** 표본 수를 반드시 같이 남깁니다.

---

## C-13 — 데이터 품질 화면 + 실패 카운터 노출 · 1일

**무엇을**

`infra/app/pages/4_데이터품질.py` — 품질 기록의 시계열
(커버리지 · 종류 커버리지 · 큐 대기 수 · 매칭 비율)과 매칭 방법 분포를 그립니다.
**표본 수가 다른 점끼리는 선을 잇지 않습니다** — 점만 찍고 경고 배너를 띄웁니다.

카운터 쪽 — 지금 카운터를 **읽는 코드가 0개**입니다(올리는 곳은 저장 코드 안 7곳).
그래서 `/health` 응답에 세 가지를 추가합니다 —
**카운터 · 프로세스 번호 · 서버 시작 시각.**
가짜 서버가 이 값들을 싣게 하고, B는 실서버에 같은 것을 구현합니다.
응답에 항목을 더하는 것이라 엔드포인트 개수 규칙은 안 깨집니다.

화면에는 written / promoted / duplicate / failed 등을 표로 보여주고,
**실패가 하나라도 있으면 빨간 배너**를 띄웁니다.

**완료 기준**

`make contract` 가 여전히 통과 AND `/health` 응답에 세 항목이 다 있음 AND
`make app-test` 에서 실패 카운터를 넣으면 경고가 렌더됨.

**막히면** C-12가 늦으면 카운터 표만 먼저 붙입니다(품질 기록과 무관).
반대로 카운터가 막히면 시계열만 먼저 냅니다 — 두 반쪽이 서로 독립입니다.

> ⚠️ **조용히 틀리는 방식**
>
> 카운터는 프로세스 메모리라서, 워커를 1개보다 많이 띄우면
> `/health` 가 워커 하나만 읽어 **실패 건수가 1/N로 보이면서 화면은 정상**입니다.
> Dockerfile에 워커 1개를 박고, 프로세스 번호를 화면에 띄웁니다.
>
> 재시작하면 카운터가 0이 되는데, 화면은 "실패 0건 = 건강"으로 읽힙니다.
> 시작 시각을 함께 띄우고 "누적이 아님"을 문구로 박습니다.
>
> 가장 큰 것: **기록 저장 코드를 부르는 곳이 지금 하나도 없어서**,
> B가 연결하기 전까지 이 화면은 영원히 0을 표시합니다.
> **"아직 배선 안 됨"과 "실패 없음"이 화면에서 같아 보이면 안 됩니다** —
> 배선이 됐는지를 별도 상태로 표시합니다.

---

## C-14 — 처음부터 끝까지 다시 밟기 · 1일

**무엇을**

`make up-all` 로 화면 4종과 API를 띄우고, 데모 시나리오를 문서 없이 한 번에 통과합니다.
`.dockerignore` 를 확정하고(`.venv`·원본 데이터·`scripts/bench/out`·`.git`),
`infra/app/00_README.md` 에 **"어떤 화면이 무엇을 읽는가 / 지금 비어 있는 것은 무엇 때문인가"** 표를 씁니다.

- 추천 기록 0행 → B가 연결해야
- 재료 정리 결과 0행 → A의 배치
- 검수 큐 → C-8이 채움

**Grafana는 범위 밖으로 명시합니다.** 대시보드 폴더가 비어 있으므로 만들지 않고,
관측은 Streamlit 화면 4종으로 끝냅니다.

**완료 기준**

```
make down-v && make bootstrap && make up-all && make app-test && make eval-test && make dq-snapshot
```

이 처음부터 끝까지 성공. 그리고 화면 4종 스크린샷 각 1장.

**막히면** 해당 없음. 마지막 주 초반에 배치해서 남은 이틀을 수정에 씁니다.

> ⚠️ **조용히 틀리는 방식**
>
> 개발 머신에서만 되는 상태로 끝납니다 —
> 실제로 DB와 캐시 컨테이너가 **9시간째 떠 있어서 초기화 경로를 한 번도 안 밟았습니다.**
> `down-v` 부터 다시 밟지 않으면 초기 스크립트 순서·시드 적재·인덱스 중 하나가
> 깨진 채로 있다가 **발표 당일에 처음 드러납니다.**
>
> 두 번째: 화면이 가짜 서버를 보는지 진짜 엔진을 보는지 배지가 없으면
> **난수 점수를 진짜 추천이라고 시연**하게 되고, 질문 한 번에 무너집니다.

---

## 다른 두 사람과 맞물리는 곳

**바꾸기 전에 말해야 하는 것들입니다.**

- **`infra/app/Dockerfile.api` 는 C가 만들고, 그 안에서 도는 API는 B 것입니다.**
  기본값은 가짜 서버, B가 완성하면 환경변수 하나로 진짜 서버로 바뀝니다.
  화면이 API에 의존하도록 선언돼 있어서, 이 파일이 없으면 **화면도 안 뜹니다.**
  그래서 내용은 B 것이어도 빌드 파일은 C가 먼저 만듭니다.

- **API 응답 약속(`app/schemas/api.py`)이 곧 계약이고, 화면은 이것만 봅니다.**
  약속에 없는 항목이 오면 거부하도록 되어 있어서,
  **B가 응답에 필드를 하나 늘리면 화면이 즉시 죽습니다.**
  계약을 바꿀 때는 반드시 `make contract` 를 먼저 통과시키고 알립니다.

- **`/health` 에 붙일 3가지(카운터·프로세스 번호·시작 시각)** — C가 가짜 서버에 먼저 넣고,
  B가 실서버에 똑같이 구현합니다. 이게 없으면
  로그 실패 카운터가 프로세스 밖으로 나올 통로가 아예 없습니다.

- **합성 기록 픽스처** — C가 먼저 만들어 B에게 넘깁니다.
  B는 기록 연결이 끝나면 실제 기록 1행과 사용자 1행을 C에게 줍니다.

- **후보를 몇 개까지 저장하느냐는 B가 정합니다**(실서빙 50 · 시뮬 10 · 부하 테스트 0).
  **B가 이 값을 바꾸면 Recall의 분모가 조용히 틀립니다.**

- **검수 큐는 C 소유**지만, `seeds/` 에 쓰는 경로는
  기존 `scripts/bench/review_apply.py` **하나뿐으로 유지합니다.**
  A는 계속 `seeds/ingredient_alias.csv` 를 사전의 원본으로 읽습니다.

- **A의 재료 정리 코드를 C가 읽기 전용으로 씁니다.**
  A가 함수 모양을 바꾸면 `scripts/bench/queue_seed.py` 와 `app/services/eval/dq_snapshot.py` 가 깨집니다 —
  바꿀 일이 생기면 알려달라고 미리 말해 둡니다.

- **데이터 품질 화면이 곧 A의 진도판입니다.**
  A의 배치가 돌면 매칭 행 수가 0에서 451,862로 올라가는 것이 그대로 화면에 나옵니다.
