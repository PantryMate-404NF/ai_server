# 개발 스텝 (세분화) — AI 파트

> 작성자: 박재우 · 작성일: 2026-09-03

> `04_실행계획.md` 3-1 의 S0~S8 을 **하위 스텝**으로 쪼갠 것이다.
> 3-1 이 *무엇부터*를 정한다면 여기는 *하루 단위로 무엇을*을 정한다.
>
> **완료 기준(DoD)은 전부 "명령 하나로 검증된다"** 로 잡았다.
> 검증할 수 없는 완료는 완료가 아니다 — 이 프로젝트에서 실제로
> **조용한 유실이 열한 번** 있었다 (후기 60만 건 · 커버리지 게이트 · propensity ·
> 재료 원문 2배 적재 · 묘비가 정본 차단 · 커넥션 풀 1개 캐시 · 사전 로딩 사망 등).
> 전부 화면상으론 정상이었다.

범례 —  ✅ 완료 · 🔨 진행 · ⬜ 대기 · 🔴 소급 불가(늦으면 그 기간 데이터를 잃는다)

> 🔴 **담당은 09-02 3트랙 전환 기준이다.** `04_실행계획.md` 3-1 은 2인 시절이라
> S4·S5 를 ~~B~~ 로 적어 뒀다. 지금은 **S3 = B · S4·S5 = C · S6·S7 = A** 다
> (소유 폴더: A `app/services/normalize/` · B `app/services/recommends/` `app/db/` · C `infra/app/` `app/services/eval/`).

> 🔴 **S3 의 하위 번호를 다시 매기지 않는다.** 다른 문서가 `S3-15` 를 가리키고
> 있다(C-11). 09-03 에 빠져 있던 피처 2종을 넣을 때도 **뒤에 붙였다**(S3-17·S3-18).

---

## S0 — 규약 동결  ✅ 2026-09-01~02

| 스텝 | 내용 | DoD | 상태 |
|---|---|---|---|
| S0-1 | 🔴 로그 규약 — propensity 의미론 · 동결 키 10종 · `served ⊆ candidates` | `make contract` | ✅ |
| S0-2 | 🔴 온보딩 문항 — 알러지(그룹명) · 맛 3축 · 기피 재료 · 가구원수 | `docs/02` 결정 기록 · `make ddl-test` (응답 원본 왕복) | ✅ |
| S0-3 | 🔴 개인정보 동의 문구 초안 | 팀이 파기일·연락처 기입 | ✅ |
| S0-4 | 🔴 후기 수집 규약 — 닉네임 HMAC 해시 | `make ddl-test` | ✅ |
| S0-5 | 🔴 소진/폐기 1비트 — `removed_reason` 3값 | `make contract` | ✅ |

> 🔑 **"동결" 은 결정을 내렸다는 뜻이지 데이터가 묶였다는 뜻이 아니다.**
> 09-02 실측으로 추천 기록·행동·실유저·온보딩 응답이 **전부 0행**이다 —
> 지금 고치면 공짜다. **진짜로 잠기는 시점은 기록 개통(S3-15)과 유저 모집(S8-3)** 이다.
> 이미 잠긴 것은 `.env` 의 `REVIEW_SALT` 하나뿐이다(후기 624,422건의 작성자 해시).

---

## S1 — DB 액세스 레이어  ✅ 2026-09-02  (실소요 반나절)

| 스텝 | 내용 | DoD | 상태 |
|---|---|---|---|
| S1-1 | `app/db/pool.py` — 커넥션 풀 | 20회 호출 후 커넥션 증가 없음 | ✅ |
| S1-2 | `app/db/retrieval.py` — `retrieve_for_user()` → `list[Candidate]` | `make smoke-py` 19건 | ✅ |
| S1-3 | `RetrievalRequest` 계약 신설 (서빙 경로) | `make contract` 경계값 5종 거부 | ✅ |
| S1-4 | 스모크 9건을 파이썬 경로로 재통과 | `make smoke-py` 19건 | ✅ |
| S1-5 | 🔴 `p_include_test` 인자 — `feature_version='test-*'` 를 조회에서 제외 *(09-03)* | `make ddl-test` | ✅ |

> 🔴 **S1-5 로 `retrieve_candidates`·`retrieve_for_user` 의 시그니처가 바뀌었다.**
> PostgreSQL 은 인자가 다르면 **다른 함수로 같이 남긴다** — 옛 정의가 살아 있으면
> 호출이 조용히 그쪽으로 갈 수 있다. 그래서 정의 앞에 `DROP FUNCTION IF EXISTS`
> 를 붙였다. `make doc-check` 가 "`retrieve_for_user` 가 한 벌만"을 검사한다.

---

## S2 — 로그 쓰기 경로  ✅ 2026-09-02  (실소요 반나절)

| 스텝 | 내용 | DoD | 상태 |
|---|---|---|---|
| S2-1 | 🔴 `event_log.source` 신설 — impression 시대 구분 | `make ddl-test` served·viewport 공존 | ✅ |
| S2-2 | 🔴 `merge_served_detail()` — propensity 저장 경로 | 8건 (`make contract` 중) | ✅ |
| S2-3 | `app/core/writer.py` — 1행 + N행, 동기·단일 트랜잭션 | `make log-test` | ✅ |
| S2-4 | `app/core/counters.py` — 실패를 삼키되 센다 | 카운터에 `failed:*` | ✅ |
| S2-5 | 묘비 승격 — 재시도가 정본을 버리지 않게 | `make log-test` [6b] | ✅ |
| S2-6 | mock 결함 3건 수정 (정렬·재현성·degraded) | 점수 역전 0/59 | ✅ |
| S2-7 | 🔴 탐색 슬롯을 **실제로 꽂는다** *(09-02)* | `make contract` "탐색 아이템이 여러 위치에 퍼진다" | ✅ |
| S2-8 | 🔴 `session_id` 접두어에 `d-` 추가 — 개발·디버거·시딩 | `make ddl-test` · 실사용자 뷰가 `d-` 와 `mock-%` 를 거른다 | ✅ |

> 🔴 **S2-7 은 계산해 놓고 안 쓰던 결과였다.** `exploration_slots()` 가 뽑은 위치를
> 버려서 탐색 아이템이 **점수 순서 그대로** 남았다 — 즉 항상 비슷한 자리에 왔다.
> 그러면 위치별 클릭률이 검사확률 곡선이 되지 않아 **IPS 보정이 성립하지 않는다.**
> 지금은 8개 위치에 고르게 퍼진다.
>
> 🔴 **S2-8 은 소급 불가다.** 접두어 없이 쌓인 개발 트래픽은 나중에 실사용자와
> 갈라낼 방법이 없다. `c-` 실사용자 · `g-` 게스트 · `d-` 개발.

---

## S3 — 피처 + 스코어러  ⬜ 다음  (4일 · B 담당)

> **현재 상태**: 가중치 표는 `common.py:100` 에 있는데 **계산 코드가 없다.**
> `f_expiring` 만 SQL 함수로 존재한다 (`04_functions.sql:245`).
> `app/services/recommends/` 에는 `explore.py`·`reason.py`·`serendipity.py` 만 있고
> **`scorer.py` 가 없다.** mock 은 난수를 채운다 — 즉 **지금 추천에 개인화가 없다.**

> 🔴 **~~피처 12종~~ → 전체 17종, 그중 w>0 이 10종이다.**
> 아래 표가 오래도록 `f_cooccur`(0.10)·`f_season`(0.02) 을 빠뜨려 **Σw 가 0.88** 이었다.
> 09-03 에 S3-17·S3-18 로 채웠다 (번호는 뒤에 붙였다 — 위의 번호 규칙 참조).
>
> | 구분 | 개수 | Σw |
> |---|---|---|
> | 오늘 데이터로 계산 **가능**한 것 (= S3 에서 짤 것) | 8 | **0.94** ← 다 짜면 이만큼으로 돈다 |
> | 수단은 있는데 데이터가 없는 것 (`f_cuisine`·`f_season`) | 2 | 0.06 |
> | 켜지 않는 것 (w=0) | 4 | 0 |
> | 계산 수단 자체가 없는 것 (`f_content`·`f_ing_cf`·`f_group_pref`) | 3 | 0 |
> | **합계** | **17** | **1.00** (설계 의도) |

### S3-A. Retrieval 이 이미 주는 것 — 여기부터 (Σw 0.29)

| 스텝 | 피처 | w | 계산 근거 | DoD |
|---|---|---|---|---|
| S3-1 | `f_coverage` | **0.24** | `coverage` 그대로 | 단위테스트 |
| S3-2 | `f_missing` | 0.05 | `1/(1+missing_count)` | 단위테스트 |

> 🔑 **S3-1 하나가 전체 가중치의 24% 다.** 여기까지만 해도 mock 난수보다 낫다.

### S3-B. pantry + 시드로 되는 것 (Σw 0.15)

| 스텝 | 피처 | w | 계산 근거 | DoD |
|---|---|---|---|---|
| S3-3 | `f_expiring` | **0.15** | SQL 함수 이미 있음 — 파이썬에 연결만 | 임박 재료 있는 유저에서 상승 |
| S3-4 | `f_pantry_use` | 0.0 | 보유 재료 소진율 | 켜지 않음(ablation 대상) |

> 🔴 **기준일이 09-03 에 바뀌었다.** `effective_expiry` 가
> `COALESCE(purchased_at, added_at::date)` 로 센다 — 앱에 넣은 날이 아니라 **구매일**이다.
> 값을 넣어 주는 쪽이 **B-18**(팬트리 쓰기)이라, 그게 없으면 이 피처의 기준일이 없다.
>
> 🔴 **DB 시간대가 `Asia/Seoul` 이어야 한다.** UTC 면 한국 자정~오전 9시에
> `current_date` 가 **하루 전**이라 임박 판정이 어긋난다. 컨테이너 `TZ`·`PGTZ` 만으로는
> 부족하다 — 밖에서 붙는 psycopg2 세션에는 적용되지 않는다. `01_extensions.sql` 의
> `ALTER DATABASE` 가 정본이고, `make doc-check` 가 그 값을 검사한다.

### S3-C. 온보딩이 여는 것 (Σw 0.27 · ~~0.31~~)

> ⚠️ **온보딩 응답이 있어야 계산된다.** 유저 모집(S8) 전에는 시뮬 데이터로만 검증.
> 🔴 ~~`f_cuisine`(0.04) 도 여기~~ — 아니다. 온보딩이 아니라 **레시피 쪽 데이터**가
> 없어서 못 켠다. S3-E 로 옮겼다. 온보딩에 묶인 것은 **0.27** 이다.

| 스텝 | 피처 | w | 계산 근거 | DoD |
|---|---|---|---|---|
| S3-5 | `f_taste` | **0.16** | 선택 레시피 `flavor_vec` 평균 ↔ 레시피 코사인 | 중심화 μ 반영 |
| S3-6 | `f_ing_pref` | 0.11 | 선호/기피 재료 | 기피 재료가 점수를 내린다 |
| S3-8 | `f_dish_type` | 0.0 | 음식 유형 | 켜지 않음 |

### S3-D. 크롤 + 정규화가 여는 것 (Σw 0.23 · ~~0.13~~)

| 스텝 | 피처 | w | 계산 근거 | DoD |
|---|---|---|---|---|
| S3-9 | `f_popularity` | 0.10 | `review_count` **순위 백분위** (`row_number` + `recipe_id` tie-break) | 3분위가 비지 않는다 |
| S3-17 | `f_cooccur` | **0.10** | 내 재료끼리의 동시 출현. `ingredient.freq_count` 필요 | 🔴 **09-03 신설** — 표에 없었다 |
| S3-10 | `f_time_fit` | 0.03 | 조리시간 ↔ 사용자 상한. **95.5% 만 값이 있다** | NULL 이 버려지지 않는다 |
| S3-11 | `f_skill_fit` | 0.0 | 난이도 5단계 | 켜지 않음 |
| S3-12 | `f_quality` | 0.0 | — | 켜지 않음 |

> 🔴 **인기도는 `percent_rank` 가 아니다** (결정 D-9 정정). `percent_rank` 는
> 3분위가 비고 **8,470건이 동점**이 되어 후보 500컷이 비결정적이 된다 —
> 그러면 propensity 를 재현할 수 없다. `row_number` + `recipe_id` 로 동점을 가른다.
>
> 🔴 **S3-17 은 A-14(`freq_count` 채우기)를 기다린다.** 값이 0 이면 `None` 으로 낸다
> (0.0 으로 메우면 "계산했더니 0" 과 구별되지 않는다).

### S3-E. 수단은 있는데 데이터가 없는 것 (Σw 0.06 · 오늘은 계산 안 됨)

| 스텝 | 피처 | w | 왜 못 켜나 | DoD |
|---|---|---|---|---|
| S3-7 | `f_cuisine` | 0.04 | `cuisine_family` 가 **46,353건 전수 0건**. 크롤에 4축이 안 왔고 categories 는 자유 태그 45,529종 | 태그→유형 매핑(약 12h) 후 판단 (B-8) |
| S3-18 | `f_season` | 0.02 | 제철 시드가 없다. `season_vec` 은 칸만 있다 | 🔴 **09-03 신설** — 표에 없었다 |

> 🔑 **가중치를 0 으로 내리지 않는다.** 결측 처리가 `raw = Σwᵢfᵢ / Σwᵢ` (fᵢ≠None 인 것만)
> 이라 항상 None 인 피처는 분자·분모에서 함께 빠진다 — 비례 재분배와 수학적으로 같다.
> 그래서 **지금 점수는 옳고**, 데이터가 오면 가중치를 다시 유도하지 않고 그대로 켜진다.
> 문서에 쓰는 오늘의 숫자는 `ACTIVE_WEIGHT_TODAY = 0.94` 다.

### S3-F. 스코어러 본체

| 스텝 | 내용 | DoD |
|---|---|---|
| S3-13 | `app/services/recommends/scorer.py` — 선형가중합 + **결측 나눗셈 정규화** | `raw = Σwᵢfᵢ / Σwᵢ` (재분배 아님) |
| S3-14 | `scoring_config` 등록 — `config_hash` 로 가중치 복원 | 해시로 되찾아진다 |
| S3-15 | 🔴 라이터를 실서빙에 연결 | `recommendation_log` 에 실제 행이 쌓인다 |
| S3-16 | z-salience 이유 생성 연결 | 이유가 상위 2피처를 가리킨다 |

> 🔴 **S3-15 가 S2 의 실제 개통 지점이다.** 지금은 라이터를 호출하는
> 프로덕션 코드가 0개다 — mock 은 메모리에만 담는다.

### S3-G. 저장 경로 — 온보딩 · 팬트리  *(09-03 추가 · B-17 · B-18)*

> 🔴 **위의 Σw 0.27 과 0.15 를 넣을 곳이 없었다.** 계약은 09-02 에 생겼고
> (`OnboardingIn`/`OnboardingOut` · `PantryItemIn.purchased_at`),
> mock 은 메모리에 담는다. DB 에 넣는 코드가 아직 없다.

| 스텝 | 내용 | DoD |
|---|---|---|
| B-17 | 온보딩 저장 — `user_vector` · `user_ingredient_pref` · `user_allergy` | `make contract` 온보딩 실호출 200 |
| B-18 | 팬트리 쓰기 — `purchased_at` · 소진/폐기 묘비 | 지운 재료의 사유가 남는다 |

- 🔴 **원본을 남긴다.** `user_vector.onboarding_picks` · `onboarding_scales`.
  `taste_vec` 은 고른 레시피들의 **평균**이라 결과만 저장하면 시드가 바뀔 때
  다시 계산할 수 없다 — 실제로 09-02 에 맛 시드 2건을 고쳤다
  (식용유 기름짐 0.60→1.00 · 멸치 3종 분리). 원본이 없으면 **유저를 다시 모아야 한다.**
- 🔴 **알러지는 `severity='allergy'` 로 저장한다.** DB 기본값 `'avoid'` 에 맡기면
  그룹 확산이 **조용히 꺼져** 본인이 적은 재료만 막힌다 —
  아몬드만 등록한 사람에게 호두·잣이 그대로 추천된다. 에러가 안 나서 더 위험하다.
- `PantryIn.removed` 를 **버리지 않는다** (mock 도 09-03 부터 보관한다).

---

## S4 — 평가 하네스  ⬜  (2일 · **C 담당** · ~~B~~ · S3 와 병행 가능)

| 스텝 | 내용 | DoD |
|---|---|---|
| S4-1 | 골든 픽스처 — 손으로 쓴 배열 | DB 불필요 |
| S4-2 | 🔴 nDCG — **IDCG 를 top-K 안에서만 계산하지 않는다** | 편향 +0.182 재현 테스트 |
| S4-3 | Recall@K · Coverage · Diversity | 골든에서 기대값 일치 |
| S4-4 | IPS off-policy — propensity 분모 | propensity=0 이면 예외 |

> 🔑 **테스트가 먼저여야 한다.** IDCG 버그의 편향(+0.182)이 재려는
> 효과크기(0.01~0.03)의 **6~18배**다. 하네스가 틀리면 모든 실험이 틀린다.

---

## S5 — 화면 2종  ⬜  (5일 · **C 담당** · ~~B~~ · **가장 급하다**)

> `infra/app/` 디렉터리가 **아예 없다**(09-03 실측 · ~~비어 있다~~). `docker-compose.yml` 은
> 이미 빌드 대상으로 선언해 놓았으므로 **지금 `make up-all` 은 뜨지 않는다.**
> **B 의 스코어러가 아니라 C 의 착수가 병목이다.**
>
> 🔴 검수 큐 테이블(`normalization_queue`)도 **0행**이다 — 화면(S5-4)보다
> **채워 넣는 일(C-8)이 먼저**다. 채울 재료는 이미 있다:
> `scripts/bench/out/review_sheet.tsv` 2,000종 · `unmatched.tsv` 24,701종.

| 스텝 | 내용 | DoD |
|---|---|---|
| S5-1 | `infra/app/Dockerfile.dashboard` | `make up-all` 이 뜬다 |
| S5-2 | 추천 디버거 — 요청 1건의 피처·점수·이유 | mock 으로 먼저 붙인다 |
| S5-3 | 스테이지 추적 시각화 (①→②→③ 감소) | `stage_trace` 를 읽는다 |
| S5-4 | 🔴 검수 큐 — `suggested` 후보가 **미리 채워져** 클릭만 | 비면 반나절이 3일 된다 |
| S5-5 | 데이터 품질 화면 — 커버리지 추이 | `data_quality_snapshot` |
| S5-6 | 로그 쓰기 실패 카운터 노출 | `counters()` 를 읽는다 |

---

## S6 — 로더 + 정규화 배치  ✅ 부분완료

| 스텝 | 내용 | DoD | 상태 |
|---|---|---|---|
| S6-1 | `scripts/load_recipes.py` — 레시피·재료원문·후기 | 46,353건 적재 | ✅ |
| S6-2 | 조리순서 적재 | 362,677단계 (97.4%) | ✅ |
| S6-3 | 조리도구 필터 · 미매칭 사전 | 커버리지 69.2% → **80.8%** | ✅ |
| S6-4 | 검수 시트 2,000종 | `make review-sheet` | ✅ |
| S6-5 | ⬜ 검수 300종 수행 (사용자) | 커버리지 **89.4%** 목표 | ⬜ |
| S6-6 | ⬜ `make review-apply WRITE=1` | 재정규화 후 커버리지 재측정 | ⬜ |
| S6-7 | 🔴 로더 멱등성 제약 *(09-02)* | 두 번 돌려도 행 수가 그대로 | ✅ |

> 🔑 **검수는 병목이 아니다.** `recipe_ingredient_raw` 가 불변이라
> 사전을 고치면 언제든 재정규화된다. 로그와 달리 늦어도 잃는 것이 없다.
>
> 🔴 **S6-7 전에는 로더를 2회 돌리면 전량 2배가 됐다** (재료 원문 451,862 → 903,724).
> 에러도 안 났다. `recipe_ingredient_raw(recipe_id, position)` 유니크와
> `recipe_review` 의 `UNIQUE NULLS NOT DISTINCT` 로 막았다.
> 🔴 단 `.env` 의 `REVIEW_SALT` 를 새로 만들면 후기 작성자 해시가 전부 달라져
> **그 보장이 깨진다** — 지우거나 재생성하지 않는다.
>
> ⚠️ `scripts/bench/out/review_sheet.tsv` 머리말이 아직 "우리 사전(525종)" 이라고 적는다.
> 실제는 **536종**이다 (생성기 `scripts/bench/review_sheet.py` 쪽 문구 — A 트랙 소관).

---

## S7 — `recipe_feature` 빌더 + 피처 5종  ⬜  (6일 · A 담당)

> 🔴 **지금 `recipe_feature` 가 0행이라 실데이터 추천이 0건이다.**
> 빌더 게이트(`n_total > 0`)에 전부 걸린다. S3 를 다 짜도 여기가 없으면
> 실레시피가 한 건도 안 나온다.
> 실측(09-03): `recipe_feature` 0행 · `recipe_ingredient` 0행 · 원문만 451,862행.

| 스텝 | 내용 | DoD |
|---|---|---|
| S7-1 | P1~P4(전처리·파싱·사전매칭·역할판정)를 46,353건에 배치 적용 | `recipe_ingredient` 채워짐 |
| S7-2 | `essential_ids` / `all_ids` 배열 생성 | 배열 GIN 이 쓰인다 |
| S7-3 | `is_staple` 제외 규칙 | staple 39종이 essential 에서 빠진다 |
| S7-4 | `flavor_vec` 계산 + 코퍼스 평균 μ | `feature_stats.flavor_mu` 채워짐 |
| S7-5 | k-means K≈50 → `cluster_id` | 우연성이 균등 폴백에서 벗어난다 |
| S7-6 | 실데이터 스모크 재통과 | 후보가 0건이 아니다 |

- 🔴 **S7-4 의 μ 는 두 번만 갱신한다** (A-5 · A-13 · 결정 D-13). 갱신하면 B·C 에 알린다 —
  중심화 기준이 바뀌면 이미 계산한 `f_taste` 점수와 섞인다.
- 🔴 **S7-5 는 A-4 직후 한 번만 매긴다** (D-12). 로그에 `cluster_version` 칸이 **없어서**,
  다시 매기면 옛 기록의 `cluster_id` 가 무엇이었는지 복원할 수 없다.
- `flavor_vec` 은 **6축으로 저장**한다. 실제로 쓸 축은 W3 에서 정한다 (D-11).
- 필수재료 0개(7.2%·약 3,340건)는 **SQL 함수에서** 미매칭 비율 0.3 으로 가른다 (D-14).

---

## S8 — 재랭킹 · 유저 · 가중치 교체  ⬜  (잔여)

| 스텝 | 내용 | DoD |
|---|---|---|
| S8-1 | MMR 다양성 | 근중복이 줄어든다 |
| S8-2 | 혼합 탐색 (균등 + Thompson) + **위치 분산** | propensity > 0 · 탐색이 여러 위치에 |
| S8-3 | 🔴 유저 모집 + 온보딩 실시 | 실유저 50명 (받는 쪽은 B-17) |
| S8-4 | 쌍대비교 라벨 수집 — **블록별 120쌍 층화** (20명 × 30쌍 = 600쌍 + 5% 중복) | 블록별 차분 분포를 찍는다 |
| S8-5 | BT 로 가중치 학습 → v0 교체 | ablation 으로 검증 |
| S8-6 | 최종 리포트 | — |
| S8-7 | ⏳ 트랙1(오늘의 추천) 사전 계산 배치 | **3주 계획 밖** — 테이블만 있다 |

> 🔴 **S8-4 는 블록별로 뽑아야 한다** (D-18). 차분이 0인 쌍만 모이면 그 블록의
> 가중치가 **식별되지 않고, 그 사실조차 모른다.** 가장 위험한 것은 임박소진 —
> w=0.15 인데 피처가 하나뿐이라 대체할 것이 없다. 그래서 냉장고 시나리오에
> **임박 재료를 일부러 넣는다.** 수집 수단은 구글폼(개발 0일).
> 뽑기 전에 `|차분| > 0.1` 인 쌍이 블록당 120개 있는지 **미리 확인**한다.
>
> ⏳ **S8-7 은 만들기로만 정했다** (D-19). `daily_recommendation` 테이블은 있고
> (`reason_source` · `pantry_fingerprint` · `config_hash` · `stats_version` · `batch_run_id`),
> **조회 API 계약도 배치도 없다** — `reco/` 에 이 테이블을 읽고 쓰는 코드가 0개다.
> 기능명세 `MAIN-000` 이 프론트에 약속한 것이라 **못 만들면 알려야 한다.**
> 배치인 이유는 지연이다 — 사유 10건에 833 tok/s 가 필요한데 로컬 최고가 90.9 다.
> 미결로 남은 것: 어디서 돌리나 · LLM 출력 검수 · stale 을 표시만 할지 재생성할지 · Top-N.

---

## 의존 관계 — 무엇이 무엇을 막는가

```
S0 ✅ ─┬─ S1 ✅ ─ S2 ✅ ─ S3 ⬜ ─┐
       ├─ S4 ⬜ ────────────────┼─ S8
       └─ S5 ⬜ ────────────────┘
                    ↑
   S6 ✅ ─ S7 ⬜ ───┘   (실데이터가 흐르려면 S7 필수)
```

**지금 병목 셋**

1. **S5 (C 착수)** — `infra/app/` 이 아직 없다. 5일짜리인데 아직 시작 안 함
2. **S7 (빌더 · A)** — 없으면 S3 를 다 짜도 실레시피 0건
3. **S8-3 (유저 모집)** — Σw **0.27** 이 온보딩에 묶여 있다 (~~0.31~~ · `f_cuisine` 은
   온보딩이 아니라 레시피 데이터 문제였다)

**병목이 아닌 것**: 검수(S6-5). 원문이 불변이라 언제 해도 된다.

---

## 09-02~03 에 바뀐 것 — 한눈에

| 무엇이 | 어디에 반영됐나 |
|---|---|
| `event_log.source` (served/viewport/client) · 부분 유니크 | S2-1 |
| `session_id` 접두어 `d-` (개발·디버거·시딩) | S2-8 |
| 탐색 슬롯을 실제로 꽂는다 | S2-7 · S8-2 |
| `user_vector.onboarding_picks` · `onboarding_scales` | S3-G (B-17) |
| `pantry_item.purchased_at` · `effective_expiry` 기준일 변경 | S3-B · S3-G (B-18) |
| `daily_recommendation` 테이블 (트랙1) | S8-7 |
| `retrieve_*` 에 `p_include_test` · 합성 피처 `test-*` 격리 | S1-5 |
| 로더 멱등성 제약 | S6-7 |
| DB 시간대 `Asia/Seoul` | S3-B |
| `OnboardingIn`/`OnboardingOut` · `PantryItemIn.purchased_at` 계약 | S3-G |
| `PENDING_DATA_FEATURES` · `ACTIVE_WEIGHT_TODAY=0.94` | S3-E |
| 빠져 있던 `f_cooccur`·`f_season` | S3-17 · S3-18 |
| `pyproject.toml` + `uv.lock` (requirements.txt 폐지) | [01_기술스택.md](01_기술스택.md) |

검증 명령 실측 —
`contract` **98** · `ddl-test` **47** · `normalize-test` **113**(74+23+16) ·
`smoke` 13 · `smoke-py` 19 · `log-test` 48.
