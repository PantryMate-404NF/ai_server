# 개발 스텝 — AI 파트

> 작성자: 박재우 · 작성일: 2026-09-03

> 8주 중 **5주차**. 무엇을 언제 하는지와, 지금 무엇이 막혀 있는지.
>
> 🔑 **3인 병렬로 전환했다 (09-02).** 실제 작업 지시는
> [03_작업분담_공통.md](03_작업분담_공통.md) 와 트랙별 문서를 본다.
> 이 문서는 전체 그림용이다.

| 트랙 | 담당 | 여기서 맡는 것 | 작업분담 문서의 번호 |
|---|---|---|---|
| **A** 데이터 | | S6 나머지 · S7 | A-1 ~ A-14 |
| **B** 엔진 | | S3 · 로그 개통 · 온보딩/팬트리 저장 | B-0 ~ B-18 |
| **C** 관측 | | S4 · S5 | C-1 ~ C-14 |

> 🔴 **담당이 09-02 에 바뀌었다.** 2인 시절 계획(`04_실행계획.md` 3-1)은
> S4·S5 를 ~~B~~ 로 적어 뒀다. 3트랙 전환 후 **S4·S5 는 C** 다
> (`infra/app/` · `app/services/eval/` 소유자가 C). 아래 각 절도 그 기준으로 고쳤다.


범례
- ✅ 완료 · ⬜ 대기
- 🔴 **지금 살아 있는 경고** — 늦으면 그때까지의 데이터를 잃는다
- 🔒 **되돌릴 수 없어서 먼저 처리함** — 이미 닫혔다. 재논의하면 데이터가 버려진다

---

## S0. 규약 정하기  ✅

나중에 바꾸면 그때까지 모은 데이터를 버려야 하는 것들을 먼저 못박았다.

> 🔒 **전부 닫혔다.** 여기 있는 다섯은 다시 논의하면 그 사이 쌓인 데이터를
> 버려야 한다. 바꿔야 할 이유가 생기면 **데이터를 버리는 비용부터** 계산해야 한다.
>
> 🔑 다만 **지금은 그 비용이 0 이다** — 추천 기록·행동·실유저·온보딩 응답이
> 전부 0행이다(09-02 실측). "닫았다"는 것은 *결정을 내렸다*는 뜻이지 아직
> 데이터가 묶였다는 뜻이 아니다. **기록을 개통하는 순간(S3-15)부터 진짜로 잠긴다.**

| | 무엇을 | 왜 먼저인가 |
|---|---|---|
| S0-1 | 🔒 기록 규약 | 추천한 순간에만 존재하는 값이라 나중에 못 만든다 |
| S0-2 | 🔒 온보딩 문항 | 100명 모은 뒤 바꾸면 데이터가 반쯤 버려진다 |
| S0-3 | 🔒 개인정보 동의 문구 | 모집 안내문에 들어가야 한다 |
| S0-4 | 🔒 후기 수집 방식 | 닉네임을 되돌릴 수 없게 처리 |
| S0-5 | 🔒 재료 소진·폐기 구분 | 안 물어보면 나중에 물을 대상이 없다 |

---

## S1. 추천 엔진이 DB를 읽게 하기  ✅  *(반나절)*

| | 무엇을 | 완료 확인 |
|---|---|---|
| S1-1 | 접속 관리 | 반복 호출에도 접속이 늘지 않는다 |
| S1-2 | 후보 조회 연결 | 정확성 검사 19건 통과 |
| S1-3 | 요청 형식 확정 | 잘못된 값이 거부된다 |
| S1-4 | 시험용 레시피 특성 격리 *(09-03)* | 조회가 시험용 데이터를 **기본으로 건너뛴다** |

> 🔴 **S1-4 로 조회 함수의 인자가 하나 늘었다.** 예전 형태가 DB 에 남아 있으면
> 둘 다 호출 가능한 상태가 되어 어느 쪽이 돌았는지 알 수 없다 —
> 그래서 새로 만들기 전에 옛것을 지운다.

---

## S2. 추천 기록 남기기  ✅  *(반나절)*

추천 1건이 나갈 때 **무엇을 왜 보여줬는지**를 기록한다. 이게 없으면 나중에
"왜 이 추천이 나왔나"에 답할 수 없고, 모델을 개선할 근거도 없다.

> 🔒 S2-1·S2-2 는 기록 형식이라 나중에 바꾸면 옛 데이터와 섞인다. 그래서
> 실제 기록이 시작되기 **전에** 닫아 뒀다 (실제 개통은 S3-15).

| | 무엇을 | 완료 확인 |
|---|---|---|
| S2-1 | 🔒 노출 기록 방식 구분 | 나중에 방식을 바꿔도 옛 데이터와 섞이지 않는다 |
| S2-2 | 🔒 추천 확률 보존 | 평가에 꼭 필요한 값이 빠지지 않는다 |
| S2-3 | 기록 저장 | 종단 검사 48건 통과 |
| S2-4 | 실패해도 추천은 살린다 | 대신 실패 횟수를 센다 |
| S2-5 | 🔒 세션 구분 — 실사용자 / 게스트 / **개발·디버거** | 개발 트래픽이 실사용자 지표에서 빠진다 |

> 🔑 **기록 실패가 추천을 죽이면 안 된다.** 그렇다고 조용히 넘기면 데이터가
> 비어가는 걸 아무도 모른다. 그래서 **넘기되 반드시 센다.**

---

## S3. 개인화 점수 매기기  ⬜ **다음**  *(4일 · 담당 B)*

지금은 추천 순서가 **난수**다. 진짜 개인화가 여기서 시작된다.
17개 항목에 가중치를 곱해 더한다. 그중 **비중이 0보다 큰 것은 10개**다.

> 🔴 **번호는 [15_개발스텝_세분화.md](15_개발스텝_세분화.md) 와 같은 것을 쓴다.**
> 예전에 두 문서가 서로 다른 S3-9·S3-11 을 가리켰다 —
> 다른 문서(C-11)가 **S3-15** 를 지목하고 있어서 세분화 쪽 번호로 통일했다.

### 지금 바로 되는 것 — 전체 가중치의 **29%**

| | 항목 | 비중 | 뜻 |
|---|---|---|---|
| S3-1 | 재료 충족도 | **24%** | 가진 재료로 얼마나 만들 수 있나 |
| S3-2 | 부족 재료 수 | 5% | 몇 개 더 사야 하나 |

> 🔑 **S3-1 하나가 24%다.** 여기까지만 해도 지금보다 낫다.

### 냉장고 정보로 되는 것 — **15%**

| | 항목 | 비중 | 뜻 |
|---|---|---|---|
| S3-3 | 소비기한 임박 | **15%** | 곧 못 먹게 되는 재료를 먼저 쓰게 |

> 🔴 기준일은 **구매일**이다(앱에 넣은 날이 아니라). 소비기한을 안 적어도
> 구매일 + 재료별 일수로 추정된다. 임박 판정은 **한국 시간 기준 오늘**이라야 한다.

### 온보딩이 열어주는 것 — **27%**  *(~~31%~~ 정정)*

| | 항목 | 비중 | 뜻 |
|---|---|---|---|
| S3-5 | 맛 선호 | **16%** | 고른 레시피 20개로 취향을 잰다 |
| S3-6 | 선호·기피 재료 | 11% | 싫어하는 재료는 점수를 내린다 |

> ⚠️ **실사용자를 모아야 켜진다.** 그전에는 가상 데이터로만 확인 가능.
>
> 🔴 ~~요리 계열 4% 도 온보딩이 연다~~ — 아니다. 온보딩이 아니라 **레시피 쪽
> 데이터**가 없어서 못 켠다(아래 "아직 못 켜는 것"). 온보딩에 묶인 것은 **27%** 다.

### 수집·정규화가 열어주는 것 — **23%**

| | 항목 | 비중 | 뜻 |
|---|---|---|---|
| S3-9 | 인기도 | 10% | 후기가 많은 레시피 |
| S3-17 | 함께 쓰는 재료 | 10% | 내 냉장고 재료끼리 자주 같이 쓰이나 |
| S3-10 | 조리시간 적합 | 3% | 사용자가 낸 시간 안에 되나 |

> 🔴 **함께 쓰는 재료(10%)가 이 표에 빠져 있었다.** 그래서 이 문서의 비중 합이
> 오래도록 88% 였다. 정규화가 끝나야(A-3·A-14) 계산된다.

### 아직 못 켜는 것 — **6%** *(수단은 있는데 데이터가 없다)*

| | 항목 | 비중 | 왜 못 켜나 |
|---|---|---|---|
| S3-7 | 요리 계열 | 4% | 레시피 쪽 요리 유형이 **46,353건 전수 비어 있다** |
| S3-18 | 제철 | 2% | 제철 시드가 없다 (칸만 있다) |

> 🔑 **가중치를 0 으로 내리지 않는다.** 값이 없는 항목은 분자·분모에서 함께
> 빠지는 방식(나눗셈 정규화)이라, 데이터가 오면 **비중을 다시 정하지 않고 켜진다.**
> 그래서 설계 합계는 100% 이고 **오늘 실제로 도는 것은 94%** 다.

### 켜지 않는 것 — 0%

`f_pantry_use`(S3-4) · `f_dish_type`(S3-8) · `f_skill_fit`(S3-11) · `f_quality`(S3-12)
는 계산은 되지만 비중 0 으로 둔다(비교 실험용). `f_content` · `f_ing_cf` ·
`f_group_pref` 는 **계산 수단 자체가 없다** — 되살리려면 그 방법부터 만들어야 한다.

### 마무리

| | 무엇을 |
|---|---|
| S3-13 | 점수 계산기 본체 |
| S3-14 | 가중치 버전 기록 — 나중에 그때 점수를 재현할 수 있게 |
| S3-15 | 🔴 기록을 실서비스에 연결 — 여기서 실제 데이터가 쌓이기 시작한다 |
| S3-16 | 추천 이유 문구 연결 |

### 저장 경로 2종 — 09-03 추가 *(B-17 · B-18)*

| | 무엇을 | 왜 여기인가 |
|---|---|---|
| B-17 | 온보딩 응답 저장 (mock → DB) | 이게 없으면 위의 **27%** 를 넣을 곳이 없다 |
| B-18 | 팬트리 쓰기 — 구매일 · 소진/폐기 묘비 | 이게 없으면 **15%** 의 기준일이 없다 |

> 🔴 **온보딩은 고른 레시피 번호와 척도를 원본 그대로 남긴다.** 취향 벡터는
> 평균값이라, 결과만 저장하면 기준 레시피의 맛 값을 고칠 때 **다시 계산할 수 없다** —
> 실제로 09-02 에 맛 시드 2건을 고쳤다. 알레르기는 반드시 '차단' 등급으로 저장한다.

---

## S4. 평가 도구  ⬜  *(2일 · 담당 C · S3와 동시 진행 가능)*

| | 무엇을 |
|---|---|
| S4-1 | 정답이 정해진 예제로 도구 자체를 검증 |
| S4-2 | 🔴 순위 품질 지표 |
| S4-3 | 다양성·커버리지 지표 |
| S4-4 | 편향 보정 평가 |

> 🔑 **평가 도구가 먼저다.** 도구에 오차가 있으면 재려는 차이보다
> 오차가 **6~18배** 커서 모든 실험이 무의미해진다.

---

## S5. 화면 2종  ⬜  *(5일 · 담당 C · **가장 급함**)*

| | 무엇을 |
|---|---|
| S5-1 | 추천 디버거 — 요청 1건이 왜 그 순서인지 본다 |
| S5-2 | 단계별 후보 감소 시각화 |
| S5-3 | 🔴 검수 화면 — 후보가 **미리 채워져 클릭만** 하면 되게 |
| S5-4 | 데이터 품질 추이 |
| S5-5 | 기록 실패 현황 |

> 🔴 **S5-3에서 후보가 비어 있으면 검수자가 타이핑을 해야 한다.**
> 그러면 반나절 작업이 3일이 된다.
> 지금 검수 큐 테이블은 **0행**이다(실측) — 화면보다 **채워 넣는 일(C-8)이 먼저**다.

---

## S6. 데이터 수집·정제  ✅ 대부분 완료  *(담당 A)*

| | 무엇을 | 결과 |
|---|---|---|
| S6-1 | 레시피 적재 | **46,353건** |
| S6-2 | 조리순서 적재 | 레시피의 **97.4%** |
| S6-3 | 재료 인식률 개선 | 69.2% → **80.8%** |
| S6-4 | 검수 목록 만들기 | 2,000종 |
| S6-5 | ⬜ 검수 300종 (사용자) | → **89.4%** 목표 |
| S6-6 | ⬜ 검수 결과 반영 | |

> 🔑 **검수는 급하지 않다.** 원문을 그대로 보관하고 있어서
> 사전을 고치면 언제든 다시 처리된다. **늦어도 잃는 것이 없다.**

---

## S7. 레시피 재료 정리  ⬜  *(6일 · 담당 A)*

| | 무엇을 |
|---|---|
| S7-1 | 46,353건 전체에 정규화 적용 |
| S7-2 | 레시피별 필수/전체 재료 목록 생성 |
| S7-3 | 조미료류 제외 처리 (기본 양념 39종) |
| S7-4 | 맛 특성 수치화 |
| S7-5 | 비슷한 레시피 묶기 — **A-4 직후 딱 한 번만 매긴다** |

> 🔴 **지금 이게 비어 있어서 실제 레시피 추천이 0건이다.**
> S3를 다 만들어도 여기가 없으면 **한 건도 안 나온다.**
> 실측: 레시피 특성 **0행** · 정규화된 재료 **0행** (원문만 451,862행 있다).
>
> 🔑 **묶음(S7-5)을 다시 매기지 않는 이유** — 기록에 "몇 판째 묶음인지" 적는 칸이
> 없다. 다시 매기면 옛 기록의 묶음 번호가 무엇을 가리켰는지 복원할 수 없다.

---

## S8. 마무리  ⬜

| | 무엇을 |
|---|---|
| S8-1 | 비슷한 것만 나오지 않게 흩뜨리기 |
| S8-2 | 새로운 레시피를 섞어 보여주기 — 🔨 mock 에서는 이미 **여러 위치에 흩어 꽂는다** |
| S8-3 | 🔴 사용자 모집 + 온보딩 실시 |
| S8-4 | 선호도 비교 데이터 수집 — 블록별 120쌍 + 구글폼 |
| S8-5 | 가중치를 실제 데이터로 재학습 |
| S8-6 | 최종 리포트 |
| S8-7 | ⏳ 오늘의 추천(트랙1) 사전 계산 배치 — **3주 계획 밖** |

> ⏳ **S8-7 은 아직 만들기로만 정했다** (결정 D-19). 저장 테이블은 있고,
> 조회 API 와 배치는 없다. 기능명세 `MAIN-000` 이 프론트에 약속한 것이라
> **기간 안에 못 만들면 알려야 한다.**
>
> 왜 실시간이 아니라 배치인가 — 사유 문구를 10건 만들려면 초당 833 토큰이
> 필요한데 가장 빠른 로컬 모델이 90.9 다. **배치로 밀면 시간 제약이 사라진다.**
> 대신 새벽에 계산한 사유가 낮에 어긋나므로(재료를 다 씀),
> 계산 시점의 냉장고 지문을 남겨 **어긋났음을 표시**한다.

---

## 🔴 남은 경고 다섯

아직 안 한 것 중 **늦으면 되돌릴 수 없는** 것들이다.

| | 늦으면 무엇을 잃나 |
|---|---|
| S3-15 기록 개통 *(B-1)* | 개통 전 추천은 기록이 없다. **그 기간은 통째로 분석 불가** |
| S4-2 순위 지표 | 지표가 틀린 채 실험하면 그 실험 결과를 못 믿는다 |
| S5-3 검수 화면 | 후보가 비면 검수 반나절이 3일이 된다 |
| S7 재료 정리 | 이게 없으면 실제 추천이 0건이라 유저 모집을 못 한다 |
| S8-3 유저 모집 | 남은 기간이 짧아지면 온보딩 데이터가 안 모인다 |

> 🔑 **S3-15 가 가장 급하다.** 기록이 안 켜진 상태로 추천을 내보내면
> 그 기간 데이터는 나중에 어떤 방법으로도 못 만든다.
>
> 지금은 **아직 아무것도 잠기지 않았다** — 추천 기록·행동·실유저·온보딩 응답이
> 전부 0행이다. 그래서 규약을 고칠 마지막 기회이기도 하다.

## 지금 막혀 있는 것 셋

```
S0 ✅ ─┬─ S1 ✅ ─ S2 ✅ ─ S3 ⬜ ─┐
       ├─ S4 ⬜ ────────────────┼─ S8
       └─ S5 ⬜ ────────────────┘
                    ↑
   S6 ✅ ─ S7 ⬜ ───┘
```

**1. 화면(S5 · C)** — 아직 시작 못 함. `infra/app/` 이 **아직 없다**(C-1 이 만든다).
5일짜리인데 남은 기간을 생각하면 여기가 제일 급하다.

**2. 레시피 재료 정리(S7 · A)** — 없으면 실제 추천이 0건. 점수 계산을 다 만들어도
보여줄 게 없다. `recipe_feature` 는 지금도 **0행**이다(실측).

**3. 사용자 모집(S8-3)** — 개인화 가중치의 **27%**가 온보딩에 묶여 있다.
받아 둘 API(B-17)부터 있어야 모집한 날 바로 저장된다.

**급하지 않은 것**: 검수(S6-5). 언제 해도 된다.
